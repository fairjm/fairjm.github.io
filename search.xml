<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Akka实践一些总结</title>
    <url>/2018/11/28/10035181/</url>
    <content><![CDATA[<p>最近在一些服务中使用了akka,主要用来做异步解耦和本地消息分发(路由),这里简单总结一下用法.  </p>
<span id="more"></span>
<h1 id="与spring集成"><a href="#与spring集成" class="headerlink" title="与spring集成"></a>与spring集成</h1><p>网上有不少集成的例子,要使用到spring的扩展.<br>我这边没有这样处理,而是简单把<code>ActorSystem</code>创建的actor的过程放在了spring configuration里,把<code>ActorRef</code>作为bean,毕竟actor本身不能为单例但是ref可以.  </p>
<p>actor要使用一些bean的话就全部都由构造函数传入.<br>如下:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserActor</span> <span class="token keyword">extends</span> <span class="token class-name">AbstractLoggingActor</span> <span class="token punctuation">&#123;</span>

    <span class="token keyword">private</span> <span class="token class-name">UserService</span> userService<span class="token punctuation">;</span>

    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">Props</span> <span class="token function">props</span><span class="token punctuation">(</span><span class="token class-name">UserService</span> userService<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">return</span> <span class="token class-name">Props</span><span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token class-name">UserActor</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token keyword">new</span> <span class="token class-name">UserActor</span><span class="token punctuation">(</span>userService<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

    <span class="token keyword">public</span> <span class="token class-name">UserActor</span><span class="token punctuation">(</span><span class="token class-name">UserService</span> userService<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">this</span><span class="token punctuation">.</span>userService <span class="token operator">=</span> userService<span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span></code></pre>

<h1 id="在java中使用"><a href="#在java中使用" class="headerlink" title="在java中使用"></a>在java中使用</h1><p>使用中能感觉和java还是有些隔阂的…<br>特别是容器(Collection)之间的转换和处理.<br>给出一些例子,可能是我没发现更方便的写法..  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Routee</span><span class="token punctuation">></span></span> routees <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
senders<span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span>e <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">ActorRef</span> ref <span class="token operator">=</span> <span class="token function">getContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">actorOf</span><span class="token punctuation">(</span>createdProps<span class="token punctuation">.</span><span class="token function">apply</span><span class="token punctuation">(</span>e<span class="token punctuation">)</span><span class="token punctuation">,</span> prefix <span class="token operator">+</span> <span class="token string">"-"</span> <span class="token operator">+</span> e<span class="token punctuation">.</span><span class="token function">getId</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                routees<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">ActorRefRoutee</span><span class="token punctuation">(</span>ref<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InvalidActorNameException</span> ex<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                <span class="token function">log</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span>ex<span class="token punctuation">,</span> <span class="token string">"name already in use"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
router <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Router</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">RoundRobinRoutingLogic</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> routees<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>这边本来用一个map就能解决了,但会要求类型强转Router的第二个参数是<code>java.lang.Iterable&lt;Routee&gt;</code>而不是<code>java.lang.Iterable&lt;? extends Routee&gt;</code>(ActorRefRoutee是其子类),再加上还要处理重名错误,还是用了这种错误的forEach使用方式.  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">void</span> <span class="token function">stopRoutee</span><span class="token punctuation">(</span><span class="token class-name">Router</span> router<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Routee</span><span class="token punctuation">></span></span> routees <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token function">seqAsJavaList</span><span class="token punctuation">(</span>router<span class="token punctuation">.</span><span class="token function">routees</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    routees<span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span>e <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">ActorRefRoutee</span> ae <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">ActorRefRoutee</span><span class="token punctuation">)</span> e<span class="token punctuation">;</span>
        <span class="token class-name">ActorRef</span> ref <span class="token operator">=</span> ae<span class="token punctuation">.</span><span class="token function">ref</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token comment">// 移除这个routee</span>
        router<span class="token punctuation">.</span><span class="token function">removeRoutee</span><span class="token punctuation">(</span>ae<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token comment">// 停止这个ref</span>
        <span class="token function">context</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">stop</span><span class="token punctuation">(</span>ref<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>router.routees()返回的是个IndexSeq…和java的没有兼容和对应.<br>最开始找了一些,后来发现有<code>scala.collection.JavaConversions</code>可以做兼容…感受到了隔阂.  </p>
<p>此外ask模式,akka为java提供了<code>PatternsCS</code>.这个类返回的是java的<code>CompletionStage</code>类型相比<code>Patterns</code>,比较友好.  </p>
<h1 id="使用误区"><a href="#使用误区" class="headerlink" title="使用误区"></a>使用误区</h1><p>最容易误用的一点就是消息处理中有阻塞操作,比如直接在actor中进行数据库操作和处理网络请求,而使用的actor也没有和线程绑定,这种情况需要使用额外的线程池,这个其实和用netty是一样的情况,用来进行应用调度的线程数有限.  </p>
<p>由于上面提到的点,于是在actor内可能就会存在一些回调,一不小心就可能直接调用/改变actor内部的状态(例如在回调直接访问field).  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">></span></span> list<span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token keyword">public</span> <span class="token class-name">Receive</span> <span class="token function">createReceive</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> <span class="token function">receiveBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">match</span><span class="token punctuation">(</span><span class="token class-name">Message</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> e <span class="token operator">-></span> xxxx<span class="token punctuation">.</span><span class="token function">onSuccess</span><span class="token punctuation">(</span>r <span class="token operator">-></span> list<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span>r<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span></code></pre>
<p>这样等于内部状态被多线程访问,破坏了actor内部的状态,正确的做法是在回调中给自己发送消息.</p>
]]></content>
  </entry>
  <entry>
    <title>2019年中总结~</title>
    <url>/2019/06/11/2019-half-year-summary/</url>
    <content><![CDATA[<img src="/2019/06/11/2019-half-year-summary/tendou.png" class="">

<p>很快2019年已经过去了一半,机械般重复的工作和生活让时间有了一种停滞感,每天就像往瓶子里滴一滴水,没什么感觉,但一回过神来已经满了半瓶.  </p>
<span id="more"></span>

<h1 id="工作"><a href="#工作" class="headerlink" title="工作"></a>工作</h1><p>这半年来是真正淹没在业务的海洋里了,一个需求接着另一个需求,新业务的不断到来,旧业务的修修补补.<br>唯独剩下技术债越欠越多,甚至一些还没来得及还整个业务就消失不见了.<br>技术债往往看上去没那么迫切,代码打个补丁照样能跑,有些时候复制粘贴增减逻辑比重构来得快.<br>很多时候都能听到说”我也没办法,基础不支持,我就只能按照挫的方式做”.<br>但如果每个人把这些东西都推给其他人来,自己不动手只拘泥于已经熟悉的领域,那么身在”互联网”和身在”传统企业”又有何区别.</p>
<h2 id="中台建设的感想"><a href="#中台建设的感想" class="headerlink" title="中台建设的感想"></a>中台建设的感想</h2><p>一些功能和早年前失败下线的差不多,但因为来到了不同的时间点又有了新的希望,不过类似的东西再写一遍对开发来说也是不小的体力活.<br>为了避免这样的情况发生公司在去年中就在开始提倡”业务中台”,实施下来十几个业务中台起来提供一些通用的能力,现在新业务接入支付,订单等方便了很多节省了不少时间.<br>但相对的,在没有更多开发到来的情况,服务变多,且中台会被外部接入,同时产生一些新需求的时候,开发的压力就直接增大了.<br>毕竟新功能/业务开发才是主要的现金来源,中台这种短期看不到结果,长期来看可能带来便利的优先级相对就没那么低了,随之而来的是代码复制粘贴,修修补补,没法独立出中台,就先按业务拆解出独立的服务,而维护独立的服务又需要人手.<br>仿佛进入了一个死循环.<br>上半年手上就维护之前已经做完的两个中台应用,没有什么较大的修改,本来要做的一个也因为业务优先级直接炸了不准备做了.  </p>
<p>不过半年结束也招到一些人,希望接下去的业务压力能小一些吧.  </p>
<h2 id="新技术的投入"><a href="#新技术的投入" class="headerlink" title="新技术的投入"></a>新技术的投入</h2><p>很惨,在工作上基本没有投入什么新技术.<br>零零散散引入一些三方工具jar提升效率这种小事根本不足挂齿.<br>关于新技术在业务上的投入大家基本也是相当谨慎的态度,随着<code>postmortem</code>文化逐渐沦为空壳,取而代之的每个较大的故障都要定责甚至评审劝退,写业务代码也要(本当)小心前行,何况引入新框架呢,新技术呢.<br>这个对于业务团队来说真的挺悲哀的,不过好在一些非线上的内部应用没有这样的限制,但内部应用的访问频率和使用人数又怎能和线上相比呢.<br>逐渐陷入了两难的场景.<br>一方面使用在线上的技术必须是自己有把握可以hold住的,另一方面往往实践才能发现一些问题积累经验.  </p>
<p>一些团队开始使用<code>go</code>来写一些内部应用,对于偏业务的我个人是不太喜欢的,毕竟<code>go</code>的语法有种现代<code>C语言</code>的感觉,相比之下本身是<code>java</code>的团队,<code>.Net Core</code>应该是一种更适合的替代品.两方的阵营不融洽就是另一件事了.  </p>
<h1 id="学习"><a href="#学习" class="headerlink" title="学习"></a>学习</h1><p>零零散散看了一些书,记了一些笔记.<br>safaribooksonline也进行了一波改变,safari不怎么透出变成了learning.<br>但这半年来没有什么实实在在的收获,可能是一直在看书,但没多少实践产出的关系,自己也缺少明确的目的,只想看看自己不知道的领域.  </p>
<h2 id="elixir"><a href="#elixir" class="headerlink" title="elixir"></a>elixir</h2><p>看<code>elixir in action</code>第二版出来又看了一遍.<br>之前杂谈里也说过就不复读了.<br>应该无法用到实际中去,当长长见识了.</p>
<h2 id="rust"><a href="#rust" class="headerlink" title="rust"></a>rust</h2><p>之前都是接触带运行时/GC的语言,想看看人肉GC是如何的,ownership和lifetime等等概念具体是什么意思,而且更接近底层顺便能了解一些知识就更好了.<br>现在看官方的the book到第十章,有几节的智商真的感觉不是很够用…. …..  </p>
<p>接下去也差不多这样,继续看<code>rust</code>,其他本身一些新的东西,比如<code>RSocket</code>之类的也持续跟进.</p>
<h1 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h1><p>除了每周要回家练车也没什么变化.<br>父母依旧暗示找对象结婚.<br>上半年补了w和kabuto,动画感觉看得越来越少了,我都不好意思说自己喜欢看动画了.<br>游戏终于把买了2年多的巫师3主线通了,买的生化危机2Re玩得瑟瑟发抖以及不太敢碰了,最近买的AC奥德赛不错,体验了哪都能爬上去也不用担心摔死,游戏真好啊,每方50元的旅人买不起除外.</p>
<p>嘛,希望下半年能一切顺利吧.<br>潦草结尾要睡了…</p>
]]></content>
      <tags>
        <tag>life</tag>
      </tags>
  </entry>
  <entry>
    <title>编码之痛:操作系统迁移后redis缓存无法命中</title>
    <url>/2018/04/19/8886406/</url>
    <content><![CDATA[<p>前几天一台内网服务器从ubuntu迁移到了centos，检查一切正常后就没有太在意。  </p>
<p>今天有同事反馈迁移后的机器上的服务一个缓存总是无法获取，对比了下环境、JVM参数，尝试了war包替换等方式照样复现。<br>在有问题的机器上装了redis-cli让同事去看也是可以获取到的，一时没有了头绪。<br>打算请教其他同事，让有问题的同事把查询等发我一下，然后就看到了… …. key设置为了中文，一下子头大了… …  </p>
<span id="more"></span>
<p>检查机器的locale都没什么问题：</p>
<pre class="language-bash" data-language="bash"><code class="language-bash">$ <span class="token function">env</span> <span class="token operator">|</span><span class="token function">grep</span> <span class="token environment constant">LANG</span>
<span class="token assign-left variable"><span class="token environment constant">LANG</span></span><span class="token operator">=</span>en_US.UTF-8

$ locale
<span class="token assign-left variable"><span class="token environment constant">LANG</span></span><span class="token operator">=</span>en_US.UTF-8</code></pre>
<p>但用jinfo查看centos那台后发现被设置为了ANSI… …  </p>
<pre class="language-bash" data-language="bash"><code class="language-bash">file.encoding <span class="token operator">=</span> ANSI_X3.4-1968</code></pre>

<p>代码里一检查果然代码里没有设置charset使用了默认的，默认的获取规则:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">Charset</span> <span class="token function">defaultCharset</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>defaultCharset <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">synchronized</span> <span class="token punctuation">(</span><span class="token class-name">Charset</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">String</span> csn <span class="token operator">=</span> <span class="token class-name">AccessController</span><span class="token punctuation">.</span><span class="token function">doPrivileged</span><span class="token punctuation">(</span>
                <span class="token keyword">new</span> <span class="token class-name">GetPropertyAction</span><span class="token punctuation">(</span><span class="token string">"file.encoding"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token class-name">Charset</span> cs <span class="token operator">=</span> <span class="token function">lookup</span><span class="token punctuation">(</span>csn<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>cs <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span>
                defaultCharset <span class="token operator">=</span> cs<span class="token punctuation">;</span>
            <span class="token keyword">else</span>
                defaultCharset <span class="token operator">=</span> <span class="token function">forName</span><span class="token punctuation">(</span><span class="token string">"UTF-8"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">return</span> defaultCharset<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>加上file.encoding之后就可以了…  </p>
<p>这也暴露了两个问题，一个是代码依赖了默认编码，另一个是服务器的JVM参数设置不全面，没覆盖常见的场景。<br>还有最重要的一点…凡是用中文做key的都应该被烧死！！！！！！！</p>
<p>感觉又虚度了光阴。  </p>
<hr>
<p>UPDATE 2018/07/09<br>迁了docker之后发现生成的文件也乱码了,变成了???…后来一看是LOCALE设置的问题<br>只需要加上(zh_CN用的image不支持..):  </p>
<pre class="language-none"><code class="language-none">ENV LANG en_US.UTF-8  
ENV LANGUAGE en_US:en  
ENV LC_ALL en_US.UTF-8</code></pre>
<p>就好了</p>
]]></content>
  </entry>
  <entry>
    <title>使用反射处理依赖不兼容问题</title>
    <url>/2018/06/21/9209258/</url>
    <content><![CDATA[<p>来自于最近的一个实际场景中.<br>某个应用的依赖A依赖了B,应用A也直接依赖了B.(简化处理,实际情况还有几层)<br>如下:<br><img src="/images/1244488-20180621152133510-1799988936.png">  </p>
<span id="more"></span>
<p>也是现实中比较常见的一个依赖关系.<br>但有一个问题,应用和A用的B版本不一致,且使用的版本不兼容.<br>不兼容体现在包名发生了变更,新的B的包名被改了,之前的spring配置需要更改才能对新的B有效.<br>这样的依赖关系倒逼着应用升级,不升级会导致依赖A在运行期出现各种符号找不到的错误.(直接依赖覆盖间接依赖)<br>但在升级过程中发现新的依赖去除了一些功能使得在当前应用里无法正常工作.<br>不过运气好的是老依赖的实现在A中可以正常工作.    </p>
<p>总结一下<br>新的B在应用中无法工作,只能使用老版本里的B,A可以兼容新老的B.(那为什么不把A依赖的B降回去?那是因为其他一些应用已经接了并且升级…)  </p>
<p>那如何处理呢?<br>通过一些讨论和思考,这样的依赖应该被切断,A不能依赖B.<br><img src="/images/1244488-20180621152153271-1414230010.png">  </p>
<p>那A如何工作,答案就是使用反射.<br>A这样依赖外部提供的B(其实和maven的provide scope类似,但这里的问题是高低版本兼容….).<br>并且A根据不同的B调用不同的方法.<br>A中代码改动如下:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@PostConstruct</span>
<span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">init</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">ClassNotFoundException</span><span class="token punctuation">,</span> <span class="token class-name">NoSuchMethodException</span><span class="token punctuation">,</span> <span class="token class-name">SecurityException</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">Class</span> clazz <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
    <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
        clazz <span class="token operator">=</span> <span class="token class-name">Class</span><span class="token punctuation">.</span><span class="token function">forName</span><span class="token punctuation">(</span><span class="token string">"com.b.package1"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">ClassNotFoundException</span> e<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        LOG<span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"init - com.b.package1 failed"</span><span class="token punctuation">,</span> e<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>clazz <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
            clazz <span class="token operator">=</span> <span class="token class-name">Class</span><span class="token punctuation">.</span><span class="token function">forName</span><span class="token punctuation">(</span><span class="token string">"com.b.package2"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">ClassNotFoundException</span> e<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            LOG<span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"init - com.b.package2 failed"</span><span class="token punctuation">,</span> e<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>clazz <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">ClassNotFoundException</span><span class="token punctuation">(</span><span class="token string">"dependency not found. please add b "</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    usedMethod<span class="token operator">=</span> clazz<span class="token punctuation">.</span><span class="token function">getMethod</span><span class="token punctuation">(</span><span class="token string">"methodName"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    bean<span class="token operator">=</span> applicationContext<span class="token punctuation">.</span><span class="token function">getBean</span><span class="token punctuation">(</span><span class="token string">"beanName"</span><span class="token punctuation">,</span> clazz<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>bean<span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">UnsatisfiedDependencyException</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"beanName"</span><span class="token punctuation">,</span>
                <span class="token string">"bean"</span><span class="token punctuation">,</span> <span class="token string">"failed to init"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>ps:当前问题因为记录需要被简化了,实际中A作为一个偏底层的依赖去直接依赖应用方直接使用的B本身是不合理的,这个做法纯粹是为了解决眼下的兼容性问题.<br>其次在日常升级中,实现不兼容,方法签名变更等升级大版本号之类就足够了,但如果整个依赖的基础包名都改了的话,最好还是修改artifactId吧…. …</p>
]]></content>
  </entry>
  <entry>
    <title>记一次令人窒息的线上fullgc调优</title>
    <url>/2018/06/21/9211629/</url>
    <content><![CDATA[<p>今天第二篇采坑了… …<br>现场因为处理太急促没有保留,而且是一旁协助,没有收集到所有信息实在是有些遗憾…只能靠记忆回想一些细节  </p>
<hr>
<span id="more"></span>
<p>情况是一台服务器一启动就开始full gc,短短1分钟可以有几十次的full gc.<br>主要几个配置参数为<code>-Xmx2g</code> <code>-XX:NewRatio=3</code>(CMS相关和其他的和这次无关不列了) 很简单的参数.<br>下意识dump了heap,结果并没什么特别明显的问题.<br>在想是不是内存不够,加到3g还是一样的问题.  </p>
<p>那似乎只能看是不是业务代码有内存泄漏了.<br>用<code>jmap -histo:live</code>看了下也是很常见的一些类(而且上面还做过heap分析).  </p>
<p>开始陷入沉思,突然发现了一个诡异的点…<code>jmap -heap</code> old区很小… …多试了几次依旧是很小…<br>这不科学 </p>
<p>用<code>jstat -gcold</code>和<code>jstat -gcnew</code>一看….old的容量只有400M 而young的只有100多M 差不多是设置的newRatio的值<br>使用<code>jcmd pid VM.flags</code>一看,发现<code>-XX:MaxNewSize</code>被设置成了100多M… …就算是Xmx2g,实际使用才400多M,这点内存完全不能满足应用的正常使用.<br><img src="/images/1244488-20180622013849504-1123204511.png"></p>
<p>之后添加了<code>-Xms</code>解决.(这点存疑 估计还修改了其他配置 见下面分析)  </p>
<p>但其实还是有些问题遗留,默认的MaxNewSize为什么只有100多M…<br>简单的分析,MaxNewSize没有默认设置,那他的值来源于Xmx和NewRatio(Xmx / (newRatio + 1)),验证如下:<br><code>java -XX:NewRatio=3 -XX:+PrintFlagsFinal -version | grep New</code>(不去连线上了..本地操作 windows下用findstr)<br><img src="/images/1244488-20180622020301776-186647314.png"><br>的确如此.  </p>
<p>线上出问题的只有100多M,也就是Xmx参数可能被错误配置或者被覆盖了(模拟后面的Xmx覆盖了前面的)<br><img src="/images/1244488-20180622020355675-166184672.png">  </p>
<p>最后的配置修改不是亲手做的一些项也无法确定了…  </p>
<hr>
<p>不过这次发现的问题也很明显<br>首先是监控,这台服务的gc日志没有被收集导致一开始没有历史记录(图1),有的话一眼就看出heap过小.<br>其次是思维定势了,遇到fgc想当然先去分析dump,浪费了时间.  </p>
<hr>
<p>总结一下这次使用的命令:<br>寻找java进程:<br><code>jps</code>  </p>
<p>查看gc情况(最后两个数字是间隔ms和打印次数):<br><code>jstat -gcutil|gcold|gcnew pid 1000 10</code>   </p>
<p>查看系统属性:<br><code>jcmd process_id VM.system_properties</code>  </p>
<p>JVM启动用的命令行:<br><code>jcmd process_id VM.command_line</code>  </p>
<p>显示调优标志:<br><code>jcmd process_id VM.flags [-all]</code></p>
<p>heap dump:<br><code>jmap -dump:format=b,file=test.bin pid</code>  </p>
<p>查看堆使用情况:<br><code>jmap -heap pid</code>  </p>
<p>查看存活对象:<br><code>jmap -histo:live pid</code>  </p>
<p>查看默认配置:<br><code>java -XX:+PrintFlagsFinal -version | grep HeapSize</code>  </p>
<hr>
<p>参考资料:<br><a href="https://docs.oracle.com/cd/E19900-01/819-4742/abeik/index.html">https://docs.oracle.com/cd/E19900-01/819-4742/abeik/index.html</a><br><a href="https://stackoverflow.com/questions/4667483/how-is-the-default-java-heap-size-determined">https://stackoverflow.com/questions/4667483/how-is-the-default-java-heap-size-determined</a><br><a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/tooldescr007.html">https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/tooldescr007.html</a></p>
]]></content>
  </entry>
  <entry>
    <title>微服务模式笔记:服务分解策略</title>
    <url>/2019/02/12/Microservices-Patterns-ch2-notes/</url>
    <content><![CDATA[<p>Microservice Patterns第二章的读书笔记<br>原章节链接: <a href="https://learning.oreilly.com/library/view/microservices-patterns/9781617294549/kindle_split_010.html">https://learning.oreilly.com/library/view/microservices-patterns/9781617294549/kindle_split_010.html</a>  </p>
<span id="more"></span>
<p>Decomposition strategies </p>
<p>微服务最关键的挑战 也就是微服务的本质是如何将应用的功能分解到服务中去<br>服务是业务相关而不是技术相关</p>
<h1 id="2-1-What-is-the-microservice-architecture-exactly"><a href="#2-1-What-is-the-microservice-architecture-exactly" class="headerlink" title="2.1. What is the microservice architecture exactly?"></a>2.1. What is the microservice architecture exactly?</h1><h2 id="2-1-1-What-is-software-architecture-and-why-does-it-matter"><a href="#2-1-1-What-is-software-architecture-and-why-does-it-matter" class="headerlink" title="2.1.1. What is software architecture and why does it matter?"></a>2.1.1. What is software architecture and why does it matter?</h2><p>应用架构是将应用分解成各个部分(元素)和这些部分间的关系</p>
<p>软件架构的4+1 view model<br>简单理解就是从几个角度来描述架构<br><img src="/images/1244488-20190213011614249-992071651.png"></p>
<p><a href="www.cs.ubc.ca/~gregor/teaching/papers/4+1view-architecture.pdf">www.cs.ubc.ca/~gregor/teaching/papers/4+1view-architecture.pdf</a></p>
<ul>
<li>Logical view : 开发者创建的软件元素。就像类和包 类之间的继承 关联和依赖</li>
<li>Implementataion view: 构建系统的产出。比如java是jar或者war</li>
<li>Process view:运行时组件.每个元素都是一个进程，和各个进程间的进程间通信。</li>
<li>Deployment：进程是如何映射到机器的。</li>
</ul>
<p>架构对于功能需求基本没有什么帮助<br>但架构又是重要的 因为他让应用满足第二种需求: 服务质量(quality of service,QoS)需求</p>
<h2 id="2-1-2-Overview-of-architectural-styles"><a href="#2-1-2-Overview-of-architectural-styles" class="headerlink" title="2.1.2. Overview of architectural styles"></a>2.1.2. Overview of architectural styles</h2><p>一种特定的架构提供有限的元素(组件)调色盘和关系(connectors)<br>你从中定义出应用架构的视图</p>
<h3 id="分层架构"><a href="#分层架构" class="headerlink" title="分层架构"></a>分层架构</h3><p>定义了三层:</p>
<ul>
<li>Presentation layer: 包含实现了用户界面和额外的API的代码</li>
<li>Business logic layer： 包含业务逻辑</li>
<li>Persistence layer：包含和数据库交互的逻辑</li>
</ul>
<p>但是也有明显的缺点：</p>
<ul>
<li>单一的展示层</li>
<li>单一的持久层</li>
<li>定义的业务逻辑依赖于持久层 离开数据库无法测试<br>会存在依赖倒置，业务层定义的数据访问接口最后需要持久层定义DAO类去实现(低层依赖高层所提供的接口)</li>
</ul>
<h3 id="六边形架构"><a href="#六边形架构" class="headerlink" title="六边形架构"></a>六边形架构</h3><p>将业务放在了最中心<br><code>inbound adapter</code><br><code>outbound adapter</code><br>业务逻辑不依赖于adapter 相反是adapter依赖于业务逻辑<br><img src="/images/1244488-20190213010815889-2062967823.png">  </p>
<p>一个业务逻辑有一个或多个<code>ports</code><br>一个<code>port</code>定义了一系列业务逻辑和外部交互的操作</p>
<p>六边形架构最大的好处是将展示层和数据访问逻辑从业务逻辑中解耦</p>
<h2 id="2-1-3-The-microservice-architecture-is-an-architectural-style"><a href="#2-1-3-The-microservice-architecture-is-an-architectural-style" class="headerlink" title="2.1.3. The microservice architecture is an architectural style"></a>2.1.3. The microservice architecture is an architectural style</h2><p>单体架构在implement view就是一个单独可执行的jar或war<br><a href="http://microservices.io/patterns/monolithic.html">http://microservices.io/patterns/monolithic.html</a></p>
<p><a href="http://microservices.io/patterns/microservices.html">http://microservices.io/patterns/microservices.html</a><br><img src="/images/1244488-20190213010839549-1161206549.png"></p>
<h3 id="什么是服务"><a href="#什么是服务" class="headerlink" title="什么是服务"></a>什么是服务</h3><p>服务是独立的 无依赖的可部署软件组件 实现了一些有用的功能<br><img src="/images/1244488-20190213010850162-1923323128.png"></p>
<p>不同服务间只能通过API调用 不像单体还可以通过代码</p>
<h3 id="松耦合"><a href="#松耦合" class="headerlink" title="松耦合"></a>松耦合</h3><p><a href="https://en.wikipedia.org/wiki/Loose_coupling">https://en.wikipedia.org/wiki/Loose_coupling</a><br>服务间的交互只通过API 封装细节</p>
<h3 id="共享库"><a href="#共享库" class="headerlink" title="共享库"></a>共享库</h3><p>只在不太可能会发生变化的地方使用共享库<br>和具体业务无关 通用的功能上</p>
<h3 id="服务的大小不重要"><a href="#服务的大小不重要" class="headerlink" title="服务的大小不重要"></a>服务的大小不重要</h3><p>和组织结构相关</p>
<h1 id="2-2-Defining-an-application’s-microservice-architecture"><a href="#2-2-Defining-an-application’s-microservice-architecture" class="headerlink" title="2.2. Defining an application’s microservice architecture"></a>2.2. Defining an application’s microservice architecture</h1><p>3步定义应用的微服务架构<br>这里只是展示一个例子 现实中会有更多的问题<br>需要迭代等<br><img src="/images/1244488-20190213010902827-949019283.png">  </p>
<p>第一步找到服务的具体操作 这里定义为了<code>system operation</code><br>其实也就是找到各个请求和回应(或者找到各个事件)<br>服务拆分的一些阻碍:  </p>
<ul>
<li>网络延迟</li>
<li>服务间的同步调用降低了可用性</li>
<li>在不同服务间维持数据一致性</li>
</ul>
<h2 id="2-2-1-Identifying-the-system-operations"><a href="#2-2-1-Identifying-the-system-operations" class="headerlink" title="2.2.1. Identifying the system operations"></a>2.2.1. Identifying the system operations</h2><p>从应用的需求出发 包括user story和对应的用户场景</p>
<p>有两种类型的<code>system operation</code><br>commands:创建 更新和删除数据<br>queries: 查询数据</p>
<p>分析user stories和场景中的动词<br>一个例子:<br><img src="/images/1244488-20190213011117033-940471440.png"></p>
<h2 id="2-2-2-Defining-services-by-applying-the-Decompose-by-business-capability-pattern"><a href="#2-2-2-Defining-services-by-applying-the-Decompose-by-business-capability-pattern" class="headerlink" title="2.2.2. Defining services by applying the Decompose by business capability pattern"></a>2.2.2. Defining services by applying the Decompose by business capability pattern</h2><p>通过业务能力分解<br><a href="http://microservices.io/patterns/decomposition/decompose-by-business-capability.html">http://microservices.io/patterns/decomposition/decompose-by-business-capability.html</a>  </p>
<p><img src="/images/1244488-20190213011136593-1822789472.png"></p>
<p>业务能力 子能力以及与服务的对应</p>
<p>但业务变迁<br>频繁的RPC带来的性能损耗<br>会带来一定的阻碍  </p>
<h2 id="2-2-3-Defining-services-by-applying-the-Decompose-by-sub-domain-pattern"><a href="#2-2-3-Defining-services-by-applying-the-Decompose-by-sub-domain-pattern" class="headerlink" title="2.2.3. Defining services by applying the Decompose by sub-domain pattern"></a>2.2.3. Defining services by applying the Decompose by sub-domain pattern</h2><p>DDD<br>subdmain<br>bounded context<br><a href="http://microservices.io/patterns/decomposition/decompose-by-subdomain.html">http://microservices.io/patterns/decomposition/decompose-by-subdomain.html</a></p>
<p><img src="/images/1244488-20190213011153564-1888824784.png"></p>
<h2 id="2-2-4-Decomposition-guidelines"><a href="#2-2-4-Decomposition-guidelines" class="headerlink" title="2.2.4. Decomposition guidelines"></a>2.2.4. Decomposition guidelines</h2><p>一些分解的原则<br>可以沿用一些OO里的原则</p>
<h3 id="单一职责原则"><a href="#单一职责原则" class="headerlink" title="单一职责原则"></a>单一职责原则</h3><h3 id="共同封闭原则-Common-Closure-Principle"><a href="#共同封闭原则-Common-Closure-Principle" class="headerlink" title="共同封闭原则(Common Closure Principle)"></a>共同封闭原则(Common Closure Principle)</h3><p>因为一个相同的原因导致两个类需要修改 这两个类应该在一个包下<br>对应于微服务 把要因为同一个原因修改的组件放到同一个服务里去</p>
<p>通过业务能力和子域 结合SRP和CCP是一个很好的分解一个应用到服务的技术<br>为了应用他们成功地开发微服务架构 你必须解决一些事务管理和跨进程通信问题</p>
<h2 id="2-2-5-Obstacles-to-decomposing-an-application-into-services"><a href="#2-2-5-Obstacles-to-decomposing-an-application-into-services" class="headerlink" title="2.2.5. Obstacles to decomposing an application into services"></a>2.2.5. Obstacles to decomposing an application into services</h2><p>一些阻碍:</p>
<ul>
<li>网络延迟</li>
<li>同步通信导致的可用性降低</li>
<li>跨服务的数据一致性维护</li>
<li>获取数据的一致性视图</li>
<li>上帝类阻止分解</li>
</ul>
<h3 id="网络延迟"><a href="#网络延迟" class="headerlink" title="网络延迟"></a>网络延迟</h3><p>网络延迟是分布式系统长期困扰的问题.<br>服务拆解之后可能会发现有大量的服务间的往返通信<br>可以提供批处理API在一次往返中<br>但一些其他场景 可能就需要整合服务了</p>
<h3 id="同步通信导致的可用性降低"><a href="#同步通信导致的可用性降低" class="headerlink" title="同步通信导致的可用性降低"></a>同步通信导致的可用性降低</h3><p>改进方法是异步消息传输</p>
<h3 id="跨服务的数据一致性维护"><a href="#跨服务的数据一致性维护" class="headerlink" title="跨服务的数据一致性维护"></a>跨服务的数据一致性维护</h3><p>涉及到分布式事务 saga 最终一致性</p>
<h3 id="获取数据的一致性视图"><a href="#获取数据的一致性视图" class="headerlink" title="获取数据的一致性视图"></a>获取数据的一致性视图</h3><p>实践中一般不会是太大的问题</p>
<h3 id="上帝类阻止分解"><a href="#上帝类阻止分解" class="headerlink" title="上帝类阻止分解"></a>上帝类阻止分解</h3><p>上帝类(god classes)是一个庞大的类 在整个项目中被贯穿使用<br>一般都是为了实现了一个应用不同方面的业务逻辑 有大量的field<br><img src="/images/1244488-20190213011219565-1974762110.png"></p>
<p>方案1:<br>提供一个库 并创建一个中央的order数据库<br>问题是紧耦合…所有的服务都要连到这个库</p>
<p>方案2:<br>封装order数据库到order服务<br>这样就把他变为了类似数据库API的服务 只是为了提供order数据库的访问而存在</p>
<p>方案3:<br>使用DDD 将order的概念分解 各个子域有自己的order<br><img src="/images/1244488-20190213011237612-887560591.png"></p>
<p>但这样会影响用户体验 在用户体验和多个实际模型间要有一种翻译</p>
<h2 id="2-2-6-Defining-service-APIs"><a href="#2-2-6-Defining-service-APIs" class="headerlink" title="2.2.6. Defining service APIs"></a>2.2.6. Defining service APIs</h2><p>定义服务API<br>他的操作和事件</p>
<p>基于两点:<br>1.<code>System operation</code>需要<br>这个API可能是被客户端直接调用 也可能是服务端直接调用 用来完成这个服务里的某个具体操作  </p>
<p>2.支持服务间协作<br>为其他服务的实现提供一些帮助</p>
]]></content>
  </entry>
  <entry>
    <title>使用异步任务降低API延迟_实践总结</title>
    <url>/2018/02/13/async-api-latency/</url>
    <content><![CDATA[<p>之前在想如何降低API的延迟，这些API里有几个比较耗时的操作且是串行执行，那通过异步执行的方式理论上可以降低运行的时间，如下图所示：<br><img src="/images/1244488-20180213120710156-2118264085.png">  </p>
<span id="more"></span>
<p>具体的实现比较简单，例如这样：</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">ParallelRetrievalExample</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">final</span> <span class="token class-name">CacheRetriever</span> cacheRetriever<span class="token punctuation">;</span>
    <span class="token keyword">final</span> <span class="token class-name">DBRetriever</span> dbRetriever<span class="token punctuation">;</span>
    <span class="token class-name">ParallelRetrievalExample</span><span class="token punctuation">(</span><span class="token class-name">CacheRetriever</span> cacheRetriever<span class="token punctuation">,</span>
                             <span class="token class-name">DBRetriever</span> dbRetriever<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">this</span><span class="token punctuation">.</span>cacheRetriever <span class="token operator">=</span> cacheRetriever<span class="token punctuation">;</span>
        <span class="token keyword">this</span><span class="token punctuation">.</span>dbRetriever <span class="token operator">=</span> dbRetriever<span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">public</span> <span class="token class-name">Object</span> <span class="token function">retrieveCustomer</span><span class="token punctuation">(</span><span class="token keyword">final</span> <span class="token keyword">long</span> id<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">final</span> <span class="token class-name">CompletableFuture</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Object</span><span class="token punctuation">></span></span> cacheFuture <span class="token operator">=</span>
           <span class="token class-name">CompletableFuture</span><span class="token punctuation">.</span><span class="token function">supplyAsync</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
                    <span class="token keyword">return</span> cacheRetriever<span class="token punctuation">.</span><span class="token function">getCustomer</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">final</span> <span class="token class-name">CompletableFuture</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Object</span><span class="token punctuation">></span></span> dbFuture <span class="token operator">=</span>
           <span class="token class-name">CompletableFuture</span><span class="token punctuation">.</span><span class="token function">supplyAsync</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
                    <span class="token keyword">return</span> dbRetriever<span class="token punctuation">.</span><span class="token function">getCustomer</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">return</span> <span class="token class-name">CompletableFuture</span><span class="token punctuation">.</span><span class="token function">anyOf</span><span class="token punctuation">(</span>
            cacheFuture<span class="token punctuation">,</span> dbFuture<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span></code></pre>
<p>用java8引入的<code>CompletableFuture</code>即可。</p>
<p>这里不再赘述。  </p>
<p>主要讲一下这样实践遇到的坑和一些自己的理解。</p>
<h1 id="性能测试"><a href="#性能测试" class="headerlink" title="性能测试"></a>性能测试</h1><p>优化后的代码需要和未修改(基准)的版本做比较，要考虑在不同负载下的性能情况。<br>针对API的修改可以使用AB工具，比较方便，能通过设定不同的并发用户模拟不同的负载。<br>测试是必要的，很多直觉上会提高性能的点可能会在实际表现上收到资源的限制等原因无法提高甚至不如优化前的性能。</p>
<h1 id="适合处理的任务-amp-线程池的设定"><a href="#适合处理的任务-amp-线程池的设定" class="headerlink" title="适合处理的任务 &amp; 线程池的设定"></a>适合处理的任务 &amp; 线程池的设定</h1><h2 id="我们要优化怎样的任务呢？"><a href="#我们要优化怎样的任务呢？" class="headerlink" title="我们要优化怎样的任务呢？"></a>我们要优化怎样的任务呢？</h2><p>任务也就三大分类，计算密集，IO密集和混合，其中混合里面也可以通过细化变为前两类。<br>在一般的web开发中计算不太会成为瓶颈，主要是IO。<br>一些耗时的阻塞IO操作（数据库，RPC调用）往往是导致接口慢的原因，这里要优化的就是这类操作。<br>不过与其说是优化，更恰当的说法是让这些阻塞操作异步化，缩短整体的时间，这里也要注意这些任务所在的位置，如果在API的最后面的逻辑里那优化他们也没什么必要，或者在不影响业务逻辑的情况下可以把他们置前。</p>
<h2 id="我们需要的怎样的线程池？"><a href="#我们需要的怎样的线程池？" class="headerlink" title="我们需要的怎样的线程池？"></a>我们需要的怎样的线程池？</h2><p>如上所说要优化的任务几乎都是阻塞IO，也就意味着这些任务占用CPU的时间很短，主要是处在waiting状态下，这种线程的增加最大的开销就是内存，对上下文切换影响较小。<br>其次，线程数必定要有限，java的线程过于重量，不考虑CPU因素也需要考虑内存因素。<br>最后还要考虑线程池耗尽的情况，最差的情况是回到没优化之前，也就是在调用者线程上执行。</p>
<p><code>CompletableFuture</code>的<code>runAsync</code>和<code>supplyAsync</code>方法有不带<code>Executor</code>的版本，首先看一下默认的线程池是否合适。  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Executor</span> asyncPool <span class="token operator">=</span> useCommonPool <span class="token operator">?</span>
    <span class="token class-name">ForkJoinPool</span><span class="token punctuation">.</span><span class="token function">commonPool</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">:</span> <span class="token keyword">new</span> <span class="token class-name">ThreadPerTaskExecutor</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p><code>useCommonPool</code>的判断是根据<code>ForkJoinPool</code>的并行度，可以简单地先认为多核下会返回true(也可以通过<code>java.util.concurrent.ForkJoinPool.common.parallelism</code>参数进行设定)。<br>而使用的<code>commonPool()</code>线程数量不是很多（默认和CPU核数相等），其次<code>ForkJoinPool</code>是设计用于短任务的运行，不适合做阻塞IO，我们要优化的主要慢操作几乎都是阻塞IO带来的。  </p>
<p>接下去看需求比较接近的<code>Executors.newFixedThreadPool</code>，但通过实现不难发现他的队列是无界的，如果线程耗尽新的任务就会等待，也无法使用拒绝策略。</p>
<p>只有定制了，根据上面说到的需求，定制如下：</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">ThreadPoolExecutor</span> IO <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ThreadPoolExecutor</span><span class="token punctuation">(</span><span class="token number">20</span><span class="token punctuation">,</span> <span class="token number">20</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token class-name">TimeUnit</span><span class="token punctuation">.</span>MILLISECONDS<span class="token punctuation">,</span>
            <span class="token keyword">new</span> <span class="token class-name">SynchronousQueue</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Runnable</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
            <span class="token keyword">new</span> <span class="token class-name">CallerRunsPolicy</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>线程数量定长，数量的多少可以根据测试情况做下调整，使用<code>SynchronousQueue</code>不产生队列，拒绝策略使用在调用者线程上运行，满足了所需。  </p>
<p>这个线程池专门为IO密集任务使用，不要让计算密集的代码使用。<br>在实践中遇到了使用这种方式结果测试时性能降低了5倍左右的情况，一看代码中除了从数据库获取数据还有几个for循环在做修改字段的工作，导致上下文切换带来了很大的开销。</p>
<h1 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h1><p>上述实现中，限制线程数量的原因是因为线程的开销（这里主要是在内存上）过大，这就意味着在这里使用了线程过重了，更好的实现应该使用类似绿色线程的技术，和系统线程进行1对多的映射。<br>此外这种场景下用事件驱动的方式可能会更好。<br>追究其核心原因还是java世界中同步阻塞操作还是占多数，而主要的优化手段底层还是使用了昂贵的线程，一些在其他语言/平台上很容易实现的扩展在java上就会遇到问题。<br>此外，异步没有得到语言上的支持，造成异步编程在java上比较麻烦和显式，这点<code>C#</code>的<code>async</code>和<code>await</code>语法糖就要甜的多。<br>java之后的发展还是任重而道远啊。</p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p><a href="https://httpd.apache.org/docs/2.4/programs/ab.html">apache ab</a>  </p>
<p><a href="https://www.safaribooksonline.com/library/view/reactive-design-patterns/9781617291807/">reactive design pattern</a> 上述的图和<code>ParallelRetrievalExample</code>代码取自这里  </p>
<p><a href="http://www.cnblogs.com/ktgu/p/3529144.html">多线程的代价及上下文切换</a></p>
<p><a href="http://colobu.com/2016/02/29/Java-CompletableFuture/">Java CompletableFuture 详解</a></p>
<p><a href="http://jolestar.com/parallel-programming-model-thread-goroutine-actor/">并发之痛 Thread，Goroutine，Actor</a></p>
]]></content>
  </entry>
  <entry>
    <title>AWS安装cloudwatch agent后没有指标出现解决</title>
    <url>/2024/06/05/aws_cloudwatch_agent_no_metric/</url>
    <content><![CDATA[<p>EC2的指标没有内存相关,而内存是个很重要的指标,要在cloudwatch中监控需要使用cloudwatch agent(CWAgent), 照着官方文档做结果发现没有指标… 最后发现文档里少说了一步</p>
<span id="more"></span>

<p>只要按着这个步骤就行: <a href="https://docs.aws.amazon.com/zh_cn/AmazonCloudWatch/latest/monitoring/installing-cloudwatch-agent-commandline.html">使用命令行安装 CloudWatch 代理</a><br>大致分为3步：</p>
<ol>
<li>EC2上安装cloudwatch agent</li>
<li>创建IAM账号</li>
<li>配置/启动</li>
</ol>
<p>基本就是安装完 创建完IAM角色 再执行以下命令就完成了:</p>
<pre class="language-bash" data-language="bash"><code class="language-bash">/opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-config-wizard

/opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl -a fetch-config -m ec2 -s -c file:/opt/aws/amazon-cloudwatch-agent/bin/config.json</code></pre>

<p>配置时要注意下默认项是否匹配.</p>
<p>正常来说在cloudwatch的metric下会增加一个CWAgent的自定义命名空间.<br>但实际上没有.. ..<br>这时候需要查看日志:   </p>
<pre class="language-none"><code class="language-none">tail -f &#x2F;opt&#x2F;aws&#x2F;amazon-cloudwatch-agent&#x2F;logs&#x2F;amazon-cloudwatch-agent.log</code></pre>
<p>如果你看到了:</p>
<pre class="language-json" data-language="json"><code class="language-json"><span class="token punctuation">&#123;</span><span class="token property">"caller"</span><span class="token operator">:</span><span class="token string">"ec2tagger/ec2tagger.go:465"</span><span class="token punctuation">,</span><span class="token property">"msg"</span><span class="token operator">:</span><span class="token string">"ec2tagger: Unable to describe ec2 tags for initial retrieval"</span><span class="token punctuation">,</span><span class="token property">"kind"</span><span class="token operator">:</span><span class="token string">"processor"</span><span class="token punctuation">,</span><span class="token property">"name"</span><span class="token operator">:</span><span class="token string">"ec2tagger"</span><span class="token punctuation">,</span><span class="token property">"pipeline"</span><span class="token operator">:</span><span class="token string">"metrics/host"</span><span class="token punctuation">,</span><span class="token property">"error"</span><span class="token operator">:</span><span class="token string">"NoCredentialProviders: no valid providers in chain\ncaused by: EnvAccessKeyNotFound: failed to find credentials in the environment.\nSharedCredsLoad: failed to load profile, .\nEC2RoleRequestError: no EC2 instance role found\ncaused by: EC2MetadataError: failed to make EC2Metadata request\n&lt;?xml version=\"1.0\" encoding=\"iso-8859-1\"?>\n&lt;!DOCTYPE html PUBLIC \"-//W3C//DTD XHTML 1.0 Transitional//EN\"\n\t\t \"http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd\">\n&lt;html xmlns=\"http://www.w3.org/1999/xhtml\" xml:lang=\"en\" lang=\"en\">\n &lt;head>\n  &lt;title>404 - Not Found&lt;/title>\n &lt;/head>\n &lt;body>\n  &lt;h1>404 - Not Found&lt;/h1>\n &lt;/body>\n&lt;/html>\n\n\tstatus code: 404, request id: "</span><span class="token punctuation">&#125;</span></code></pre>

<p>那大概率和我遇上了一样的问题<br>那就是EC2实例没和创建的IAM角色关联… …<br>需要在EC2设置里关联上按文档里创建的IAM角色即可… …<br>其他错误也对照着报错改就行, 比如没装<code>collectd</code>导致启动出错等  </p>
<p>很神奇…文档里居然漏了这一步..还是这一步太基础不用写到文档里去呢…</p>
]]></content>
      <tags>
        <tag>aws</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Azure openai java SDK序列化/反序列化message</title>
    <url>/2024/06/04/azure-openai-java-sdk-message-erialization-deserialization/</url>
    <content><![CDATA[<p>有个功能需要在数据库中配置模板将用户输入填入占位符, 因为要配置多轮message以及各个参数,使用自己配置的方式会比较麻烦,就想着把<code>ChatCompletionsOptions</code>整个序列化到数据库里,然后拿出来再填入占位符.<br>但是直接使用jackson会导致对于user的<code>ChatRequestUserMessage</code>会序列化和反序列化都失败.  </p>
<span id="more"></span>
<p>序列化会变为: </p>
<pre class="language-json" data-language="json"><code class="language-json"> <span class="token punctuation">&#123;</span>
    <span class="token property">"role"</span><span class="token operator">:</span> <span class="token string">"user"</span><span class="token punctuation">,</span>
    <span class="token property">"content"</span><span class="token operator">:</span> <span class="token punctuation">&#123;</span>
        <span class="token property">"length"</span><span class="token operator">:</span> <span class="token number">22709</span><span class="token punctuation">,</span>
        <span class="token property">"replayable"</span><span class="token operator">:</span> <span class="token boolean">true</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>
    <span class="token property">"name"</span><span class="token operator">:</span> <span class="token null keyword">null</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>因为整个类型的定义:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">final</span> <span class="token keyword">class</span> <span class="token class-name">ChatRequestUserMessage</span> <span class="token keyword">extends</span> <span class="token class-name">ChatRequestMessage</span> <span class="token punctuation">&#123;</span>

    <span class="token comment">/*
     * The contents of the user message, with available input types varying by selected model.
     */</span>
    <span class="token annotation punctuation">@Generated</span>
    <span class="token annotation punctuation">@JsonProperty</span><span class="token punctuation">(</span>value <span class="token operator">=</span> <span class="token string">"content"</span><span class="token punctuation">)</span>
    <span class="token keyword">private</span> <span class="token class-name">BinaryData</span> content<span class="token punctuation">;</span></code></pre>

<p>content是个BinaryData<br>自己写openai的格式反序列化也一样  </p>
<p>解决方法很简单,因为引入了微软自己的BinaryData 使用这个进行序列化和反序列化就行了:<br>反序列化例子:</p>
<pre class="language-none"><code class="language-none">String template &#x3D; &quot;&quot;&quot;
        &#123;
            &quot;messages&quot;: [
                &#123;
                    &quot;role&quot;: &quot;system&quot;,
                    &quot;content&quot;: &quot;system message&quot;,
                    &quot;name&quot;: null
                &#125;,
                &#123;
                    &quot;role&quot;: &quot;user&quot;,
                    &quot;content&quot;: [
                        &#123;
                            &quot;type&quot;: &quot;text&quot;,
                            &quot;text&quot;:&quot;user message&quot;
                        &#125;,
                        &#123;
                            &quot;type&quot;: &quot;image_url&quot;,
                            &quot;image_url&quot;: &#123;&quot;url&quot;: &quot;%s&quot;&#125;
                        &#125;
                    ]
                &#125;
            ],
            &quot;max_tokens&quot;: 2048,
            &quot;temperature&quot;: null,
            &quot;top_p&quot;: null,
            &quot;logit_bias&quot;: null,
            &quot;user&quot;: null,
            &quot;n&quot;: null,
            &quot;stop&quot;: null,
            &quot;presence_penalty&quot;: null,
            &quot;frequency_penalty&quot;: null,
            &quot;stream&quot;: null,
            &quot;model&quot;: &quot;gpt-4o-2024-05-13&quot;,
            &quot;functions&quot;: null,
            &quot;function_call&quot;: null,
            &quot;data_sources&quot;: null,
            &quot;enhancements&quot;: null,
            &quot;seed&quot;: null,
            &quot;response_format&quot;: null,
            &quot;tools&quot;: null,
            &quot;tool_choice&quot;: null,
            &quot;logprobs&quot;: null,
            &quot;top_logprobs&quot;: null
        &#125;
        &quot;&quot;&quot;.formatted(imageBase64);

final ChatCompletionsOptions chatCompletionsOptions &#x3D; BinaryData.fromString(template).toObject(ChatCompletionsOptions.class);
</code></pre>

<p>不过试验了下还是会有点问题…因为用了<code>BinaryData</code>所以反序列化它没法识别是<code>List</code>还是<code>String</code>,最后全部变成了<code>String</code>结构破坏了.<br>提了一个issue: <a href="https://github.com/Azure/azure-sdk-for-java/issues/40481">[BUG] The result is inconsistent after serializing and deserializing ChatRequestUserMessage</a>  </p>
<p>弄了个不怎么优雅的临时解法:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">void</span> <span class="token function">fixMessage</span><span class="token punctuation">(</span><span class="token class-name">ChatCompletionsOptions</span> options<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">boolean</span> changed <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">ChatRequestMessage</span><span class="token punctuation">></span></span> messages <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">ChatRequestMessage</span> message <span class="token operator">:</span> options<span class="token punctuation">.</span><span class="token function">getMessages</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>message <span class="token keyword">instanceof</span> <span class="token class-name">ChatRequestUserMessage</span> u<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">final</span> <span class="token class-name">BinaryData</span> content <span class="token operator">=</span> u<span class="token punctuation">.</span><span class="token function">getContent</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
                <span class="token keyword">final</span> <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">ChatMessageContentItem</span><span class="token punctuation">></span></span> items <span class="token operator">=</span> content<span class="token punctuation">.</span><span class="token function">toObject</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">TypeReference</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                messages<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">ChatRequestUserMessage</span><span class="token punctuation">(</span>items<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                changed <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
                <span class="token keyword">continue</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Exception</span> ignored<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span>
        messages<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span>message<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>changed<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">Field</span> messagesField <span class="token operator">=</span> <span class="token class-name">ChatCompletionsOptions</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"messages"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            messagesField<span class="token punctuation">.</span><span class="token function">setAccessible</span><span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            messagesField<span class="token punctuation">.</span><span class="token function">set</span><span class="token punctuation">(</span>options<span class="token punctuation">,</span> messages<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Exception</span> e<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            log<span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"fixMessage - set message error"</span><span class="token punctuation">,</span> e<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>就先这样吧… …</p>
<p>参考: <a href="https://github.com/Azure/azure-sdk-for-java/issues/36077">[openAI] Serialized Storage Context</a></p>
]]></content>
      <tags>
        <tag>openai</tag>
        <tag>azure</tag>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Azure openai java SDK在流式输出时处理functioncall</title>
    <url>/2023/11/14/azure-openai-java-sdk-stream-function-handle/</url>
    <content><![CDATA[<p>emmm,新的功能和api都出了,还在写旧格式的感觉有点过时。<br>之前都是用非流式的使用function, 刚好遇到这个处理了下顺便记录一下。</p>
<span id="more"></span>

<p>流式的function也是分段返回的, 和content类似, 举个例子:</p>
<pre class="language-none"><code class="language-none">data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;role&quot;:&quot;assistant&quot;,&quot;function_call&quot;:&#123;&quot;name&quot;:&quot;urlContent&quot;,&quot;arguments&quot;:&quot;&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;&#123;\n&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot; &quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot; \&quot;&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;url&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;\&quot;:&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot; \&quot;&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;www&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;.baidu&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;.com&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;\&quot;\n&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;function_call&quot;:&#123;&quot;arguments&quot;:&quot;&#125;&quot;&#125;&#125;&#125;]&#125;

data:&#123;&quot;id&quot;:&quot;chatcmpl-8KsmRHXFf9HQRr3xDpHzJK6C71hGX&quot;,&quot;created&quot;:1699987827.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;finish_reason&quot;:&quot;function_call&quot;,&quot;delta&quot;:&#123;&#125;&#125;]&#125;
</code></pre>

<p>内容依旧在<code>choices/0/delta</code>中,只不过从content变成了function_call.<br>content的例子:</p>
<pre class="language-none"><code class="language-none">data:&#123;&quot;id&quot;:&quot;chatcmpl-8Krqrqqn1rKPPBatGpBhvOfN3sN7G&quot;,&quot;created&quot;:1699984257.000000000,&quot;choices&quot;:[&#123;&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;content&quot;:&quot; today&quot;&#125;&#125;]&#125;</code></pre>

<p>所以要处理就比较简单了, 在<code>IterableStream&lt;ChatCompletions&gt;</code>的循环中去把function_call给拼出来,之前content只需要拼一个字段,这个拼两个字段即可.<br>代码如下:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">String</span> role <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token class-name">StringBuilder</span> contentBuilder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">StringBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">StringBuilder</span> functionCallNameBuilder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">StringBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">StringBuilder</span> functionCallArgumentsBuilder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">StringBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">ChatCompletions</span> e <span class="token operator">:</span> stream<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">final</span> <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">ChatChoice</span><span class="token punctuation">></span></span> choices <span class="token operator">=</span> e<span class="token punctuation">.</span><span class="token function">getChoices</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token class-name">CollectionUtils</span><span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span>choices<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">continue</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
        <span class="token keyword">final</span> <span class="token class-name">ChatChoice</span> chatChoice <span class="token operator">=</span> choices<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>chatChoice<span class="token punctuation">.</span><span class="token function">getFinishReason</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">break</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>

        <span class="token keyword">final</span> <span class="token class-name">ChatMessage</span> chatMessage <span class="token operator">=</span> chatChoice<span class="token punctuation">.</span><span class="token function">getDelta</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>chatMessage <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">continue</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>chatMessage<span class="token punctuation">.</span><span class="token function">getRole</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            role <span class="token operator">=</span> chatMessage<span class="token punctuation">.</span><span class="token function">getRole</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
        <span class="token keyword">final</span> <span class="token class-name">FunctionCall</span> functionCall <span class="token operator">=</span> chatMessage<span class="token punctuation">.</span><span class="token function">getFunctionCall</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>functionCall <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>functionCall<span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                functionCallNameBuilder<span class="token punctuation">.</span><span class="token function">append</span><span class="token punctuation">(</span>functionCall<span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>functionCall<span class="token punctuation">.</span><span class="token function">getArguments</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                functionCallArgumentsBuilder<span class="token punctuation">.</span><span class="token function">append</span><span class="token punctuation">(</span>functionCall<span class="token punctuation">.</span><span class="token function">getArguments</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span>
        <span class="token comment">// 这里不写else 不排除这两者都返回的情况</span>
        <span class="token keyword">final</span> <span class="token class-name">String</span> content <span class="token operator">=</span> chatMessage<span class="token punctuation">.</span><span class="token function">getContent</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>content <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            contentBuilder<span class="token punctuation">.</span><span class="token function">append</span><span class="token punctuation">(</span>content<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">IOException</span> ex<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        log<span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token string">"chat - for loop"</span><span class="token punctuation">,</span> ex<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>role <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    log<span class="token punctuation">.</span><span class="token function">warn</span><span class="token punctuation">(</span><span class="token string">"chat - role is null"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
<span class="token class-name">String</span> content <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>contentBuilder<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    content <span class="token operator">=</span> contentBuilder<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
<span class="token comment">// 要用可以直接构造Map或者FunctionCall</span>
<span class="token class-name">String</span> functionMap <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>functionCallNameBuilder<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">></span></span> map <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    map<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span><span class="token string">"name"</span><span class="token punctuation">,</span> functionCallNameBuilder<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>functionCallArgumentsBuilder<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        map<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span><span class="token string">"arguments"</span><span class="token punctuation">,</span> functionCallArgumentsBuilder<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    functionMap <span class="token operator">=</span> <span class="token class-name">JsonUtils</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span>map<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">orElseThrow</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>拿到了content和function的完整结构后续逻辑就和非流式一样了</p>
]]></content>
      <tags>
        <tag>openai</tag>
        <tag>azure</tag>
        <tag>SSE</tag>
      </tags>
  </entry>
  <entry>
    <title>blazor maui hybrid app显示本地图片</title>
    <url>/2023/12/25/blazor-maui-hybrid-app-display-local-image/</url>
    <content><![CDATA[<p>啊… …<br>一通操作下来感觉就是两个字 折磨<br>跨平台有跨平台的好处 但框架本身支持的有限 很多东西做起来很曲折 哎</p>
<p>这里总结一下笔者为了折腾本地图片显示的尝试<br>为什么要做本地图片展示呢 如果是做需要网络连接的app 这个一般是不需要的(要做上传前预览/编辑的话还是要的)<br>但对于离线的app肯定是要的 总会有场景用户导入图片/文件之类的吧  </p>
<p>笔者只测试了windows和安卓这两个平台,mac和iOS因为没有设备和开发者账号所以调试不了😀</p>
<span id="more"></span>

<h1 id="复制到wwwroot"><a href="#复制到wwwroot" class="headerlink" title="复制到wwwroot"></a>复制到wwwroot</h1><p>这是最简单直接的方法,什么都不用改,把图片复制到wwwroot下然后直接使用图片地址就行了<br>代码如下:  </p>
<pre class="language-C#" data-language="C#"><code class="language-C#">public async Task PickAndShow(PickOptions options)
&#123;
    try
    &#123;
        var result &#x3D; await FilePicker.Default.PickAsync(options);
        if (result !&#x3D; null)
        &#123;
            await using var stream &#x3D; await result.OpenReadAsync();
            var originalFileName &#x3D; result.FileName;
            var extension &#x3D; Path.GetExtension(originalFileName);
            var targetFileName &#x3D; Guid.NewGuid() + extension;

            var path &#x3D; Path.Combine(AppDomain.CurrentDomain.BaseDirectory, &quot;wwwroot&quot;, &quot;images&quot;, targetFileName);
            Directory.CreateDirectory(Path.Combine(AppDomain.CurrentDomain.BaseDirectory, &quot;wwwroot&quot;, &quot;images&quot;));
            await using var targetStream &#x3D; new FileStream(path, FileMode.Create);
            Debug.WriteLine($&quot;copy path:&#123;path&#125;&quot;);
            await stream.CopyToAsync(targetStream);
            _objUrl &#x3D; $&quot;images&#x2F;&#123;targetFileName&#125;&quot;;
        &#125;
    &#125;
    catch (Exception ex)
    &#123;
        Debug.WriteLine(ex);
    &#125;
&#125;</code></pre>

<p>这个的优点就是简单直接<br>缺点就是安卓不支持… 安卓的wwwroot在app bundle里 只能读不能写<br>适合只需要windows的使用<br>但你只需要windows 还用maui是不是有🍪  </p>
<h1 id="base64-objectURL"><a href="#base64-objectURL" class="headerlink" title="base64/objectURL"></a>base64/objectURL</h1><p>这两个我一起说 因为原理是一样的<br>存在sqlite appdata或者其他什么地方<br>需要用的时候读取转化为base64和objectURL  </p>
<p>但是使用base64的时候我发现了很严重的内存泄漏,传了很多图片测试之后应用的内存直接炸了.<br>见: <a href="https://stackoverflow.com/questions/77513507/how-to-avoid-memory-leaks-when-using-base64-images-in-blazor">https://stackoverflow.com/questions/77513507/how-to-avoid-memory-leaks-when-using-base64-images-in-blazor</a>  </p>
<p>可能是我用的png有关,但他的确会造成内存较大的消耗,并且有不必要的开销,你转成base64,浏览器还得给他转回来.  </p>
<p>用objectURL是个更好的选择<br>在index.html中放入这段代码:  </p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript"><span class="token operator">&lt;</span>script<span class="token operator">></span>
    window<span class="token punctuation">.</span><span class="token function-variable function">createObjectURL</span> <span class="token operator">=</span> <span class="token keyword">async</span> <span class="token punctuation">(</span><span class="token parameter">imageStream</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">const</span> arrayBuffer <span class="token operator">=</span> <span class="token keyword">await</span> imageStream<span class="token punctuation">.</span><span class="token function">arrayBuffer</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">const</span> blob <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Blob</span><span class="token punctuation">(</span><span class="token punctuation">[</span>arrayBuffer<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">return</span> <span class="token constant">URL</span><span class="token punctuation">.</span><span class="token function">createObjectURL</span><span class="token punctuation">(</span>blob<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

    window<span class="token punctuation">.</span><span class="token function-variable function">revokeObjectURL</span> <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token parameter">url</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
        <span class="token constant">URL</span><span class="token punctuation">.</span><span class="token function">revokeObjectURL</span><span class="token punctuation">(</span>url<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token operator">&lt;</span><span class="token operator">/</span>script<span class="token operator">></span></code></pre>

<p>扩展一下<code>IJSRuntime</code>,这是为了方便使用,也可以直接调,都是一样的:  </p>
<pre class="language-C#" data-language="C#"><code class="language-C#">public static class IJSRuntimeExtensions
&#123;
    public static ValueTask&lt;string&gt; CreateObjectUrl(this IJSRuntime js, Stream stream, bool leaveOpen &#x3D; false)
    &#123;
        var dotNetStreamReference &#x3D; new DotNetStreamReference(stream, leaveOpen);
        return js.InvokeAsync&lt;string&gt;(&quot;createObjectURL&quot;, dotNetStreamReference);
    &#125;

    public static async Task RevokeObjectUrl(this IJSRuntime js, string? url)
    &#123;
        Debug.WriteLine($&quot;Revoke call url &#123;url&#125;&quot;);
        if (url is not null)
        &#123;
            await js.InvokeVoidAsync(&quot;revokeObjectURL&quot;, url);
        &#125;
    &#125;
&#125;</code></pre>

<p>哪里需要用调用一下获得objectURL即可  </p>
<h1 id="复制到appdata下-覆盖CreateFileProvider"><a href="#复制到appdata下-覆盖CreateFileProvider" class="headerlink" title="复制到appdata下 覆盖CreateFileProvider"></a>复制到appdata下 覆盖CreateFileProvider</h1><p>从爆栈上发现的,测试了下windows和安卓上都可用,意外的简单直接.<br>见:<br><a href="https://stackoverflow.com/a/75282680/2078863">https://stackoverflow.com/a/75282680/2078863</a></p>
<pre class="language-C#" data-language="C#"><code class="language-C#">public class CustomFilesBlazorWebView : BlazorWebView
&#123;
    public override IFileProvider CreateFileProvider(string contentRootDir)
    &#123;
        var lPhysicalFiles &#x3D; new PhysicalFileProvider(FileSystem.Current.AppDataDirectory);
        return new CompositeFileProvider(lPhysicalFiles, base.CreateFileProvider(contentRootDir));
    &#125;
&#125;</code></pre>

<p>然后用这个替换原本的BlazorWebView即可.<br>之后就将文件复制到Appdata下:</p>
<pre class="language-C#" data-language="C#"><code class="language-C#">public static readonly string ImageDir &#x3D; &quot;image&quot;;

public async Task&lt;string&gt; CopyToImageDir(byte[] bytes, string originalFileName)
&#123;
    var appFilePath &#x3D; Path.Combine(GetImageDirPath(), CreateNewName(originalFileName));
    Directory.CreateDirectory(GetImageDirPath());
    await using var fileStream &#x3D; File.Create(appFilePath);
    await fileStream.WriteAsync(bytes);
    return appFilePath;
&#125;

public string GetImageDirPath()
&#123;
    return Path.Combine(FileSystem.AppDataDirectory, ImageDir);
&#125;

private string CreateNewName(string originalFileName)
&#123;
    var extension &#x3D; Path.GetExtension(originalFileName);
    var uuid &#x3D; Guid.NewGuid().ToString();
    var newFileName &#x3D; $&quot;&#123;Path.GetFileNameWithoutExtension(originalFileName)&#125;_&#123;uuid&#125;&#123;extension&#125;&quot;;
    return newFileName;
&#125;</code></pre>

<p>url使用<code>image/filename</code>即可  </p>
<hr>
<p>这些方案都不可避免要将文件复制都某个地方.<br>其实优化下可以直接读原始文件, 但是不同平台限制不太一样, 比如在安卓上<code>FilePicker</code>返回的fullPath是cache地址不是实际地址, 需要自己写native代码用安卓原生的文件选择.<br>其他还有不少大佬的方案,这里只做几个实现比较简单的总结.<br>市面上有许多由资深开发者提出的复杂方案,但在这里,我只总结了几个实现起来相对简单的方法.<br>唉,MAUI本身的使用确实颇具挑战性.虽然将Blazor集成进UI在某种程度上缓解了这一问题,但由于其跨平台的特性,很多功能似乎都不尽如人意.<br>初看上去,基础的使用似乎没什么问题,但一旦动手实现更复杂的功能,就会发现受到了各种限制.想要突破这些限制,就必须编写与平台相关的代码,而这又要求你必须深入了解Windows、安卓和iOS等系统的原生代码是如何编写的.</p>
<p>这种局面确实令人尴尬,这或许也是跨平台框架普遍存在的问题.</p>
<p>微软的努力将决定未来的发展,但从他们那边来看,情况似乎并不乐观.<br>就以本文读取本地图片的问题为例：<br><a href="https://github.com/dotnet/maui/issues/2907">https://github.com/dotnet/maui/issues/2907</a><br><a href="https://github.com/dotnet/aspnetcore/issues/25274">https://github.com/dotnet/aspnetcore/issues/25274</a><br>早在2021年甚至2020年,就有人提出了这个问题.<br>经过多个相关问题的讨论,认为在<code>.NET 7</code>中实现这一功能风险太大,决定推迟到<code>.NET 8</code>.  </p>
<blockquote>
<p>mkArtakMSFT modified the milestones: Backlog, .NET 7 Planning on Nov 6, 2021<br>danroth27 modified the milestones: .NET 7 Planning, .NET 8 Planning on Aug 24, 2022<br>While we have made progress on this feature, at this point we think the risk is too high to include this in the .NET 7 release. Moving to .NET 8. </p>
<p>2023-06-30 bot: We’ve moved this issue to the Backlog milestone. This means that it is not going to be worked on for the coming release. We will reassess the backlog following the current release and consider this item at that time. To learn more about our issue management process and to have better expectation regarding different types of issues you can read our Triage Process.</p>
</blockquote>
<p>然而,即便<code>.NET 8</code>已经发布,这个功能依旧没有得到官方的实现, 只有各种workaround……这实在令人沮丧.</p>
<p>MAUI给人的感觉好像是一个被遗弃的项目.随着.NET 9或.NET 10的推出,微软可能会宣布停止支持MAUI,并推出另一个全新的框架.<br>毕竟微软的作风一贯如此.</p>
<p>相关参考链接:<br><a href="https://learn.microsoft.com/en-us/aspnet/core/blazor/images?view=aspnetcore-8.0">Work with images in ASP.NET Core Blazor</a></p>
<p><a href="https://github.com/dotnet/maui/issues/2907">[Bug] Not allowed to load local resource for Android</a>  </p>
<p><a href="https://github.com/dotnet/aspnetcore/issues/25274">Blazor Image component to display images that are not accessible through HTTP endpoints</a></p>
<p><a href="https://stackoverflow.com/questions/72513093/how-to-display-local-image-as-well-as-resources-image-in-net-maui-blazor/75282680#75282680">How to display local image as well as resources image in .Net MAUI Blazor</a>  </p>
<p><a href="https://github.com/dotnet/maui/issues/8857">Overriding CreateFileProvider for BlazorWebView throws exception</a></p>
]]></content>
      <tags>
        <tag>maui</tag>
        <tag>blazor</tag>
        <tag>ASP.NET</tag>
        <tag>C#</tag>
        <tag>dotnet</tag>
      </tags>
  </entry>
  <entry>
    <title>ChatGPT Plugin开发setup - Java(Spring Boot) Python(fastapi)</title>
    <url>/2023/05/15/chatgpt-plugin-setup-java-python/</url>
    <content><![CDATA[<p>记录一下快速模板,整体很简单,如果不接auth,只需要以下:</p>
<ul>
<li>提供一个<code>/.well-known/ai-plugin.json</code>接口,返回openAI所需要的格式</li>
<li>提供openAPI规范的文档</li>
<li>CORS设置</li>
</ul>
<p>其他的和普通的web开发类似.  </p>
<p>本地开发就直接使用localhost即可,前几天官方localhost<a href="https://community.openai.com/t/localhost-dev-plugins-not-working-any-more/203968">无法联通</a>,最近应该修复了.  </p>
<p>要让GPT更好理解接口内容,接口需要写详细的文档,在文档内写清楚各个参数作用和可选值以及示例.</p>
<span id="more"></span>  

<h1 id="Spring-Boot"><a href="#Spring-Boot" class="headerlink" title="Spring Boot"></a>Spring Boot</h1><h2 id="增加对文档的依赖"><a href="#增加对文档的依赖" class="headerlink" title="增加对文档的依赖"></a>增加对文档的依赖</h2><pre class="language-xml" data-language="xml"><code class="language-xml"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">></span></span>org.springdoc<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">></span></span>springdoc-openapi-starter-webmvc-ui<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>version</span><span class="token punctuation">></span></span>2.1.0<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>version</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">></span></span></code></pre>

<p>增加一个bean配置:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@Bean</span>
<span class="token keyword">public</span> <span class="token class-name">OpenAPI</span> <span class="token function">openAPI</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">OpenAPI</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
            <span class="token punctuation">.</span><span class="token function">info</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">Info</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">title</span><span class="token punctuation">(</span><span class="token string">"html fetcher"</span><span class="token punctuation">)</span>
                            <span class="token punctuation">.</span><span class="token function">description</span><span class="token punctuation">(</span><span class="token string">"get content from url"</span><span class="token punctuation">)</span>
                            <span class="token punctuation">.</span><span class="token function">version</span><span class="token punctuation">(</span><span class="token string">"1.0"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>文档的地址为<code>/v3/api-docs</code></p>
<h2 id="增加ai-plugin-json接口"><a href="#增加ai-plugin-json接口" class="headerlink" title="增加ai-plugin.json接口"></a>增加ai-plugin.json接口</h2><pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@GetMapping</span><span class="token punctuation">(</span>value <span class="token operator">=</span> <span class="token string">"/.well-known/ai-plugin.json"</span><span class="token punctuation">,</span> produces <span class="token operator">=</span> <span class="token string">"application/json"</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token class-name">String</span> <span class="token function">aiPlugin</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> <span class="token triple-quoted-string string">"""
            &#123;
                    "schema_version": "v1",
                    "name_for_human": "html fetcher Plugin",
                    "name_for_model": "html_fetcher",
                    "description_for_human": "Plugin for getting content from url",
                    "description_for_model": "Plugin for getting content from url",
                    "auth": &#123;
                        "type": "none"
                    &#125;,
                    "api": &#123;
                        "type": "openapi",
                        "url": "http://localhost:8080/v3/api-docs",
                        "is_user_authenticated": false
                    &#125;,
                    "logo_url": "http://localhost:8080/logo.png",
                    "contact_email": "support@example.com",
                    "legal_info_url": "http://www.example.com/legal"
                &#125;
    """</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>logo直接放到<code>\resources\static</code>中<br>内容根据自己插件修改,本地开发直接写localhost,部署写对应的网站地址.</p>
<h2 id="CORS设置"><a href="#CORS设置" class="headerlink" title="CORS设置"></a>CORS设置</h2><p>测试的时候允许可以写*, 后续上线更改为openai.com:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@Bean</span>
<span class="token keyword">public</span> <span class="token class-name">CorsFilter</span> <span class="token function">corsFilter</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">UrlBasedCorsConfigurationSource</span> source <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">UrlBasedCorsConfigurationSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">CorsConfiguration</span> config <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">CorsConfiguration</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    config<span class="token punctuation">.</span><span class="token function">addAllowedOrigin</span><span class="token punctuation">(</span><span class="token string">"*"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    config<span class="token punctuation">.</span><span class="token function">addAllowedHeader</span><span class="token punctuation">(</span><span class="token string">"*"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    config<span class="token punctuation">.</span><span class="token function">addAllowedMethod</span><span class="token punctuation">(</span><span class="token string">"*"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    source<span class="token punctuation">.</span><span class="token function">registerCorsConfiguration</span><span class="token punctuation">(</span><span class="token string">"/**"</span><span class="token punctuation">,</span> config<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">CorsFilter</span><span class="token punctuation">(</span>source<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>要写自定义文档可以用spring-doc的相关注解.<br>比如写在接口上用<code>@Operation</code>, 字段上使用<code>@Schema</code>注解.<br><code>@Schema</code>内可以用<code>allowableValues</code>和<code>example</code>等来约束openai的查询.</p>
<h1 id="fastapi"><a href="#fastapi" class="headerlink" title="fastapi"></a>fastapi</h1><p>fastapi自带openAPI集成,只需要把json dump成yaml即可, setup比较简单, 这里直接全部放一起了:</p>
<pre class="language-python" data-language="python"><code class="language-python">app <span class="token operator">=</span> FastAPI<span class="token punctuation">(</span><span class="token punctuation">)</span>

app<span class="token punctuation">.</span>add_middleware<span class="token punctuation">(</span>
    CORSMiddleware<span class="token punctuation">,</span>
    allow_origins<span class="token operator">=</span><span class="token punctuation">[</span><span class="token string">"*"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
    allow_credentials<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span>
    allow_methods<span class="token operator">=</span><span class="token punctuation">[</span><span class="token string">"*"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
    allow_headers<span class="token operator">=</span><span class="token punctuation">[</span><span class="token string">"*"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
<span class="token punctuation">)</span>

<span class="token comment"># static 文件夹用来做静态资源host 放logo</span>
app<span class="token punctuation">.</span>mount<span class="token punctuation">(</span><span class="token string">"/static"</span><span class="token punctuation">,</span> StaticFiles<span class="token punctuation">(</span>directory<span class="token operator">=</span><span class="token string">"static"</span><span class="token punctuation">)</span><span class="token punctuation">,</span> name<span class="token operator">=</span><span class="token string">"static"</span><span class="token punctuation">)</span>

<span class="token comment"># json -> yaml</span>
<span class="token decorator annotation punctuation">@app<span class="token punctuation">.</span>get</span><span class="token punctuation">(</span><span class="token string">"/openapi.yaml"</span><span class="token punctuation">)</span>
<span class="token keyword">async</span> <span class="token keyword">def</span> <span class="token function">get_openapi</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
    <span class="token keyword">return</span> Response<span class="token punctuation">(</span>content<span class="token operator">=</span>yaml<span class="token punctuation">.</span>dump<span class="token punctuation">(</span>app<span class="token punctuation">.</span>openapi<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">,</span> media_type<span class="token operator">=</span><span class="token string">"application/yaml"</span><span class="token punctuation">)</span>


<span class="token decorator annotation punctuation">@app<span class="token punctuation">.</span>get</span><span class="token punctuation">(</span><span class="token string">"/.well-known/ai-plugin.json"</span><span class="token punctuation">)</span>
<span class="token keyword">async</span> <span class="token keyword">def</span> <span class="token function">openai_api_plugin</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
    <span class="token keyword">return</span> <span class="token punctuation">&#123;</span>
        <span class="token string">"schema_version"</span><span class="token punctuation">:</span> <span class="token string">"v1"</span><span class="token punctuation">,</span>
        <span class="token string">"name_for_human"</span><span class="token punctuation">:</span> <span class="token string">""</span><span class="token punctuation">,</span>
        <span class="token string">"name_for_model"</span><span class="token punctuation">:</span> <span class="token string">""</span><span class="token punctuation">,</span>
        <span class="token string">"description_for_human"</span><span class="token punctuation">:</span> <span class="token string">""</span><span class="token punctuation">,</span>
        <span class="token string">"description_for_model"</span><span class="token punctuation">:</span> <span class="token string">""</span><span class="token punctuation">,</span>
        <span class="token string">"auth"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span>
            <span class="token string">"type"</span><span class="token punctuation">:</span> <span class="token string">"none"</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>
        <span class="token string">"api"</span><span class="token punctuation">:</span> <span class="token punctuation">&#123;</span>
            <span class="token string">"type"</span><span class="token punctuation">:</span> <span class="token string">"openapi"</span><span class="token punctuation">,</span>
            <span class="token string">"url"</span><span class="token punctuation">:</span> <span class="token string">"http://localhost:8000/openapi.yaml"</span><span class="token punctuation">,</span>
            <span class="token string">"is_user_authenticated"</span><span class="token punctuation">:</span> <span class="token boolean">False</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>
        <span class="token string">"logo_url"</span><span class="token punctuation">:</span> <span class="token string">"http://localhost:8000/static/logo.png"</span><span class="token punctuation">,</span>
        <span class="token string">"contact_email"</span><span class="token punctuation">:</span> <span class="token string">"support@example.com"</span><span class="token punctuation">,</span>
        <span class="token string">"legal_info_url"</span><span class="token punctuation">:</span> <span class="token string">"http://www.example.com/legal"</span>
    <span class="token punctuation">&#125;</span></code></pre>

<p>自定义文档内容对于接口的可以用<code>summary</code>,<code>response_description</code>参数, query参数可以用Annotationd, 一个例子:  </p>
<pre class="language-python" data-language="python"><code class="language-python"><span class="token decorator annotation punctuation">@app<span class="token punctuation">.</span>get</span><span class="token punctuation">(</span><span class="token string">"/api/query_profit_data"</span><span class="token punctuation">,</span> summary<span class="token operator">=</span><span class="token string">'query profit data by company code, year and quarter'</span><span class="token punctuation">,</span> response_description<span class="token operator">=</span><span class="token triple-quoted-string string">"""
return profit data in format &#123;"key":&#123;"0":"value"&#125;&#125;, panda's dataframe"""</span><span class="token punctuation">)</span>
<span class="token keyword">async</span> <span class="token keyword">def</span> <span class="token function">query_profit_data</span><span class="token punctuation">(</span>code<span class="token punctuation">:</span> Annotated<span class="token punctuation">[</span><span class="token builtin">str</span><span class="token punctuation">,</span> Query<span class="token punctuation">(</span>description<span class="token operator">=</span><span class="token string">"the company code"</span><span class="token punctuation">,</span> example<span class="token operator">=</span><span class="token string">"sh.600000"</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
                            year<span class="token punctuation">:</span> Annotated<span class="token punctuation">[</span><span class="token builtin">int</span><span class="token punctuation">,</span> Query<span class="token punctuation">(</span>description<span class="token operator">=</span><span class="token string">"year to get profit"</span><span class="token punctuation">,</span> example<span class="token operator">=</span><span class="token number">2023</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
                            quarter<span class="token punctuation">:</span> Annotated<span class="token punctuation">[</span>
                                <span class="token builtin">int</span><span class="token punctuation">,</span> Query<span class="token punctuation">(</span>description<span class="token operator">=</span><span class="token string">"quarter to get profit. allow values:1,2,3,4"</span><span class="token punctuation">,</span> example<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">:</span></code></pre>

<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p>chatgpt plugin: <a href="https://openai.com/blog/chatgpt-plugins">https://openai.com/blog/chatgpt-plugins</a>  </p>
<p>spring doc: <a href="https://springdoc.org/v2/">https://springdoc.org/v2/</a>  </p>
<p>fastapi: <a href="https://fastapi.tiangolo.com/">https://fastapi.tiangolo.com/</a></p>
]]></content>
      <tags>
        <tag>java</tag>
        <tag>GPT</tag>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>设计数据密集型应用笔记1:可靠 可扩展可维护的应用</title>
    <url>/2018/03/20/ddia-note-chapter1/</url>
    <content><![CDATA[<p>原书地址:<a href="https://www.safaribooksonline.com/library/view/designing-data-intensive-applications/9781491903063/">Designing Data-Intensive Applications, 1st Edition</a><br>之前群里有人推荐,再在safaribooksonline上有60多个好评就先看了一章,阅读体验良好,这边记录一下笔记.<br>似乎之前看到过图灵社区在招这本书的译者,不过那时候可能都读了很多了.<br>主要是笔记的性质,记录一下概要和重要概念,有兴趣的可以购买阅读或者订阅safaribooksonline,此外作者在<a href="https://github.com/ept/ddia-references">github</a>放出了这本书所有章节的引用,看了些第一章的引用都是很不错的资料,或许可以不看这本书直接看提供的资料也可以(笑</p>
<hr>
<span id="more"></span>
<p><img src="/images/1244488-20180321014845699-543810406.png"></p>
<p>现今的很多应用都是数据密集型的应用,CPU很少是应用的限制因素,而数据量,数据复杂度和数据产生的速度是更大的问题.</p>
<p>身为开发不仅仅知识和开发应用程序，还需要设计数据系统。  </p>
<p>应用的设计需要考虑以下几个方面:</p>
<ul>
<li>可靠性: 系统在面对问题(软硬件 人为问题)时依旧可以正常工作</li>
<li>可扩展性: 可以应对系统的增长(数据量 传输量和复杂度)</li>
<li>可维护性</li>
</ul>
<h1 id="可靠性"><a href="#可靠性" class="headerlink" title="可靠性"></a>可靠性</h1><p>错误(fault)和失败(failure)不等价</p>
<p>错误常常定义为一个组件偏离了他的规范(例如非预期的不正常运行)</p>
<p>失败是指系统作为一个整体停止了其服务</p>
<h2 id="硬件错误"><a href="#硬件错误" class="headerlink" title="硬件错误"></a>硬件错误</h2><p>例如硬盘的平均失败时间(MTFF mean time to failure)</p>
<p>应对方式:</p>
<ul>
<li>增加冗余存储 RAID</li>
<li>电源保障 UPS</li>
<li>热替换CPU</li>
<li>服务备份  </li>
</ul>
<h2 id="软件错误"><a href="#软件错误" class="headerlink" title="软件错误"></a>软件错误</h2><p>一般认为硬件错误是随机的.<br>如果是因为硬件问题,一台机器的硬盘坏了不一定暗示了其他机器的会坏,错误之间常常弱相关.</p>
<p>而软件bug则会因为特定条件被触发,带来的问题可能会更严重,也更难排查.  </p>
<h2 id="人为错误"><a href="#人为错误" class="headerlink" title="人为错误"></a>人为错误</h2><p>最主要的错误来源.</p>
<p>应对方式:</p>
<ul>
<li>做设计时尽量减少产生错误的机会，例如更友好的管理界面。</li>
<li>将容易产生失误的地方和造成错误的地方解耦，沙箱环境。</li>
<li>测试，从单测到系统集成测试到人力测试。</li>
<li>快速且容易的恢复机制。</li>
<li>设置详细且清晰的监控，例如性能指标和错误率。</li>
<li>优秀的管理和训练。</li>
</ul>
<h1 id="可扩展性"><a href="#可扩展性" class="headerlink" title="可扩展性"></a>可扩展性</h1><p>如何应对不断上升的负载.  </p>
<h2 id="描述负载"><a href="#描述负载" class="headerlink" title="描述负载"></a>描述负载</h2><p>首先要能简明地描述当前系统负载,不然无法讨论负载增长的问题.</p>
<p>可以使用一些数字来描述：负载参数(load parameter)  </p>
<p>选择的参数取决于系统架构:</p>
<ul>
<li>对于web服务来说可以是QPS</li>
<li>对于数据库来说是读写比</li>
<li>对于聊天室来说是并发活跃连接数</li>
<li>对于缓存是命中率</li>
</ul>
<h2 id="描述性能"><a href="#描述性能" class="headerlink" title="描述性能"></a>描述性能</h2><p>描述好负载之后就可以研究在负载增加的时候系统的表现如何.<br>可以从两方面考虑:</p>
<ul>
<li>系统资源(CPU 内存 网络带宽）不变的情况下系统受到的影响。</li>
<li>需要增加多少资源来保持性能不受到影响。</li>
</ul>
<p>性能指代:对于处理系统例如hadoop来说是吞吐量,对在线系统来说是响应时间.</p>
<p>响应时间-百分比 几个9<br>要考虑可能最慢的请求是因为数据过多,而大量数据的用户则是核心用户.<br>百分比常常用于SLO(service level objectives)和SLA(service level agreements)</p>
<h2 id="处理负载的方式"><a href="#处理负载的方式" class="headerlink" title="处理负载的方式:"></a>处理负载的方式:</h2><p>scaling up 和 scaling out<br>垂直扩展会变得昂贵<br>需要考虑一个合适的方式,一些情况下几台高性能机器会比一堆低性能虚拟机更加简单和便宜.</p>
<p>一些系统是elastic,弹性收缩的,在负载增加时自动增加计算资源.</p>
<p>无状态服务的水平扩展会很容易,但如果要处理状态的话就会带来额外的复杂度。<br>现在一些通用的做法是在达到极限或者要满足HA之前数据库是单个节点。<br>但分布式数据系统是未来的趋势。  </p>
<p>要根据实际的问题来设计,没有银弹.<br>如何设计取决于负载参数,例如读写比,对于读多比不同的应用设计可能会是截然不同的.  </p>
<h2 id="可维护性"><a href="#可维护性" class="headerlink" title="可维护性"></a>可维护性</h2><p>易操作</p>
<p>设计简洁</p>
<p>易迭代</p>
]]></content>
  </entry>
  <entry>
    <title>docker container目录下log过大解决</title>
    <url>/2024/01/26/docker-container-log-too-large/</url>
    <content><![CDATA[<p>现在和朋友一起搞点东西,部署都很简单,服务器上装个<code>docker</code>,服务丢上去,写个docker compose配置就跑着.<br>有个服务最近流量涨的比较多,再加上有段时间没有部署,看服务器上的磁盘使用越来越多.  </p>
<p>起初以为是应用的日志太多了,应用日志每天归档为gzip,保留60天,看了下并没有多少,用lsof检查也确认没有roll掉的被持有,再去容器内部看了下发现体积远没有这么大.  </p>
<p>于是开始用<code>du --max-depth=1</code> 检查发现是<code>/var/lib/docker/containers/</code>目录, 再一看容器id就是这个服务, 再定位是<code>&lt;container-id&gt;-json.log </code>文件.</p>
<span id="more"></span>

<p>当时磁盘已经90%以上了,那就先直接truncate掉,运行：</p>
<pre class="language-shell" data-language="shell"><code class="language-shell"><span class="token function">sudo</span> truncate -s <span class="token number">0</span> /var/lib/docker/containers/<span class="token operator">&lt;</span>container-id<span class="token operator">></span>/<span class="token operator">&lt;</span>container-id<span class="token operator">></span>-json.log</code></pre>

<p>后续找了相关参数给他配置上.  </p>
<p>如果是纯docker跑的,配置:  </p>
<pre class="language-shell" data-language="shell"><code class="language-shell">docker run --log-opt max-size<span class="token operator">=</span>1g --log-opt max-file<span class="token operator">=</span><span class="token number">3</span> <span class="token operator">&lt;</span>image<span class="token operator">></span></code></pre>

<p>docker compose的配置:  </p>
<pre class="language-none"><code class="language-none">version: &#39;3&#39;
services:
  your-service:
    image: your-image
    logging:
      driver: json-file
      options:
        max-size: &quot;1g&quot;
        max-file: &quot;3&quot;</code></pre>

<p>检查是否生效直接inspect一下:  </p>
<pre class="language-shell" data-language="shell"><code class="language-shell">docker inspect --format<span class="token operator">=</span><span class="token string">'&#123;&#123;.HostConfig.LogConfig&#125;&#125;'</span> <span class="token operator">&lt;</span>container-id<span class="token operator">></span></code></pre>

<p>或者直接grep一下LogConfig</p>
<p>之前频繁开发部署,容器销毁重新构建没发现这个问题,也算是又增加了一点点小知识.  </p>
<p>相关参考链接:<br><a href="https://docs.docker.com/config/containers/logging/json-file/">JSON File logging driver</a></p>
]]></content>
      <tags>
        <tag>docker</tag>
        <tag>docker compose</tag>
      </tags>
  </entry>
  <entry>
    <title>使用maven插件构建docker镜像</title>
    <url>/2017/12/22/docker-maven-springboot/</url>
    <content><![CDATA[<h1 id="为什么要用插件"><a href="#为什么要用插件" class="headerlink" title="为什么要用插件"></a>为什么要用插件</h1><p>主要还是自动化的考虑，如果额外使用Dockerfile进行镜像生成，可能会需要自己手动指定jar/war位置，并且打包和生成镜像间不同步，带来很多琐碎的工作。  </p>
<h1 id="插件选择"><a href="#插件选择" class="headerlink" title="插件选择"></a>插件选择</h1><p>使用比较多的是spotify的插件:<a href="https://github.com/spotify/docker-maven-plugin">https://github.com/spotify/docker-maven-plugin</a><br>和 <a href="https://github.com/spotify/dockerfile-maven%E3%80%82">https://github.com/spotify/dockerfile-maven。</a><br>但这里我选择另一款插件：<a href="https://github.com/fabric8io/docker-maven-plugin%E3%80%82">https://github.com/fabric8io/docker-maven-plugin。</a><br>因为他文档比较详细，在使用上也比较方便。<br>文档地址：<a href="https://dmp.fabric8.io/">https://dmp.fabric8.io/</a>  </p>
<span id="more"></span>
<h1 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h1><p>这里使用一个spring boot项目，只有一个最简单的<code>HelloController</code>，如下：</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@RestController</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">HelloController</span> <span class="token punctuation">&#123;</span>
    <span class="token annotation punctuation">@GetMapping</span><span class="token punctuation">(</span><span class="token string">"/"</span><span class="token punctuation">)</span>
    <span class="token keyword">public</span> <span class="token class-name">String</span> <span class="token function">hello</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">return</span> <span class="token string">"hello"</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p><code>pom.xml</code>改动如下:</p>
<pre class="language-xml" data-language="xml"><code class="language-xml"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>plugin</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">></span></span>io.fabric8<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">></span></span>docker-maven-plugin<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>version</span><span class="token punctuation">></span></span>0.23.0<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>version</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>configuration</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dockerHost</span><span class="token punctuation">></span></span>tcp://192.168.99.100:2376<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dockerHost</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>certPath</span><span class="token punctuation">></span></span>C:\Users\fairjm\.docker\machine\machines\default<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>certPath</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>images</span><span class="token punctuation">></span></span>
            <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>image</span><span class="token punctuation">></span></span>
                <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>name</span><span class="token punctuation">></span></span>$&#123;project.name&#125;:$&#123;project.version&#125;<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>name</span><span class="token punctuation">></span></span>
                <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>build</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>from</span><span class="token punctuation">></span></span>openjdk:8-jre<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>from</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>args</span><span class="token punctuation">></span></span>
                        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>JAR_FILE</span><span class="token punctuation">></span></span>$&#123;project.name&#125;-$&#123;project.version&#125;.jar<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>JAR_FILE</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>args</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>assembly</span><span class="token punctuation">></span></span>
                        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>descriptorRef</span><span class="token punctuation">></span></span>artifact<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>descriptorRef</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>assembly</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>entryPoint</span><span class="token punctuation">></span></span>["java"]<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>entryPoint</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>cmd</span><span class="token punctuation">></span></span>["-jar","maven/$&#123;project.name&#125;-$&#123;project.version&#125;.jar"]<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>cmd</span><span class="token punctuation">></span></span>
                <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>build</span><span class="token punctuation">></span></span>
                <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>run</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>ports</span><span class="token punctuation">></span></span>
                        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>port</span><span class="token punctuation">></span></span>8888:8080<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>port</span><span class="token punctuation">></span></span>
                    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>ports</span><span class="token punctuation">></span></span>
                <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>run</span><span class="token punctuation">></span></span>
            <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>image</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>images</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>configuration</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>plugin</span><span class="token punctuation">></span></span>
```  
这里使用了在xml里写操作而不是使用Dockerfile的方式，个人感觉这种方式更加简单一点，不需要额外再维护一份文件，和Dockerfile相比使用的语法(注意entrypoint和cmd)也类似。  

接下来介绍一下configuration配置。  
`dockerHost`和`certPath`是连接docker使用，毕竟插件本身不包含docker和对应功能只是调用docker提供的API。  
这两个值在docker toolbox上可以通过`docker-machine env`获得。  
```bash
$ docker-machine env
export DOCKER_TLS_VERIFY="1"
export DOCKER_HOST="tcp://192.168.99.100:2376"
export DOCKER_CERT_PATH="C:\Users\fairjm\.docker\machine\machines\default"
export DOCKER_MACHINE_NAME="default"
export COMPOSE_CONVERT_WINDOWS_PATHS="true"
# Run this command to configure your shell:
# eval $("D:\Docker Toolbox\docker-machine.exe" env)</code></pre>

<p>image的build指定了构建相关的设置：  </p>
<ul>
<li>name指定image名，这里使用了项目名，tag使用项目版本；   </li>
<li>from指定基于的image，和Dockerfile的FROM一致；  </li>
<li>args和ARG一致（在这个例子中没有实际意义）；  </li>
<li>assembly用来定义哪些文件进入镜像中，使用了插件已经定义好的行为，spring-boot生成的是fat jar不需要拷贝依赖所以选择了artifact。这个的配置比较丰富，可以查看文档获取更多的信息。  </li>
<li>entryPoint和cmd也对应同样的Dockerfile命令。 </li>
</ul>
<p>接着通过<code>mvn clean package docker:build</code>执行打包和build</p>
<pre class="language-bash" data-language="bash"><code class="language-bash"><span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> --- maven-jar-plugin:2.6:jar <span class="token punctuation">(</span>default-jar<span class="token punctuation">)</span> @ docker-test ---
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> Building jar: D:<span class="token punctuation">\</span>sts_workspace<span class="token punctuation">\</span>docker-test<span class="token punctuation">\</span>target<span class="token punctuation">\</span>docker-test-0.0.1-SNAPSHOT.jar
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span>
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> --- spring-boot-maven-plugin:1.5.9.RELEASE:repackage <span class="token punctuation">(</span>default<span class="token punctuation">)</span> @ docker-test ---
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span>
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> --- docker-maven-plugin:0.23.0:build <span class="token punctuation">(</span>default-cli<span class="token punctuation">)</span> @ docker-test ---
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> Copying files to D:<span class="token punctuation">\</span>sts_workspace<span class="token punctuation">\</span>docker-test<span class="token punctuation">\</span>target<span class="token punctuation">\</span>docker<span class="token punctuation">\</span>docker-test<span class="token punctuation">\</span><span class="token number">0.0</span>.1-SNAPSHOT<span class="token punctuation">\</span>build<span class="token punctuation">\</span>maven
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> Building tar: D:<span class="token punctuation">\</span>sts_workspace<span class="token punctuation">\</span>docker-test<span class="token punctuation">\</span>target<span class="token punctuation">\</span>docker<span class="token punctuation">\</span>docker-test<span class="token punctuation">\</span><span class="token number">0.0</span>.1-SNAPSHOT<span class="token punctuation">\</span>tmp<span class="token punctuation">\</span>docker-build.tar
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> DOCKER<span class="token operator">></span> <span class="token punctuation">[</span>docker-test:0.0.1-SNAPSHOT<span class="token punctuation">]</span>: Created docker-build.tar <span class="token keyword">in</span> <span class="token number">1</span> second
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> DOCKER<span class="token operator">></span> <span class="token punctuation">[</span>docker-test:0.0.1-SNAPSHOT<span class="token punctuation">]</span>: Built image sha256:303c3
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> DOCKER<span class="token operator">></span> <span class="token punctuation">[</span>docker-test:0.0.1-SNAPSHOT<span class="token punctuation">]</span>: Removed old image sha256:ea8a7
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> ------------------------------------------------------------------------
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> BUILD SUCCESS
<span class="token punctuation">[</span>INFO<span class="token punctuation">]</span> ------------------------------------------------------------------------</code></pre>
<p>完成打包，在对应连接的docker上也会出现这个镜像：  </p>
<pre class="language-bash" data-language="bash"><code class="language-bash">$ docker image <span class="token function">ls</span>
REPOSITORY                        TAG                 IMAGE ID            CREATED             SIZE
docker-test                       <span class="token number">0.0</span>.1-SNAPSHOT      303c39c7d253        <span class="token number">13</span> seconds ago      552MB</code></pre>

<p>run指定了运行参数 这里将host的8888和容器的8080绑定，可以使用<code>mvn docker:start</code>启动，访问8888端口即可连接到服务器。<br>与其配对的是<code>mvn docker:stop</code>，可以停止并移除启动的容器。  </p>
<h1 id="示例2"><a href="#示例2" class="headerlink" title="示例2"></a>示例2</h1><p>上述使用xml的配置方式，这里再简单描述一下使用<code>Dockerfile</code>的配置方式。<br>在进行一些操作的时候，可以发现使用xml会有些问题，比如指令的执行顺序。<br>该插件xml的执行顺序和命令的定义顺序不一定一致，可能会带来一些问题，比如将<code>&lt;user&gt;</code>放于<code>&lt;runCmds&gt;</code>前但还是<code>&lt;runCmds&gt;</code>先触发，一些需要root权限的命令就会失败。<br>比如这个issus(但不确定是feature还是bug，感觉是feature)：<a href="https://github.com/fabric8io/docker-maven-plugin/issues/913">https://github.com/fabric8io/docker-maven-plugin/issues/913</a>  </p>
<p>这时候就需要直接使用<code>Dockerfile</code>来进行配置。<br>这里取一个实际的打成war的工程。<br>插件配置如下：</p>
<pre class="language-none"><code class="language-none">&lt;images&gt;
    &lt;image&gt;
        &lt;name&gt;$&#123;project.name&#125;:$&#123;project.version&#125;&lt;&#x2F;name&gt;
        &lt;build&gt;
            &lt;assembly&gt;
                &lt;descriptorRef&gt;rootWar&lt;&#x2F;descriptorRef&gt;
            &lt;&#x2F;assembly&gt;
            &lt;dockerFile&gt;Dockerfile&lt;&#x2F;dockerFile&gt;
        &lt;&#x2F;build&gt;
        &lt;run&gt;
            &lt;ports&gt;
                &lt;port&gt;8888:8080&lt;&#x2F;port&gt;
            &lt;&#x2F;ports&gt;
        &lt;&#x2F;run&gt;
    &lt;&#x2F;image&gt;
&lt;&#x2F;images&gt;</code></pre>
<p>这里更改了<code>descriptorRef</code>，换成<code>rootWar</code>，这会将target下的项目war拷贝到<code>maven\</code>下并且取名为<code>ROOT.war</code>。<br><code>Dockerfile</code>默认放置的位置是<code>src/main/docker</code>，我们在这里建对应的文件：  </p>
<pre class="language-none"><code class="language-none">FROM jetty
USER root
ENV JAVA_OPTIONS&#x3D;-Xmx1g
RUN mkdir -p &#x2F;root&#x2F;xxx &amp;&amp; touch &#x2F;root&#x2F;xxx&#x2F;yyy &amp;&amp; echo zz &gt; &#x2F;root&#x2F;xxx&#x2F;yyy
COPY maven&#x2F; &#x2F;var&#x2F;lib&#x2F;jetty&#x2F;webapps</code></pre>
<p>基本和上面的配置类似，base image改为了jetty，查看jetty的<code>Dockerfile</code>可以发现他使用了一个新用户<code>jetty</code>，使用这个用户无法在root下建立目录，并且由于项目本身之前使用sudo执行的，所以为了能正常运行选择使用root用户。<br>最后一步将<code>ROOT.war</code>拷贝到jetty的webapps目录下。    </p>
<p>关于<code>maven/</code>这个目录，在打包后，会在target下生成<code>target\docker\项目名\0.0.1-SNAPSHOT\build</code>，对应的<code>Dockerfile</code>和<code>maven\</code>就在这个目录下，实际执行的不是<code>src/main/docker/Dockerfile</code>，而是拷贝到上述目录下的<code>Dockerfile</code>，此外使用xml的方式也是在这个位置生成了一份<code>Dockerfile</code>（USER 总是被放置于最后…）。</p>
<h1 id="更多"><a href="#更多" class="headerlink" title="更多"></a>更多</h1><p>本文简要说明了使用<code>fabric8</code>的docker maven插件进行构建运行相关的操作，该插件还有其他的功能可以通过上面的文档获取帮助。  </p>
<h1 id="源码下载-示例1"><a href="#源码下载-示例1" class="headerlink" title="源码下载(示例1)"></a>源码下载(示例1)</h1><p><a href="https://github.com/fairjm/spring-boot-docker">https://github.com/fairjm/spring-boot-docker</a></p>
]]></content>
  </entry>
  <entry>
    <title>ES踩坑笔记</title>
    <url>/2018/11/13/elasticsearch-in-trouble/</url>
    <content><![CDATA[<p>现在开始在业务上使用ES,记录一些踩坑经历,做点笔记.  </p>
<hr>
<span id="more"></span>
<p>2018-11-13  </p>
<h1 id="source不返回问题"><a href="#source不返回问题" class="headerlink" title="source不返回问题"></a>source不返回问题</h1><p>使用了角色校验,客户端插入成功之后获取数据没有source,和查询参数无关.<br>检查mapping,发现获取mapping也是空…<br>如下:  </p>
<pre class="language-none"><code class="language-none">&#123;&#39;test_index&#39;: &#123;&#39;mappings&#39;: &#123;&#39;test_doc&#39;: &#123;&#39;properties&#39;: &#123;&#125;&#125;&#125;&#125;&#125;  </code></pre>
<p>排查了一会儿..找不出原因.<br>后来要到了一个高权限的账号去kibana看了眼…发现  </p>
<p><img src="/images/1244488-20181113154038997-784426282.png"><br>能获取的fields为空… …<br>emmmmm….<br>设置为<code>*</code>后解决  </p>
<p>参考链接:<br><a href="https://www.elastic.co/guide/en/elastic-stack-overview/6.4/field-level-security.html">https://www.elastic.co/guide/en/elastic-stack-overview/6.4/field-level-security.html</a>  </p>
<hr>
<p>2018-11-16  </p>
<h1 id="termQuery不返回结果"><a href="#termQuery不返回结果" class="headerlink" title="termQuery不返回结果"></a>termQuery不返回结果</h1><p>emmmmm 一个id是用了dynamic mapping插入,默认使用的是standard分词器.<br>一些id有中划线,分词结果:</p>
<pre class="language-none"><code class="language-none">GET &#x2F;_analyze
&#123;
  &quot;analyzer&quot;: &quot;standard&quot;,
  &quot;text&quot;: &quot;1111-2222-4444-3333-55555&quot;
&#125;</code></pre>
<pre class="language-none"><code class="language-none">&#123;
  &quot;tokens&quot;: [
    &#123;
      &quot;token&quot;: &quot;1111&quot;,
      &quot;start_offset&quot;: 0,
      &quot;end_offset&quot;: 4,
      &quot;type&quot;: &quot;&lt;NUM&gt;&quot;,
      &quot;position&quot;: 0
    &#125;,
    &#123;
      &quot;token&quot;: &quot;2222&quot;,
      &quot;start_offset&quot;: 5,
      &quot;end_offset&quot;: 9,
      &quot;type&quot;: &quot;&lt;NUM&gt;&quot;,
      &quot;position&quot;: 1
    &#125;,
    &#123;
      &quot;token&quot;: &quot;4444&quot;,
      &quot;start_offset&quot;: 10,
      &quot;end_offset&quot;: 14,
      &quot;type&quot;: &quot;&lt;NUM&gt;&quot;,
      &quot;position&quot;: 2
    &#125;,
    &#123;
      &quot;token&quot;: &quot;3333&quot;,
      &quot;start_offset&quot;: 15,
      &quot;end_offset&quot;: 19,
      &quot;type&quot;: &quot;&lt;NUM&gt;&quot;,
      &quot;position&quot;: 3
    &#125;,
    &#123;
      &quot;token&quot;: &quot;55555&quot;,
      &quot;start_offset&quot;: 20,
      &quot;end_offset&quot;: 25,
      &quot;type&quot;: &quot;&lt;NUM&gt;&quot;,
      &quot;position&quot;: 4
    &#125;
  ]
&#125;</code></pre>
<p>然后在代码里使用的是termQuery:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">SearchRequest</span> searchRequest <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">SearchRequest</span><span class="token punctuation">(</span>INDEX_NAME<span class="token punctuation">)</span><span class="token punctuation">;</span>
searchRequest<span class="token punctuation">.</span><span class="token function">types</span><span class="token punctuation">(</span>TYPE_NAME<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">SearchSourceBuilder</span> sourceBuilder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">SearchSourceBuilder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
sourceBuilder<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span><span class="token class-name">QueryBuilders</span><span class="token punctuation">.</span><span class="token function">termQuery</span><span class="token punctuation">(</span>ID<span class="token punctuation">,</span> id<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>termQuery是不对搜索词分词的…<br>于是就什么都查不出来..<br>解决方式是用dynamic mapping自动生成的keyword field.  </p>
<p>参考:<br><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis-analyzers.html">https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis-analyzers.html</a>  </p>
<hr>
<p>2018-12-14</p>
<h1 id="处理深分页"><a href="#处理深分页" class="headerlink" title="处理深分页"></a>处理深分页</h1><p>默认的<code>from</code>+<code>size</code>限制是10000,大于这个会报错:  </p>
<pre class="language-none"><code class="language-none">&#123;
  &quot;error&quot;: &#123;
    &quot;root_cause&quot;: [
      &#123;
        &quot;type&quot;: &quot;query_phase_execution_exception&quot;,
        &quot;reason&quot;: &quot;Result window is too large, from + size must be less than or equal to: [1000000] but was [1000001]. See the scroll api for a more efficient way to request large data sets. This limit can be set by changing the [index.max_result_window] index level setting.&quot;
      &#125;
    ],</code></pre>
<p>深分页的操作对coordinator会造成很大的影响,会占用大量heap存放数据并进行排序操作.<br>业务上没有注意,结果就超了… …<br>可以设定<code>index.max_result_window</code>来提高上限.<br>但如果真的要有这么深,还是使用search after比较可靠.又因为是实时业务查询,所以用scroll是不合适的.  </p>
<p>参考:<br><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/index-modules.html">https://www.elastic.co/guide/en/elasticsearch/reference/current/index-modules.html</a><br><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/search-request-search-after.html">https://www.elastic.co/guide/en/elasticsearch/reference/current/search-request-search-after.html</a></p>
]]></content>
  </entry>
  <entry>
    <title>理一理Edge-triggered和Level-triggered</title>
    <url>/2019/10/03/et-lt-intro/</url>
    <content><![CDATA[<p>最近群里又在讨论java的NIO,提到了NIO使用的lt而netty使用JNI在linux和MacOS/BSD中封装了et.<br>之前对这两个概念笼统了解了下,并没有去查阅额外资料,仅限知道lt在缓冲区还有数据的情况下就会被poll出来,而et则需要有新的请求/事件发生.<br>这次查阅了点资料,汇总一些数据来简单(毕竟也没有那么深入..)谈谈这两个概念.  </p>
<span id="more"></span>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>这两个名词应该来源于电气,用于激活电路.摘取一段爆栈网的回答:</p>
<blockquote>
<p>Level Triggering: In level triggering the circuit will become active when the gating or clock pulse is on a particular level. This level is decided by the designer. We can have a negative level triggering in which the circuit is active when the clock signal is low or a positive level triggering in which the circuit is active when the clock signal is high.<br>Edge Triggering: In edge triggering the circuit becomes active at negative or positive edge of the clock signal. For example if the circuit is positive edge triggered, it will take input at exactly the time in which the clock signal goes from low to high. Similarly input is taken at exactly the time in which the clock signal goes from high to low in negative edge triggering. But keep in mind after the the input, it can be processed in all the time till the next input is taken.  </p>
</blockquote>
<p><a href="https://electronics.stackexchange.com/questions/21886/what-does-edge-triggered-and-level-triggered-mean#21891">来源.</a><br>翻译就是LT是根据设定的阈值来控制是否激发而ET是根据信号的高到低或低到高这个变化来控制.  </p>
<p>字面意思上level就是水平指的是某值,而edge是边,是值和值之间的变迁.<br>用来控制操作是否进行的一种机制.</p>
<p>对应于epoll中的概念也类似.<br>拿文档中的例子:</p>
<blockquote>
<ol>
<li>The file descriptor that represents the read side of a pipe (rfd) is registered on the epoll instance.</li>
<li>A pipe writer writes 2 kB of data on the write side of the pipe.</li>
<li>A call to epoll_wait(2) is done that will return rfd as a ready file descriptor.</li>
<li>The pipe reader reads 1 kB of data from rfd.</li>
<li>A call to epoll_wait(2) is done.</li>
</ol>
</blockquote>
<p>使用LT的话,因为依旧有1KB残留所以wait会立即返回开始下一次操作,而ET这次变迁已经结束了,但因为没有处理完所以后续变化也不会再返回了.(Since the read operation done in 4 does not consume the whole buffer data, the call to epoll_wait(2) done in step 5 might block indefinitely.)  </p>
<p>ET在使用上建议遵循以下的规则:  </p>
<blockquote>
<p>i   with nonblocking file descriptors; and<br>ii  by waiting for an event only after read(2) or write(2)<br>                  return EAGAIN.  </p>
</blockquote>
<p>非阻塞的文件描述符(FD或SD)以及要read或write在返回EAGAIN的情况下才开始这个描述符的下一次事件监听.(对于网络IO来说也可能是EWOULDBLOCK, 表示被标记为非阻塞的操作会发生阻塞的异常)  </p>
<p>相对来说使用ET的要求和操作会需要更严谨一些,当然LT写得有问题,比如漏了一个事件的处理,可能会导致出现跑满CPU的死循环.</p>
<h1 id="简单使用"><a href="#简单使用" class="headerlink" title="简单使用"></a>简单使用</h1><p>这里使用mio来演示这两种的使用方式.代码直接改的官网TcpServer的示例.<br>完整代码见:<a href="https://github.com/fairjm/feel_boring/blob/master/edge_level_test/src/main.rs">main.rs</a>.  </p>
<p>首先最外层是一样的,选择感兴趣的事件注册到poll中,poll进行wait等待可用事件.事件的处理上就有些许差异.  </p>
<p>首先是LT:  </p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">fn</span> <span class="token function-definition function">read_level</span><span class="token punctuation">(</span>stream<span class="token punctuation">:</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> <span class="token class-name">TcpStream</span><span class="token punctuation">,</span> poll<span class="token punctuation">:</span> <span class="token operator">&amp;</span><span class="token class-name">Poll</span><span class="token punctuation">)</span> <span class="token punctuation">-></span> <span class="token class-name">Result</span><span class="token operator">&lt;</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">></span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> connection_closed <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token keyword">loop</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">let</span> <span class="token keyword">mut</span> buf <span class="token operator">=</span> <span class="token macro property">vec!</span><span class="token punctuation">[</span><span class="token number">0u8</span><span class="token punctuation">;</span> <span class="token number">8</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
        <span class="token keyword">match</span> stream<span class="token punctuation">.</span><span class="token function">read</span><span class="token punctuation">(</span><span class="token operator">&amp;</span><span class="token keyword">mut</span> buf<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">Ok</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                connection_closed <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Ok</span><span class="token punctuation">(</span>_n<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"======come new read======"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"read:&#123;:?&#125;"</span><span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">::</span><span class="token function">from_utf8</span><span class="token punctuation">(</span>buf<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token comment">// just return is ok for level</span>
                <span class="token keyword">return</span> <span class="token class-name">Ok</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span><span class="token keyword">ref</span> err<span class="token punctuation">)</span> <span class="token keyword">if</span> <span class="token function">would_block</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"would_block happened"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span><span class="token keyword">ref</span> err<span class="token punctuation">)</span> <span class="token keyword">if</span> <span class="token function">interrupted</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"interrupted happened"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token keyword">continue</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token keyword">return</span> <span class="token class-name">Err</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> connection_closed <span class="token punctuation">&#123;</span>
        <span class="token comment">// must have this one</span>
        poll<span class="token punctuation">.</span><span class="token function">deregister</span><span class="token punctuation">(</span>stream<span class="token punctuation">)</span><span class="token operator">?</span><span class="token punctuation">;</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"&#123;:?&#125; Connection closed"</span><span class="token punctuation">,</span> stream<span class="token punctuation">.</span><span class="token function">peer_addr</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token class-name">Ok</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>在<code>OK(_n)</code>中,当前只读取部分byte并返回是可以的,并且如果未读完会立即发起下一个事件.但要注意如果连接关闭需要移除,不然可能会有奇怪的问题.(在本机上如果不移除可能会导致无限的read事件)  </p>
<p>而ET的话就需要读到出现WouldBlock</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">fn</span> <span class="token function-definition function">read_edge</span><span class="token punctuation">(</span>stream<span class="token punctuation">:</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> <span class="token class-name">TcpStream</span><span class="token punctuation">)</span> <span class="token punctuation">-></span> <span class="token class-name">Result</span><span class="token operator">&lt;</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">></span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> <span class="token keyword">mut</span> connection_closed <span class="token operator">=</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token keyword">loop</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">let</span> <span class="token keyword">mut</span> buf <span class="token operator">=</span> <span class="token macro property">vec!</span><span class="token punctuation">[</span><span class="token number">0u8</span><span class="token punctuation">;</span> <span class="token number">8</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
        <span class="token keyword">match</span> stream<span class="token punctuation">.</span><span class="token function">read</span><span class="token punctuation">(</span><span class="token operator">&amp;</span><span class="token keyword">mut</span> buf<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">Ok</span><span class="token punctuation">(</span><span class="token number">0</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                connection_closed <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Ok</span><span class="token punctuation">(</span>_n<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"======come new read======"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"read:&#123;:?&#125;"</span><span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">::</span><span class="token function">from_utf8</span><span class="token punctuation">(</span>buf<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span><span class="token keyword">ref</span> err<span class="token punctuation">)</span> <span class="token keyword">if</span> <span class="token function">would_block</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"would_block happened"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token comment">// edge rely this to return, without this or just return after read(like level)</span>
                <span class="token comment">// the connection will not be read anymore</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span><span class="token keyword">ref</span> err<span class="token punctuation">)</span> <span class="token keyword">if</span> <span class="token function">interrupted</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">&#123;</span>
                <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"interrupted happened"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token keyword">continue</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
            <span class="token comment">// Other errors we'll consider fatal.</span>
            <span class="token class-name">Err</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token keyword">return</span> <span class="token class-name">Err</span><span class="token punctuation">(</span>err<span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> connection_closed <span class="token punctuation">&#123;</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"&#123;:?&#125; Connection closed"</span><span class="token punctuation">,</span> stream<span class="token punctuation">.</span><span class="token function">peer_addr</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token class-name">Ok</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>这里无法像LT一样直接读完部分返回,需要在出现阻塞的情况下才能继续操作.<br>因为mio相对底层,所以描述起来和epoll文档也类似,java的NIO也类似,但因为只提供了LT所以就没有ET什么事了.  </p>
<h1 id="选择"><a href="#选择" class="headerlink" title="选择"></a>选择</h1><p>ET的问题主要是需要更严谨的操作,而LT是更频繁的wait唤醒.<br>在某种程度上,ET更加’惰性’而LT更加积极,你如果不处理或者还不想处理就会反复收到事件,除非你取消注册他.<br>所以在不少java的NIO代码中,常常会有channel在读后取消读注册写,在写后取消写注册读的代码,当可以写,但是写的内容还没准备好时,使用LT会遇到不少问题(所以一般是准备写的内容已经好了,再给channel注册上写的兴趣).<br>ET在读上会更加麻烦,不读完等到返回EAGAIN,该描述符可能之后的事件触发就会有问题.<br>具体还需要看自己的需求和场景.<br>此外多线程场景下,在同一个描述符上等待ET是保证只会唤醒一个线程,但要注意多个不同的数据块请求可能会导致在一个FD/SD上返回多个事件,需要使用<code>EPOLLONESHOT</code>来确保只返回一个(这个flag是接受到一个事件后就解绑和FD/SD关系的).  </p>
<p>java中只支持LT,netty通过JNI实现了ET,选择的原因在一个邮件里提到了一些<a href="http://mail.openjdk.java.net/pipermail/nio-dev/2008-December/000329.html">Any reason why select() uses only level-triggered notification mode?</a>.<br>大致的意思是ET和I/O提供的方法更加耦合,可能是为了更高的兼容性放弃了这个机制的提供吧.  </p>
<p>这边就进行了简单的一些资料整合,没有涉及到更深的内容,如果文中有什么错误欢迎评论.</p>
<p>参考资料:<br><a href="https://netty.io/wiki/native-transports.html">https://netty.io/wiki/native-transports.html</a>  </p>
<p><a href="http://man7.org/linux/man-pages/man7/epoll.7.html">http://man7.org/linux/man-pages/man7/epoll.7.html</a></p>
<p><a href="https://github.com/tokio-rs/mio">https://github.com/tokio-rs/mio</a></p>
]]></content>
      <tags>
        <tag>OS</tag>
        <tag>rust</tag>
      </tags>
  </entry>
  <entry>
    <title>常用失败控制模式</title>
    <url>/2018/02/01/failure-control-patterns/</url>
    <content><![CDATA[<p>本文内容主要翻译自<a href="https://www.safaribooksonline.com/library/view/reactive-systems-architecture/9781491980705/">Reactive Systems Architecture</a>第一章1.9节。</p>
<span id="more"></span>
<p>在分布式系统中，有组件发生故障造成对应服务失败是很平常的一件事。这里简单阐述一下在发生这些错误时的一些常用处理模式。</p>
<p>失败控制(failure control)的一个目标是在系统或他的一部分失败的情况下如何实现期望的一致性和传递保证。在部分系统失败的情况下，不影响到系统其他部分的稳定性。</p>
<p>但通常工程师不会意识到失败是很自然的一件事。</p>
<ul>
<li>只是记录异常log 如果log不被人处理就一直保持失败</li>
<li>一个服务持续的失败可能导致来自于其他服务的过度重试，导致该服务被压垮，也可能造成依赖他的服务的级联失败。</li>
<li>每个服务都有性能限制，过度访问会造成服务失败(DoS).</li>
</ul>
<p>下面简单介绍一下一些失败处理的方式</p>
<h2 id="隔离和挡板-Isolation-and-bulkheading"><a href="#隔离和挡板-Isolation-and-bulkheading" class="headerlink" title="隔离和挡板(Isolation and bulkheading)"></a>隔离和挡板(Isolation and bulkheading)</h2><p><img src="/images/1244488-20180201210130265-855744444.png"><br>隔离失败 让失败不影响到其他系统<br>系统组件不要过度耦合<br>Hystrix</p>
<h2 id="背压Backpressure"><a href="#背压Backpressure" class="headerlink" title="背压Backpressure"></a>背压Backpressure</h2><p>生产者根据消费者的消费步调来调整自己的生产速度<br>一般采用消费者(服务器)去获取内容(队列里面拿请求) 而不是生产者(客户端)去推送到消费者(服务器)<br>Akka Streams<br>Apache Spark Streaming<br>简单点的做法是用限流(RateLimiter)的方式 丢弃多余的请求</p>
<h2 id="监督者（Supervision"><a href="#监督者（Supervision" class="headerlink" title="监督者（Supervision)"></a>监督者（Supervision)</h2><p>模块都会有一个监督者<br>组件挂了对应的监督者会收到通知 监督者会应用指定的策略去处理失败<br><img src="/images/1244488-20180201210144890-1192081513.png"><br>Akka</p>
<h2 id="断路器-Circuit-breaker"><a href="#断路器-Circuit-breaker" class="headerlink" title="断路器(Circuit breaker)"></a>断路器(Circuit breaker)</h2><p><img src="/images/1244488-20180201210151796-962438983.png"></p>
<ul>
<li><p>关闭状态:调用失败或超时会让失败计数增加 成功的调用置零计数器 当达到最大的失败次数时 断路器切换为打开状态</p>
</li>
<li><p>打开状态:所有调用立即返回失败(断路) 避免给挂了的服务增加额外压力，造成联机(比如大量调用超时 浪费客户端资源)失败。在经过配置时间之后 切换为半开状态</p>
</li>
<li><p>半开状态:放开一个调用查看是否成功 成功了切换至关闭状态。</p>
</li>
</ul>
<h2 id="部分执行-Partial-execution"><a href="#部分执行-Partial-execution" class="headerlink" title="部分执行(Partial execution)"></a>部分执行(Partial execution)</h2><p>调用几个方法完成指定工作，其中有几个调用失败的情况。<br>有以下的一些处理方式：<br>事务保证: Atomic Broadcast 2PC<br>日志保证:journalling或write ahead log<br>事务补偿</p>
<h2 id="服务降级-Service-level-degradation-control"><a href="#服务降级-Service-level-degradation-control" class="headerlink" title="服务降级(Service level degradation control)"></a>服务降级(Service level degradation control)</h2><p>系统出错时大多数工程师会返回错误，这常常会导致服务的不可用。<br>通过提供部分的服务可用可以降低服务直接不可用的开销。<br>fallback策略</p>
<p>参考链接:<br><a href="https://www.cnblogs.com/iceTing/p/6238207.html">https://www.cnblogs.com/iceTing/p/6238207.html</a><br><a href="https://www.safaribooksonline.com/library/view/reactive-systems-architecture/9781491980705/">https://www.safaribooksonline.com/library/view/reactive-systems-architecture/9781491980705/</a></p>
]]></content>
  </entry>
  <entry>
    <title>flink kafka source的avro反序列化问题</title>
    <url>/2022/05/11/flink_kafka_avro_genericrecord/</url>
    <content><![CDATA[<p>对接了一个<code>kafka</code>,里面的topic使用<code>avro</code>编码,且每个topic的<code>schema</code>都不相同使用schema registry管理。<br>现在要反序列化<code>key</code>和<code>message</code>,查看了现有的:</p>
<pre class="language-xml" data-language="xml"><code class="language-xml"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">></span></span>org.apache.flink<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">></span></span>flink-avro-confluent-registry<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">></span></span></code></pre>
<p>内的<code>ConfluentRegistryAvroDeserializationSchema</code>.<br>发现无论是<code>forSpecific</code>返回一个特定的类,还是<code>forGeneric</code>返回<code>GenericRecord</code>都需要指定<code>read schame</code>,而registry仅作为<code>write schema</code>使用, 也就是虽然返回的是<code>GenericRecord</code>, 但实际上都是同一个<code>Schema</code>反序列化的, 每个record的格式是一致的.</p>
<span id="more"></span>
<p>不太理解为什么是这个设计, 参考<a href="https://stackoverflow.com/questions/58849635/is-it-possible-to-deserialize-avro-messageconsuming-message-from-kafka-without">Is it possible to deserialize Avro message(consuming message from Kafka) without giving Reader schema in ConfluentRegistryAvroDeserializationSchema</a>,实现了下:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">KafkaGenericAvroDeserializationSchema</span>
    <span class="token keyword">implements</span> <span class="token class-name">KafkaDeserializationSchema</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Tuple2</span><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">,</span> <span class="token class-name">GenericRecord</span><span class="token punctuation">></span><span class="token punctuation">></span></span> <span class="token punctuation">&#123;</span>

  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">String</span> registryUrl<span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">transient</span> <span class="token class-name">KafkaAvroDeserializer</span> inner<span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token class-name">KafkaGenericAvroDeserializationSchema2</span><span class="token punctuation">(</span><span class="token class-name">String</span> registryUrl<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>registryUrl <span class="token operator">=</span> registryUrl<span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>

  <span class="token keyword">private</span> <span class="token keyword">void</span> <span class="token function">checkInitialized</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>inner <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
      <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">Object</span><span class="token punctuation">></span></span> props <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      props<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span><span class="token class-name">AbstractKafkaSchemaSerDeConfig</span><span class="token punctuation">.</span>SCHEMA_REGISTRY_URL_CONFIG<span class="token punctuation">,</span> registryUrl<span class="token punctuation">)</span><span class="token punctuation">;</span>
      props<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span><span class="token class-name">KafkaAvroDeserializerConfig</span><span class="token punctuation">.</span>SPECIFIC_AVRO_READER_CONFIG<span class="token punctuation">,</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token class-name">SchemaRegistryClient</span> client <span class="token operator">=</span>
          <span class="token keyword">new</span> <span class="token class-name">CachedSchemaRegistryClient</span><span class="token punctuation">(</span>
              registryUrl<span class="token punctuation">,</span> <span class="token class-name">AbstractKafkaSchemaSerDeConfig</span><span class="token punctuation">.</span>MAX_SCHEMAS_PER_SUBJECT_DEFAULT<span class="token punctuation">)</span><span class="token punctuation">;</span>
      inner <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">KafkaAvroDeserializer</span><span class="token punctuation">(</span>client<span class="token punctuation">,</span> props<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
  <span class="token punctuation">&#125;</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">boolean</span> <span class="token function">isEndOfStream</span><span class="token punctuation">(</span><span class="token class-name">Tuple2</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">,</span> <span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span> nextElement<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token class-name">Tuple2</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">,</span> <span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span> <span class="token function">deserialize</span><span class="token punctuation">(</span><span class="token class-name">ConsumerRecord</span><span class="token operator">&lt;</span><span class="token keyword">byte</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token keyword">byte</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token operator">></span> <span class="token keyword">record</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token function">checkInitialized</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">final</span> <span class="token class-name">String</span> topic <span class="token operator">=</span> <span class="token keyword">record</span><span class="token punctuation">.</span><span class="token function">topic</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">GenericRecord</span> key <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">GenericRecord</span><span class="token punctuation">)</span> inner<span class="token punctuation">.</span><span class="token function">deserialize</span><span class="token punctuation">(</span>topic<span class="token punctuation">,</span> <span class="token keyword">record</span><span class="token punctuation">.</span><span class="token function">key</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">GenericRecord</span> value <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">GenericRecord</span><span class="token punctuation">)</span> inner<span class="token punctuation">.</span><span class="token function">deserialize</span><span class="token punctuation">(</span>topic<span class="token punctuation">,</span> <span class="token keyword">record</span><span class="token punctuation">.</span><span class="token function">value</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> <span class="token class-name">Tuple2</span><span class="token punctuation">.</span><span class="token function">of</span><span class="token punctuation">(</span>key<span class="token punctuation">,</span> value<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token class-name">TypeInformation</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Tuple2</span><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">,</span> <span class="token class-name">GenericRecord</span><span class="token punctuation">></span><span class="token punctuation">></span></span> <span class="token function">getProducedType</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">TupleTypeInfo</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token class-name">TypeExtractor</span><span class="token punctuation">.</span><span class="token function">getForClass</span><span class="token punctuation">(</span><span class="token class-name">GenericRecord</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
                               <span class="token class-name">TypeExtractor</span><span class="token punctuation">.</span><span class="token function">getForClass</span><span class="token punctuation">(</span><span class="token class-name">GenericRecord</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span></code></pre>
<p>main:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">KafkaSource</span><span class="token punctuation">.</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Tuple2</span><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">,</span> <span class="token class-name">GenericRecord</span><span class="token punctuation">></span><span class="token punctuation">></span></span><span class="token function">builder</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
                           <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
                           <span class="token punctuation">.</span><span class="token function">setDeserializer</span><span class="token punctuation">(</span>
                                   <span class="token class-name">KafkaRecordDeserializationSchema</span><span class="token punctuation">.</span><span class="token function">of</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">KafkaGenericAvroDeserializationSchema</span><span class="token punctuation">(</span>
                                           SCHAME_REGISTRY
                                   <span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span></code></pre>
<p>运行报错:</p>
<pre class="language-none"><code class="language-none">Caused by: java.lang.UnsupportedOperationException
	at java.util.Collections$UnmodifiableCollection.add(Collections.java:1057)
	at com.esotericsoftware.kryo.serializers.CollectionSerializer.read(CollectionSerializer.java:109)
	at com.esotericsoftware.kryo.serializers.CollectionSerializer.read(CollectionSerializer.java:22)
	at com.esotericsoftware.kryo.Kryo.readObject(Kryo.java:679)
	at com.esotericsoftware.kryo.serializers.ObjectField.read(ObjectField.java:106)
	... 37 more</code></pre>
<p>emmmm, <code>kryo</code>无法序列化<code>GenericRecord</code>,就算加上自定义的<code>Collections$UnmodifiableCollection</code>也会有其他报错.  </p>
<p>再查阅<a href="https://flink.apache.org/news/2020/04/15/flink-serialization-tuning-vol-1.html">资料</a>发现:</p>
<blockquote>
<p>Since Avro’s Schema class is not serializable, it can not be sent around as is. You can work around this by converting it to a String and parsing it back when needed. If you only do this once on initialization, there is practically no difference to sending it directly.<br><code>GenericRecord</code>中包含<code>Schema</code>用来指导get/set工作, 这个类无法被<code>kryo</code>序列化.  </p>
</blockquote>
<p>这也能得出为什么<code>ConfluentRegistryAvroDeserializationSchema</code>的都需要指定<code>Schame</code>,因为有work around可以解决:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">class</span> <span class="token class-name">AvroGenericSource</span> <span class="token keyword">implements</span> <span class="token class-name">SourceFunction</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span><span class="token punctuation">,</span> <span class="token class-name">ResultTypeQueryable</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">GenericRecordAvroTypeInfo</span> producedType<span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token class-name">AvroGenericSource</span><span class="token punctuation">(</span><span class="token class-name">Schema</span> schema<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>producedType <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">GenericRecordAvroTypeInfo</span><span class="token punctuation">(</span>schema<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
  
  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token class-name">TypeInformation</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span> <span class="token function">getProducedType</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">return</span> producedType<span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span>

<span class="token class-name">DataStream</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">GenericRecord</span><span class="token punctuation">></span></span> sourceStream <span class="token operator">=</span>
    env<span class="token punctuation">.</span><span class="token function">addSource</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">AvroGenericSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">returns</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">GenericRecordAvroTypeInfo</span><span class="token punctuation">(</span>schema<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>其实就是这个场景下虽然返回了<code>GenericRecord</code>,但其实并不Generic,只有一种<code>schema</code>来使用,<code>kryo</code>就能正常处理了.  </p>
<p>既然不能直接用, 那直接<code>toString()</code>然后在后续算子当json解析即可,或者在反序列化的方法里拿到自己需要的数据组成能序列化的数据.<br>在我这个场景中, 每个<code>topic</code>需要的字段名是已知的, 在反序列化中得到<code>GenericRecord</code>之后get出来再组装成POJO即可.  </p>
]]></content>
  </entry>
  <entry>
    <title>记一次尴尬的git reset丢失分支故障</title>
    <url>/2018/06/29/git-reset-lose-branch/</url>
    <content><![CDATA[<p>最近…似乎一直在踩坑…<br>也不是什么故障,只是把一个分支的功能弄没了,之后在reflog里找到又恢复了.  </p>
<span id="more"></span>
<p>产生原因是有同事错误地把分支B merge到了分支A并push.<br>我直接在分支A上reset到了merge前的一个节点(但这个节点其实是B分支的).<br>这导致分支A的头跑到了B分支上,A本来那个分支没有引用就丢了.<br>先说下解决方法,git reflog找到那个reset,或者直接用<code>git fsck --no-reflogs --unreachable</code>找到那个commit之后直接再reset回去即可.  </p>
<p>主要原因是git merge之后的log会显示另一个分支的commit记录(毕竟没有真正的分支…).<br>这里简单重现下:  </p>
<pre class="language-none"><code class="language-none">*   dacbc52 (HEAD -&gt; master) Merge branch &#39;a&#39;
|\
| * 7b83cf6 (a) a commit
* | 9f4488b master commit
|&#x2F;
* 47a0ab3 init</code></pre>
<p>这样的一个分支情况,master merge了a节点产生2126这个commit.<br>这个时候看一下master的log,信息如下:   </p>
<pre class="language-none"><code class="language-none">commit dacbc528153d48f3a210a9673ebc8148cfdb50d3
Merge: 9f4488b 7b83cf6
Date:   Fri Jun 29 21:21:10 2018 +0800

    Merge branch &#39;a&#39;

commit 7b83cf6664cd5b84c5f66ef99f0fb39fb5ab57bd
Date:   Fri Jun 29 21:19:59 2018 +0800

    a commit

commit 9f4488ba008889b583205fc9405f4760150a2337
Date:   Fri Jun 29 21:18:26 2018 +0800

    master commit

commit 47a0ab3a70e7b4a663f99bc23542a51a90af152c
Date:   Fri Jun 29 21:18:03 2018 +0800

    init</code></pre>
<p>可以看到a节点上的commit也被包含在了里面….(PS:之前我们使用的是hg,分支的概念有所不同,merge之后查看当前分支只是会显示本分支和merge节点,并且也没有git里的fast-forward概念)  </p>
<p>误操作就是因为要撤销master的merge,执行了<code>git reset --hard 7b83c</code>(因为刚好是merge下面的第一个节点)<br>执行后的结果:  </p>
<pre class="language-none"><code class="language-none">* 7b83cf6 (HEAD -&gt; master, a) a commit
* 47a0ab3 init</code></pre>
<p>可以看到default的结果已经不复存在了  </p>
<p>找回的话</p>
<pre class="language-none"><code class="language-none">&gt; git reflog
7b83cf6 HEAD@&#123;0&#125;: reset: moving to 7b83c
dacbc52 HEAD@&#123;1&#125;: merge a: Merge made by the &#39;recursive&#39; strategy.</code></pre>
<p>可以直接reset回<code>dacbc52</code>再进行正常操作  </p>
<p>主要原因还是对于git的理解不深再加上之前使用hg的惯性产生了误判.  </p>
<hr>
<p>参考资料:<br><a href="https://git-scm.com/book/zh/v1/Git-%E5%86%85%E9%83%A8%E5%8E%9F%E7%90%86-%E7%BB%B4%E6%8A%A4%E5%8F%8A%E6%95%B0%E6%8D%AE%E6%81%A2%E5%A4%8D">https://git-scm.com/book/zh/v1/Git-%E5%86%85%E9%83%A8%E5%8E%9F%E7%90%86-%E7%BB%B4%E6%8A%A4%E5%8F%8A%E6%95%B0%E6%8D%AE%E6%81%A2%E5%A4%8D</a><br><a href="https://git-scm.com/book/zh/v1/Git-%E5%88%86%E6%94%AF-%E5%88%86%E6%94%AF%E7%9A%84%E6%96%B0%E5%BB%BA%E4%B8%8E%E5%90%88%E5%B9%B6">https://git-scm.com/book/zh/v1/Git-%E5%88%86%E6%94%AF-%E5%88%86%E6%94%AF%E7%9A%84%E6%96%B0%E5%BB%BA%E4%B8%8E%E5%90%88%E5%B9%B6</a></p>
]]></content>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2021/05/05/hello-world/</url>
    <content><![CDATA[<p>五一没有什么事就想把博客捡起来看看.<br>emmmmm,结果惊奇的发现之前用的域名<a href="fairjm.com">fairjm.com</a>已经过期被回收了/(ㄒoㄒ)/~~  </p>
<span id="more"></span>
<p>只能重新用了个域名开始.<br>之前博客用的<code>Hexo</code>的版本已经过低了,主题什么兼容性也不太好,就想着全部重新来一遍.<br>换了<code>Next</code>主题,不得不说<a href="https://theme-next.js.org/docs/getting-started/">文档</a>还是非常友好的,相关的插件集成也比较丰富.<br>遇上的问题也不多,主要是代码高亮那一块,使用的<code>hljs</code>不知道为什么不能<code>lineNumber</code>设置为false之后就不正常工作了,换成了<code>prismjs</code>.<br>之前写代码块的时候<code>&quot;```&quot;</code>会在后面打上三个空格,老版本的<code>hexo</code>以及其他的编辑器(比如<code>vscode</code>等)的插件都能正常渲染,换上新版本就不行了,全部替换了一遍.<br>一些写到其他地方的博客就不搬运了,全部都放在了<a href="https://github.com/fairjm/blog_posts">blog_posts</a>里.<br>总之搭好了就继续用着吧~😀  </p>
<p>之前的作者名叫<code>fairjm</code>,顺带也改一个叫<code>abaabaqua</code>了,比较朗朗上口(阿巴阿巴夸🐱).<br>因为自己实在写不出啥了,主要还是翻译+搬运为主了,自己可能就写写牢骚了~</p>
]]></content>
      <tags>
        <tag>life</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo github action注意事项</title>
    <url>/2022/01/07/hexo-github-action/</url>
    <content><![CDATA[<p>因为repo所在的笔记本不怎么常用,而且也懒得每个地方维护repo,所以写博客频率就很低.<br>看了下使用<code>github action</code>比较合适,就写个md,然后deploy一下即可.  </p>
<p>这里列一些注意事项吧.  </p>
<span id="more"></span>
<h1 id="deploy失败"><a href="#deploy失败" class="headerlink" title="deploy失败"></a>deploy失败</h1><p>具体报错是<code>fatal: could not read Username for ’https://github.com’: No such device or address</code><br>很久没用github了,之前习惯用https+用户名密码的方式提交.<br>但之前已经禁了.<br>这个问题的原因就是<code>_config.yaml</code>的<code>deploy.repo</code>用的是https+用户密码的方式去deploy.<br>在本机会让你输入,但是action这边没有设置就gg了.<br>我用的action是写入ssh密钥的方式所以直接换成git地址即可.  </p>
<h1 id="index-html为空白"><a href="#index-html为空白" class="headerlink" title="index.html为空白"></a>index.html为空白</h1><p>这个问题是因为之前主题我是拉取的主题的repo,设置了gitmodules.<br>在github上他没有拉取子module,后来删除让repo包含theme的文件即可.<br>另外也要保证安装的插件之类的和本地一致,有额外插件就自己追加<code>npm install xxx</code>即可.</p>
<h1 id="不要push触发构建"><a href="#不要push触发构建" class="headerlink" title="不要push触发构建"></a>不要push触发构建</h1><p>这个很简单,因为网上大多数的配置on配置了push.<br>更改一下为manual的方式即可,比如<code>on: workflow_dispatch</code>  </p>
<p>希望之后可以多写(搬运)一点吧.🤣</p>
]]></content>
  </entry>
  <entry>
    <title>高性能MySQL(第4版) 第一章 MySQL架构 读书笔记</title>
    <url>/2022/11/08/high-performance-mysql-4th-ch01-note/</url>
    <content><![CDATA[<p>这本书去年11月出的,今年中文版也出了,并且直接上了<a href="https://weread.qq.com/web/bookDetail/00a32b70813ab746fg018ec7">微信读书</a>,之后有空就读一读,分享下读书笔记~    </p>
<p>原文内容比较充实,建议有时间可以读一下原文.<br>第一章主要是个概览.</p>
<h1 id="MySQL的逻辑架构"><a href="#MySQL的逻辑架构" class="headerlink" title="MySQL的逻辑架构"></a>MySQL的逻辑架构</h1><p><img src="/images/hpm4_ch01_01.png"><br>默认情况下,每个客户端连接都会在服务器进程中拥有一个线程,该连接的查询只会在这个单独的线程中执行,该线程驻留在一个内核或者CPU上.<br>线程池</p>
<p>优化器会向存储引擎询问它的一些功能、某个具体操作的成本,以及表数据的统计信息.</p>
<p>query cache 5.7.20弃用 8.0移除<br>考虑应用自己在redis中缓存</p>
<span id="more"></span>

<h1 id="并发控制"><a href="#并发控制" class="headerlink" title="并发控制"></a>并发控制</h1><p>只要有多个查询需要同时修改数据,就会产生并发控制问题</p>
<h2 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h2><p>处理并发读/写访问的系统通常实现一个由两种锁类型组成的锁系统<br>这两种锁通常被称为共享锁(shared lock)和排他锁(exclusive lock),也叫读锁(read lock)和写锁(write lock)</p>
<h2 id="锁的粒度"><a href="#锁的粒度" class="headerlink" title="锁的粒度"></a>锁的粒度</h2><p>通过降低锁的粒度提高共享资源并发性<br>只锁定包含需要修改的部分数据</p>
<h3 id="表锁"><a href="#表锁" class="headerlink" title="表锁"></a>表锁</h3><p>table lock<br>最基本 开销最小的锁策略(锁本身的开销 不是指查询/修改性能)  </p>
<h3 id="行级锁"><a href="#行级锁" class="headerlink" title="行级锁"></a>行级锁</h3><p>row lock<br>最大程度支持并发处理 最大的锁开销<br>行级锁是在存储引擎中实现  </p>
<h1 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h1><p>事务就是一组SQL语句,作为一个工作单元以原子方式进行处理<br>要么全部执行成功 要么全部执行失败</p>
<p>ACID</p>
<h2 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h2><p>这里谈的是ANSI SQL中的定义</p>
<ul>
<li>READ UNCOMMITTED: 未提交读<br>在事务中可以查看其他事务中还没有提交的修改<br>脏读(dirty read): 读取未提交的数据</li>
<li>READ COMMITTED: 已提交读<br>大多数数据库系统的默认级别<br>但MySQL不是<br>一个事务可以看到其他事务在它开始之后提交的修改,但在该事务提交之前,其所做的任何修改对其他事务都是不可见的.<br>允许不可重复读(nonrepeatable read) 同一个事务中两次执行相同语句 可能看到不同结果  </li>
<li>REPEATABLE READ: 可重复读<br>解决了不可重复读<br>无法解决幻读(phantom read) 读取范围数据时 如果另一个事务在该范围插入了新的 再次读取会产生换行(phantom row)<br>InnoDB和XtraDB通过MVCC解决<br>这也是MySQL默认的隔离级别  </li>
<li>SERIALIZABLE: 可串行化<br>该级别通过强制事务按序执行,使不同事务之间不可能产生冲突,从而解决了前面说的幻读问题.<br>会在读取的每一行数据上都加锁,所以可能导致大量的超时和锁争用的问题.<br>使用场景较少 除非需要严格确保数据安全 并且接收并发性能下降  </li>
</ul>
<p><img src="/images/hpm4_ch01_02.png"></p>
<h2 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h2><p>两个或多个事务相互持有和请求相同资源上的锁,产生了循环依赖.<br>当多个事务试图以不同的顺序锁定资源时会导致死锁.</p>
<p>数据库系统实现了各种是说检测和锁超时机制<br>InnoDB目前处理死锁的方式是将持有最少行级排他锁的事务回滚<br>锁的行为和顺序和存储引擎相关</p>
<pre class="language-sql" data-language="sql"><code class="language-sql">console1:
<span class="token keyword">start</span> <span class="token keyword">transaction</span> <span class="token punctuation">;</span>
<span class="token keyword">update</span> <span class="token keyword">user</span> <span class="token keyword">set</span> user_name<span class="token operator">=</span><span class="token string">'cc11'</span> <span class="token keyword">where</span> user_id<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">;</span>
<span class="token keyword">update</span> <span class="token keyword">user</span> <span class="token keyword">set</span> user_name<span class="token operator">=</span><span class="token string">'cc22'</span> <span class="token keyword">where</span> user_id<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">;</span>
<span class="token keyword">commit</span> <span class="token punctuation">;</span></code></pre>

<pre class="language-sql" data-language="sql"><code class="language-sql">console2:
<span class="token keyword">start</span> <span class="token keyword">transaction</span> <span class="token punctuation">;</span>
<span class="token keyword">update</span> <span class="token keyword">user</span> <span class="token keyword">set</span> user_name<span class="token operator">=</span><span class="token string">'cc222'</span> <span class="token keyword">where</span> user_id<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">;</span>
<span class="token keyword">update</span> <span class="token keyword">user</span> <span class="token keyword">set</span> user_name<span class="token operator">=</span><span class="token string">'cc111'</span> <span class="token keyword">where</span> user_id<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">;</span>
<span class="token keyword">commit</span> <span class="token punctuation">;</span></code></pre>

<p>单步执行 </p>
<pre class="language-none"><code class="language-none">[40001][1213] Deadlock found when trying to get lock; try restarting transaction</code></pre>
<h2 id="事务日志"><a href="#事务日志" class="headerlink" title="事务日志"></a>事务日志</h2><p>事务日志有助于提高事务的效率.存储引擎只需要更改内存中的数据副本,而不用每次修改磁盘中的表,这会非常快.  </p>
<p>事务日志只追加 顺序I/O<br>WAL write-ahead logging 预写日志 修改数据最终要两次磁盘写入  </p>
<h2 id="MySQL中的事务"><a href="#MySQL中的事务" class="headerlink" title="MySQL中的事务"></a>MySQL中的事务</h2><p>描述的是InnoDB引擎中的事务</p>
<h3 id="理解AUTOCOMMIT"><a href="#理解AUTOCOMMIT" class="headerlink" title="理解AUTOCOMMIT"></a>理解AUTOCOMMIT</h3><p>默认开启 单个语句也是包裹在事务中 自动提交<br>可以通过set autocommit=0/1进行开关<br>用begin或者start transaction来开启事务<br>用commit提交 rollback回滚</p>
<p>有一些命令,当在活动的事务中发出时,会导致MySQL在事务的所有语句执行完毕前提交当前事务<br>比如一些DDL命令 alter table等  </p>
<p>可以通过SET TRANSACTION ISOLATION LEVEL改变隔离级别 下一个事务开始时生效  </p>
<h3 id="在事务中混合使用存储引擎"><a href="#在事务中混合使用存储引擎" class="headerlink" title="在事务中混合使用存储引擎"></a>在事务中混合使用存储引擎</h3><p>MySQL的事务由下层存储引擎实现<br>在同一个事务中,混合使用多种存储引擎是不可靠的.</p>
<h3 id="隐式锁定和显式锁定"><a href="#隐式锁定和显式锁定" class="headerlink" title="隐式锁定和显式锁定"></a>隐式锁定和显式锁定</h3><p>InnoDB使用两阶段锁定协议(two-phase locking protocol).<br>事务执行期间,随时都可以获取锁,但锁只有在提交或回滚后才会释放,并且所有的锁会同时释放.<br>前面描述的锁定机制都是隐式的.InnoDB会根据隔离级别自动处理锁.  </p>
<p>显式的(不属于SQL规范)  </p>
<pre><code class="sql">SELECT … FOR SHARE
SELECT … FOR UPDATE
</code></pre>
<p>MySQL还支持LOCK TABLES和UNLOCK TABLES<br>这两个命令在服务器级别实现<br>因为InnoDB支持行级锁 没必要使用</p>
<p>建议: 除了在禁用AUTOCOMMIT的事务中可以使用之外,其他任何时候都不要显式地执行LOCK TABLES,不管使用的是什么存储引擎.  </p>
<h1 id="多版本并发控制"><a href="#多版本并发控制" class="headerlink" title="多版本并发控制"></a>多版本并发控制</h1><p>MySQL的大多数事务型存储引擎使用的都不是简单的行级锁机制.它们会将行级锁和可以提高并发性能的多版本并发控制(MVCC)技术结合使用.</p>
<p>不同数据库的实现细节不一样</p>
<p>可以认为MVCC是行级锁的一种变种 它在很多情况下避免加锁 因此开销更低</p>
<p>通过数据快照实现</p>
<ul>
<li>InnoDB为每个事务启动时分配一个事务ID</li>
<li>该事务修改记录时 向Undo log写入一条如何恢复回去的undo记录 事务回滚指针指向该记录</li>
<li>当不同会话读取聚簇主键索引记录时 InnoDB会把记录的事务ID和该会话的读取视图比较 如果更改他的事务未提交 则跟踪undo log直到一个符合可见条件的事务ID</li>
</ul>
<p>大多数读取通过这种方式不需要获取锁(通过读取快照) 缺点是存储引擎会对每一行存储更多数据 做更多工作</p>
<p>MVCC仅适用于REPEATABLE READ和READ COMMITTED隔离级别.<br>(可以想象对于可重复读 读取的事务id固定为事务进行中第一次读的可见事务id 对于读已提交 读最新的可见事务id<br>另外两个因为不需要事务版本(一个是脏读 一个是串行化的) 和MVCC不是很适配(当然要看不同引擎的实现)  </p>
<h1 id="复制"><a href="#复制" class="headerlink" title="复制"></a>复制</h1><p>Replication<br>一主多从</p>
<h1 id="数据文件结构"><a href="#数据文件结构" class="headerlink" title="数据文件结构"></a>数据文件结构</h1><p>在8.0版本中,MySQL将表的元数据重新设计为一种数据字典,包含在表的.ibd文件中<br>使得表结构上的信息支持事务和原子级数据定义更改</p>
<p>除了以来information_schema检索表定义和元数据<br>引入了字典对象缓存 LRU的内存缓存<br>使得服务器访问表的元数据减少了I/O<br>每个表的.ibd和.frm文件被替换为已经被序列化的字典信息(.sdi).</p>
<h1 id="InnoDB引擎"><a href="#InnoDB引擎" class="headerlink" title="InnoDB引擎"></a>InnoDB引擎</h1><p>为处理大量短期事务而设计 这些事务预期通常是正常提交 很少会被回滚</p>
<p>默认情况下,InnoDB将数据存储在一系列的数据文件中,这些文件统被称为表空间(tablespace)</p>
<p>InnoDB使用MVCC来实现高并发性,并实现了所有4个SQL标准隔离级别.<br>默认为REPEATABLE READ隔离级别,并且通过间隙锁(next-key locking)策略来防止在这个隔离级别上的幻读：<br>InnoDB不只锁定在查询中涉及的行,还会对索引结构中的间隙进行锁定,以防止幻行被插入</p>
<p>基于聚簇索引构建<br>但是,因为二级索引（secondary index,非主键索引）需要包含主键列,如果主键较大,则其他索引也会很大.如果表中的索引较多,主键应当尽量小.</p>
]]></content>
      <tags>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>如何更简单方便地执行SQL操作?</title>
    <url>/2018/07/13/how-to-write-sql-happily/</url>
    <content><![CDATA[<p>现在公司使用<code>mybatis</code>作为DAL层的框架.<br>使用起来比较简单,使用xml进行SQL的书写,java代码使用接口执行.<br>但在写一些简单SQL的时候会显得非常繁琐:  </p>
<ol>
<li>xml和java分离(设计上为了解耦),一些字段是否设置等需要反复查看(虽然可以通过插件直达);</li>
<li> 原生无法热加载,修改xml后需要重启(可以使用三方实现);  </li>
<li>xml的动态SQL没有java灵活.    <span id="more"></span>
上述这些”缺点”仅仅针对写简单的sql,特别是第一条.<br>我对一张表的需求是简单的CRUD,那使用xml就会非常麻烦.  </li>
</ol>
<p>最基本的需求:  </p>
<ul>
<li>简单安全的SQL拼接方案;</li>
<li>方便的类型映射.  </li>
</ul>
<p>探索了几个方案,接下来一一介绍.  </p>
<h1 id="JOOQ"><a href="#JOOQ" class="headerlink" title="JOOQ"></a>JOOQ</h1><p>这是考虑的第一个方案,有个没好的愿景,类型安全的SQL,不用担心出现SQL的语法错误.<br>但实际使用遇到了比较麻烦的事情,他的codegen.  </p>
<pre class="language-xml" data-language="xml"><code class="language-xml"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>generate</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>pojos</span><span class="token punctuation">></span></span>true<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>pojos</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>daos</span><span class="token punctuation">></span></span>true<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>daos</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>keys</span><span class="token punctuation">></span></span>false<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>keys</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>indexes</span><span class="token punctuation">></span></span>false<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>indexes</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>globalObjectReferences</span><span class="token punctuation">></span></span>false<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>globalObjectReferences</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>generate</span><span class="token punctuation">></span></span></code></pre>
<p>自己加了<code>pojo</code>和<code>dao</code>,一定会生成<code>Table</code>,<code>Record</code>,洋洋洒洒一堆文件摆在眼前.<br>因为是互联网企业,业务也在发现会相当不稳定,管理这坨生成的代码会很麻烦,而且不简洁.<br>当然也有不是用codegen的方式,但最后感觉还是过于庞大,也比之前直接用mybatis更麻烦了.  </p>
<h1 id="ActiveJDBC"><a href="#ActiveJDBC" class="headerlink" title="ActiveJDBC"></a>ActiveJDBC</h1><p><code>active record</code>的实践.<br>总体感觉非常简洁清晰,想起了当初用JFinal的感觉.<br>当时看了文档就感觉是他了,不需要自己手写SQL,同时兼容多种方言,单测容易用<code>H2</code>替换运行.<br>但实际使用发现了一个严峻的问题,严峻到不得不放弃他的问题… …<br>他需要<code>Instrument</code>的支持.(子类使用父类的静态方法但是要在字节码中改为子类调用(复制了那些方法到子类… …))<br>且无论是动态方案还是静态方案都需要修改IDE配置.<br>这意味着我只要在现有项目里使用就会有一群开发过来揍我  </p>
<h1 id="Mybatis-SqlBuilder-amp-SelectProvider"><a href="#Mybatis-SqlBuilder-amp-SelectProvider" class="headerlink" title="Mybatis SqlBuilder&amp;@SelectProvider"></a>Mybatis SqlBuilder&amp;@SelectProvider</h1><p>绕了一圈,回到了原点… …<br>但这个方案的确是满足了解决了上面3个问题.<br><code>org.apache.ibatis.jdbc.SQL</code>本身提供了几个方法方便拼写简单的SQL.<br>例子如下:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">new</span> <span class="token function">SQL</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">SELECT</span><span class="token punctuation">(</span>COLUMNS<span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">FROM</span><span class="token punctuation">(</span><span class="token string">"user AS u"</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">INNER_JOIN</span><span class="token punctuation">(</span><span class="token string">"user_info AS ui ON u.user_id = ui.user_id"</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">WHERE</span><span class="token punctuation">(</span><span class="token string">"u.user_type = #&#123;userType&#125;"</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">WHERE</span><span class="token punctuation">(</span><span class="token string">"u.deleted = 0"</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">ORDER</span><span class="token punctuation">(</span><span class="token string">"u.user_id"</span><span class="token punctuation">)</span>
    <span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token operator">+</span> <span class="token string">" LIMIT #&#123;start&#125;,#&#123;num&#125;"</span><span class="token punctuation">;</span></code></pre>
<p>LIMIT要自己拼,因为他不是标准SQL.<br>接下来接口一样定义,用上对应注解(<code>@UpdateProvider</code>,<code>@InsertProvider</code>,<code>@DeleteProvider</code>)  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@SelectProvider</span><span class="token punctuation">(</span>type <span class="token operator">=</span> <span class="token class-name">UserProvider</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> method <span class="token operator">=</span> <span class="token string">"getUserByType"</span><span class="token punctuation">)</span>
<span class="token class-name">UserInfo</span> <span class="token function">getUserByType</span><span class="token punctuation">(</span><span class="token annotation punctuation">@Param</span><span class="token punctuation">(</span><span class="token string">"userType"</span><span class="token punctuation">)</span> <span class="token keyword">int</span> userType<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<h1 id="JdbcTemplate"><a href="#JdbcTemplate" class="headerlink" title="JdbcTemplate"></a>JdbcTemplate</h1><p>使用<code>NamedParameterJdbcTemplate</code>会感觉好一点,占位符可以用<code>:name</code>的形式.<br>我们spring也一起用,对应的依赖也有.<br>这个也可行,而且写起来还可以,不过他本身缺少SQL拼接方便的工具,适合写简单没有使用条件分支生成SQL的情况.<br>这个网上资料很多,这里稍微列一些常用的需求.  </p>
<h2 id="根据class自动映射"><a href="#根据class自动映射" class="headerlink" title="根据class自动映射"></a>根据class自动映射</h2><p>可以使用<code>BeanPropertyRowMapper</code>,会按照属性进行SQL字段的关联,java的驼峰会变成小写和下划线的形式进行匹配.<br>例子:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">BeanPropertyRowMapper</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">User</span><span class="token punctuation">></span></span> USER_MAPPER <span class="token operator">=</span> <span class="token class-name">BeanPropertyRowMapper</span><span class="token punctuation">.</span><span class="token function">newInstance</span><span class="token punctuation">(</span><span class="token class-name">User</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  

<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>

<span class="token class-name">MapSqlParameterSource</span> params <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">MapSqlParameterSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
params<span class="token punctuation">.</span><span class="token function">addValue</span><span class="token punctuation">(</span><span class="token string">"userId"</span><span class="token punctuation">,</span> userId<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">ServiceApply</span><span class="token punctuation">></span></span> applies <span class="token operator">=</span> mTemplate<span class="token punctuation">.</span><span class="token function">query</span><span class="token punctuation">(</span>
        <span class="token string">"SELECT user_id,user_name,created FROM user WHERE user_id=:userId"</span><span class="token punctuation">,</span>
        params<span class="token punctuation">,</span> USER_MAPPER<span class="token punctuation">)</span><span class="token punctuation">;</span>
    </code></pre>

<h2 id="获取插入ID"><a href="#获取插入ID" class="headerlink" title="获取插入ID"></a>获取插入ID</h2><pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">KeyHolder</span> key <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">GeneratedKeyHolder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
mTemplate<span class="token punctuation">.</span><span class="token function">update</span><span class="token punctuation">(</span>
        <span class="token string">"INSERT INTO user(user_name,age) VALUES (:name,:age)"</span><span class="token punctuation">,</span>
        params<span class="token punctuation">,</span> key<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">Number</span> keyValue <span class="token operator">=</span> key<span class="token punctuation">.</span><span class="token function">getKey</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<h2 id="批量插入"><a href="#批量插入" class="headerlink" title="批量插入"></a>批量插入</h2><pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">MapSqlParameterSource</span><span class="token punctuation">></span></span> batchParams <span class="token operator">=</span> names
        <span class="token punctuation">.</span><span class="token function">stream</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">distinct</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">map</span><span class="token punctuation">(</span>e <span class="token operator">-></span> <span class="token keyword">new</span> <span class="token class-name">MapSqlParameterSource</span><span class="token punctuation">(</span><span class="token string">"name"</span><span class="token punctuation">,</span> name<span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">.</span><span class="token function">collect</span><span class="token punctuation">(</span><span class="token class-name">Collectors</span><span class="token punctuation">.</span><span class="token function">toList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
mTemplate<span class="token punctuation">.</span><span class="token function">batchUpdate</span><span class="token punctuation">(</span>
        <span class="token string">"INSERT IGNORE INTO user(user_name) VALUES (:name)"</span><span class="token punctuation">,</span>
        batchParams<span class="token punctuation">.</span><span class="token function">toArray</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">SqlParameterSource</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<hr>
<p>参考链接:<br><a href="https://github.com/shakarelic/mybatis-reload">https://github.com/shakarelic/mybatis-reload</a>  </p>
<p><a href="https://www.jooq.org/">https://www.jooq.org/</a>  </p>
<p><a href="http://javalite.io/activejdbc">http://javalite.io/activejdbc</a>  </p>
<p><a href="https://zh.wikipedia.org/wiki/Active_Record">https://zh.wikipedia.org/wiki/Active_Record</a>  </p>
<p><a href="http://www.mybatis.org/mybatis-3/statement-builders.html">http://www.mybatis.org/mybatis-3/statement-builders.html</a>  </p>
<p><a href="https://docs.spring.io/spring/docs/current/javadoc-api/org/springframework/jdbc/core/namedparam/NamedParameterJdbcTemplate.html">https://docs.spring.io/spring/docs/current/javadoc-api/org/springframework/jdbc/core/namedparam/NamedParameterJdbcTemplate.html</a></p>
]]></content>
      <tags>
        <tag>java</tag>
        <tag>MySQL</tag>
        <tag>mybatis</tag>
        <tag>JOOQ</tag>
      </tags>
  </entry>
  <entry>
    <title>几种方式加速网页视频播放速度</title>
    <url>/2017/10/21/html5-flash-video-speed-up/</url>
    <content><![CDATA[<p>现在有不少视频网站,自带了播放加速功能,例如油管,bilibili,慕课等等.节省了很多看视频的时间,特别是看一些技术教程类的视频,不管是念ppt还是手把手演示.  </p>
<p>在自己付费的一些网站中,一些是自带播放器不支持视频加速的.因为已经被加速惯坏,变得很不习惯,今天特意研究了一下,对于几种形式给出一些解决方法.  </p>
<span id="more"></span>
<h1 id="html5播放器"><a href="#html5播放器" class="headerlink" title="html5播放器"></a>html5播放器</h1><p>主要标志是<code>&lt;video&gt;</code>,这种是最方便实现加速的,因为原生支持.<br>主要依靠这两个属性:  </p>
<blockquote>
<p>defaultPlaybackRate* (float): The playback speed at which the video should be played<br>playbackRate* (float): The current playback speed: 1.0 is normal, 2.0 is two times faster forward, -3.0 is three times faster backwards and so on   </p>
</blockquote>
<p>可以自己在console中修改实现(<a href="https://stackoverflow.com/questions/3027707/how-to-change-the-playing-speed-of-videos-in-html5">来源</a>):  </p>
<pre><code>/* play video twice as fast */
document.querySelector(&#39;video&#39;).defaultPlaybackRate = 2.0;
document.querySelector(&#39;video&#39;).play();

/* now play three times as fast just for the heck of it */
document.querySelector(&#39;video&#39;).playbackRate = 3.0;  
</code></pre>
<p>对应的插件chrome市场也有,用的比较多的有<code>video speed controller</code>.<br><a href="https://chrome.google.com/webstore/detail/video-speed-controller/nffaoalbilbmmfgbnbgppjihopabppdk">市场地址</a>  </p>
<p><a href="https://github.com/igrigorik/videospeed">github</a></p>
<h1 id="flash播放器"><a href="#flash播放器" class="headerlink" title="flash播放器"></a>flash播放器</h1><p>比较头疼的一个,chrome市场中也没有找到对应的插件.<br>比较早之前傲游浏览器做过类似功能,实现的具体讨论见 <a href="https://www.zhihu.com/question/22797950?sort=created">新版傲游主打的「马上看」是如何实现对视频广告的快进的？</a>.<br>不过这个功能已经凉了无法使用.<br>网上其他比较多的方案是<a href="http://www.enounce.com/myspeed">myspeed</a>.不过是收费软件,并且一切反馈里表示看一会儿就挂了.<br>那只能用下载的方式拿到源文件,然后再用本地播放器比如potplayer等加速了.<br>但一些网站采用付费课程的形式,做了相应的举措防止用户下载,这样就感觉无计可施了.  </p>
<p>不过有一些迂回的方式,这类网站还在使用flash没有升级到html5,但在mac上使用flash并不方便,所以他们可能为了mac或者iOS的用户采用了其他的播放方式.  </p>
<p>今天遇到了一个这样的例子:  </p>
<pre><code>if(isMobile() &amp;&amp; isSafari() )
&#123;
    
 $(&quot;#player&quot;).html(&#39;&lt;video src=&quot;&#39;+vodadd_m3+&#39;&quot; class=&quot;videoimg&quot;  controls=&quot;controls&quot;&gt;&lt;/video&gt;&#39;);    
&#125;
else
&#123;
    var fls = flashChecker();
     
    if (fls!=null &amp;&amp; !fls.f)
    &#123;
        alert(&quot;请安装flash播放控件,并使用QQ浏览器观看！&quot;);
         
    &#125;
 
 $(&quot;#player&quot;).html(&#39; &lt;embed src=&quot;&#39;+vodadd+&#39;&quot; quality=&quot;high&quot;  class=&quot;videoimg&quot;   allowScriptAccess=&quot;always&quot; allownetworking=&quot;all&quot; &#39;+
&#39;allowfullscreen=&quot;true&quot; type=&quot;application/x-shockwave-flash&quot; wmode=&quot;window&quot; pluginspage=&quot;http://www.macromedia.com/go/getflashplayer&quot;&gt;&lt;/embed&gt;&#39;);    

&#125;
</code></pre>
<p>js里有增加<code>video</code>标签的代码! 这样就转换到了上面的html5播放器的解决方法了.<br><code>isMobile</code>和<code>isSafari</code>的判断用UA就可以糊弄过去了.(chrome F12设备选择ipad即可, 或者用<code>Mozilla/5.0(iPad; U; CPU iPhone OS 3_2 like Mac OS X; en-us) AppleWebKit/531.21.10 (KHTML, like Gecko) Version/4.0.4 Mobile/7B314 Safari/531.21.10</code>)<br>然额并打不开,因为他又使用了<code>m3u8</code>这种格式,所以才在js里判断safari.<br>但有对应chrome插件支持,在应用市场安装<code>Native HLS Playback</code>在页面点击<code>play embedded</code>即可.<br>这样因为之前又装了<code>video speed controller</code>,就出现了调整速度的选项.<br>至此在这种特殊情况下的flash播放问题也解决了.  </p>
<p>不过针对只有flash播放器的环境还是没找到合适的解决方法.<br>但可以通过查看判断的代码,改变UA等方式再找找有没有可以迂回的方式.</p>
]]></content>
  </entry>
  <entry>
    <title>记一次尴尬的Java应用内存泄露排查</title>
    <url>/2018/09/21/java-application-offheap-memory-leak-analyze/</url>
    <content><![CDATA[<p>这星期被线上JVM内存占用不断增大的问题所困扰,自己提出了一些假设,然后去实施验证都一一失败了,有一些经验和教训在这里分享下.<br>之所以是尴尬,是最后因为修复了另一个看似不相关的问题导致内存不再上升,但这之间的关系还未明了,还需要继续追踪.<br>这里讲述一下这次排查的过程.  </p>
<hr>
<span id="more"></span>
<h1 id="直接内存的错误判断"><a href="#直接内存的错误判断" class="headerlink" title="直接内存的错误判断"></a>直接内存的错误判断</h1><p>服务器的JVM配置为<code>Xmx3g</code>,使用G1,没有设置<code>Xms</code>考虑自然收缩和fgc之后的空间回拢.<br>没有发生过fgc,且堆内存的增长正常,排除掉堆内存的问题.<br>使用NMT查看各个区域的内存正常,committed内存不足4G,但实际情况内存占用不断增大,甚至出现了OOM killer杀掉java进程.   </p>
<pre class="language-none"><code class="language-none">Total: reserved&#x3D;5037MB, committed&#x3D;3926MB
-                 Java Heap (reserved&#x3D;3072MB, committed&#x3D;3072MB)
                            (mmap: reserved&#x3D;3072MB, committed&#x3D;3072MB)

-                     Class (reserved&#x3D;1196MB, committed&#x3D;192MB)
                            (classes #29793)
                            (malloc&#x3D;6MB #86203)
                            (mmap: reserved&#x3D;1190MB, committed&#x3D;186MB)

-                    Thread (reserved&#x3D;186MB, committed&#x3D;186MB)
                            (thread #184)
                            (stack: reserved&#x3D;184MB, committed&#x3D;184MB)
                            (malloc&#x3D;1MB #922)
                            (arena&#x3D;1MB #366)

-                      Code (reserved&#x3D;273MB, committed&#x3D;171MB)
                            (malloc&#x3D;29MB #39947)
                            (mmap: reserved&#x3D;244MB, committed&#x3D;142MB)

-                        GC (reserved&#x3D;176MB, committed&#x3D;176MB)
                            (malloc&#x3D;30MB #114115)
                            (mmap: reserved&#x3D;146MB, committed&#x3D;146MB)

-                  Compiler (reserved&#x3D;1MB, committed&#x3D;1MB)

-                  Internal (reserved&#x3D;88MB, committed&#x3D;88MB)
                            (malloc&#x3D;88MB #83467)

-                    Symbol (reserved&#x3D;31MB, committed&#x3D;31MB)
                            (malloc&#x3D;27MB #312661)
                            (arena&#x3D;4MB #1)

-    Native Memory Tracking (reserved&#x3D;10MB, committed&#x3D;10MB)
                            (tracking overhead&#x3D;10MB)

-                   Unknown (reserved&#x3D;6MB, committed&#x3D;0MB)
                            (mmap: reserved&#x3D;6MB, committed&#x3D;0MB)</code></pre>
<p><img src="/images/1244488-20180921225245348-1616182920.png"><br>内存增长情况(中间下降是进行fgc或重启服务)  </p>
<p>最开始考虑是直接内存的问题,观察GC行为发现没有发生过一次fgc,想当然觉得应该是直接内存无法回收,并且在手动执行gc之后内存占用的确是下降的.<br>于是调整了一波JVM参数,将堆大小设置为2500M,并配置MaxDirectMemory用来触发fgc.<br>但最后发现内存依旧增长,且还是没有fgc.<br>再观察了下heap(jmap -heap)的情况,发现手工触发fgc后,heap释放的大小和内存下降几乎一致.<br>在使用JMX查看了direct pool(java.nio.BufferPool)后惊奇地发现..直接内存并没有多少使用(我们底层用了netty,下意识认为出了问题):</p>
<p><img src="/images/1244488-20180921230143974-1129789118.png"><br>图中所示最大只用了4M左右.  </p>
<p>最后设置了<code>Xms</code>之后发现fgc已经没有作用,内存的使用量不会再下降,排除掉了这个因素.<br>这个错误的判断最主要的原因还是先入为主,没有收集支撑的直接证据.  </p>
<h1 id="stack中的间隙"><a href="#stack中的间隙" class="headerlink" title="stack中的间隙"></a>stack中的间隙</h1><p>接下去只能使用<code>pmap</code>(pamp -X pid)看下具体的内存信息.<br><img src="/images/1244488-20180921230450727-543896772.png">  </p>
<p>发现了一些有趣的东西,大量的64M块.<br>直觉告诉我,快找出问题了.<br>搜索了下发现类似的问题:  </p>
<p><img src="/images/1244488-20180921230615858-2043303086.png"></p>
<p>解决方法也很直接换一个分配内存的库.<br>可选的也只有tcmalloc和jemalloc.  </p>
<p>选择了前者,配置之后效果出众.<br>已经没有上图中那么多的64M块了.<br>并且启动后的内存使用是掉了下来,但是好景不长.  </p>
<p><img src="/images/1244488-20180921230953366-1591448704.png"><br>(蓝线是使用了tcmalloc,红线是对比方)<br>刚开始的确蓝线内存使用好一点(红线在20:00的时候重启 内存降低了).<br>但随后还是增长了,比较迷的一点是在8点后内存陡降(但JVM没有重启,PID也没有变化),恢复到了最初的水平.  </p>
<p>看来问题不在这里.  </p>
<h1 id="看似没有关联的问题"><a href="#看似没有关联的问题" class="headerlink" title="看似没有关联的问题"></a>看似没有关联的问题</h1><p>周常部署的时候出现一个问题导致CPU飙升,跟了下发现一个处理图片的接口被频繁调用并且在处理较大尺寸的图片,看似和这个堆外内存引起的问题没有关联就没怎么在意.  </p>
<p>第二天同事修复了这个问题,迁移走了这个接口到另一个服务.<br>tcmalloc的heap profiling无法使用,一开启<code>libunwind</code>就<code>segment fault</code>退出,换成jemalloc后开启.<br>还在想是不是常见的那个zip溢出的问题,但是搜索了代码库没有发现忘记关闭的情况,等着看下增长和dump验证下.<br>观察了一会儿,突然发现内存不再增长了.   </p>
<p><img src="/images/1244488-20180921231556902-865599480.png"><br>(蓝线是之前一直留着的机器,中间降低是进行了重启)<br>非常平稳地度过了一天.  </p>
<p>那坨profiling也失去了意义… …  </p>
<p>这就感觉非常尴尬了…</p>
<h1 id="继续追踪"><a href="#继续追踪" class="headerlink" title="继续追踪"></a>继续追踪</h1><p>更尴尬的是另一个服务炸了… …<br>继续跟进,先把jemalloc配置过去,等下一次增长看下dump.  </p>
<p>update 2018/09/25<br><img src="/images/1244488-20180925095006464-1897340578.png"><br>继续观察了一段时间… 发现在内存在突然增大的时候这个部分增加比较多<br>java2d相关,代码里确实有图片处理.<br>但放着不管(之前都重启了或者被OOM killer干掉了..)接下去会被回收掉…    </p>
<p>update 2018/09/27<br>之后做了一个实验,读取图片(jpeg),的确是会申请很多native内存.<br>在爆栈提了个问题:<br><a href="https://stackoverflow.com/questions/52519253/why-the-code-using-image-io-mallocs-so-much-memory">https://stackoverflow.com/questions/52519253/why-the-code-using-image-io-mallocs-so-much-memory</a><br>多线程同时读取内存会升高明显.  </p>
<p>大体确定是业务原因,读取处理图片是同步进行,会导致堆外内存占用升高.<br>fix应该是用任务队列的方式去修改业务实现.<br>再看看之后会不会有问题.  </p>
<hr>
<p>随着时间的推移遇见了各种各样的问题.<br>这次排查除了对之前积累的知识的运用,更多像是一次对搜索引擎熟练度的提升 :( .  </p>
<p>但过程还是很不错的,也看到了一些自己以前没有接触过的知识.<br>深深感受到自己知识的匮乏,就像深渊(??)一样,逐渐深入才会发现不一样的风景.<br>生活中还是要多(xie)些bug才会更精彩的啊… …  </p>
<p><img src="/images/1244488-20180921233424486-892720676.png"></p>
<hr>
<p>参考资料:</p>
<p><a href="http://lovestblog.cn/blog/2015/05/12/direct-buffer/">http://lovestblog.cn/blog/2015/05/12/direct-buffer/</a>  </p>
<p><a href="https://yq.aliyun.com/articles/227924">https://yq.aliyun.com/articles/227924</a></p>
<p><a href="https://github.com/jeffgriffith/native-jvm-leaks">https://github.com/jeffgriffith/native-jvm-leaks</a></p>
<p>glibc issue原问题:<br><a href="https://serverfault.com/questions/341579/what-consumes-memory-in-java-process?newreg=394e0ea40e55493d8149010909011719">https://serverfault.com/questions/341579/what-consumes-memory-in-java-process?newreg=394e0ea40e55493d8149010909011719</a>   </p>
<p>关于NMT的internal 包含了直接内存 不包含DirectMemory:<br><a href="https://stackoverflow.com/questions/47309859/what-is-contained-in-code-internal-sections-of-jcmd">https://stackoverflow.com/questions/47309859/what-is-contained-in-code-internal-sections-of-jcmd</a></p>
]]></content>
  </entry>
  <entry>
    <title>java并发编程知识点备忘</title>
    <url>/2018/04/25/java-concurrent-note/</url>
    <content><![CDATA[<p>最近有在回顾这方面的知识,稍微进行一些整理和归纳防止看了就忘记.<br>会随着进度不断更新内容,比较零散但尽量做的覆盖广一点.<br>如有错误烦请指正~</p>
<hr>
<span id="more"></span>
<h1 id="java线程状态图"><a href="#java线程状态图" class="headerlink" title="java线程状态图"></a>java线程状态图</h1><p><img src="/images/1244488-20180426021937095-779874420.png"></p>
<hr>
<h1 id="线程活跃性问题"><a href="#线程活跃性问题" class="headerlink" title="线程活跃性问题"></a>线程活跃性问题</h1><ul>
<li>死锁</li>
<li>饥饿</li>
<li>活锁  </li>
</ul>
<p>饥饿原因:  </p>
<ul>
<li>高优先级造成低优先级无法运行(概率吧)</li>
<li>无法进入同步块(比如进入的线程陷入死循环)</li>
<li>无法被唤醒(没有notify)</li>
</ul>
<hr>
<p>线程安全性问题的条件:</p>
<ul>
<li>多线程环境下</li>
<li>多线程共享同个资源</li>
<li>存在非原子性操作<br>破坏掉其中一条即可 </li>
</ul>
<hr>
<h1 id="synchronized"><a href="#synchronized" class="headerlink" title="synchronized"></a>synchronized</h1><p>内置锁<br>涉及字节码:monitorenter monitorexit<br>锁的信息存在对象头中</p>
<p>偏向锁 轻量级锁 重量级锁相关:<br><img src="/images/1244488-20180426022827527-266095883.png"><br><img src="/images/1244488-20180426022837940-1764731172.png"></p>
<p>参考资料:<br><a href="https://wiki.openjdk.java.net/display/HotSpot/Synchronization">Synchronization</a><br><a href="https://blog.csdn.net/zqz_zqz/article/details/70233767">java 中的锁 – 偏向锁、轻量级锁、自旋锁、重量级锁</a></p>
<hr>
<h1 id="单例模式实现"><a href="#单例模式实现" class="headerlink" title="单例模式实现"></a>单例模式实现</h1><p>积极加载<br>懒加载:double check,静态内部类<br>枚举实现  </p>
<p>参考资料:<br><a href="https://blog.csdn.net/goodlixueyong/article/details/51935526">Java单例模式——并非看起来那么简单</a>  </p>
<hr>
<h1 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h1><p>硬盘 - 内存 - CPU缓存<br>lock指令: 当前处理器缓存行写回内存,其他处理器该内存地址数据失效  </p>
<p>参考资料:<br><a href="http://www.importnew.com/18126.html">Java并发编程：volatile关键字解析</a>  </p>
<hr>
<h1 id="原子更新类"><a href="#原子更新类" class="headerlink" title="原子更新类"></a>原子更新类</h1><p>JDK5开始提供,J.U.C.atomic包<br>底层多是使用了Unsafe类的CAS操作.<br>基本分类:  </p>
<ul>
<li>原子更新基本类型: AtomicInteger</li>
<li>原子更新数组: AtomicIntegerArray  </li>
<li>原子更新引用类型: AtomicReference</li>
<li>原子更新字段: AtomicIntegerFieldUpdater  </li>
</ul>
<p>参考资料:<br><a href="http://ifeve.com/java-atomic/">Java中的Atomic包使用指南</a>  </p>
<hr>
<h1 id="AQS"><a href="#AQS" class="headerlink" title="AQS"></a>AQS</h1><p>可以作为锁 同步器的基础.<br>本身是一个模板类,只需要重写所需的抽象方法:<br><img src="/images/1244488-20180609201705069-2063211362.png">  </p>
<p>获取和释放(独占)的分析:<br><img src="/images/1244488-20180609201936193-1665209808.png"><br>获取成功下直接返回.<br>如果获取失败,并且被中断下就直接返回.  </p>
<p><img src="/images/1244488-20180609202057443-79184992.png"><br>如果没有抢占成功,则判断是否需要park,park后线程将不再被调度,park解除后返回中断状态,(但这个实现中即使被中断也没关系,在acquireInterruptibly中则会直接抛错)  </p>
<p><img src="/images/1244488-20180609203155633-1825835499.png"><br>是否需要park是由上一个节点的状态来决定的,在父节点为SIGNAL的情况下需要park,如果大于0(表示cancel,这个节点以及前面连续的cancel节点需要从队列里出去了)<br>这里可以看到不是SIGNAL的情况下父节点要么被移出要么被设置为了SIGNAL,注意这段代码外层是一个死循环,所以最终如果一直没抢占到,这个线程肯定会被park的.<br>这个设计在源码里就有说明,SIGNAL不是用来控制当前节点而是控制他的子节点,同时也说明这个节点的子节点正在等待.<br>对应release的时候也是,释放的节点(头节点)需要判断自己的状态是否小于0(为0的话说明没有子节点被添加要唤醒).  </p>
<p>自己看源码的简单分析,只看了这一个流程.    </p>
<p>公平和非公平主要就是对于队列的操作,公平的实现直接获取下一个节点即可.  </p>
<p>更为详细的请参考:<br><a href="%22https://www.cnblogs.com/leesf456/p/5350186.html%22">【JUC】JDK1.8源码分析之AbstractQueuedSynchronizer（二）</a><br><a href="%22http://ifeve.com/introduce-abstractqueuedsynchronizer/%22">AbstractQueuedSynchronizer的介绍和原理分析</a>  </p>
<p>2018年6月09日20点42分</p>
<hr>
<h1 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h1><p>要注意用了一个state存了两个锁的状态 共享锁(读锁)高16位 互斥锁(写锁)低16位<br><img src="/images/1244488-20180719022157877-172838333.png"></p>
<p>读锁获取的前置条件:<br><img src="/images/1244488-20180719022316487-1653952882.png"><br>读锁要获取前提是没有写锁或者有写锁但是写锁是自己持有的<br>这也是锁能降级的实现原理 一个线程持有写锁后可以自己持有读锁  </p>
<p><img src="/images/1244488-20180719022519821-1742436118.png"><br>要要获取读锁的话,得保证没有读锁,这也就是<code>ReentrantReadWriteLock</code>无法实现升级的原因.<br>所以千万不要写出这样的代码</p>
<pre class="language-java" data-language="java"><code class="language-java">readLock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
writeLock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
readLock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
writeLock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>获取写锁时就会发生死锁.   </p>
<p>2018年7月19日02点37分</p>
<p>Update:<br>2018年4月26日02点38分 init<br>2018年5月5日16点17分 单例-原子更新类</p>
]]></content>
  </entry>
  <entry>
    <title>jetty8 text/plain默认字符编码的坑</title>
    <url>/2018/03/28/jetty-8-default-character-encoding/</url>
    <content><![CDATA[<p>今天在测试一个content-type为<code>text/plain</code>的API时发现后端requestBody乱码了，而线上正常。<br>自己本地使用jetty8版本，插件自带版本，而线上使用jetty9。<br>最开始没有特别注意版本的差异，毕竟这个插件也用了很久了一直没问题，就先从请求分析起。    </p>
<span id="more"></span>
<p>检查了下发送的请求中没有设置charset，但项目里是设置了spring的编码过滤器，CharacterEncodingFilter中的逻辑：</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">String</span> encoding <span class="token operator">=</span> <span class="token function">getEncoding</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>encoding <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">isForceRequestEncoding</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">||</span> request<span class="token punctuation">.</span><span class="token function">getCharacterEncoding</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        request<span class="token punctuation">.</span><span class="token function">setCharacterEncoding</span><span class="token punctuation">(</span>encoding<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token function">isForceResponseEncoding</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        response<span class="token punctuation">.</span><span class="token function">setCharacterEncoding</span><span class="token punctuation">(</span>encoding<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>encoding配置里设置为<code>UTF-8</code>，强制设置没有开启，所以在request没有设置字符编码时会进行设置。<br>但奇怪的是乱码的请求里是先被设置成了<code>ISO-8859-1</code>，在想会不会是gateway转发影响，进行抓包分析但还是没有看见charset。<br>远程debug了正常的测试环境发现字符编码没有被预先设置。<br>这就非常尴尬了，虽然测试环境没有问题，但本地这个问题的确是存在的，万一上了预发环境也有问题就麻烦了，看了一会儿diff没发现有配置上的变更。<br>在同事的提醒下，也只有jetty版本的原因了。   </p>
<p>拉取了下jetty8的源码（jetty-8.8.0-SNAPSHOT），查看了一下，发现的确有个坑。</p>
<p><code>AbstractHttpConnection</code>中的<code>parsedHeader</code>负责解析header，其中对应charset的解析代码:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">case</span> <span class="token class-name">HttpHeaders</span><span class="token punctuation">.</span>CONTENT_TYPE_ORDINAL<span class="token operator">:</span>
    value <span class="token operator">=</span> <span class="token class-name">MimeTypes</span><span class="token punctuation">.</span>CACHE<span class="token punctuation">.</span><span class="token function">lookup</span><span class="token punctuation">(</span>value<span class="token punctuation">)</span><span class="token punctuation">;</span>
    _charset<span class="token operator">=</span><span class="token class-name">MimeTypes</span><span class="token punctuation">.</span><span class="token function">getCharsetFromContentType</span><span class="token punctuation">(</span>value<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">break</span><span class="token punctuation">;</span></code></pre>

<p>解析出来的_charset会在<code>headerComplete</code>中设置进request内:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">if</span><span class="token punctuation">(</span>_charset<span class="token operator">!=</span><span class="token keyword">null</span><span class="token punctuation">)</span>
    _request<span class="token punctuation">.</span><span class="token function">setCharacterEncodingUnchecked</span><span class="token punctuation">(</span>_charset<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>主要的判断代码在<code>MimeTypes</code>中</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">String</span> <span class="token function">getCharsetFromContentType</span><span class="token punctuation">(</span><span class="token class-name">Buffer</span> value<span class="token punctuation">)</span>
<span class="token punctuation">&#123;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>value <span class="token keyword">instanceof</span> <span class="token class-name">CachedBuffer</span><span class="token punctuation">)</span>
    <span class="token punctuation">&#123;</span>
        <span class="token keyword">switch</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token class-name">CachedBuffer</span><span class="token punctuation">)</span>value<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getOrdinal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">&#123;</span>
            <span class="token keyword">case</span> TEXT_HTML_8859_1_ORDINAL<span class="token operator">:</span>
            <span class="token keyword">case</span> TEXT_PLAIN_8859_1_ORDINAL<span class="token operator">:</span>
            <span class="token keyword">case</span> TEXT_XML_8859_1_ORDINAL<span class="token operator">:</span>
                <span class="token keyword">return</span> <span class="token class-name">StringUtil</span><span class="token punctuation">.</span>__ISO_8859_1<span class="token punctuation">;</span>

            <span class="token keyword">case</span> TEXT_HTML_UTF_8_ORDINAL<span class="token operator">:</span>
            <span class="token keyword">case</span> TEXT_PLAIN_UTF_8_ORDINAL<span class="token operator">:</span>
            <span class="token keyword">case</span> TEXT_XML_UTF_8_ORDINAL<span class="token operator">:</span>
            <span class="token keyword">case</span> TEXT_JSON_UTF_8_ORDINAL<span class="token operator">:</span>
                <span class="token keyword">return</span> <span class="token class-name">StringUtil</span><span class="token punctuation">.</span>__UTF8<span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token comment">//下面是用来解析charset的</span>
    <span class="token keyword">int</span> i<span class="token operator">=</span>value<span class="token punctuation">.</span><span class="token function">getIndex</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">final</span> <span class="token keyword">int</span> end<span class="token operator">=</span>value<span class="token punctuation">.</span><span class="token function">putIndex</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> start<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span>
    <span class="token keyword">boolean</span> quote<span class="token operator">=</span><span class="token boolean">false</span><span class="token punctuation">;</span>
    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token punctuation">;</span>i<span class="token operator">&lt;</span>end<span class="token punctuation">;</span>i<span class="token operator">++</span><span class="token punctuation">)</span>
    <span class="token punctuation">&#123;</span>
        <span class="token keyword">final</span> <span class="token keyword">byte</span> b <span class="token operator">=</span> value<span class="token punctuation">.</span><span class="token function">peek</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>

        <span class="token keyword">if</span> <span class="token punctuation">(</span>quote <span class="token operator">&amp;&amp;</span> state<span class="token operator">!=</span><span class="token number">10</span><span class="token punctuation">)</span>
        <span class="token punctuation">&#123;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'"'</span><span class="token operator">==</span>b<span class="token punctuation">)</span>
                quote<span class="token operator">=</span><span class="token boolean">false</span><span class="token punctuation">;</span>
            <span class="token keyword">continue</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>

        <span class="token keyword">switch</span><span class="token punctuation">(</span>state<span class="token punctuation">)</span>
        <span class="token punctuation">&#123;</span>
            <span class="token keyword">case</span> <span class="token number">0</span><span class="token operator">:</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'"'</span><span class="token operator">==</span>b<span class="token punctuation">)</span>
                <span class="token punctuation">&#123;</span>
                    quote<span class="token operator">=</span><span class="token boolean">true</span><span class="token punctuation">;</span>
                    <span class="token keyword">break</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">';'</span><span class="token operator">==</span>b<span class="token punctuation">)</span>
                    state<span class="token operator">=</span><span class="token number">1</span><span class="token punctuation">;</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>

            <span class="token keyword">case</span> <span class="token number">1</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'c'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">2</span><span class="token punctuation">;</span> <span class="token keyword">else</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">' '</span><span class="token operator">!=</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span> <span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">2</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'h'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">3</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">3</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'a'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">4</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">4</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'r'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">5</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">5</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'s'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">6</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">6</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'e'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">7</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>
            <span class="token keyword">case</span> <span class="token number">7</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'t'</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">8</span><span class="token punctuation">;</span> <span class="token keyword">else</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token keyword">break</span><span class="token punctuation">;</span>

            <span class="token keyword">case</span> <span class="token number">8</span><span class="token operator">:</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'='</span><span class="token operator">==</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">9</span><span class="token punctuation">;</span> <span class="token keyword">else</span> <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">' '</span><span class="token operator">!=</span>b<span class="token punctuation">)</span> state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">;</span> <span class="token keyword">break</span><span class="token punctuation">;</span>

            <span class="token keyword">case</span> <span class="token number">9</span><span class="token operator">:</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">' '</span><span class="token operator">==</span>b<span class="token punctuation">)</span>
                    <span class="token keyword">break</span><span class="token punctuation">;</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">'"'</span><span class="token operator">==</span>b<span class="token punctuation">)</span>
                <span class="token punctuation">&#123;</span>
                    quote<span class="token operator">=</span><span class="token boolean">true</span><span class="token punctuation">;</span>
                    start<span class="token operator">=</span>i<span class="token operator">+</span><span class="token number">1</span><span class="token punctuation">;</span>
                    state<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
                    <span class="token keyword">break</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span>
                start<span class="token operator">=</span>i<span class="token punctuation">;</span>
                state<span class="token operator">=</span><span class="token number">10</span><span class="token punctuation">;</span>
                <span class="token keyword">break</span><span class="token punctuation">;</span>

            <span class="token keyword">case</span> <span class="token number">10</span><span class="token operator">:</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>quote <span class="token operator">&amp;&amp;</span> <span class="token punctuation">(</span><span class="token string">';'</span><span class="token operator">==</span>b <span class="token operator">||</span> <span class="token string">' '</span><span class="token operator">==</span>b <span class="token punctuation">)</span><span class="token operator">||</span>
                    <span class="token punctuation">(</span>quote <span class="token operator">&amp;&amp;</span> <span class="token string">'"'</span><span class="token operator">==</span>b <span class="token punctuation">)</span><span class="token punctuation">)</span>
                    <span class="token keyword">return</span> CACHE<span class="token punctuation">.</span><span class="token function">lookup</span><span class="token punctuation">(</span>value<span class="token punctuation">.</span><span class="token function">peek</span><span class="token punctuation">(</span>start<span class="token punctuation">,</span>i<span class="token operator">-</span>start<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token class-name">StringUtil</span><span class="token punctuation">.</span>__UTF8<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>state<span class="token operator">==</span><span class="token number">10</span><span class="token punctuation">)</span>
        <span class="token keyword">return</span> CACHE<span class="token punctuation">.</span><span class="token function">lookup</span><span class="token punctuation">(</span>value<span class="token punctuation">.</span><span class="token function">peek</span><span class="token punctuation">(</span>start<span class="token punctuation">,</span>i<span class="token operator">-</span>start<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token class-name">StringUtil</span><span class="token punctuation">.</span>__UTF8<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">//默认路径</span>
    <span class="token keyword">return</span> <span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">)</span>__encodings<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>value<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p> 简单来看首先检查是否是预设的一些content-type，如果是的话直接返回。<br> 而传入的<code>text/plain</code>不在默认的范围内，接下里的代码是寻找charset或者”charset”，而传入的内容里并没有。<br> 最后走到了<code>return (String)__encodings.get(value);</code>，通过上面的代码可以找到这个属性的设置:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">final</span> <span class="token class-name">ResourceBundle</span> encoding <span class="token operator">=</span> <span class="token class-name">ResourceBundle</span><span class="token punctuation">.</span><span class="token function">getBundle</span><span class="token punctuation">(</span><span class="token string">"org/eclipse/jetty/http/encoding"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">final</span> <span class="token class-name">Enumeration</span> i <span class="token operator">=</span> encoding<span class="token punctuation">.</span><span class="token function">getKeys</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">while</span><span class="token punctuation">(</span>i<span class="token punctuation">.</span><span class="token function">hasMoreElements</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
<span class="token punctuation">&#123;</span>
    <span class="token keyword">final</span> <span class="token class-name">Buffer</span> type <span class="token operator">=</span> <span class="token function">normalizeMimeType</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">)</span>i<span class="token punctuation">.</span><span class="token function">nextElement</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    __encodings<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span>type<span class="token punctuation">,</span>encoding<span class="token punctuation">.</span><span class="token function">getString</span><span class="token punctuation">(</span>type<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>使用了ResourceBundle，对应的资源文件内容：</p>
<pre class="language-none"><code class="language-none">text&#x2F;html	&#x3D; ISO-8859-1
text&#x2F;plain	&#x3D; ISO-8859-1
text&#x2F;xml	&#x3D; UTF-8
text&#x2F;json   &#x3D; UTF-8</code></pre>
<p>… …默认值为ISO-8859-1。<br>而jetty9呢，切回master分支，jetty9设置的代码直接在<code>Request</code>类中了，如下：</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@Override</span>
<span class="token keyword">public</span> <span class="token class-name">String</span> <span class="token function">getCharacterEncoding</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">&#123;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>_characterEncoding<span class="token operator">==</span><span class="token keyword">null</span><span class="token punctuation">)</span>
        <span class="token function">getContentType</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> _characterEncoding<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>



<span class="token annotation punctuation">@Override</span>
<span class="token keyword">public</span> <span class="token class-name">String</span> <span class="token function">getContentType</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">&#123;</span>
    <span class="token class-name">MetaData<span class="token punctuation">.</span>Request</span> metadata <span class="token operator">=</span> _metaData<span class="token punctuation">;</span>
    <span class="token class-name">String</span> content_type <span class="token operator">=</span> metadata<span class="token operator">==</span><span class="token keyword">null</span><span class="token operator">?</span><span class="token keyword">null</span><span class="token operator">:</span>metadata<span class="token punctuation">.</span><span class="token function">getFields</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token class-name">HttpHeader</span><span class="token punctuation">.</span>CONTENT_TYPE<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>_characterEncoding<span class="token operator">==</span><span class="token keyword">null</span> <span class="token operator">&amp;&amp;</span> content_type<span class="token operator">!=</span><span class="token keyword">null</span><span class="token punctuation">)</span>
    <span class="token punctuation">&#123;</span>
        <span class="token class-name">MimeTypes<span class="token punctuation">.</span>Type</span> mime <span class="token operator">=</span> <span class="token class-name">MimeTypes</span><span class="token punctuation">.</span>CACHE<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>content_type<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token class-name">String</span> charset <span class="token operator">=</span> <span class="token punctuation">(</span>mime <span class="token operator">==</span> <span class="token keyword">null</span> <span class="token operator">||</span> mime<span class="token punctuation">.</span><span class="token function">getCharset</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token operator">?</span> <span class="token class-name">MimeTypes</span><span class="token punctuation">.</span><span class="token function">getCharsetFromContentType</span><span class="token punctuation">(</span>content_type<span class="token punctuation">)</span> <span class="token operator">:</span> mime<span class="token punctuation">.</span><span class="token function">getCharset</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>charset <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span>
            _characterEncoding<span class="token operator">=</span>charset<span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
    <span class="token keyword">return</span> content_type<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>   类似的逻辑还是继续走<code>MimeTypes</code>的方法，但最后发生了变化:</p>
<pre class="language-java" data-language="java"><code class="language-java">       <span class="token keyword">if</span> <span class="token punctuation">(</span>state<span class="token operator">==</span><span class="token number">10</span><span class="token punctuation">)</span>
        <span class="token keyword">return</span> <span class="token class-name">StringUtil</span><span class="token punctuation">.</span><span class="token function">normalizeCharset</span><span class="token punctuation">(</span>value<span class="token punctuation">,</span>start<span class="token punctuation">,</span>i<span class="token operator">-</span>start<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token keyword">return</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>已经没有那个默认逻辑，直接返回null了。<br>这个改动已经非常久远了。<br>避免踩这种坑，勤升版本很重要啊… …<br>感觉又荒废了时光…</p>
]]></content>
  </entry>
  <entry>
    <title>异步请求中jetty处理ServletRequestListener的坑</title>
    <url>/2017/11/19/jetty-ServletRequestListener-async/</url>
    <content><![CDATA[<p>标题起得比较诡异，其实并不是坑，而是jetty似乎压根就没做对异步request的<code>ServletRequestListener</code>的特殊处理，如果文中有错误欢迎提出，可能自己有所疏漏了。</p>
<span id="more"></span>
<p>之前遇到了一个bug，在Listener中重写<code>requestDestroyed</code>清理资源后，这些资源在异步任务中就不可用了。<br>这与预期不符，直觉上request应该在任务完成之后才触发<code>requestDestroyed</code>，而不应该是开始异步操作返回后就触发。<br>正确的触发时机应该是异步任务完成之后。<br>后来查阅了下，发现<code>servlet 3</code>的规范中只是增加了异步servlet和异步filter的支持，listener如何处理没有定义，也就是各个容器的实现可能会差异（我们自己使用的是jetty）。<br>但讲道理的话，合理的实现应该判断当前request是否处于异步处理中，如果是的话将销毁的触发的时机放置在<code>AsyncContext</code>完成之后。</p>
<p>自己试了下使用两个容器：<code>Tomcat 8.5.15</code>和<code>Jetty 9.4.7.v20170914</code>。</p>
<p>测试代码比较简单，为了去除其他框架的影响使用纯的servlet api。<br>servlet：   </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@WebServlet</span><span class="token punctuation">(</span>asyncSupported <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">,</span> urlPatterns <span class="token operator">=</span> <span class="token punctuation">&#123;</span> <span class="token string">"/test"</span> <span class="token punctuation">&#125;</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">TestServlet</span> <span class="token keyword">extends</span> <span class="token class-name">HttpServlet</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> serialVersionUID <span class="token operator">=</span> <span class="token number">7395865716615939512L</span><span class="token punctuation">;</span>

    <span class="token keyword">private</span> <span class="token class-name">ExecutorService</span> pool <span class="token operator">=</span> <span class="token class-name">Executors</span><span class="token punctuation">.</span><span class="token function">newCachedThreadPool</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">destroy</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        pool<span class="token punctuation">.</span><span class="token function">shutdown</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">protected</span> <span class="token keyword">void</span> <span class="token function">doGet</span><span class="token punctuation">(</span><span class="token class-name">HttpServletRequest</span> request<span class="token punctuation">,</span> <span class="token class-name">HttpServletResponse</span> response<span class="token punctuation">)</span>
            <span class="token keyword">throws</span> <span class="token class-name">ServletException</span><span class="token punctuation">,</span> <span class="token class-name">IOException</span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" get start "</span> <span class="token operator">+</span> request<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token class-name">AsyncContext</span> context <span class="token operator">=</span> request<span class="token punctuation">.</span><span class="token function">startAsync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        context<span class="token punctuation">.</span><span class="token function">addListener</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">AsyncListener</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token annotation punctuation">@Override</span>
            <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">onTimeout</span><span class="token punctuation">(</span><span class="token class-name">AsyncEvent</span> event<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">IOException</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" async onTimeout "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getSuppliedRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>

            <span class="token annotation punctuation">@Override</span>
            <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">onStartAsync</span><span class="token punctuation">(</span><span class="token class-name">AsyncEvent</span> event<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">IOException</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" async onStartAsync "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getSuppliedRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>

            <span class="token annotation punctuation">@Override</span>
            <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">onError</span><span class="token punctuation">(</span><span class="token class-name">AsyncEvent</span> event<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">IOException</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" async onError "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getSuppliedRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>

            <span class="token annotation punctuation">@Override</span>
            <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">onComplete</span><span class="token punctuation">(</span><span class="token class-name">AsyncEvent</span> event<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">IOException</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" async onComplete "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getSuppliedRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">,</span> request<span class="token punctuation">,</span> response<span class="token punctuation">)</span><span class="token punctuation">;</span>
        pool<span class="token punctuation">.</span><span class="token function">execute</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-</span><span class="token operator">&amp;</span>gt<span class="token punctuation">;</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">sleep</span><span class="token punctuation">(</span><span class="token class-name">TimeUnit</span><span class="token punctuation">.</span>SECONDS<span class="token punctuation">.</span><span class="token function">toMillis</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" job done"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                context<span class="token punctuation">.</span><span class="token function">complete</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                e<span class="token punctuation">.</span><span class="token function">printStackTrace</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" get return"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

<span class="token punctuation">&#125;</span></code></pre>
<p>listener:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@WebListener</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">TestListener</span> <span class="token keyword">implements</span> <span class="token class-name">ServletRequestListener</span> <span class="token punctuation">&#123;</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">requestDestroyed</span><span class="token punctuation">(</span><span class="token class-name">ServletRequestEvent</span> event<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" TestListener requestDestroyed "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getServletRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">requestInitialized</span><span class="token punctuation">(</span><span class="token class-name">ServletRequestEvent</span> event<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">LocalDateTime</span><span class="token punctuation">.</span><span class="token function">now</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" TestListener requestInitialized "</span> <span class="token operator">+</span> event<span class="token punctuation">.</span><span class="token function">getServletRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>

<span class="token punctuation">&#125;</span></code></pre>
<p>使用tomcat返回的结果:    </p>
<pre class="language-none"><code class="language-none">2017-11-19T16:42:04.256 TestListener requestInitialized org.apache.catalina.connector.RequestFacade@7c2df671
2017-11-19T16:42:04.257 get start org.apache.catalina.connector.RequestFacade@7c2df671
2017-11-19T16:42:04.261 get return
2017-11-19T16:42:09.261 job done
2017-11-19T16:42:09.261 async onComplete org.apache.catalina.connector.RequestFacade@7c2df671
2017-11-19T16:42:09.261 TestListener requestDestroyed org.apache.catalina.connector.RequestFacade@7c2df671</code></pre>
<p>使用jetty返回的结果：    </p>
<pre class="language-none"><code class="language-none">2017-11-19T16:46:35.231 TestListener requestInitialized (GET &#x2F;&#x2F;localhost:8080&#x2F;test)@1124787543 org.eclipse.jetty.server.Request@430ae557
2017-11-19T16:46:35.232 get start (GET &#x2F;&#x2F;localhost:8080&#x2F;test)@1124787543 org.eclipse.jetty.server.Request@430ae557
2017-11-19T16:46:35.234 get return
2017-11-19T16:46:35.235 TestListener requestDestroyed [GET &#x2F;&#x2F;localhost:8080&#x2F;test]@1124787543 org.eclipse.jetty.server.Request@430ae557
2017-11-19T16:46:40.235 job done
2017-11-19T16:46:58.019 async onComplete [GET &#x2F;&#x2F;localhost:8080&#x2F;test]@1124787543 org.eclipse.jetty.server.Request@430ae557</code></pre>
<p>jetty果然没有讲道理… …  </p>
<p>先看下讲道理的tomcat，果不其然他做了特殊判断。<br>在<code>StandardHostValve</code>的<code>invoke</code>方法中：  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>request<span class="token punctuation">.</span><span class="token function">isAsync</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&amp;</span>amp<span class="token punctuation">;</span><span class="token operator">&amp;</span>amp<span class="token punctuation">;</span> <span class="token operator">!</span>asyncAtStart<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    context<span class="token punctuation">.</span><span class="token function">fireRequestDestroyEvent</span><span class="token punctuation">(</span>request<span class="token punctuation">.</span><span class="token function">getRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>并且销毁的时机和预想的一样是在<code>AsyncContext</code>完成之后：  </p>
<p><img src="/images/1244488-20171119174205312-2127854720.png">  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">fireOnComplete</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">List</span><span class="token operator">&amp;</span>lt<span class="token punctuation">;</span><span class="token class-name">AsyncListenerWrapper</span><span class="token operator">&amp;</span>gt<span class="token punctuation">;</span> listenersCopy <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token operator">&amp;</span>lt<span class="token punctuation">;</span><span class="token operator">&amp;</span>gt<span class="token punctuation">;</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    listenersCopy<span class="token punctuation">.</span><span class="token function">addAll</span><span class="token punctuation">(</span>listeners<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token class-name">ClassLoader</span> oldCL <span class="token operator">=</span> context<span class="token punctuation">.</span><span class="token function">bind</span><span class="token punctuation">(</span><span class="token class-name">Globals</span><span class="token punctuation">.</span>IS_SECURITY_ENABLED<span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">AsyncListenerWrapper</span> listener <span class="token operator">:</span> listenersCopy<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">try</span> <span class="token punctuation">&#123;</span>
                listener<span class="token punctuation">.</span><span class="token function">fireOnComplete</span><span class="token punctuation">(</span>event<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Throwable</span> t<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">ExceptionUtils</span><span class="token punctuation">.</span><span class="token function">handleThrowable</span><span class="token punctuation">(</span>t<span class="token punctuation">)</span><span class="token punctuation">;</span>
                log<span class="token punctuation">.</span><span class="token function">warn</span><span class="token punctuation">(</span><span class="token string">"onComplete() failed for listener of type ["</span> <span class="token operator">+</span>
                        listener<span class="token punctuation">.</span><span class="token function">getClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">"]"</span><span class="token punctuation">,</span> t<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span> <span class="token keyword">finally</span> <span class="token punctuation">&#123;</span>
        context<span class="token punctuation">.</span><span class="token function">fireRequestDestroyEvent</span><span class="token punctuation">(</span>request<span class="token punctuation">.</span><span class="token function">getRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token function">clearServletRequestResponse</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        context<span class="token punctuation">.</span><span class="token function">unbind</span><span class="token punctuation">(</span><span class="token class-name">Globals</span><span class="token punctuation">.</span>IS_SECURITY_ENABLED<span class="token punctuation">,</span> oldCL<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>而jetty没有处理，在<code>ContextHandler</code>的<code>doHandle</code>中没有异步请求的判断：  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">finally</span>
        <span class="token punctuation">&#123;</span>
            <span class="token comment">// Handle more REALLY SILLY request events!</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>new_context<span class="token punctuation">)</span>
            <span class="token punctuation">&#123;</span>
                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>_servletRequestListeners<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
                <span class="token punctuation">&#123;</span>
                    <span class="token keyword">final</span> <span class="token class-name">ServletRequestEvent</span> sre <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ServletRequestEvent</span><span class="token punctuation">(</span>_scontext<span class="token punctuation">,</span>request<span class="token punctuation">)</span><span class="token punctuation">;</span>
                    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> i<span class="token operator">=</span>_servletRequestListeners<span class="token punctuation">.</span><span class="token function">size</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>i<span class="token operator">--</span><span class="token operator">&amp;</span>gt<span class="token punctuation">;</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token punctuation">)</span>
                        _servletRequestListeners<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">requestDestroyed</span><span class="token punctuation">(</span>sre<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span>

                <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>_servletRequestAttributeListeners<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
                <span class="token punctuation">&#123;</span>
                    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> i<span class="token operator">=</span>_servletRequestAttributeListeners<span class="token punctuation">.</span><span class="token function">size</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>i<span class="token operator">--</span><span class="token operator">&amp;</span>gt<span class="token punctuation">;</span><span class="token number">0</span><span class="token punctuation">;</span><span class="token punctuation">)</span>
                        baseRequest<span class="token punctuation">.</span><span class="token function">removeEventListener</span><span class="token punctuation">(</span>_servletRequestAttributeListeners<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">&#125;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span></code></pre>
<p>new_content用来在第一次调用返回true之后是false，和异步处理无关。  </p>
<p>网上查了些也不清楚为什么jetty没有像tomcat那样实现这个，还是jetty希望用户通过<code>AsyncListener</code>来做处理呢。</p>
]]></content>
  </entry>
  <entry>
    <title>[翻译]Java排错指南 - 5 确定崩溃何地发生</title>
    <url>/2018/12/05/jvm-crash-troubleshoot/</url>
    <content><![CDATA[<p>原文地址: <a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/crashes001.html">https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/crashes001.html</a></p>
<p>这几天公司其他组遇到了一个segmentation fault的问题,找到了这个官方文档,基于Java8,感觉不错就翻译了下.<br>一些地方翻译比较生硬,如有问题请麻烦指正~^_^ by fairjm</p>
<hr>
<span id="more"></span>

<h1 id="5-1-确定崩溃何地发生"><a href="#5-1-确定崩溃何地发生" class="headerlink" title="5.1 确定崩溃何地发生"></a>5.1 确定崩溃何地发生</h1><p>这一节提供了一些例子来演示如何使用错误日志来找到崩溃的原因,并且给出一些排查这些问题的建议.  </p>
<p>错误日志的头指出了错误的类型和有问题的帧(frame),thread stack指出了当前的线程和堆栈轨迹.查看<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/felog003.html#BABFFJBB">Header Format</a>  </p>
<p>Crash in Native Code</p>
<p>Crash in Compiled Code</p>
<p>Crash in HotSpot Compiler Thread</p>
<p>Crash in VM Thread</p>
<p>Crash Due to Stack Overflow  </p>
<h2 id="5-1-1-本地代码崩溃"><a href="#5-1-1-本地代码崩溃" class="headerlink" title="5.1.1 本地代码崩溃"></a>5.1.1 本地代码崩溃</h2><p>如果致命错误日志(fatal error log)指出的问题帧来自于本地库,那么可能是本地库或者JNI库代码存在bug.这种崩溃当然也有可能是其他原因造成的,但是分析这个库和其他的core file或者crash dump是一个很好的开始.<br>以下是一个致命错误日志的头:  </p>
<pre class="language-none"><code class="language-none"># An unexpected error has been detected by HotSpot Virtual Machine:
#
#  SIGSEGV (0xb) at pc&#x3D;0x417789d7, pid&#x3D;21139, tid&#x3D;1024
#
# Java VM: Java HotSpot(TM) Server VM (6-beta2-b63 mixed mode)
# Problematic frame:
# C  [libApplication.so+0x9d7]</code></pre>

<p>在这个例子中,<code>SIGSEGV</code>发生在线程执行<code>libApplication.so</code>中的代码.<br>一些其他例子是Java VM的本地库导致的.在下面的例子中,<code>JavaThread </code>在<code>_thread_in_vm </code>状态中失败了(表明它正在执行Java VM代码)  </p>
<pre class="language-none"><code class="language-none"># An unexpected error has been detected by HotSpot Virtual Machine:
#
#  EXCEPTION_ACCESS_VIOLATION (0xc0000005) at pc&#x3D;0x08083d77, pid&#x3D;3700, tid&#x3D;2896
#
# Java VM: Java HotSpot(TM) Client VM (1.5-internal mixed mode)
# Problematic frame:
# V  [jvm.dll+0x83d77]

---------------  T H R E A D  ---------------

Current thread (0x00036960):  JavaThread &quot;main&quot; [_thread_in_vm, id&#x3D;2896]
 :
Stack: [0x00040000,0x00080000),  sp&#x3D;0x0007f9f8,  free space&#x3D;254k
Native frames: (J&#x3D;compiled Java code, j&#x3D;interpreted, Vv&#x3D;VM code, C&#x3D;native code)
V  [jvm.dll+0x83d77]
C  [App.dll+0x1047]          &lt;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D; C&#x2F;native frame
j  Test.foo()V+0
j  Test.main([Ljava&#x2F;lang&#x2F;String;)V+0
v  ~StubRoutines::call_stub
V  [jvm.dll+0x80f13]
V  [jvm.dll+0xd3842]
V  [jvm.dll+0x80de4]
V  [jvm.dll+0x87cd2]
C  [java.exe+0x14c0]
C  [java.exe+0x64cd]
C  [kernel32.dll+0x214c7]
 :</code></pre>
<p>在这个例子中,虽然出问题的帧是VM的,线程栈显示一个本地例子(native routine)<code>App.dll</code>已经被VM调用(可能是通过JNI).  </p>
<p>解决这个本地库崩溃的第一步是调查本地库发生崩溃的那段源代码.  </p>
<ul>
<li>如果本地库是由你的应用程序提供,那么调查本地库的源代码.大量的问题可以通过在运行应用程序时使用<code>-Xcheck:jni</code>参数被识别.查看<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/clopts002.html#CHDDEGBI">The -Xcheck:jni Option</a>.  </li>
<li>如果这个本地库是由其他供应商提供,被你的程序所使用,那么就向他们提供bug报告和致命错误日志信息.  </li>
<li>如果本地库是来自于JRE的(比如awt.dll,net.dll或其他的),那有可能你遇到了一个库或者API的bug.那么就尽可能获取足够的信息提交一个bug并指名库名称.你可以在JRE的发行版中的 jre/lib 或 jre/bin目录中找到JRE的库.  </li>
</ul>
<p>如果可能的话,你可以通过本地debugger attach到core file或crash dump的方式来排查本地库崩溃.取决于你所使用的系统,本地debugger有<code>dbx</code>,<code>gdb</code>,或<code>windbg</code>.查看<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/tooldescr020.html#BABBHHIE"> Native Operating System Tools</a></p>
<h2 id="5-1-2-编译代码崩溃"><a href="#5-1-2-编译代码崩溃" class="headerlink" title="5.1.2 编译代码崩溃"></a>5.1.2 编译代码崩溃</h2><p>如果致命错误日志指出崩溃发生在编译代码(compiled code),那有可能你遇到了一个编译器导致的不正确代码生成的bug.你可以通过问题堆栈的类型是<code>J</code>(代表一个compiled java frame)来识别.  </p>
<pre class="language-none"><code class="language-none"># An unexpected error has been detected by HotSpot Virtual Machine:
#
#  SIGSEGV (0xb) at pc&#x3D;0x0000002a99eb0c10, pid&#x3D;6106, tid&#x3D;278546
#
# Java VM: Java HotSpot(TM) 64-Bit Server VM (1.6.0-beta-b51 mixed mode)
# Problematic frame:
# J  org.foobar.Scanner.body()V
#
:
Stack: [0x0000002aea560000,0x0000002aea660000),  sp&#x3D;0x0000002aea65ddf0,
  free space&#x3D;1015k
Native frames: (J&#x3D;compiled Java code, j&#x3D;interpreted, Vv&#x3D;VM code, C&#x3D;native code)
J  org.foobar.Scanner.body()V

[error occurred during error reporting, step 120, id 0xb]</code></pre>
<p>注意:该例子无法获得一个完整的线程栈.输出的”error occurred during error reporting” 表示问题发生在试图获取堆栈跟踪(可能是栈损坏了).  </p>
<p>通过更换编译器的方式可能可以临时解决(例如使用HotSpot Client VM取代HotSpot Server VM,或反过来)或者从编译中排除掉导致崩溃的方法.在这个例子中,将64位的Server VM换成32位的Client VM可能会有用.  </p>
<p>更多可能的规避措施见<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/crashes002.html#CIHDIBJA">Working Around Crashes in the HotSpot Compiler Thread or Compiled Code</a>.</p>
<h2 id="5-1-3-HotSpot编译器线程崩溃"><a href="#5-1-3-HotSpot编译器线程崩溃" class="headerlink" title="5.1.3 HotSpot编译器线程崩溃"></a>5.1.3 HotSpot编译器线程崩溃</h2><p>如果核心错误日志显示的当前线程是一个叫<code>CompilerThread0</code>,<code>CompilerThread1</code>或<code>AdapterCompiler</code>的<code>JavaThread</code>,那么你可能遇上了编译器bug.可能解决方式和上一节一样.(懒得复读了…)  </p>
<h2 id="5-1-4-VM线程崩溃"><a href="#5-1-4-VM线程崩溃" class="headerlink" title="5.1.4 VM线程崩溃"></a>5.1.4 VM线程崩溃</h2><p>如果核心错误日志显示的当前线程是<code>VMThread</code>,那么查看一下在<code>THREAD</code>那节包含<code>VM_Operation </code>的那一行.<code>VMThread </code>是特殊的的HotSpot VM线程.它执行一些特殊的工作比如GC.如果<code>VM_Operation</code>表明操作是GC,那么你可能遇到了诸如堆损坏的问题.  </p>
<p>包括GC问题之外,它同样可能是一些其他问题(例如编译器或者运行时bug)导致的对象引用在堆中处于一个不完整和不正确的状态.这种情况下,收集尽可能多的环境信息和尝试可能的规避方案.如果发现和GC有关,你可能通过修改GC配置的方式来临时保证正常运行.  </p>
<p>对于更多的规避方案,查看<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/crashes002.html#CIHEEBDG">Working Around Crashes during Garbage Collection</a></p>
<h2 id="5-1-5-爆栈崩溃"><a href="#5-1-5-爆栈崩溃" class="headerlink" title="5.1.5 爆栈崩溃"></a>5.1.5 爆栈崩溃</h2><p>java语言的一个栈溢出通常会导致线程抛出烦人的<code>java.lang.StackOverflowError</code>异常.另一方面,C和C++写入超过了栈的结束会引起一个栈溢出.这是一个致命的错误,会导致进程终止.  </p>
<p>在HotSpot实现中,Java方法和C/C++本地代码共享栈帧,即用户本地代码和VM自身.<br>Java方法产生的代码会检查栈离栈的结束是否会有固定距离可用的空间,所以本地代码的调用可以不担心是否会超过栈空间.<br>到栈结束的距离被称为<code>Shadow Pages</code>.这个大小取决于所在平台,shadow pages在3到20页之间.<br>这个距离是可以调整的,所以应用使用到了本地代码想要比默认更大的距离时可以增加shadow page的大小.<br>增加的参数是<code>-XX:StackShadowPages=n</code>,n设定为比当前平台默认值大.  </p>
<p>如果你的应用遇上了segmentation fault但是没有core file或致命错误日志参见<a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/felog.html#fatal_error_log_vm">Appendix A</a>,或者在windows上<code>STACK_OVERFLOW_ERROR </code>,或者得到一个消息”An irrecoverable stack overflow has occurred”,这说明超过了<code>StackShadowPages</code>,需要更大的空间.  </p>
<p>如果你增加了<code>StackShadowPages</code>,你可能也需要使用<code>-Xss</code>参数增加默认的线程栈大小.增加默认的线程栈大小可能会减少可创建的线程数,所以请小心选择这个数值.线程栈的大小于不同平台上在256KB到1024KB.  </p>
<pre class="language-none"><code class="language-none"># An unexpected error has been detected by HotSpot Virtual Machine:
#
#  EXCEPTION_STACK_OVERFLOW (0xc00000fd) at pc&#x3D;0x10001011, pid&#x3D;296, tid&#x3D;2940
#
# Java VM: Java HotSpot(TM) Client VM (1.6-internal mixed mode, sharing)
# Problematic frame:
# C  [App.dll+0x1011]
#

---------------  T H R E A D  ---------------

Current thread (0x000367c0):  JavaThread &quot;main&quot; [_thread_in_native, id&#x3D;2940]
:
Stack: [0x00040000,0x00080000),  sp&#x3D;0x00041000,  free space&#x3D;4k
Native frames: (J&#x3D;compiled Java code, j&#x3D;interpreted, Vv&#x3D;VM code, C&#x3D;native code)
C  [App.dll+0x1011]
C  [App.dll+0x1020]
C  [App.dll+0x1020]
:
C  [App.dll+0x1020]
C  [App.dll+0x1020]
...&lt;more frames&gt;...

Java frames: (J&#x3D;compiled Java code, j&#x3D;interpreted, Vv&#x3D;VM code)
j  Test.foo()V+0
j  Test.main([Ljava&#x2F;lang&#x2F;String;)V+0
v  ~StubRoutines::call_stub</code></pre>
<p>你可以从例子中获得以下信息:  </p>
<ul>
<li>异常是<code>EXCEPTION_STACK_OVERFLOW</code>  </li>
<li>线程的状态是<code>_thread_in_native</code>,表示线程在执行native或者JNI代码.  </li>
<li>线程信息中,可用的空间仅仅只有4KB(windows系统中的单页).另外线程指针(sp)在<code>0x00041000</code>,很接近栈结束<code>0x00040000</code>.  </li>
<li>输出的本地栈显示一个递归的本地方法是这个问题的原因.<code>...&lt;more frames&gt;...</code>表明还有更多的帧存在但是没有输出.输出只限于100帧.  </li>
</ul>
<hr>
<p>相关资料:<br><a href="https://www.slideshare.net/AndreiPangin/do-we-need-unsafe-in-java">Do we need Unsafe in Java?</a>  </p>
<p><a href="https://codegolf.stackexchange.com/questions/4399/shortest-code-that-raises-a-sigsegv?page=1&tab=votes#tab-top">Shortest code that raises a SIGSEGV</a>  </p>
<p><a href="https://stackoverflow.com/questions/36109313/best-way-on-how-to-solve-debug-jvm-crash-sigsegv">Best way on how to solve/debug JVM crash (SIGSEGV)
</a></p>
<p>openjdk相关bug:<br><a href="https://bugs.openjdk.java.net/browse/JDK-8171429">JVM Crash in # Problematic frame: # J 569 C2 java.lang.Long.getChars(JI[C)V (221 bytes) @ 0x00007fb9b618dfd8 [0x00007fb9b618dc40+0x398]</a>  </p>
<p><a href="https://bugs.openjdk.java.net/browse/JDK-8144565">JVM crash. Problematic frame: J 4518 C2 java.lang.Long.getChar</a></p>
]]></content>
  </entry>
  <entry>
    <title>JVM内存知识备忘</title>
    <url>/2018/09/15/jvm-memory-note/</url>
    <content><![CDATA[<p>又是一篇备忘…<br>主要记录一些知识,进行一些资源的汇总.  </p>
<span id="more"></span>
<p>先来群里<code>liufor</code>大大提供的两张图,清晰易懂:<br><img src="/images/1244488-20180915225940516-535609245.png"><br>Dockerized Java<br><a href="https://www.youtube.com/watch?v=NQ5hTEp-GTM">https://www.youtube.com/watch?v=NQ5hTEp-GTM</a>    </p>
<p><img src="/images/1244488-20180915230059584-477234180.png"><br>Java on Linux for devs and ops<br><a href="https://www.slideshare.net/aragozin/java-on-linux-for-devs-and-ops">https://www.slideshare.net/aragozin/java-on-linux-for-devs-and-ops</a>  </p>
<p>一些线上排查的注意事项:<br>1.greys不能用于增强系统类,即便他给了个开关,但不要用;<br>2.btrace增强后无法撤销,要撤销需要重启服务;<br>3.NMT不要开启detail,影响性能;<br>4.配置jemalloc和tamalloc的采样的时候注意采样率,千万别配0(全部采样);<br>5.jmap的-heap,jstack和jmap -F参数会导致JVM暂停.(SA.attch)</p>
<h1 id="常用配置-amp-命令"><a href="#常用配置-amp-命令" class="headerlink" title="常用配置&amp;命令"></a>常用配置&amp;命令</h1><h2 id="常用命令-amp-工具"><a href="#常用命令-amp-工具" class="headerlink" title="常用命令&amp;工具"></a>常用命令&amp;工具</h2><h3 id="JVM启动用的命令行"><a href="#JVM启动用的命令行" class="headerlink" title="JVM启动用的命令行"></a>JVM启动用的命令行</h3><pre class="language-none"><code class="language-none">jcmd process_id VM.command_line</code></pre>

<h3 id="手工触发gc"><a href="#手工触发gc" class="headerlink" title="手工触发gc"></a>手工触发gc</h3><pre class="language-none"><code class="language-none">jcmd process_id GC.run</code></pre>

<h3 id="显示调优标志"><a href="#显示调优标志" class="headerlink" title="显示调优标志"></a>显示调优标志</h3><pre class="language-none"><code class="language-none">jcmd process_id VM.flags [-all]</code></pre>
<p>all比较有用 可以看到全部的 包括默认值 在排查一些问题的时候能看的信息比较多  </p>
<h3 id="内存dump-使用情况查看"><a href="#内存dump-使用情况查看" class="headerlink" title="内存dump 使用情况查看:"></a>内存dump 使用情况查看:</h3><pre class="language-none"><code class="language-none">jmap -dump:format&#x3D;b,file&#x3D;test.bin process_id  
jmap -heap process_id  </code></pre>

<h3 id="heap-dump分析"><a href="#heap-dump分析" class="headerlink" title="heap dump分析"></a>heap dump分析</h3><p>可以使用<code>MAT</code>,<code>Heap Analyzer</code>和<code>VisualVM</code>这些工具.  </p>
<p>一些提取操作(比如要获得一个大字符串/char[]的值)可以通过OQL表达式.<br>例子:<br>获取到了address 拿取数据(是个char[])</p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript">chars <span class="token operator">=</span> heap<span class="token punctuation">.</span><span class="token function">findObject</span><span class="token punctuation">(</span><span class="token string">"0x666666666"</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> writer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">java<span class="token punctuation">.</span>io<span class="token punctuation">.</span>FileWriter</span><span class="token punctuation">(</span><span class="token string">"D:/temp/oql/666.txt"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">var</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> chars<span class="token punctuation">.</span>length<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    writer<span class="token punctuation">.</span><span class="token function">write</span><span class="token punctuation">(</span>chars<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
writer<span class="token punctuation">.</span><span class="token function">close</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre>

<p>visualVM中拿到index(这个index是sharp后面的 不是address)  </p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript"><span class="token function">filter</span><span class="token punctuation">(</span>heap<span class="token punctuation">.</span><span class="token function">objects</span><span class="token punctuation">(</span><span class="token string">'char[]'</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
 <span class="token keyword">function</span><span class="token punctuation">(</span><span class="token parameter">it<span class="token punctuation">,</span> index</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span>index <span class="token operator">==</span> <span class="token number">66</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
      <span class="token keyword">var</span> writer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">java<span class="token punctuation">.</span>io<span class="token punctuation">.</span>FileWriter</span><span class="token punctuation">(</span><span class="token string">"D:/temp/oql/666.txt"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">var</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> it<span class="token punctuation">.</span>length<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
          writer<span class="token punctuation">.</span><span class="token function">write</span><span class="token punctuation">(</span>it<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">&#125;</span>
      writer<span class="token punctuation">.</span><span class="token function">close</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">return</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
  <span class="token punctuation">&#125;</span>
  <span class="token keyword">return</span> <span class="token boolean">false</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">)</span></code></pre>



<h3 id="更多内存信息"><a href="#更多内存信息" class="headerlink" title="更多内存信息"></a>更多内存信息</h3><p>在linux上使用,使用/proc/pid/map内的信息,以及pmap.<br>使用gdp dump出内存查看信息<br>详见: <a href="http://lysu.github.io/blog/2015/02/02/how-to-deal-with-non-heap-or-native-memory-leak/">http://lysu.github.io/blog/2015/02/02/how-to-deal-with-non-heap-or-native-memory-leak/</a>  </p>
<h2 id="GC-log相关"><a href="#GC-log相关" class="headerlink" title="GC log相关"></a>GC log相关</h2><pre class="language-none"><code class="language-none">&#x2F;&#x2F;日志数量 每个log大小 存放位置
-XX:NumberOfGCLogFiles&#x3D;7
-XX:GCLogFileSize&#x3D;64M
-Xloggc:&#x2F;opt&#x2F;jetty&#x2F;logs&#x2F;gc.log
&#x2F;&#x2F;绝对时间戳 相对的用-XX:+PrintGCTimeStamps
-XX:+PrintGCDateStamps
&#x2F;&#x2F;详细的信息 平均时间等 精简的用-XX:+PrintGC
-XX:+PrintGCDetails
</code></pre>

<h2 id="NMT"><a href="#NMT" class="headerlink" title="NMT"></a>NMT</h2><pre class="language-none"><code class="language-none">-XX:NativeMemoryTracking&#x3D;[off | summary | detail]</code></pre>
<pre class="language-none"><code class="language-none">jcmd &lt;pid&gt; VM.native_memory [summary | detail | baseline | summary.diff | detail.diff | shutdown] [scale&#x3D; KB | MB | GB]  </code></pre>
<p>可以比较有效看到几个部分的内存使用情况 以及设置baseline后 在看具体的变化量<br>使用时要先设置JVM参数  生产环节慎用detail<br>参考文档:<br><a href="https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/tooldescr007.html">https://docs.oracle.com/javase/8/docs/technotes/guides/troubleshoot/tooldescr007.html</a><br>直接内存在internal中 但不包含Mapped ByteBuffer<br><a href="https://stackoverflow.com/questions/47309859/what-is-contained-in-code-internal-sections-of-jcmd">https://stackoverflow.com/questions/47309859/what-is-contained-in-code-internal-sections-of-jcmd</a>  </p>
<h1 id="博客-amp-文章"><a href="#博客-amp-文章" class="headerlink" title="博客 &amp; 文章"></a>博客 &amp; 文章</h1><p>一些文章和不错的博客.<br>随着时间的迁移和JVM的升级,这里(包括这文)的内容可能会过时,自己甄别一下~  </p>
<p>你假笨博客<br><a href="http://lovestblog.cn/">http://lovestblog.cn/</a></p>
<p>Alexey Ragozin的博客(也是第二图ppt的作者)<br><a href="http://blog.ragozin.info/">http://blog.ragozin.info/</a>  </p>
<p>JVM内存调优相关的一些笔记（杂）<br><a href="https://zhanjindong.com/2016/03/02/jvm-memory-tunning-notes">https://zhanjindong.com/2016/03/02/jvm-memory-tunning-notes</a>  </p>
<p>REDUCE LONG GC PAUSES<br><a href="https://blog.gceasy.io/2016/11/22/reduce-long-gc-pauses/">https://blog.gceasy.io/2016/11/22/reduce-long-gc-pauses/</a>  </p>
<p>Oh the Places Your Java Memory Goes<br><a href="https://jkutner.github.io/2017/04/28/oh-the-places-your-java-memory-goes.html">https://jkutner.github.io/2017/04/28/oh-the-places-your-java-memory-goes.html</a>  </p>
<p>入门科普，围绕JVM的各种外挂技术<br><a href="http://calvin1978.blogcn.com/articles/vjtools-tools4.html">http://calvin1978.blogcn.com/articles/vjtools-tools4.html</a></p>
]]></content>
  </entry>
  <entry>
    <title>kafka学习笔记1:测试环境搭建</title>
    <url>/2017/09/24/kafka-note-install/</url>
    <content><![CDATA[<p>最近因为架构中引入了kafka,一些之前在代码中通过RPC调用强耦合但是适合异步处理的内容可以用kafka重构一下.<br>考虑从头学一下kafka了解其特性和使用场景。  </p>
<span id="more"></span> 
<h1 id="环境选择"><a href="#环境选择" class="headerlink" title="环境选择"></a>环境选择</h1><p>首先是测试环境的搭建,平时使用的是win,但kafka以及zk在win上会存在一些bug(例如 <a href="https://issues.apache.org/jira/browse/KAFKA-1194),%E6%9C%80%E5%A5%BD%E8%BF%98%E6%98%AF%E5%9C%A8linux%E5%B9%B3%E5%8F%B0%E4%B8%8A%E6%90%AD%E5%BB%BA">https://issues.apache.org/jira/browse/KAFKA-1194),最好还是在linux平台上搭建</a>.<br>虚拟机是一个不错的选择但开销比较大,日常使用的笔记本8G内存开启虚拟机不是很方便,<code>bash on windows</code>是个不错的选择.<br>(另在<code>Store</code>中可以下载<code>ubuntu</code>等子系统,现在只限于insider)<br><img src="/images/1244488-20170924141841165-1167762603.png"><br>在组件中开启后重启电脑更新.</p>
<h1 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h1><p>重启后,在命令行或者powershell中输入bash进入环境.<br><img src="/images/1244488-20170924142156571-346165170.png"><br>但这里的环境和外部的windows不互通,windwos装了java也无法在bash中使用.<br>需要先安装一些额外的组件.  </p>
<h2 id="java安装"><a href="#java安装" class="headerlink" title="java安装"></a>java安装</h2><p><img src="/images/1244488-20170924142211259-23564339.png"><br>输入后选择一个即可(这里选择了openjdk-8-jre-headless)</p>
<h2 id="zookeeper"><a href="#zookeeper" class="headerlink" title="zookeeper"></a>zookeeper</h2><p>kafka依赖zk<br>简单的关系如下(图片来源kafka权威指南):<br><img src="/images/1244488-20170924142622790-2144780732.png"><br>所以使用kafka之前需要配置好zk.  </p>
<pre><code>cd ~
wget https://mirrors.tuna.tsinghua.edu.cn/apache/zookeeper/zookeeper-3.4.6/zookeeper-3.4.6.tar.gz
tar -zxf zookeeper-3.4.6.tar.gz
mv zookeeper-3.4.6 /usr/local/zookeeper
mkdir -p /var/lib/zookeeper
vim /usr/local/zookeeper/conf/zoo.cfg
/usr/local/zookeeper/bin/zkServer.sh start  
</code></pre>
<p>zoo.cfg的内容和zoo_sample.cfg大致相同,将dataDir换为<code>/var/lib/zookeeper</code>即可.<br>内容如下:    </p>
<pre><code>tickTime=2000
dataDir=/var/lib/zookeeper
clientPort=2181
</code></pre>
<p>启动后zk在2181端口运行  </p>
<h2 id="kafka"><a href="#kafka" class="headerlink" title="kafka"></a>kafka</h2><p>kafka安装过程类似.<br>这里学习使用0.9.0.1版本,去年2月发布.<br>和所看的书保持一致避免出现不兼容问题导致书中例子无法运行阻碍进度(新版本的功能之后再额外学习).  </p>
<pre><code>wget http://mirror.bit.edu.cn/apache/kafka/0.9.0.1/kafka_2.11-0.9.0.1.tgz
tar —zxf kafka 2.11-0.9.0.1.tgz
mv kafka 2.11-0.9.0.1 /usr/local/kafka
mkdir /tmp/kafka—logs
/usr/local/kafka/bin/kafka—server—start.sh -daemon /usr/local/kafka/config/server.properties
</code></pre>
<p>创建topic并验证: </p>
<pre><code># /usr/local/kafka/bin/kafka-topics.sh --create --zookeeper localhost:2181
--replication-factor 1 --partitions 1 --topic test
Created topic &quot;test&quot;.
# /usr/local/kafka/bin/kafka-topics.sh --zookeeper localhost:2181
--describe --topic test
Topic:test    PartitionCount:1    ReplicationFactor:1    Configs:
    Topic: test    Partition: 0    Leader: 0    Replicas: 0    Isr: 0
</code></pre>
<p>同时zk的/brokers/topics下会多出一个test.  </p>
<p>至此整个安装过程就结束了.  </p>
<p>上面的<code>/usr/local/kafka/config/server.properties</code>是broker的配置,其中一些配置的含义如下:  </p>
<ul>
<li><p>broker.id<br>都必须要有一个id 默认是0<br>在一个集群里必须唯一</p>
</li>
<li><p>port<br>监听的端口 默认是9092<br>指定1024以下的端口要用root跑 但不建议</p>
</li>
<li><p>zookeeper.connent<br>连接zk的地址 默认是localhost的2181<br>格式是hostname:port/path<br>默认path不写是根  </p>
</li>
<li><p>log.dirs<br>log块的存储位置<br>可以用，分隔存多个<br>一个分区的日志会存在一个路径下  </p>
</li>
<li><p>num.recovery.threads.per.data.dir<br>用来启动和结束时做日志恢复相关<br>和dirs的数量相关 如果是3 dirs数量是2 那会有6个线程</p>
</li>
<li><p>num.partitions<br>一个新的topic会有多少个分区<br>分区数只能比这个数字大 不能比这个小</p>
</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>kafka学习笔记2:生产者</title>
    <url>/2017/09/27/kafka-note-producer/</url>
    <content><![CDATA[<p>这次的笔记主要记录一下kafka的生产者的使用和一些重要的参数。<br>文中主要截图均来自<a href="https://www.safaribooksonline.com/library/view/kafka-the-definitive/9781491936153/">kafka权威指南</a></p>
<p>主要涉及到两个类<code>KafkaProducer</code>和<code>ProducerRecord</code>.   </p>
<span id="more"></span>
<hr>
<h1 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h1><p>生产者的主要架构如下：<br><img src="/images/1244488-20170928020637934-1434965689.png"></p>
<p>首先创建了一个<code>ProducerRecord</code><br>进行序列化 kv变为<code>ByteArray</code><br>进入Partitioner 如果之前指定了分区 那这一步什么都不会做<br>接着将Record放入要被发送到的同样的topic和分区的batch中<br>另一个单独的thread会进行发送操作<br>发送成功会返回<code>RecordMetadata</code>(包含topic 分区 偏移量)<br>失败的话会返回错误 producer会重试几次(retries配置)直到成功或者放弃返回错误  </p>
<h1 id="构造Kafka-Producer"><a href="#构造Kafka-Producer" class="headerlink" title="构造Kafka Producer"></a>构造Kafka Producer</h1><p>示例代码如下:   </p>
<pre><code>Properties properties = new Properties();
properties.put(&quot;bootstrap.servers&quot;, &quot;192.168.56.101:9092&quot;);
properties.put(&quot;key.serializer&quot;, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
properties.put(&quot;value.serializer&quot;, &quot;org.apache.kafka.common.serialization.StringSerializer&quot;);
properties.put(&quot;metadata.fetch.timeout.ms&quot;, 5000);
KafkaProducer&lt;String, String&gt; producer = new KafkaProducer&lt;String, String&gt;(properties);
ProducerRecord&lt;String, String&gt; record = new ProducerRecord&lt;String, String&gt;(&quot;CustomerCountry&quot;,
        &quot;Precision Products&quot;, &quot;France&quot;);
Future&lt;RecordMetadata&gt; f = producer.send(record);
</code></pre>
<p>返回的是一个Future,有以下处理方式:</p>
<ul>
<li>不在乎异常 可以不处理  </li>
<li>直接get拿到结果 同步的获取方式<br>另外send方法也支持传入回调的方式进行异步处理  </li>
</ul>
<h2 id="bootstrap-servers"><a href="#bootstrap-servers" class="headerlink" title="bootstrap.servers"></a>bootstrap.servers</h2><p>指定了要连接到的broker,可以用<code>,</code>分隔写入多个，连接broker cluster时不需要写入全部的broker地址，broker本身会同步其他broker的信息，但最少放两个以免写一个对应的broker挂了。<br>在实际使用中，可能会出现<code>org.apache.kafka.common.errors.TimeoutException: Failed to update metadata after 60000 ms.</code>这个错误，请求本身是发送成功的，检查zk topic也建立了，但就是获取不到metadata。<br>这是因为填入的broker服务器和kafka在zk上注册的host不一致导致的(在zk上可以通过<code>get /brokers/ids/&#123;id&#125;</code>查看)，可以通过修改kafka的<code>server.properties</code>的<code>advertised.host.name</code>和<code>advertised.port</code>改正。<br>但是要注意hosts文件会影响，比如配置了自己ip到一个域名上导致失败，注意代码在运行时log打出的实际的值。<br>例如电脑上装了银联控件，结果把localhost映射到了一个奇怪的地址：<br><img src="/images/1244488-20170928022156544-25388878.png"></p>
<p>例子中已经改为了ip。<br>另外实验环境也迁移至了虚拟机上，上一节用bash on windows搭建好的环境不知道为什么无论怎么改都获取不到metadata</p>
<h2 id="key-serialization"><a href="#key-serialization" class="headerlink" title="key.serialization"></a>key.serialization</h2><p>用来指定key的序列化器(下面会提到)</p>
<hr>
<h1 id="配置Producers"><a href="#配置Producers" class="headerlink" title="配置Producers"></a>配置Producers</h1><p>文档：<a href="http://kafka.apache.org/documentation.html#producerconfigs">http://kafka.apache.org/documentation.html#producerconfigs</a><br>因为用的是0.9.0.1，一些参数在最新版已经被弃用了，自己使用多注意下。<br>这些的默认配置在ProducerConfig的static块中定义：</p>
<pre><code>static &#123;
    CONFIG = new ConfigDef().define(BOOTSTRAP_SERVERS_CONFIG, Type.LIST, Importance.HIGH, CommonClientConfigs.BOOSTRAP_SERVERS_DOC)
                            .define(BUFFER_MEMORY_CONFIG, Type.LONG, 32 * 1024 * 1024L, atLeast(0L), Importance.HIGH, BUFFER_MEMORY_DOC)
                            .define(RETRIES_CONFIG, Type.INT, 0, between(0, Integer.MAX_VALUE), Importance.HIGH, RETRIES_DOC)
                            .define(ACKS_CONFIG,
                                    Type.STRING,
                                    &quot;1&quot;,
                                    in(&quot;all&quot;, &quot;-1&quot;, &quot;0&quot;, &quot;1&quot;),
                                    Importance.HIGH,
                                    ACKS_DOC)
    ... ...
</code></pre>
<p>这里列举一下比较重要的参数：  </p>
<h2 id="ACKS"><a href="#ACKS" class="headerlink" title="ACKS"></a>ACKS</h2><p>指定一次写成功需要多少个分区分片必须收到record  </p>
<ul>
<li><p>0<br>不会等待broker的返回 不会知道是否成功 高吞吐  </p>
</li>
<li><p>1<br>接受leader的 比0安全 但也会有还来不及复制leader就挂掉等导致失败的情况</p>
</li>
<li><p>all<br>所有的复本都接收到消息并回应 最安全的模式 但相对的延迟就比较惨了</p>
</li>
</ul>
<p>默认值为1  </p>
<h2 id="BUFFER-MEMORY"><a href="#BUFFER-MEMORY" class="headerlink" title="BUFFER.MEMORY"></a>BUFFER.MEMORY</h2><p>缓存等待发送的消息<br>如果超过了 会在抛错前等待<code>max.block.ms</code>秒<br>默认32M</p>
<h2 id="COMPRESSION-TYPE"><a href="#COMPRESSION-TYPE" class="headerlink" title="COMPRESSION.TYPE"></a>COMPRESSION.TYPE</h2><p>默认是none 不压缩<br>可以是snappy gzip lz4<br>snappy用较少的cpu完成还行的压缩 性能和网络带宽的权衡<br>gzip是使用更多的cpu和时间但是压缩更好 网络带宽是主要限制时使用</p>
<h2 id="RETRIES"><a href="#RETRIES" class="headerlink" title="RETRIES"></a>RETRIES</h2><p>重试次数 默认是0<br>默认间隔100ms (通过<code>retry.backoff.ms</code>控制)<br>不是所有的错误都会重试(<code>RetriableException</code>)<br>不要在业务里做重试  </p>
<h2 id="BATCH-SIZE"><a href="#BATCH-SIZE" class="headerlink" title="BATCH.SIZE"></a>BATCH.SIZE</h2><p>多个record被发送到同一个分区的时候 producer会进行批处理<br>内存 默认16k<br>满了就发 但不意味着只有满了才会发(见下一个参数)<br>设太大不会导致他的延迟 只不过会用更多的内存<br>设太小就惨了 会导致过于频繁  </p>
<h2 id="LINGER-MS"><a href="#LINGER-MS" class="headerlink" title="LINGER.MS"></a>LINGER.MS</h2><p>在发送当前batch时等多久额外的信息<br>默认是0<br>producer会在batch满了或者这个时间到了发送 默认是0就意味着即时发送<br>大于0会导致延迟 但是增加吞吐量</p>
<h2 id="CLIENT-ID"><a href="#CLIENT-ID" class="headerlink" title="CLIENT.ID"></a>CLIENT.ID</h2><p>任何字符串 自己定义 用来打log和metrics</p>
<h2 id="MAX-IN-FLIGHT-REQUESTS-PER-CONNECTION"><a href="#MAX-IN-FLIGHT-REQUESTS-PER-CONNECTION" class="headerlink" title="MAX.IN.FLIGHT.REQUESTS.PER.CONNECTION"></a>MAX.IN.FLIGHT.REQUESTS.PER.CONNECTION</h2><p>可以设置可以发送多少个请求不等回应 也就是允许同时有多少条消息在空中飞<br>默认是5<br>设大了会增加内存使用 但是提高吞吐<br>设过大了会导致降低吞吐 因为batch变得低效了<br>设置成1可以保证消息即使是在重试下也有序(下面顺序保证中提到)</p>
<h2 id="TIMEOUT-MS-REQUEST-TIMEOUT-MS-METADATA-FETCH-TIMEOUT-MS"><a href="#TIMEOUT-MS-REQUEST-TIMEOUT-MS-METADATA-FETCH-TIMEOUT-MS" class="headerlink" title="TIMEOUT.MS, REQUEST.TIMEOUT.MS, METADATA.FETCH.TIMEOUT.MS"></a>TIMEOUT.MS, REQUEST.TIMEOUT.MS, METADATA.FETCH.TIMEOUT.MS</h2><p>第1和第3在0.11.0.1版本中被移除了。。可以参考旧文档<br>第2 从服务端等待多久回应(默认30s)</p>
<h2 id="MAX-BLOCK-MS"><a href="#MAX-BLOCK-MS" class="headerlink" title="MAX.BLOCK.MS"></a>MAX.BLOCK.MS</h2><p>在send()和显式通过partitionsFor()请求元数据中间等待的时间<br>在send buffer满了 以及metadata没返回阻塞</p>
<h2 id="MAX-REQUEST-SIZE"><a href="#MAX-REQUEST-SIZE" class="headerlink" title="MAX.REQUEST.SIZE"></a>MAX.REQUEST.SIZE</h2><p>一次请求的数据大小限制(限制最大的消息 以及批处理的大小)<br>默认1M 意味着可以同时发1000条1K的消息 或者发一条1M的消息<br>注意上一节broker里也有自己的配置 最好两个配置相匹配  </p>
<h2 id="RECEIVE-BUFFER-BYTES-SEND-BUFFER-BYTES"><a href="#RECEIVE-BUFFER-BYTES-SEND-BUFFER-BYTES" class="headerlink" title="RECEIVE.BUFFER.BYTES SEND.BUFFER.BYTES"></a>RECEIVE.BUFFER.BYTES SEND.BUFFER.BYTES</h2><p>前者默认32K<br>后者默认128K<br>TCP发送和接受的buffer 用于socket写入和读取数据<br>设置为-1是用OS默认的设置<br>不同数据中心间可以适当提高点这个值因为网络环境会更差</p>
<h2 id="顺序保证"><a href="#顺序保证" class="headerlink" title="顺序保证"></a>顺序保证</h2><p>kafka保证在一个分区里消息的顺序<br>上述可以发现 如果设置了<code>max.in.flights.request.pre.connection</code>大于1(默认5)<br>并且retries大于0(默认0)那就完蛋了<br>可能写给同一个分区第一条消息写入失败 进入retry<br>但第二条写入成功<br>建议设置per.connection为1 但这会严重限制吞吐 只用于顺序很重要的场合    </p>
<hr>
<h1 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h1><p>自定义可以自己实现<code>org.apache.kafka.common.serialization.Serializer</code>接口。  </p>
<h2 id="使用Apache-Avro"><a href="#使用Apache-Avro" class="headerlink" title="使用Apache Avro"></a>使用Apache Avro</h2><p>优点:</p>
<ul>
<li>语言中立的序列化格式  </li>
<li>兼容性  </li>
</ul>
<p>用于写和读的schema必须兼容<br>反序列化器必须能够访问用于写数据的schema  </p>
<p>保存每个record的schema开销很大(通常是两倍于record)<br>可以将schema保存在一个注册器中管理 只需要在消息中写入对应的id<br>Schema Registry - 有很多开源的选项 可以使用confluent platform<br>文档: <a href="http://docs.confluent.io/current/schema-registry/docs/index.html">http://docs.confluent.io/current/schema-registry/docs/index.html</a> </p>
<p>这个可以单独记一个笔记，等实践后再做整理。   </p>
<hr>
<h1 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h1><p>如果key为null并且默认的分区器被使用 会用round-robin来平衡<br>有key 默认的分区器会hash key(自带的hash算法) 因为要保证同一个key总是写入同样的分区 他会使用所有的分区(不管是否可用) 写到不可用的会报错<br>如果分区数改变了 映射关系就会改变 同样的key在之后可能会到其他分区中<br>所以预先确定分区的数量在这种i情况下比较重要</p>
]]></content>
  </entry>
  <entry>
    <title>rust学习感想</title>
    <url>/2019/08/15/learning-rust/</url>
    <content><![CDATA[<p>最近刚把<a href="https://doc.rust-lang.org/book/title-page.html">The book</a>看完了,手有些生,断断续续写点小东西,这边写一下这个歌阶段的一些学习感想,感受.<br>文章很水,没有语言上的指导只有自己的一些见解,部分内容还可能是完全错误的,欢迎指正~~.</p>
<p>学习目的主要是想开阔一下视野.<br>之前就偶尔会听到别人说rust在设计上有一些特别的地方,并且又由于是一门可以进行系统编程的语言,学习的收获也会比学其他的来得多,就这样开始了.  </p>
<span id="more"></span>  
<h1 id="低估的ownership概念"><a href="#低估的ownership概念" class="headerlink" title="低估的ownership概念"></a>低估的ownership概念</h1><p>最开始看不管是<code>ownership</code>还是<code>borrowing rules</code>,感觉设计上很精妙,自己仿佛变成了肉体垃圾收集器,看了书中几个例子就飘了,天真地觉得并不是很困难.这种错觉就像我能手动管理好内存的申请和释放一样,稍微复杂一点点的场景就立马吃瘪.<br>之前做练习写了个脚本,用<code>HashMap</code>做缓存传入一个函数中,并会在传入的函数中放入值.<br>先放出最后的成品:  </p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">fn</span> <span class="token function-definition function">handle</span><span class="token punctuation">(</span>client<span class="token punctuation">:</span><span class="token operator">&amp;</span><span class="token class-name">Client</span><span class="token punctuation">,</span> map<span class="token punctuation">:</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> <span class="token class-name">HashMap</span><span class="token operator">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token operator">></span><span class="token punctuation">,</span> email<span class="token punctuation">:</span> <span class="token operator">&amp;</span><span class="token keyword">str</span><span class="token punctuation">,</span> node_name <span class="token punctuation">:</span><span class="token operator">&amp;</span><span class="token keyword">str</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> node_id <span class="token operator">=</span> <span class="token keyword">if</span> <span class="token keyword">let</span> <span class="token class-name">Some</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span> <span class="token operator">=</span> map<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>node_name<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">if</span> id <span class="token operator">==</span> <span class="token string">""</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">None</span>
        <span class="token punctuation">&#125;</span> <span class="token keyword">else</span> <span class="token punctuation">&#123;</span>
            <span class="token class-name">Some</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">::</span><span class="token function">from</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span> <span class="token keyword">else</span> <span class="token punctuation">&#123;</span>
        <span class="token keyword">let</span> r <span class="token operator">=</span> <span class="token function">find_id_by_node_name</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>client<span class="token punctuation">,</span> node_name<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token keyword">let</span> <span class="token class-name">Some</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span> <span class="token operator">=</span> r <span class="token punctuation">&#123;</span>
            map<span class="token punctuation">.</span><span class="token function">insert</span><span class="token punctuation">(</span>node_name<span class="token punctuation">.</span><span class="token function">to_string</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> id<span class="token punctuation">.</span><span class="token function">clone</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token class-name">Some</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>
        <span class="token punctuation">&#125;</span> <span class="token keyword">else</span> <span class="token punctuation">&#123;</span>
            map<span class="token punctuation">.</span><span class="token function">insert</span><span class="token punctuation">(</span>node_name<span class="token punctuation">.</span><span class="token function">to_string</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">""</span><span class="token punctuation">.</span><span class="token function">to_string</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token class-name">None</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> node_id <span class="token operator">==</span> <span class="token class-name">None</span> <span class="token punctuation">&#123;</span> <span class="token keyword">return</span> <span class="token punctuation">&#125;</span><span class="token punctuation">;</span>
    <span class="token keyword">let</span> user_id <span class="token operator">=</span> <span class="token function">find_id_by_email</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>client<span class="token punctuation">,</span> email<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> user_id <span class="token operator">==</span> <span class="token class-name">None</span> <span class="token punctuation">&#123;</span> <span class="token keyword">return</span> <span class="token punctuation">&#125;</span><span class="token punctuation">;</span>
    <span class="token function">move_to_node</span><span class="token punctuation">(</span><span class="token operator">&amp;</span>client<span class="token punctuation">,</span> node_id<span class="token punctuation">.</span><span class="token function">unwrap</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">as_str</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> user_id<span class="token punctuation">.</span><span class="token function">unwrap</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">as_str</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>最开始key和value都用了<code>&amp;str</code>,出现了各种报错比如borrow超过了生命周期,lifetime不一致等,中间为了避免报错一些地方用<code>String</code>,用<code>&amp;String</code>,随后<code>ownership moved</code>之类的错也跟着出现了.<br>恶化成了编译器报错,改掉这里,其他地方又爆了原因不一样的错,而且越修越多,报的错越来越看不懂.<br>感觉就像被人(编译器)打了一顿,打的人还不断告诉你你这样错了要那样做,那样做了之后又有了其他错.<br>这实质上还是自己对于所有权的概念理解太浅薄了,我需要将在函数内部产生的<code>String</code>增加到由参数传入的<code>HashMap</code>中,<code>HashMap</code>的生命周期在定义中就比函数中产生的变量长,而我最开始的泛型类型居然是<code>&amp;str</code>,于是就产生了一个borrow活得比他实际的数据更长的问题.<br>我要将他带出去,数据的ownership必须到<code>HashMap</code>中.(不过由于数据除了放到<code>HashMap</code>中还要参与之后的运算,于是通过<code>clone</code>等方法增加一些)  </p>
<p>对于<code>ownership</code>和<code>borrow</code>这两个概念,还是要从引入他们的原因来分析,是为了取代运行时GC的.变量会在离开作用域后自动回收掉指向的数据(这里单单讨论堆,毕竟栈上的数据都是Copy),<code>ownership</code>可以想象成一根粗粗的箭头,只能有这一根并且支撑数据的存活(unsafe在此不讨论),而<code>borrow</code>则是细细的绳,如果箭头消失了,数据回收,绳就空荡荡(悬空)了.  </p>
<h1 id="大小可知-大小不可知"><a href="#大小可知-大小不可知" class="headerlink" title="大小可知 大小不可知"></a>大小可知 大小不可知</h1><p>这个是最为困惑的一个点,主要体现在ch17上的内容.<br>看到了”为了编译过”而写的代码形式:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">impl</span> <span class="token class-name">State</span> <span class="token keyword">for</span> <span class="token class-name">Draft</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">fn</span> <span class="token function-definition function">request_review</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">:</span> <span class="token class-name">Box</span><span class="token operator">&lt;</span><span class="token keyword">Self</span><span class="token operator">></span><span class="token punctuation">)</span> <span class="token punctuation">-></span> <span class="token class-name">Box</span><span class="token operator">&lt;</span><span class="token keyword">dyn</span> <span class="token class-name">State</span><span class="token operator">></span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">Box</span><span class="token punctuation">::</span><span class="token function">new</span><span class="token punctuation">(</span><span class="token class-name">PendingReview</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">)</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>使用<code>Box</code>的原因之一是为了解决编译期类型大小不可知的问题,而如果被<code>Box</code>持有的数据发生了move,那又把这个编译期大小不可知的问题给暴露出来.  </p>
<blockquote>
<p> In general, Rust doesn’t allow us to move a value out of a Box<T> because Rust doesn’t know how big the value inside the Box<T> will be: recall in Chapter 15 that we used Box<T> precisely because we had something of an unknown size that we wanted to store in a Box<T> to get a value of a known size.</p>
</blockquote>
<p>(ch20的内容)</p>
<p>move Box本身.<br>在使用<code>trait object</code>中会常用到的一点.<br>可能需要多写一些代码才能更深入理解了.  </p>
<h1 id="提供充足的声明式支持"><a href="#提供充足的声明式支持" class="headerlink" title="提供充足的声明式支持"></a>提供充足的声明式支持</h1><p>这个是比较惊喜的一点,rust的库中提供了很多声明式风格的api,语言本身也有模式匹配的支持,写起来和其他有运行时的语言一样便利(但要时刻记着<code>ownership</code>等概念).<br>这是语言设计和API设计上的选择,感觉相比某些语言还是相当友好的.<br>此外,使用了一些额外的类库也感觉到了API上的轻便,使用舒适,比如<code>serde_json</code>的<code>Value</code>设计.<br>相比java的类库<code>jackson</code>里的<code>JsonNode</code>,其子类的<code>get</code>实现会返回<code>null</code>,导致get之后必须检查一下(虽然可以用<code>Optional</code>一路串下去),<code>Value</code>有<code>Value::Null</code>(在使用[]的情况下),如果<code>jackson</code>设计不存在返回子类假设有个<code>MissingNode</code>的实例可能就会好很多.<br>不过这个具体类库具体分析了.  </p>
<h1 id="语言本身的扩展性"><a href="#语言本身的扩展性" class="headerlink" title="语言本身的扩展性"></a>语言本身的扩展性</h1><p>这个其实支持macro的语言都有.<br>感觉这是一个很棒的方向,语言本身提供足够使用的语法即可,其他靠macro进行扩展,一个小内核+插件扩展的方式.<br>这个之后还要具体学习.  </p>
<hr>
<p>以上只是当前学习过程中的一点感想,随着不断学习可能会有不同的思考.<br>rust的学习过程还是比较有意思的,特别对于之前学的所有语言都是带运行时,不用太过在意gc,带来的思想上的冲击还是比较大的,特别是被编译器教做人… … …<br>此外补充一些收集的学习资料:  </p>
<p>pingCAP的课程<br><a href="https://university.pingcap.com/talentplan.shtml">https://university.pingcap.com/talentplan.shtml</a><br>对应的github:<br><a href="https://github.com/pingcap/talent-plan/tree/master/rust/building-blocks">https://github.com/pingcap/talent-plan/tree/master/rust/building-blocks</a></p>
<p>200行的绿色线程:<br><a href="https://cfsamson.gitbook.io/green-threads-explained-in-200-lines-of-rust/">https://cfsamson.gitbook.io/green-threads-explained-in-200-lines-of-rust/</a></p>
<p>GB模拟器:<br><a href="https://blog.ryanlevick.com/DMG-01/public/book/">https://blog.ryanlevick.com/DMG-01/public/book/</a><br>nes模拟器:<br><a href="http://www.michaelburge.us/2019/03/18/nes-design.html">http://www.michaelburge.us/2019/03/18/nes-design.html</a></p>
<p>cheat sheet:<br><a href="https://cheats.rs/">https://cheats.rs/</a></p>
<p>exercism:<br><a href="https://exercism.io/tracks/rust">https://exercism.io/tracks/rust</a>  </p>
<p>async book:<br><a href="https://rust-lang.github.io/async-book/index.html">https://rust-lang.github.io/async-book/index.html</a></p>
<p><a href="http://arewegameyet.com/">http://arewegameyet.com/</a></p>
]]></content>
      <tags>
        <tag>rust</tag>
      </tags>
  </entry>
  <entry>
    <title>leptos手工集成tailwindcss</title>
    <url>/2023/04/07/leptos-tailwindcss/</url>
    <content><![CDATA[<p>最近在看<code>leptos</code>,之前使用过<code>bulma</code>作为css库,感觉样式太少,想结合<code>tailwindcss</code>.</p>
<span id="more"></span>  
<p><code>leptos</code>的官方仓库里有例子: <a href="https://github.com/leptos-rs/leptos/tree/main/examples/tailwind_csr_trunk">tailwind_csr_trunk</a>  </p>
<p>但他作为最后结果,本文就介绍下各个部分和是怎么集合的.</p>
<p>主体其实就两个:  </p>
<h1 id="cargo-leptos-trunk"><a href="#cargo-leptos-trunk" class="headerlink" title="cargo + leptos + trunk"></a>cargo + leptos + trunk</h1><p>这一套作用是编译为wasm.<br>最小的初始化是新建rust工程,然后增加leptos依赖,写一个hello world之后用trunk运行.<br>命令如下:</p>
<pre class="language-shell" data-language="shell"><code class="language-shell">cargo new leptos-test
<span class="token builtin class-name">cd</span> leptos-test
cargo <span class="token function">add</span> leptos</code></pre>

<p>在项目根目录放入一个最简单的html:</p>
<pre class="language-html" data-language="html"><code class="language-html"><span class="token doctype"><span class="token punctuation">&lt;!</span><span class="token doctype-tag">DOCTYPE</span> <span class="token name">html</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>html</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>head</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>meta</span> <span class="token attr-name">charset</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>UTF-8<span class="token punctuation">"</span></span> <span class="token punctuation">/></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>head</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>body</span><span class="token punctuation">></span></span>
        
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>body</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>html</span><span class="token punctuation">></span></span></code></pre>

<p><code>main.rs</code>中写入一个hello world:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">use</span> <span class="token namespace">leptos<span class="token punctuation">::</span></span><span class="token punctuation">&#123;</span>component<span class="token punctuation">,</span> mount_to_body<span class="token punctuation">,</span> <span class="token namespace">prelude<span class="token punctuation">::</span></span><span class="token operator">*</span><span class="token punctuation">,</span> view<span class="token punctuation">,</span> <span class="token class-name">IntoView</span><span class="token punctuation">,</span> <span class="token class-name">Scope</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span>

<span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token function">mount_to_body</span><span class="token punctuation">(</span><span class="token closure-params"><span class="token closure-punctuation punctuation">|</span>cx<span class="token closure-punctuation punctuation">|</span></span> <span class="token punctuation">&#123;</span>
        <span class="token macro property">view!</span> <span class="token punctuation">&#123;</span> cx<span class="token punctuation">,</span> <span class="token operator">&lt;</span><span class="token class-name">App</span><span class="token operator">/</span><span class="token operator">></span> <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span>

<span class="token attribute attr-name">#[component]</span>
<span class="token keyword">fn</span> <span class="token function-definition function">App</span><span class="token punctuation">(</span>cx<span class="token punctuation">:</span> <span class="token class-name">Scope</span><span class="token punctuation">)</span> <span class="token punctuation">-></span> <span class="token keyword">impl</span> <span class="token class-name">IntoView</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> <span class="token punctuation">(</span>count<span class="token punctuation">,</span> set_count<span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token function">create_signal</span><span class="token punctuation">(</span>cx<span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">let</span> double_count <span class="token operator">=</span> <span class="token keyword">move</span> <span class="token closure-params"><span class="token closure-punctuation punctuation">|</span><span class="token closure-punctuation punctuation">|</span></span> <span class="token function">count</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">*</span> <span class="token number">2</span><span class="token punctuation">;</span>

    <span class="token macro property">view!</span> <span class="token punctuation">&#123;</span> cx<span class="token punctuation">,</span>
        <span class="token operator">&lt;</span>p<span class="token operator">></span><span class="token string">"hello world"</span><span class="token operator">&lt;</span><span class="token operator">/</span>p<span class="token operator">></span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span>
</code></pre>

<p>使用<code>trunk serve</code>运行即可(指定端口<code>--port port</code>)</p>
<p>至此这部分完成,这部分和tailwindcss独立.  </p>
<h1 id="npm-tailwindcss"><a href="#npm-tailwindcss" class="headerlink" title="npm + tailwindcss"></a>npm + tailwindcss</h1><p>这一套是生成css.<br>安装就是用tailwindcss官网的<a href="https://tailwindcss.com/docs/installation">安装方式</a>: </p>
<pre class="language-shell" data-language="shell"><code class="language-shell"><span class="token function">npm</span> <span class="token function">install</span> -D tailwindcss
npx tailwindcss init</code></pre>

<p>在tailwind.config.js配置中加上对*.rs文件的扫描(让它扫描到view!宏的内容)  </p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript">module<span class="token punctuation">.</span>exports <span class="token operator">=</span> <span class="token punctuation">&#123;</span>
  content<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">"./src/**/*.rs"</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
  theme<span class="token operator">:</span> <span class="token punctuation">&#123;</span>
    extend<span class="token operator">:</span> <span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span><span class="token punctuation">,</span>
  <span class="token punctuation">&#125;</span><span class="token punctuation">,</span>
  plugins<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>input.css和官网一样, 放在项目根目录:</p>
<pre class="language-none"><code class="language-none">@tailwind base;
@tailwind components;
@tailwind utilities;</code></pre>

<p>最后将css输出到一个位置即可(不要用dist 会和trunk的冲突):</p>
<pre class="language-shell" data-language="shell"><code class="language-shell">npx tailwindcss -i ./input.css -o ./output.css --watch</code></pre>
<p>可以将上面main.rs的p中增加一些class,例如text-3xl等.</p>
<p>至此,它会根据view!内的使用的class动态生成output.css<br>这个是因为leptos使用了JSX的语法,所以也能被tailwindcss正确处理.  </p>
<p>至此在根目录生成了output.css  </p>
<h1 id="引入output-css"><a href="#引入output-css" class="headerlink" title="引入output.css"></a>引入output.css</h1><p>在index.html直接link会失败,因为这个index.html被trunk处理,无法直接link.<br>要参考trunk<a href="https://trunkrs.dev/assets/">文档</a>使用特殊的标签:</p>
<pre class="language-none"><code class="language-none">&lt;link data-trunk href&#x3D;&quot;output.css&quot; rel&#x3D;&quot;css&quot; &#x2F;&gt;
&lt;link data-trunk rel&#x3D;&quot;copy-dir&quot; href&#x3D;&quot;public&quot; &#x2F;&gt;</code></pre>
<p>第一个是引入tailwindcss生成的css,第二个是其他的一些静态文件可以放在根目录的public文件夹中,会被复制进去.</p>
<p>至此就结束了,这个流程中trunk负责wasm和整体的构建,tailwindcss就是css的生成,是两个不同的workflow.  </p>
]]></content>
      <tags>
        <tag>rust</tag>
      </tags>
  </entry>
  <entry>
    <title>linux性能优化参数小节</title>
    <url>/2018/05/28/linux-performance-arguments/</url>
    <content><![CDATA[<p>总结一些和性能相关的常见参数  </p>
<span id="more"></span>
<h1 id="内核相关参数"><a href="#内核相关参数" class="headerlink" title="内核相关参数"></a>内核相关参数</h1><p>位于<code>/etc/sysctl.conf</code>文件,向文件中添加<br>用<code>sysctl -a</code>可以查看默认配置<br>修改后可以通过<code>sysctl -p</code>执行并看看有没有错误<br>例如设置错了参数:<br><img src="/images/1244488-20180529020156928-1352852257.png"></p>
<p><code>net.core.somaxconn=65535</code><br>一个端口最大的监听TCP连接的队列长度  </p>
<p><code>net.core.netdev_max_backlog=65535</code><br>数据包速率比内核处理块时 允许送到队列的数据包的最大数目  </p>
<p><code>net.ipv4.tcp_max_syn_backlog=65535</code><br>TCP syn队列的最大长度 第一次握手的连接 参数过大可能也会遭受syn flood攻击  </p>
<p><code>net.ipv4.tcp_fin_timeout=10</code><br>fin超时时间 表示如果套接字由本端要求关闭，这个参数决定了它保持在FIN-WAIT-2状态的时间<br><code>net.ipv4.tcp_tw_reuse=1</code><br>开启重用<br><code>net.ipv4.tcp_tw_recycle=1</code><br>快速回收  </p>
<pre class="language-none"><code class="language-none">net.core.wmem_default&#x3D;87380  
net.core.wmem_max&#x3D;16777216  
net.core.rmem_default&#x3D;87380  
net.core.rmem_max&#x3D;16777216  </code></pre>
<p>缓冲区的默认值和最大值  </p>
<p><code>net.ipv4.tcp_keepalive_time=120</code><br>keepalive的检测时间间隔 单位为秒<br><code>net.ipv4.tcp_keepalive_intvl=30</code><br>检测无效时 重发消息间隔<br><code>net.ipv4.tcp_keepalive_probes=3</code><br>检测无效时 最多重复确认次数</p>
<p><code>kernel.shmmax=4294967295</code><br>linux内核参数中最重要的参数之一<br>用于定义单个共享内存段的最大值<br>64位linux 可取最大值为物理内存值-1byte 建议值为物理内存一半  </p>
<p><code>vm.swappiness=0</code><br>free -m Swap中的内容<br><img src="/images/1244488-20180529015148150-543066928.png"></p>
<p>风险:  </p>
<ul>
<li>降低操作系统性能  </li>
<li>在系统资源不足下,容易被OOM kill掉   </li>
</ul>
<p>设置为0是告诉系统除非虚拟内存完全满了 否则不要使用交换区</p>
<h1 id="增加资源限制"><a href="#增加资源限制" class="headerlink" title="增加资源限制"></a>增加资源限制</h1><p>位于 /etc/security/limit.conf</p>
<pre class="language-none"><code class="language-none">* soft nofile 65535
* hard nofile 65535</code></pre>
<ul>
<li>* 对所有用户有效</li>
<li>soft 当前系统生效的设置</li>
<li>hard 系统所能设定的最大值</li>
<li>nofile 打开文件的最大数目</li>
<li>65535 限制的数量</li>
<li>需要重启系统生效</li>
</ul>
<p>设置前:<br><img src="/images/1244488-20180529015137572-1250617565.png"></p>
<p>之后open files的值会提高为65535</p>
<h1 id="磁盘调度策略"><a href="#磁盘调度策略" class="headerlink" title="磁盘调度策略"></a>磁盘调度策略</h1><p><code>/sys/block/devname/queue/scheduler</code><br>查看可通过<code>cat /sys/block/sda/queue/scheduler</code><br><img src="/images/1244488-20180529014941012-898523088.png"><br>现在使用的cfq 可选的是noop和deadline  </p>
<p><img src="/images/1244488-20180529015114120-317499833.png"><br>用echo写入可以立即生效  </p>
<p>简介:</p>
<ul>
<li><p>noop电梯式调度策略<br>实现了一个FIFO队列 倾向饿死读而利于写 对闪存设备 RAM和嵌入式系统是最好的选择</p>
</li>
<li><p>deadline 截止时间调度策略<br>确保了在一个截止时间内服务请求 这个截止时间是可调整的 而默认读期限短于写期限<br>对于数据库类应用是最好的选择</p>
</li>
<li><p>anticipatory 预料IO调度策略<br>本质上和deadline一样 但在最后一次读操作后 要等待6ms 才能继续进行对其他IO请求进行调度 将一些小写入流合并成一个大写入流 用写入延迟换取最大的写入吞吐量 适合写入较多的环境 比如文件服务器 对数据库环境表现很差</p>
</li>
<li><p>cfq 绝对公平算法</p>
</li>
</ul>
<p>参考资料:<br><a href="https://www.cnblogs.com/jking10/p/5472386.html">TCP/IP及内核参数优化调优</a></p>
<p><a href="https://www.cnblogs.com/cobbliu/p/5389556.html">Linux IO Scheduler（Linux IO 调度器）</a></p>
]]></content>
  </entry>
  <entry>
    <title>LockSupport浅析</title>
    <url>/2017/11/29/locksuport/</url>
    <content><![CDATA[<p>最初想有没有必要写这类文章，网上相关的文章很多，有些更为透彻，自己再写一篇不免有重复造轮子的感觉。<br>但想想写文除了分享知识外也可以帮助自己总结归纳，也稍稍可以提高点自我满足感。</p>
<span id="more"></span>
<hr>
<p>基本的线程阻塞原语，被用于创建锁和其他同步类上。  </p>
<p>这个类的作用有点类似于<code>Semaphore</code>，通过许可证(<code>permit</code>)来联系使用它的线程。如果许可证可用，调用<code>park</code>方法会立即返回并在这个过程中消费这个许可，不然线程会阻塞。调用<code>unpark</code>会使许可证可用。(和<code>Semaphores</code>有些许区别,许可证不会累加，最多只有一张） </p>
<p>因为有了许可证，所以调用<code>park</code>和<code>unpark</code>的先后关系就不重要了，这里可以对比一下Object的<code>wait</code>和<code>notify</code>,如果先调用同一个对象的<code>notify</code>再<code>wait</code>，那么调用<code>wait</code>的线程依旧会被阻塞，依赖方法的调用顺序。在一些场景下，<code>LockSupport</code>的方法都可以取代<code>notify</code>和<code>wait</code>。举个简单的例子:  </p>
<pre><code>final Object lock = new Object();
Thread t1 = new Thread() &#123;
    public void run() &#123;
        try &#123;
            Thread.sleep(100);
        &#125; catch (InterruptedException e) &#123;
            e.printStackTrace();
        &#125;
        synchronized (lock) &#123;
            lock.notifyAll();
        &#125;
    &#125;;
&#125;;

Thread t2 = new Thread() &#123;
    public void run() &#123;
        synchronized (lock) &#123;
            try &#123;
                lock.wait();
                System.out.println(&quot;I am alive&quot;);
            &#125; catch (InterruptedException e) &#123;
                e.printStackTrace();
            &#125;
        &#125;
    &#125;;
&#125;;
t1.start();
t2.start();
</code></pre>
<p>这段代码，只有确保t2先进入临界区并执行wait后才能正常打印内容。<br>换成<code>LockSupport</code>就没有这种担心了。  </p>
<pre><code>Thread t2 = new Thread() &#123;
    public void run() &#123;
        try &#123;
            Thread.sleep(100);
        &#125; catch (InterruptedException e) &#123;
            e.printStackTrace();
        &#125;
        System.out.println(&quot;t2 end sleep&quot;);
        LockSupport.park(this);
        System.out.println(&quot;I am alive&quot;);
    &#125;;
&#125;;

Thread t1 = new Thread() &#123;
    public void run() &#123;
        try &#123;
            Thread.sleep(50);
        &#125; catch (InterruptedException e) &#123;
            e.printStackTrace();
        &#125;
        LockSupport.unpark(t2);
        System.out.println(&quot;I am done&quot;);
    &#125;;
&#125;;
t1.start();
t2.start();
</code></pre>
<p>可以先<code>unpark</code>再<code>park</code>，但如API文档所说，<code>unpark</code>需要传入的线程已经启动。在上述<code>t1.start()</code>后用<code>t1.join()</code>等方法在<code>t2</code>启动前调用了<code>unpark</code>就无效了。  </p>
<p>但也不意味着<code>wait</code>,<code>notify</code>就没用了。<code>wait</code>是调用对象的方法，和这个对象资源绑定，线程间不需要彼此感知到对方（能获取对方引用），只需要共有这个资源即可，另一方面<code>LockSupport</code>可以指定<code>unpark</code>的线程，这点也是前者无法做到的，两者间不能完全取代彼此。</p>
<p><code>park</code>方法也有可能在没有”任何理由”的情况下返回，所以通常要在一个循环中使用它并重新判断可以返回的条件。这一点和<code>Object.wait</code>方法一样，可以参考<a href="https://en.wikipedia.org/wiki/Spurious_wakeup">wiki</a>，大致可以理解为底层系统因为优化需要允许出现这样的情况，JVM的线程就是系统线程这种情况也无法避免。可以作为”忙等待”的优化,但是必须要注意要有配对的unpark方法。<br>使用模式如下：  </p>
<pre><code>while (!canProceed()) &#123; ... LockSupport.park(this); &#125;&#125;  
</code></pre>
<p>在提供的方法中可以看到有一组传入名为blocker对象作为参数的方法，记录这个对象可以方便许可证监视和诊断工具分析。  </p>
<p>取上述代码，不启动t1的情况直接运行t2进入阻塞，jstack可以看到:  </p>
<pre><code>&quot;Thread-0&quot; #10 prio=5 os_prio=0 tid=0x000000001b586800 nid=0x37f0 waiting on condition [0x000000001c0ae000]
java.lang.Thread.State: WAITING (parking)
        at sun.misc.Unsafe.park(Native Method)
        - parking to wait for  &lt;0x00000000d603a6e8&gt; (a test.TestLockSupport$1)
        at java.util.concurrent.locks.LockSupport.park(LockSupport.java:175)
        at test.TestLockSupport$1.run(TestLockSupport.java:21)  
</code></pre>
<p>会将传入的对象打出。  </p>
<hr>
<p>更多资料：<br><a href="https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/locks/LockSupport.html">https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/locks/LockSupport.html</a>  </p>
<p><a href="http://agapple.iteye.com/blog/970055">http://agapple.iteye.com/blog/970055</a>  </p>
<p><a href="http://blog.csdn.net/hengyunabc/article/details/28126139">http://blog.csdn.net/hengyunabc/article/details/28126139</a></p>
]]></content>
  </entry>
  <entry>
    <title>微服务化路上的只言片语</title>
    <url>/2017/11/11/microservice-thought/</url>
    <content><![CDATA[<p>类似于杂谈性质的文，总结下公司实行微服务化上遇到的一些问题。<br>虽然参与了开发过程，但整体更像是个旁观者，前期还是大家讨论怎么做，后来慢慢就由负责架构的同事专职做规划，拆分任务去完成。  </p>
<span id="more"></span>
<h1 id="万事开头难"><a href="#万事开头难" class="headerlink" title="万事开头难"></a>万事开头难</h1><p><img src="/images/1244488-20171111185303966-1783146899.png"><br>最初的架构(数据库和其他部分都做了简化)</p>
<p>初衷并不是为了赶时髦，为了团队KPI之类的，而是遇到了一个很现实的问题。<br>研发团队慢慢从几个人发展为十几，几十人，所有人都在一个项目上开发，代码仓库越来越大，merge冲突越来越频繁，部署时也往往会因为一些bug导致全站部署拖延。<br>再加上访问量越来越高，带来的不确定性越来越大，一些流量较大的业务可能会导致整站都挂掉。<br>内部和外部都出现了问题。</p>
<p>当时虽然研发团队的人比较多了，但大家都基本是做业务兼做非业务的东西，没有成立专门的非业务部门。<br>大家都是在业务开发之外安排了微服务化相关的开发，这导致了进程缓慢，并且也隐式确定了微服务化不能影响现有业务开发。  </p>
<p>最开始设想划分粒度应该粗一些，按照各个大频道来划分（有点类似于分子站）。<br>但遇上了一个问题，BL和DAL层的代码是共用的，再加上之前的模块化不合理，导致一个模块直接使用了属于另一个模块的业务代码。<br>让这更糟糕的是业务还在飞快迭代，这两层的代码也会发生很大的改变。<br>这时就有两种选择：<br>一种是复制，把需要的BL和DAL层代码复制到各服务中；<br>另一种是抽取依赖，把BL和DAL层代码抽离成一个单独的jar，被各个服务依赖。  </p>
<p>前者会需要做频繁的同步，容易出现问题，很耗费精力。<br>当时我们选择了后者，先将他变为一个技术债解决现在的问题。  </p>
<p><img src="/images/1244488-20171111190838825-2019097568.png"><br>结构就成为了这样<br>组内也有同事开始脱离业务，慢慢专注在微服务化这一方向上，随后从业务组中慢慢抽离出了一个基础架构组（简称基架）。</p>
<h1 id="路由-服务间调用"><a href="#路由-服务间调用" class="headerlink" title="路由 服务间调用"></a>路由 服务间调用</h1><p>这一部分我就基本是路人了，会更接近于讲故事。<br>现在，<code>spring cloud</code>已经成为了一个比较成熟的解决方案，<code>dubbo</code>也重新被维护。<br>但当时，比较靠谱的选择只有<code>dubbo</code>，可能是出于对今后自己维护方便的考虑，基架最后的方案是参考<code>dubbo</code>，再结合一些现有的技术来实现自己的服务注册中心和服务请求路由。<br>注册中心用zk实现，里面存储服务版本、各个微服务名称、微服务版本以及服务器列表和<code>API</code>。<br>服务间调用使用了<code>netty</code>，<code>ribbon</code>等框架。<br>具体细节在这里不做过多描述了。<br>除了保留session处理相关、文件上传下载相关服务等接口以及页面渲染，原来的主站变为了一个<code>gateway</code>。<br>于是变成了以下这样：<br><img src="/images/1244488-20171111192904138-445418165.png">  </p>
<h1 id="我们的坑"><a href="#我们的坑" class="headerlink" title="我们的坑"></a>我们的坑</h1><p>至此其实已经稍微有点微服务的样子了… …<br>但还存在很多问题。<br>细心的朋友可能发现了上图中所有的服务还都依赖一个db！（以及对应的只读实例等）<br>所以…存储挂了，该挂的还是都挂了。<br>于是再接下来的一段时间里，基架那边进行了一些比较大的业务拆分，将一些核心业务进行了分表分库使用单独的存储，这工作今日还在进行，为网站的稳定做出来很大贡献。<br><img src="/images/1244488-20171111194311809-309411812.png"></p>
<h1 id="明天"><a href="#明天" class="headerlink" title="明天"></a>明天</h1><p>其实还有很多东西都没有(链路监控已经做好了…插不进上面 这里说下):<br>一些服务间直接调用的解耦，举个例子，一个用户发完帖子后，现在是在代码里依次调用其他服务去执行一些操作，其实这里应该用消息发布的模式去做；<br>配置中心；<br>安全性；<br>等等。<br>其实想一想这些是现在的spring cloud开箱即用的功能，如果当时这方案已经成熟或许可以少走一些弯路也说不定。  </p>
<h1 id="题外话"><a href="#题外话" class="headerlink" title="题外话"></a>题外话</h1><p>和技术和业务无关。<br>专门成立了基架组其实有好处也有坏处，好处是有一部分人可以专门攻坚网站遇到的问题，坏处是其他人怎么办。<br>首先这并不是说业务组没有机会做一些技术相关的事，而是业务组做这些事的机会其实减少了，这给人带来的技术提升也不一样。<br>那在业务组怎么提升自己的技术能力呢？<br>这也是我一直思考的问题。</p>
<p>我现在的一些方式无非就是课余多看书看资料，增长自己的知识和视野。<br>但其实这样也挺容易迷茫的，如何实用化呢，怎么确保自己所学落地呢？<br>我的想法以及之后的做法会是直接在项目里用上，但这也不是说随便往项目中加入不必要的东西，而是要加入会带来性能提升潜力的东西，至于能否提升就要看自己的实战了。<br>书看完就过，没有自己的想法和思考终究只是表面上的东西，会随着时间漫漫淡忘，昨天在看NIO.2的笔记，14年记的现在基本已经忘却了，这就很糟糕了。<br>最悲惨的不是学过后忘记了相关知识，而是忘记了曾经自己学过。</p>
]]></content>
  </entry>
  <entry>
    <title>监控基础</title>
    <url>/2018/07/26/monitoring-introduction/</url>
    <content><![CDATA[<p>Monitoring with Prometheus读书笔记<br>原书见:<br><a href="https://www.safaribooksonline.com/library/view/monitoring-with-prometheus/9780988820289/">https://www.safaribooksonline.com/library/view/monitoring-with-prometheus/9780988820289/</a>  </p>
<hr>
<span id="more"></span>
<p>监控需要监控技术环境和监控业务情况  </p>
<p><img src="/images/1244488-20180726204211275-746653901.png"></p>
<h1 id="监控基本原则"><a href="#监控基本原则" class="headerlink" title="监控基本原则"></a>监控基本原则</h1><p>监控需要在开发业务前就进行考虑,结合业务情况和环境,考虑要监控的metric.    </p>
<p><img src="/images/1244488-20180726204317681-327402975.png">  </p>
<p>监控设计要从业务逻辑出发 再下沉到应用的监控和操作系统的监控<br>但这不是说通用的系统监控不需要 只是他们不能用来汇报业务价值  </p>
<p>监控服务的正确性 例如不是单单看health接口是否返回了200 而要确认里面的值  </p>
<p>不要只使用静态的阈值 阈值应该是根据窗口数据等方式计算获取  </p>
<p>保存足够多的历史监控数据(用于之后的分析)   </p>
<p>监控需要容易被设置/部署 需要自动化支持  </p>
<h1 id="监控机制"><a href="#监控机制" class="headerlink" title="监控机制"></a>监控机制</h1><p>Probe :应用外<br>Introspection:应用内  </p>
<p>推<br>拉<br>普罗米修斯基本上是基于pull的 但也有push的方式</p>
<p>监控数据的类型<br>Metrics:时间序列数据 记录应用的度量的状态<br>Logs:应用程序发出的文件事件 通常情况更有用 但这本书不怎么讲 <a href="https://www.logstashbook.com/">https://www.logstashbook.com/</a>  </p>
<h1 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics"></a>Metrics</h1><p>metrics提供了动态的,实时的基础设施状态,帮助你管理环境和给做更好的决定.<br>Metrics are measures of properties of components of software or hardware  </p>
<p>固定时间间隙收集 -&gt; 颗粒性或者分辨率   </p>
<h2 id="Metrics类型"><a href="#Metrics类型" class="headerlink" title="Metrics类型"></a>Metrics类型</h2><h3 id="guage"><a href="#guage" class="headerlink" title="guage"></a>guage</h3><p><img src="/images/1244488-20180726204558560-2093206341.png"></p>
<h3 id="counter"><a href="#counter" class="headerlink" title="counter"></a>counter</h3><p>累计数据<br><img src="/images/1244488-20180726204611839-1525545816.png">  </p>
<h3 id="Histograms"><a href="#Histograms" class="headerlink" title="Histograms"></a>Histograms</h3><p>统计数据<br><img src="/images/1244488-20180726204702984-1305783817.png">  </p>
<p>其他统计字段:<br>Counter<br>Sum<br>Average<br>Median<br>Percentiles<br>Standard deviation<br>Rates of change  </p>
<p>平均数的陷阱:<br><img src="/images/1244488-20180726204746075-945944029.png">  </p>
<p><img src="/images/1244488-20180726204757068-237046767.png">  </p>
<h1 id="监控方法论"><a href="#监控方法论" class="headerlink" title="监控方法论"></a>监控方法论</h1><h2 id="USE方法"><a href="#USE方法" class="headerlink" title="USE方法"></a>USE方法</h2><p>For every resource, check utilization, saturation, and errors<br>对于每个资源 检查利用率 饱和度和错误<br><a href="http://www.brendangregg.com/USEmethod/use-linux.html">http://www.brendangregg.com/USEmethod/use-linux.html</a>  </p>
<h2 id="Google-Four-golden-signals"><a href="#Google-Four-golden-signals" class="headerlink" title="Google Four golden signals"></a>Google Four golden signals</h2><p>Latency<br>Traffic<br>Errors<br>Saturation  </p>
<h1 id="上下文相关-有用的警报和通知"><a href="#上下文相关-有用的警报和通知" class="headerlink" title="上下文相关 有用的警报和通知"></a>上下文相关 有用的警报和通知</h1><p>Alert和notification是监控的主要输出  </p>
<ul>
<li>通知是需要可以指导行动 明确清晰的.  </li>
<li>在通知上增加上下文信息.  </li>
<li>只发送有意义的通知.  </li>
</ul>
<h1 id="可视化"><a href="#可视化" class="headerlink" title="可视化"></a>可视化</h1><ul>
<li>清晰地展示数据  </li>
<li>引导观察者思考背后的实质 而不是专注于视觉效果  </li>
<li>避免歪曲数据  </li>
<li>清晰地展示大数据集  </li>
<li>允许改变视图的粒度而不影响理解</li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>netty常用使用方式</title>
    <url>/2017/10/07/netty-common-pattern/</url>
    <content><![CDATA[<p>最近在重新看netty，在这里总结一些netty的一些常用的使用方式，类似于模板，方便速查。<br>以netty 4.1.x的API作记录，不同版本可能API有略微差异，需要注意netty5被废弃掉了（辨别方式是看<code>SimpleChannelInboundHandler</code>是否有一个<code>messageReceived</code>方法 有的话就是5)，netty3是以<code>org.jboss</code>开头为包名。</p>
<span id="more"></span>
<h1 id="统一模板"><a href="#统一模板" class="headerlink" title="统一模板"></a>统一模板</h1><p>大多数情况下使用netty的步骤是定义好<code>EventLoopGroup</code>,定义好<code>Bootstrap</code>(<code>ServerBootstrap</code>)以及使用的channel类型（一般就是<code>NioSocketChannel</code>，服务端是<code>NioServerSocketChannel</code>）。<br>剩下是业务相关，使用的<code>ChannelInitializer</code>和具体的handler。<br>主要就是2+N（N为自定义的handler数量）个类。<br>服务端启动模板（也可以不区分boss和worker 用一个）:  </p>
<pre><code>public static void main(String[] args) throws InterruptedException &#123;
        EventLoopGroup bossGroup = new NioEventLoopGroup();
        EventLoopGroup workerGroup = new NioEventLoopGroup();
        try &#123;
            ServerBootstrap serverBootstrap = new ServerBootstrap();
            serverBootstrap.group(bossGroup, workerGroup).channel(NioServerSocketChannel.class)
                    .childHandler(new MyChannelInitializer());
            ChannelFuture future = serverBootstrap.bind(8999).sync();
            future.channel().closeFuture().sync();
        &#125; finally &#123;
            bossGroup.shutdownGracefully();
            workerGroup.shutdownGracefully();
        &#125;
&#125;
</code></pre>
<p>客户端启动模板：</p>
<pre><code>EventLoopGroup group = new NioEventLoopGroup();
try &#123;
    Bootstrap bootstrap = new Bootstrap()
            .group(group)
            .channel(NioSocketChannel.class)
            .handler(new MyChannelInitializer());
    ChannelFuture future = bootstrap.connect(&quot;localhost&quot;, 8888).sync();
    future.channel().closeFuture().sync();
&#125; finally &#123;
    group.shutdownGracefully();
&#125;
</code></pre>
<p>ChannelInitializer模板（继承ChannelInitializer即可）：  </p>
<pre><code>public class MyChannelInitializer extends ChannelInitializer&lt;SocketChannel&gt; &#123;

    @Override
    protected void initChannel(SocketChannel ch) throws Exception &#123;
        ChannelPipeline pipeline = ch.pipeline();
        pipeline.addLast(...);
    &#125;

&#125;
</code></pre>
<p>接下来的例子就以这个模板为骨架，主要涉及到初始化器的代码，启动代码一致。  </p>
<h1 id="处理Http请求"><a href="#处理Http请求" class="headerlink" title="处理Http请求"></a>处理Http请求</h1><p>netty自带了对用的codec类比较方便。<br><code>pipeline.addLast(&quot;httpServerCodec&quot;, new HttpServerCodec());</code><br>自己实现的handler最简单的方式用<code>SimpleChannelInboundHandler</code>接收<code>HttpRequest</code>方法即可<br><code>class MyHttpHandler extends SimpleChannelInboundHandler&lt;HttpRequest&gt;</code><br>这里简单说下<code>SimpleChannelInboundHandler</code>这个类，他是简化处理接受信息并处理的一个类，主要做两件事。 </p>
<p>第一件事是根据泛型决定是否处理这个消息，能够处理就自己处理，不行就交给下一个（可以参考<code>acceptInboundMessage</code>和<code>channelRead</code>方法）。<br>第二件事是消息的自动回收（有构造函数支持 默认是true），消息的引用计数会减一（所以在其他地方还要使用记得再retain一下）。<br>使用它可以节省很多冗余代码的编写。<br>一个简单例子：  </p>
<pre><code>public class MyHttpHandler extends SimpleChannelInboundHandler&lt;HttpRequest&gt; &#123;
    @Override
    protected void channelRead0(ChannelHandlerContext ctx, HttpRequest msg) throws Exception &#123;
        System.out.println(msg.getClass());
        System.out.println(msg.uri());
        System.out.println(msg.method().name());
        System.out.println(ctx.channel().remoteAddress());
        System.out.println(&quot;headers:&quot;);
        msg.headers().forEach(System.out::println);
        ByteBuf buf = Unpooled.copiedBuffer(&quot;Hello World&quot;, CharsetUtil.UTF_8);
        FullHttpResponse response = new DefaultFullHttpResponse(HttpVersion.HTTP_1_1, HttpResponseStatus.OK, buf);
        response.headers().set(HttpHeaderNames.CONTENT_TYPE, &quot;text/plain&quot;);
        response.headers().set(HttpHeaderNames.CONTENT_LENGTH, buf.readableBytes());
        ctx.writeAndFlush(response);
//         ctx.channel().close();
    &#125;
&#125;
</code></pre>
<p>不过只用这个handler并不能拿到content,还需要配合<code>ChunkedWriteHandler</code>和<code>HttpObjectAggregator</code>得到<code>FullHttpRequest</code>对象。</p>
<h2 id="处理WebSocket请求"><a href="#处理WebSocket请求" class="headerlink" title="处理WebSocket请求"></a>处理WebSocket请求</h2><p>只需要在上面的基础上增加一个<code>WebSocketServerProtocolHandler</code>即可，完整如下：</p>
<pre><code>    pipeline.addLast(&quot;httpServerCodec&quot;, new HttpServerCodec());
    pipeline.addLast(new ChunkedWriteHandler());
    pipeline.addLast(new HttpObjectAggregator(8096));
    pipeline.addLast(new WebSocketServerProtocolHandler(&quot;/ws&quot;));
</code></pre>
<p>自己的处理器可以接收并处理<code>WebSocketFrame</code>的子类。  </p>
<h1 id="自定义文本协议"><a href="#自定义文本协议" class="headerlink" title="自定义文本协议"></a>自定义文本协议</h1><p>netty提供了几个比较方便的用于自定义文本协议的编解码器。  </p>
<h2 id="基于长度"><a href="#基于长度" class="headerlink" title="基于长度"></a>基于长度</h2><pre><code>pipeline.addLast(new LengthFieldBasedFrameDecoder(Integer.MAX_VALUE, 0, 4, 0, 4));
pipeline.addLast(new LengthFieldPrepender(4));
pipeline.addLast(new StringDecoder(CharsetUtil.UTF_8));
pipeline.addLast(new StringEncoder(CharsetUtil.UTF_8));
pipeline.addLast(new MyHandler());  
</code></pre>
<p>上述参数的意义直接搬了文档：  </p>
<pre><code> lengthFieldOffset   = 0
 lengthFieldLength   = 2
 lengthAdjustment    = 0
 initialBytesToStrip = 2 (= the length of the Length field)

 BEFORE DECODE (14 bytes)         AFTER DECODE (12 bytes)
 +--------+----------------+      +----------------+
 | Length | Actual Content |-----&gt;| Actual Content |
 | 0x000C | &quot;HELLO, WORLD&quot; |      | &quot;HELLO, WORLD&quot; |
 +--------+----------------+      +----------------+
</code></pre>
<h2 id="基于分隔符"><a href="#基于分隔符" class="headerlink" title="基于分隔符"></a>基于分隔符</h2><pre><code>    ChannelPipeline pipeline = ch.pipeline();
    pipeline.addLast(new DelimiterBasedFrameDecoder(4096, Delimiters.lineDelimiter()));
    pipeline.addLast(new StringDecoder(CharsetUtil.UTF_8));
    pipeline.addLast(new StringEncoder(CharsetUtil.UTF_8));  
</code></pre>
<p>第二个参数需要一个<code>ByteBuf[]</code>，自己指定下分隔符即可。<br>注意使用这种发消息的时候要带上那个分隔符，不然会处理失败（被当做未结束）。  </p>
<h1 id="广播实现"><a href="#广播实现" class="headerlink" title="广播实现"></a>广播实现</h1><p>利用了netty提供的一个很便利的类，<code>ChannelGroup</code>.<br>首先要了解一下Channel生命周期函数的调用：<br><code>channel added -&gt; channel registered -&gt; channel active -&gt; channel inactive -&gt; channel unregistered -&gt; channel removed</code><br>我们可以定义一个静态（保证共享和唯一）的<code>ChannelGroup</code>在channel added的时候把对应channel增加到<code>ChannelGroup</code>中即可（但不需要自己移除，这一点他自己实现了）<br>然后利用它的写方法就可以实现广播，或者forEach做下过滤做多播也可以。<br>这个实现和用什么协议无关，主要涉及到<code>ChannelGroup</code>使用。  </p>
<h1 id="心跳"><a href="#心跳" class="headerlink" title="心跳"></a>心跳</h1><p>netty自带的<code>IdleStateHandler</code>，超时后会向下一个handler发出<code>IdleStateEvent</code>消息，接收并处理即可。 </p>
<pre><code>@Override
public void userEventTriggered(ChannelHandlerContext ctx, Object evt) throws Exception &#123;
    super.userEventTriggered(ctx, evt);
    if (evt instanceof IdleStateEvent) &#123;
        ctx.channel().writeAndFlush(&quot;超时&quot;).addListener((ChannelFuture ch) -&gt; ch.channel().close());
    &#125;
&#125;
</code></pre>
<p>它细分为3中类型的超时，读、写、读写，通过<code>IdleStateEvent</code>的state属性可以获取，可以单独判断。  </p>
<p>其他的一些模式会在之后复习和使用过程中不断补完~<br>2017年10月08日00点53分 init<br>2017年10月10日10点02分 更新websocket</p>
]]></content>
  </entry>
  <entry>
    <title>Optimizing Java笔记:高级垃圾回收</title>
    <url>/2018/10/14/optimizing-java-ch7-note/</url>
    <content><![CDATA[<p>原书地址:<a href="https://www.safaribooksonline.com/library/view/optimizing-java/9781492039259/">https://www.safaribooksonline.com/library/view/optimizing-java/9781492039259/</a>  </p>
<p>感觉挺不错的一本书,断断续续在读.<br>来自原书第七章的笔记,记录一些感觉比较重要的概念.<br>原书这个章节内容比较丰富,笔记只是选择性地进行了一些归纳和简单翻译,建议有兴趣的不妨阅读一下原书.  </p>
<hr>
<span id="more"></span>
<h1 id="Tradeoffs-and-Pluggable-Collectors"><a href="#Tradeoffs-and-Pluggable-Collectors" class="headerlink" title="Tradeoffs and Pluggable Collectors"></a>Tradeoffs and Pluggable Collectors</h1><p>权衡的可插拔垃圾回收器  </p>
<p>在Sun的环境中 GC子系统是可插拔的子系统<br>GC的运行不能改变程序的语义 但性能上可能会有差异</p>
<p>只有权衡 没有银弹  </p>
<p>需要权衡的因素：  </p>
<ul>
<li>停顿时间</li>
<li>吞吐量（GC所占运行时时间)</li>
<li>停顿频率</li>
<li>回收效率</li>
<li>停顿一致性(是否所有停顿时间大致一样)  </li>
</ul>
<h1 id="并发GC原理"><a href="#并发GC原理" class="headerlink" title="并发GC原理"></a>并发GC原理</h1><p>在一些特定的系统 比如动画播放,常常有固定的帧率,用来提供有规律的 固定的机会执行GC.<br>但通用的GC就不会有这样的知识来提高停顿的确定性了  </p>
<h1 id="JVM-safepoints"><a href="#JVM-safepoints" class="headerlink" title="JVM safepoints"></a>JVM safepoints</h1><p>安全点<br>基于协商<br>有两个主要规则控制:  </p>
<ul>
<li>JVM不能强制一个线程进入安全点状态   </li>
<li>JVM可以阻止一个线程离开安全点状态  </li>
</ul>
<p>一个简化的过程:<br>1.JVM设置一个全局的”<code>time to safepoint</code>“ flag.<br>2.每个应用线程独自拉取这个flag 发现被设置了<br>3.他们停下并且等待再次醒来 </p>
<p>这个标志被设置意味着素有线程都必须停下来.  </p>
<p>停的快的线程也要等待还没停的线程停止(这个时间可能不会完全被pause time statistics统计到)  </p>
<p>通常的app线程使用<code>poll</code>的机制<br>解释器会在任意两个字节码之间去检查 <code>JIT</code>编译器通常会在退出编译方法的位置插入一个<code>poll</code>  </p>
<p>这个有点像<code>CountDownLatch</code> 所有线程到这个位置了才去做GC</p>
<p>自动在safepoint的状态:  </p>
<ol>
<li>正被monitor block了</li>
<li>正在执行JNI代码</li>
</ol>
<p>不可能在safepoint的状态:</p>
<ol>
<li>正执行了部分<code>bytecode</code>(解释器模式)</li>
<li>已经被<code>OS</code>中断了</li>
</ol>
<h1 id="三色标记"><a href="#三色标记" class="headerlink" title="三色标记"></a>三色标记</h1><p>tri-color marking算法<br>工作原理:</p>
<ul>
<li>GC roots染为灰色</li>
<li>所有的其他对象染成白色</li>
<li>一个标记线程移动到随机的灰色节点上</li>
<li>如果这个节点有白色子节点们 标记线程会先将子节点染成灰色 再将该节点染成黑色</li>
<li>过程一直进行直到没有灰色节点剩余</li>
<li>所有黑色节点已经被证明可达并且必须保持存活</li>
<li>白色节点可被回收</li>
</ul>
<p>并发收集也频繁利用<code>SATB</code>(snapshot at the beginning)<br>如果对象在收集开始是可达或者在收集之后被分配的 就认为是存活的<br>这带来了一些额外的工作 需要应用线程在收集过程中创建的对象染黑 非收集过程中的染白  </p>
<p>在这个过程中 应用线程会不断改变对像图<br>还有一个情况是标记黑色的对象在这个过程中又去指向了白色的对象<br><img src="/images/1244488-20181014212327110-502030643.png">  </p>
<p>解决方式:</p>
<ol>
<li>黑色更新指向外部白色对象的时候变为灰色</li>
<li>不允许黑色指向外部白色对象</li>
<li>维护一个修改的队列 然后进行第二轮fixup<br>以及其他的</li>
</ol>
<h1 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h1><p>为老年代设计的极其低停顿的收集器<br>一般和一个轻量修改的年轻代并行收集器一起用(<code>ParNew</code> 而不是<code>Parallel GC</code>)</p>
<p>步骤:  </p>
<ol>
<li>初始标记(Initial Mark) STW</li>
<li>并发标记(Concurrent Mark)</li>
<li>并发预清理(Concurrent Preclean)</li>
<li>重标记(Remark) STW</li>
<li>并发清理(Concurrent Sweep)</li>
<li>并发重置(Concurrent Reset)  </li>
</ol>
<p>将一个长的STW停顿变为两个较短的STW停顿</p>
<p>明显的影响:</p>
<ul>
<li>应用线程不会像之前一样停的久</li>
<li>单个完整的GC循环会花得更久</li>
<li>CMS GC循环的时候应用吞吐量会下降</li>
<li>GC会花更多的内存跟踪对象</li>
<li>使用更多的CPU时间用来进行GC</li>
<li>CMS不会压缩heap 所以老年代会碎片化</li>
</ul>
<p>默认情况 <code>CMS</code>会用一半可用线程来处理并发阶段 剩下一半运行Java代码<br>这时候如果<code>Eden</code>满了…就会触发<code>YGC</code> 但这时候已经没那么多线程了..<code>YGC</code>会更久<br><code>YGC</code>之后一些对象可能被提升到老年代了 这两个收集器之间需要一些协调 这也是为什么<code>CMS</code>需要一个稍微不同的Young收集器(<code>ParNew</code>)<br>另一种情况 如果<code>YGC</code>提升到老年代的对象过多…老年代没有空间…emmmm<br>就触发了并发模式失败(concurrent mode failure,<code>CMF</code>),JVM只能降级转为使用<code>ParallelOld</code><br>为了避免频繁的<code>CMF</code> <code>CMS</code>需要在老年代完全满之前就开始一轮收集循环 默认是75%<br>另外一个场景会引发<code>CMF</code>的是内存碎片 提升的对象没有连续的内存空间可放<br>降级的<code>ParallelOld</code>有压缩  </p>
<p><strong>使用CMS的主要问题是避免CMF</strong>  </p>
<p>内部<code>CMS</code>使用一个<code>free list</code>记录内存块来管理可用空间<br>在Concurrent sweep阶段 连续的可用块会被sweeper线程合并在一起 这样可以提供更大的可用空间块来避免因为内存问题造成的<code>CMF</code><br>但这个过程也是并发的 sweeper会锁上free lists.  </p>
<h2 id="基本参数"><a href="#基本参数" class="headerlink" title="基本参数"></a>基本参数</h2><pre class="language-none"><code class="language-none">-XX:+UseConcMarkSweepGC</code></pre>
<p>启用CMS(同时激活ParNew)</p>
<p>此外还有60多个参数…</p>
<h1 id="G1"><a href="#G1" class="headerlink" title="G1"></a>G1</h1><p>Java6引入 Java7里重写 Java8u40就是可靠和生产可用的<br>不建议在8u40之前使用他</p>
<p>比CMS更容易优化<br>对老年代提升更不敏感<br>在大heap下更好的scale(特别是停顿时间)<br>能够避免(或大幅降低 回退到)full STW收集  </p>
<p>G1和并行GC CMS不同 每代没有专用的 连续空间的内容<br>此外他也没有沿用半球空间(s0 s1)的布局  </p>
<h2 id="G1堆空间和Regions"><a href="#G1堆空间和Regions" class="headerlink" title="G1堆空间和Regions"></a>G1堆空间和Regions</h2><p>G1堆基于<code>regions</code>的概念.<br>默认<strong>1M</strong>(在更大堆下会更大)<br>注意G1的堆空间是连续的 只是每代本身不连续<br><img src="/images/1244488-20181014213009574-2070952287.png"></p>
<p>G1的算法允许1 2 4 - <del>64M</del> 应该为32MB<br>参考:<a href="http://hg.openjdk.java.net/jdk/jdk/file/f08c1d7a5c53/src/hotspot/share/gc/g1/heapRegionBounds.inline.hpp">heapRegionBounds</a><br>默认 他期望有2048-4095个regions  </p>
<h2 id="G1算法设计"><a href="#G1算法设计" class="headerlink" title="G1算法设计"></a>G1算法设计</h2><p>使用并发标记阶段<br>是一个evacuating(搬空)收集器<br>提供”基于统计的压缩”(statistical compaction)    </p>
<p>如果一个对象占用超过一半一个region的大小 就会被认为是一个巨大(humongous)对象.<br>直接分配在特殊的<code>humongous regions</code>中.空间连续.  </p>
<p>年轻代的概念保留 Eden survivor regions.<br>但不连续<br>空间随着整体停顿目标变化.  </p>
<p>RSets(remembered sets)<br>pre-region entries<br>记录自身对象被外部region对象引用的情况<br>这个是用来处理ygc时 一部分年轻代对象被老年代引用着<br>不需要扫描整个heap只要检查这个RSets看看外部有没有老年代的region引用着就好了<br><img src="/images/1244488-20181014213745798-1022468652.png">  </p>
<p><code>RSets</code>和<code>card table</code>都是帮助GC解决浮动垃圾(floating garbage)的问题.  dead对象间的引用.  </p>
<h2 id="G1阶段"><a href="#G1阶段" class="headerlink" title="G1阶段"></a>G1阶段</h2><ol>
<li>初始标记(Initial Mark) STW</li>
<li>并发根扫描(Concurrent Root Scan)</li>
<li>并发标记(Concurrent Mark)</li>
<li>重标记(Remark) STW</li>
<li>清理(Cleanup) STW</li>
</ol>
<h2 id="基本参数-1"><a href="#基本参数-1" class="headerlink" title="基本参数"></a>基本参数</h2><p>启用参数</p>
<pre class="language-none"><code class="language-none">-XX:UseG1GC</code></pre>
<p>G1是基于停顿目标的 允许开发者指定一个期望的停顿最大时间<br>这只是一个目标 没有保证一定能达成<br>毕竟回收的的前提是分配 分配过程很多是不可预测的  </p>
<pre class="language-none"><code class="language-none">-XX:MaxFCPauseMillis&#x3D;200</code></pre>
<p>200ms<br>实践中 100ms以下的目标是很难达到的   </p>
<pre class="language-none"><code class="language-none">-XX:G1HeapRegionSize&#x3D;n</code></pre>
<p>必须是2的倍数 1- <del>64</del> 32 单位是MB  </p>
<h1 id="Shenandoah"><a href="#Shenandoah" class="headerlink" title="Shenandoah"></a>Shenandoah</h1><p>红帽 openjdk项目<br>项目wiki:<a href="https://wiki.openjdk.java.net/display/shenandoah/Main">https://wiki.openjdk.java.net/display/shenandoah/Main</a><br>还不能用于生产  </p>
<p>和G1目的一样 是为了降低大堆下的停顿时间  </p>
<ol>
<li>初始标记 (Initial Mark) STW</li>
<li>并发标记(Concurrent Marking)</li>
<li>最终标记(Final Marking) STW</li>
<li>并发压缩(Concurrent Compaction)</li>
</ol>
<p>阶段看上去类似<br>一些实现机制比如SATB也相似<br>但有一些根本上的不同<br>最显著的特点就是在压缩阶段 应用线程依旧可以访问对象<br>使用了<code>brooks pointer</code>.    </p>
<p>每个对象使用了一个额外的字(<code>word</code>)来标记是否在上一个gc阶段发生了重定位(relocated) 并且放置了新的位置</p>
<p>如果对象没被重放置 <code>brooks pointer</code>指向内存里的下一个字  </p>
<p>压缩阶段涉及到复制 在这个过程中复制对象 然后将原对象的字指向新的复制的对象 相当于做了一个路由<br><img src="/images/1244488-20181014213907441-607899238.png"></p>
<p>并发压缩<br>现在的抽空(evacuation)步骤如下:</p>
<ol>
<li>复制对象到<code>TLAB</code>(冒险的)</li>
<li>使用<code>CAS</code>操作更新<code>brooks pointer</code>到那个冒险的副本</li>
<li>如果成功 那么压缩线程赢了这次race,之后所有的访问该对象会经过<code>Brooks pointer</code></li>
<li>如果失败 压缩对象失败.那个冒险的副本会被取消,赢得的线程设置<code>Brooks pointer</code>.  </li>
</ol>
<p>在提供了该回收器上使用</p>
<pre class="language-none"><code class="language-none">-XX:+UseShenandoahGC</code></pre>

<h1 id="C4-Azul-Zing"><a href="#C4-Azul-Zing" class="headerlink" title="C4(Azul Zing)"></a>C4(Azul Zing)</h1><h1 id="Balanced-IBM-J9"><a href="#Balanced-IBM-J9" class="headerlink" title="Balanced(IBM J9)"></a>Balanced(IBM J9)</h1><p>Open J9</p>
<h1 id="Lengacy-Hotspot-Collectors"><a href="#Lengacy-Hotspot-Collectors" class="headerlink" title="Lengacy Hotspot Collectors"></a>Lengacy Hotspot Collectors</h1><p>一些组合在Java8被废弃 在Java9会被移除  </p>
<h2 id="Serial和SerialOld"><a href="#Serial和SerialOld" class="headerlink" title="Serial和SerialOld"></a>Serial和SerialOld</h2><p>和Parallel ParallelOld类似 但使用单个线程</p>
<h2 id="Incremental-CMS-iCMS"><a href="#Incremental-CMS-iCMS" class="headerlink" title="Incremental CMS(iCMS)"></a>Incremental CMS(iCMS)</h2><pre class="language-none"><code class="language-none">-XX:+CMSIncrementalMode</code></pre>
<p>一些改进尝试 Java9中被移除</p>
]]></content>
  </entry>
  <entry>
    <title>维护之痛-数据统计任务小结</title>
    <url>/2018/08/01/painful-to-maintain-data-statistic-system/</url>
    <content><![CDATA[<p>年初转组,接触了一些新业务.<br>交接了一个数据统计系统,去年下半年开发完成,不再有新的需求.<br>emmmm,妥妥的一个遗留系统… …<br>有对应文档但比较老,一些存储和细节记录在了原开发自己的笔记中.<br>最主要是bug累累,之前使用量和用户较少没有多少bug反馈,但随着今年用户增加基本每个月都会有一些bug.  </p>
<span id="more"></span>
<p>该系统的简单架构如下:<br><img src="/images/1244488-20180801215156575-1411459964.png">  </p>
<p>由一位同事单独完成,历时几个月,难以想象他经历了什么…但听闻他在开发过程中几乎崩溃了,自己完成了数据的获取,处理,聚合.<br>ES和spark的资源还是借用了其他组已经搭好的存储.  </p>
<p>我们有专门的数据团队产出一些业务指标,但这位同事做这套系统的时候那边提供的帮助有限,差不是是无法获得数据源(日志)由他们提供,但如果数据库或其他存储里有数据源自己处理.  </p>
<p>介于该系统的诸多问题,数据团队那边已经决定改为所需的指标都由他们提供,我们做一下对接的方式,现在正在进行中.<br>该文就来提一下我接手之后遇到的坑,以及一些处理方式.  </p>
<h1 id="遇到的问题"><a href="#遇到的问题" class="headerlink" title="遇到的问题"></a>遇到的问题</h1><h2 id="数据指标定义有误"><a href="#数据指标定义有误" class="headerlink" title="数据指标定义有误"></a>数据指标定义有误</h2><p>这是一个非常尴尬的问题,维护一个系统最沮丧的莫过于,你发现他一开始做的就是错的.  </p>
<p>数据系统产出的有趋势数据和汇总数据,趋势数据也就是每天的数据,而之前实现的时候汇总数据是趋势数据的累加.  </p>
<p><img src="/images/1244488-20180830115528234-596690092.png">  </p>
<p>但现实场景下一些指标是无法累加的.<br>例如上述场景下帖子的删除是可恢复的,今天删除恢复后明天又进行了删除,趋势数据里表示的是两天删除数都是1,<br>累加是2,但看这两天的汇总数据实际上就删除了1次,但还是会显示2,这时候用户就会疑惑自己的帖子明明就少了一个但为什么这周期汇总的数据是2呢.  </p>
<p>当然这里也可以提示用户是删除这个行为发生了2次,但显然在这个指标下汇总数据上用户更关注是结果.  </p>
<p>其他的诸如被回帖的帖子数,同一个帖子被两天都被回复和这两天的汇总量之间是不能简单相加的.    </p>
<p>要去重需要有历史数据支撑,但该系统的计算都是以天为单位,要扩展这个功能需要进行很大的改造.<br>最后还是放弃了,现在正在接入数据团队的结果.  </p>
<h2 id="系统稳定性问题"><a href="#系统稳定性问题" class="headerlink" title="系统稳定性问题"></a>系统稳定性问题</h2><p>该系统上线后一直不是很稳定,上图的链路过长,且业务都由之前开发一人维护.  </p>
<p>中间任何一环出了问题都会导致数据项的缺失或者数据不准确.  </p>
<p>这里总结一些遇到的一些问题和解决办法.</p>
<h3 id="spring-batch挂了"><a href="#spring-batch挂了" class="headerlink" title="spring batch挂了"></a>spring batch挂了</h3><p>这个是接手后最先遇到的,当时还没看到这个系统的全貌,只看到了这冰山一角.  </p>
<p>上述的链路的依赖是时间,系统实际运行过程中常常会有spring batch在计算了但上游数据还没准备完的情况.<br>这种问题比较难发现,往往是某几个账号的数据项不准确了.(趋势数据大幅度掉落等)  </p>
<p>但处理方式是比较简单的,定时任务追加了一次,每天跑两次定时任务进行计算.  </p>
<h3 id="spark-job挂了"><a href="#spark-job挂了" class="headerlink" title="spark job挂了"></a>spark job挂了</h3><p>遇到过几次,主要是代码问题.<br>解决方式也就是登录控制台看一下报错log根据错误内容修改代码重新设置任务.  </p>
<p>代码bug无法避免,想通过邮件告警的方式进行及时提醒,但对应平台的邮箱配置有问题,询问了几次没有解决最后也搁置了.  </p>
<h3 id="ES某个index数据被清空"><a href="#ES某个index数据被清空" class="headerlink" title="ES某个index数据被清空"></a>ES某个index数据被清空</h3><p>这个就比较难受了,发现计算一个指标使用的index里的doc少了很多,导致趋势图直接断崖了.  </p>
<p>但去检查该<code>index</code>的<code>primary shard</code>又是好的… … …<br>联系存储的运维看了下也没发现什么问题.<br>当务之急只能先导回数据,还好原始数据存储在了数据库内,最后用<code>logstash</code>导入恢复.<br>之后没有再出现这个问题.  </p>
<h3 id="kafka-sink挂了"><a href="#kafka-sink挂了" class="headerlink" title="kafka sink挂了"></a>kafka sink挂了</h3><p>原开发是找了内网两台空闲的机器起了<code>kafka connector</code>来将数据导入到ES中.<br>而且这个细节只存在于原开发的笔记中,因为基本没有交接,当时这个出问题的导致数据缺失的时候瞎猫找死耗子弄了好久.  </p>
<p><img src="/images/1244488-20180830162506504-991089972.png"><br>kafka当时已经接入了prometheus,配置了一下报警规则,这一块出现问题可以及时发现.  </p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>现在回头想想,或许这套系统就不该做出来… …<br>我们有独立的数据团队来做这些,当时因为对方无暇去做这个,整个项目就赶鸭子上架一样由一位开发搭建整个系统和整套流程.<br>重复了对方重复的工作不说,还做得更糟,数据团队也不免吐槽了这个一番.    </p>
<p>其次,这套系统上了之后缺乏关注和维护,他就像鸡肋一样,舍不得丢,也不愿专人维护,就连原开发自己都苦恼不已.最后就这样挂到我头上.  </p>
<p>除去吐槽,简单总结一下这套系统不合理之处:  </p>
<ul>
<li>以时间作为任务触发的条件.<br>这个上面也有提到,不管是多个<code>spark</code>任务,<code>spring batch</code>任务,还是整个流程,都是以时间作为任务触发的.<br>上游任务出现延迟就会出现问题,接手之后的处理是在白天再跑一次,但最开始设计应该用hook更为合适.  </li>
<li>缺少测试环境.<br>这套系统只有线上,batch可以本地跑,但除此之外没有ES和spark的内网环境,改了直接上线,验证线上数据.  </li>
<li>无法快速验证.<br>最开始所有的fix都需要跑至少一天的数据才能看出问题(且ES和spark的聚合没办法,一些任务都不带userId).之后在batch中加入了账号参数可以只跑一个账号的数据.</li>
</ul>
<p>最后自己问题也不少,当初接手这套系统的时候没有了解他大体的结构.<br>只关注了<code>spring batch</code>和服务器一侧的实现,其他部分是在出现bug时才会去跟进发现.  </p>
<p>并且自己掌握的技术能力有限,<code>ES</code>和<code>spark</code>的知识积累不够,现在也还在补这块的内容,自己身上有短板.  </p>
<p>整个系统对于一个业务开发来说可以说是一个很好的学习机会了,这样的机会也来之不易.</p>
]]></content>
  </entry>
  <entry>
    <title>playwright 一些方法解决被cloudflare block的问题</title>
    <url>/2024/01/31/playwright-cloudflare-bypass/</url>
    <content><![CDATA[<p>在尝试从一个使用Cloudflare Web应用程序防火墙（WAF）保护的网站获取数据时,我遇到了一些挑战.该网站的安全措施非常严格,以至于在正常浏览几个页面后,Cloudflare的检查页面就会出现.</p>
<p>传统的HTTP客户端方法,如直接使用httpclient来抓取页面数据,很快就会遭遇阻碍.</p>
<p>即便尝试使用代理IP池,问题依旧存在,因为Cloudflare的检测机制能够在短时间内多次访问后迅速触发.在多次尝试后,我决定使用playwright这个自动化库来模拟正常的浏览器行为.</p>
<p>虽然在使用playwright的过程中遇到了一些问题,但我最终找到了解决方案.现在,尽管速度稍慢,但我能够正常地从网站获取数据.接下来,我将分享如何克服这些挑战的经验.</p>
<span id="more"></span>

<h1 id="直接403"><a href="#直接403" class="headerlink" title="直接403"></a>直接403</h1><p>这是cloudflare的基本防护,他会检查是否使用了webdriver进行模拟爬取.<br>解决方式:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token keyword">final</span> <span class="token class-name">BrowserContext</span> context <span class="token operator">=</span> browser<span class="token punctuation">.</span><span class="token function">newContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
context<span class="token punctuation">.</span><span class="token function">setDefaultTimeout</span><span class="token punctuation">(</span><span class="token number">180_000</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
context<span class="token punctuation">.</span><span class="token function">setDefaultNavigationTimeout</span><span class="token punctuation">(</span><span class="token number">180_000</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
context<span class="token punctuation">.</span><span class="token function">addInitScript</span><span class="token punctuation">(</span><span class="token string">"Object.defineProperty(navigator, 'webdriver', &#123;get: () => undefined&#125;)"</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>这里同时也将默认超时拉长,避免在等待某些元素触发的条件下超时.</p>
<h1 id="使用本地安装的浏览器"><a href="#使用本地安装的浏览器" class="headerlink" title="使用本地安装的浏览器"></a>使用本地安装的浏览器</h1><p>可以直接指定本地安装的浏览器:  </p>
<blockquote>
<p>While Playwright can download and use the recent Chromium build, it can operate against the branded Google Chrome and Microsoft Edge browsers available on the machine (note that Playwright doesn’t install them by default). In particular, the current Playwright version will support Stable and Beta channels of these browsers.</p>
</blockquote>
<blockquote>
<p>Available channels are chrome, msedge, chrome-beta, msedge-beta or msedge-dev.  </p>
</blockquote>
<p>启动的时候指定即可:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">Browser</span> browser <span class="token operator">=</span> playwright<span class="token punctuation">.</span><span class="token function">chromium</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">launch</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">BrowserType<span class="token punctuation">.</span>LaunchOptions</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">setChannel</span><span class="token punctuation">(</span><span class="token string">"msedge"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>


<h1 id="是否触发了cloudflare判断"><a href="#是否触发了cloudflare判断" class="headerlink" title="是否触发了cloudflare判断"></a>是否触发了cloudflare判断</h1><p>因为触发了cloudflare页面的url并不会有明显的变化,只有多一次跳转.<br>这里直接使用页面元素即可, 处理代码如下:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">checkMeetChallenge</span><span class="token punctuation">(</span><span class="token class-name">Page</span> page<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">Exception</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">ElementHandle</span> stage <span class="token operator">=</span> page<span class="token punctuation">.</span><span class="token function">querySelector</span><span class="token punctuation">(</span><span class="token string">"div#challenge-stage"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> checkCount <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
    <span class="token keyword">while</span> <span class="token punctuation">(</span>stage <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token function">tryToClickChallenge</span><span class="token punctuation">(</span>page<span class="token punctuation">,</span> stage<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>checkCount <span class="token operator">>=</span> <span class="token number">6</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>page<span class="token punctuation">.</span><span class="token function">querySelector</span><span class="token punctuation">(</span><span class="token string">"div#challenge-stage"</span><span class="token punctuation">)</span> <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Exception</span><span class="token punctuation">(</span><span class="token string">"meet challenge restart"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span>
        log<span class="token punctuation">.</span><span class="token function">info</span><span class="token punctuation">(</span><span class="token string">"handleCategory - meet challenge. wait 20s to check it again. count:&#123;&#125;"</span><span class="token punctuation">,</span> checkCount<span class="token punctuation">)</span><span class="token punctuation">;</span>
        checkCount<span class="token operator">++</span><span class="token punctuation">;</span>
        page<span class="token punctuation">.</span><span class="token function">waitForTimeout</span><span class="token punctuation">(</span><span class="token number">20_000</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        stage <span class="token operator">=</span> page<span class="token punctuation">.</span><span class="token function">querySelector</span><span class="token punctuation">(</span><span class="token string">"div#challenge-stage"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>在上层的调用代码中,我先判断了我需要的元素是否存在,如果不存在有两种可能,一种是已经爬完了,另一种是触发了cloudflare.<br>那个meet challenge restart是一层兜底, 如果点击失败加上长时间等待还在, 那就重启浏览器, 重启之后大概率不会重新触发防护, 也算是最后一个措施了.</p>
<h1 id="处理防护页的人工点击"><a href="#处理防护页的人工点击" class="headerlink" title="处理防护页的人工点击"></a>处理防护页的人工点击</h1><p>首先先看一下那个防护页面的结构:  </p>
<pre class="language-html" data-language="html"><code class="language-html"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>challenge-stage<span class="token punctuation">"</span></span> <span class="token style-attr"><span class="token attr-name">style</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span><span class="token style language-css"><span class="token property">display</span><span class="token punctuation">:</span> flex<span class="token punctuation">;</span></span><span class="token punctuation">"</span></span></span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>turnstile-wrapper<span class="token punctuation">"</span></span> <span class="token attr-name">class</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>captcha-prompt spacer<span class="token punctuation">"</span></span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span><span class="token punctuation">></span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>iframe</span>
                <span class="token attr-name">src</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>https://challenges.cloudflare.com/cdn-cgi/challenge-platform/xxxxxx<span class="token punctuation">"</span></span>
                <span class="token attr-name">allow</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>cross-origin-isolated; fullscreen<span class="token punctuation">"</span></span>
                <span class="token attr-name">sandbox</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>allow-same-origin allow-scripts allow-popups<span class="token punctuation">"</span></span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>cf-chl-widget-sppzq<span class="token punctuation">"</span></span> <span class="token attr-name">tabindex</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>0<span class="token punctuation">"</span></span>
                <span class="token attr-name">title</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>包含  Cloudflare  安全质询的小组件 <span class="token punctuation">"</span></span>
                <span class="token style-attr"><span class="token attr-name">style</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span><span class="token style language-css"><span class="token property">border</span><span class="token punctuation">:</span> none<span class="token punctuation">;</span> <span class="token property">overflow</span><span class="token punctuation">:</span> hidden<span class="token punctuation">;</span> <span class="token property">width</span><span class="token punctuation">:</span> 300px<span class="token punctuation">;</span> <span class="token property">height</span><span class="token punctuation">:</span> 65px<span class="token punctuation">;</span></span><span class="token punctuation">"</span></span></span><span class="token punctuation">></span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>iframe</span><span class="token punctuation">></span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>input</span>
                <span class="token attr-name">type</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>hidden<span class="token punctuation">"</span></span> <span class="token attr-name">name</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>cf-turnstile-response<span class="token punctuation">"</span></span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">"</span>cf-chl-widget-sppzq_response<span class="token punctuation">"</span></span><span class="token punctuation">></span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">></span></span></code></pre>

<p>点击的组件被放在了一个iframe里,正常要获取元素点击比较麻烦.<br>但是<code>playwright</code>直接模拟鼠标在位置上点击即可.<br>具体思路是定位到外层的<code>div#challenge-stage</code>, 然后用<code>page.mouse().move</code>移动到元素上,在用click()点击.<br>代码:  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">BoundingBox</span> box <span class="token operator">=</span> stageDiv<span class="token punctuation">.</span><span class="token function">boundingBox</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
page<span class="token punctuation">.</span><span class="token function">mouse</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">move</span><span class="token punctuation">(</span>box<span class="token punctuation">.</span>x <span class="token operator">+</span> <span class="token number">100</span><span class="token punctuation">,</span> box<span class="token punctuation">.</span>y <span class="token operator">+</span> box<span class="token punctuation">.</span>height <span class="token operator">/</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
page<span class="token punctuation">.</span><span class="token function">waitForTimeout</span><span class="token punctuation">(</span><span class="token number">1_000</span> <span class="token operator">+</span> <span class="token class-name">ThreadLocalRandom</span><span class="token punctuation">.</span><span class="token function">current</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">nextInt</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span> <span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
page<span class="token punctuation">.</span><span class="token function">mouse</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">click</span><span class="token punctuation">(</span>box<span class="token punctuation">.</span>x <span class="token operator">+</span> <span class="token number">100</span><span class="token punctuation">,</span> box<span class="token punctuation">.</span>y <span class="token operator">+</span> box<span class="token punctuation">.</span>height <span class="token operator">/</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>其实你不点击它也没关系,我在测试的时候发现这个challenge页面会在1分钟左右自己消失,可以在检测出是challenge之后循环waitForTimeout即可.  </p>
<p>最后就是要注意时间间隔,中间留一点时间.<br>这些做完基本就可以慢慢获取数据了.  </p>
<p>我之前还做了很多随机滚动(mouse.wheel),移动到下一页点击等措施,结果发现做不做一样,可能在一些情况下比较有用.<br>因为<code>playwright</code>很多动作其实是要scheduled的,它并不立即触发,比如你click下一页之后就算<code>waitForLoadState</code>再用<code>locator</code>或<code>querySelector</code>获取元素他还是会报错,这种情况可以手动<code>waitForTimeout</code>一下,我不知道这样处理是不是常规做法,但至少解决了我的问题.</p>
<p>相关参考链接:<br><a href="https://playwright.dev/">playwright</a><br><a href="https://playwright.dev/docs/browsers">playwright Browsers</a><br><a href="https://www.zenrows.com/blog/playwright-cloudflare-bypass#what-is-cloudflare">How to Bypass Cloudflare with Playwright in 2024</a>  </p>
]]></content>
      <tags>
        <tag>java</tag>
        <tag>playwright</tag>
      </tags>
  </entry>
  <entry>
    <title>prestosql(trino) hive时间戳显示不正确排查</title>
    <url>/2022/02/19/prestosql-prestodb-different-time-same-timestamp/</url>
    <content><![CDATA[<p>TLDR: 服务器的时区设置有问题。  </p>
<span id="more"></span>
<hr>
<p><code>prestosql</code>也老早改名<code>trino</code>了…<br>公司的数仓很早就建了且没有升级，所以版本很老。<br><code>hive</code>使用的是1.1版本,对应的<code>ORC</code>版本是0.12。<br>装了一个远古版本的<code>prestodb</code>，后来在2019年又装了一个<code>prestosql</code>。  </p>
<p>讲完了背景说一下问题。<br><code>hive</code>中有<code>timestamp</code>字段的表，在<code>hive</code>和<code>prestodb</code>中显示正常，比如<code>2022-02-19 08:00:00</code>,但是在<code>prestosql</code>中显示的是<code>2022-02-19 16:00:00</code>，也就是多了8H。<br>最开始排查，检查<code>hive</code>,<code>prestosql</code>和<code>prestodb</code>的当前时间和时区，都显示为正常。排除物理机时区不同或者设置了不同时区。    </p>
<p>既然数据库设置没问题，那看一下<code>ORC</code>,dump了一下元信息，发现对应字段类型是<code>timestamp</code>，dump一下数据，发现存储的是<code>2022-02-19 08:00:00</code>。<br>???<br>存的是<code>2022-02-19 08:00:00</code>？看<code>ORC</code>的类型可知时间戳分类两个类型，一个是UTC一个是with timezone offset的，stripe信息中没有timezone信息，为什么timestamp存的是东八区的时间，而不是UTC？  </p>
<p>看到这个渐渐有点明朗，东八区的时间的确是<code>2022-02-19 08:00:00</code>，但时间戳存的也是<code>2022-02-19 08:00:00</code>，那也就是<code>hive</code>和<code>prestodb</code>直接把这个当作东八区来用了所以正常显示，而<code>prestosql</code>是认为<code>2022-02-19 08:00:00</code>是UTC时间，所以查询显示的时候自动加了当前时区的8小时。  </p>
<p>再检查了一下使用的<code>ORC</code>版本，发现版本很低，对比release信息，这个版本是不支持timezone offset的。<br>找到定义文档，发现一段话：  </p>
<blockquote>
<p>Before ORC-135, the local timezone offset was included and they were stored as minimum and maximum. After ORC-135, the timestamp is adjusted to UTC before being converted to milliseconds and stored in minimumUtc and maximumUtc.<br>emmm，所以他存的timestamp实际上是包括了本地时间偏移。  </p>
</blockquote>
<p><del>再加上<code>hive</code>和<code>prestodb</code>用的版本过低，读取timestamp的时候用的就是当前时区。所以显示没问题。<br>而<code>prestosql</code>版本更新，且支持<code>ORC</code>的timestamp with timezone offset，那如果stripe里没有时区会怎么样呢，我们使用的版本看起来是会默认把<code>timestamp</code>当作UTC</del>.(见后面分析)</p>
<p>经过上面一顿分析，参考trino的文档找到了一个参数<code>hive.orc.time-zone</code>，实测无效因为这个是341版本实装的，版本过低。<br>在看了一眼老版本要用参数<code>hive.time-zone</code>，设置回<code>Asia/Shanghai</code>后回复正常了。  </p>
<hr>
<p>本来以为事情就这么解决。<br>但后来一想这个<code>trino</code>升级应该不会这么不谨慎吧，如果有这个问题那应该大面积爆发且爆栈，issue很多才对。<br>实际上这个资料都很难搜到。<br>那可不可能是自己的问题呢。  </p>
<p>再次对比<code>prestodb</code>和<code>presosql</code>，他们现在配置的区别就是<code>prestosql</code>设置了time-zone。<br>这个参数很早就支持了，那为什么<code>prestodb</code>不设置也可以呢。<br>查了下这个参数的默认值用的是<code>JVM Default</code>。<br>emmm， 那主要就是两个<code>user.timezone</code>这个属性。<br>用<code>jcmd pid VM.system_properties</code>检查了一下… …<br>果然，<code>prestosql</code>写的是<code>user.timezone=UTC</code>，而<code>prestodb</code>的是<code>user.timezone=Asia/Shanghai</code>。<br>破案… …<br>这个属性一般不用自己设置，看了下服务器，emmm，部署<code>prestosql</code>的用的是UTC，所以他把<code>hive.time-zone</code>设置为了<code>UTC</code>,读取<code>ORC</code>也错了。  </p>
<p>哎，走了一些弯路还在怀疑版本更新之后的一些兼容性问题。<br>不过也是必要的，最后发现是环境问题也是因为知道了对应的参数和其默认值。<br>排查问题最主要的还是要尽量刨根究底，不然会有错误的认知和结论。<br>哎，还是太菜了 …( ＿ ＿)ノ｜。</p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p><a href="https://orc.apache.org/specification/ORCv1/">ORC Specification v1</a><br><a href="https://trino.io/docs/current/release/release-341.html">Trino Release 341(8 Sep 2020)</a><br><a href="https://stackoverflow.com/questions/2493749/how-to-set-a-jvm-timezone-properly">How to set a JVM TimeZone Properly</a></p>
]]></content>
  </entry>
  <entry>
    <title>react native的expo router安卓下modal不生效的解法</title>
    <url>/2024/06/19/react-native-expo-router-android-modal-not-working-solution/</url>
    <content><![CDATA[<p>最近跟着教程学<code>react native</code>,教程使用的是iOS,而我在windows下开发用的安卓真机,除了一些样式上的区别大体的功能差不多.<br>但路由的<code>modal</code>显示不一样感觉观感差的有点多.<br>这是因为安卓原生是没有iOS那种modal的路由的,<code>expo router</code>使用的是<code>react navigation</code>的<code>native stack</code>,这个设置项在安卓下没有效果.</p>
<span id="more"></span>

<p>解法很简单,在github的issue中有人提到过了,其实<code>expo router</code>的老的文档里也有提到,新的文档没这部分内容了.<br>见<a href="https://expo.github.io/router/docs/migration/react-navigation/stack">stack</a><br>代码:</p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript"><span class="token keyword">import</span> <span class="token punctuation">&#123;</span>
  <span class="token comment">// Import the creation function</span>
  createStackNavigator<span class="token punctuation">,</span>
  <span class="token comment">// Import the types</span>
  StackNavigationOptions<span class="token punctuation">,</span>
<span class="token punctuation">&#125;</span> <span class="token keyword">from</span> <span class="token string">"@react-navigation/stack"</span><span class="token punctuation">;</span>

<span class="token keyword">import</span> <span class="token punctuation">&#123;</span> withLayoutContext <span class="token punctuation">&#125;</span> <span class="token keyword">from</span> <span class="token string">"expo-router"</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> <span class="token punctuation">&#123;</span> Navigator <span class="token punctuation">&#125;</span> <span class="token operator">=</span> <span class="token function">createStackNavigator</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// This can be used like `&lt;CustomStack />`</span>
<span class="token keyword">export</span> <span class="token keyword">const</span> CustomStack <span class="token operator">=</span> withLayoutContext<span class="token operator">&lt;</span>
  StackNavigationOptions<span class="token punctuation">,</span>
  <span class="token keyword">typeof</span> Navigator
<span class="token operator">></span><span class="token punctuation">(</span>Navigator<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>但这个代码在现在版本的expo里不能运行,会报类型错误.  </p>
<p>在一个github的讨论中有新的代码:<br><a href="https://github.com/expo/router/issues/640">presentation: ‘modal’ does’t work for Stack in Android</a><br>如下:</p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript"><span class="token keyword">import</span> <span class="token punctuation">&#123;</span> Stack<span class="token punctuation">,</span> withLayoutContext <span class="token punctuation">&#125;</span> <span class="token keyword">from</span> <span class="token string">"expo-router"</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">&#123;</span>
  createStackNavigator<span class="token punctuation">,</span>
  StackNavigationEventMap<span class="token punctuation">,</span>
  StackNavigationOptions<span class="token punctuation">,</span>
  TransitionPresets<span class="token punctuation">,</span>
<span class="token punctuation">&#125;</span> <span class="token keyword">from</span> <span class="token string">"@react-navigation/stack"</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">&#123;</span> ParamListBase<span class="token punctuation">,</span> StackNavigationState <span class="token punctuation">&#125;</span> <span class="token keyword">from</span> <span class="token string">"@react-navigation/native"</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> <span class="token punctuation">&#123;</span> Navigator <span class="token punctuation">&#125;</span> <span class="token operator">=</span> <span class="token function">createStackNavigator</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">const</span> JsStack <span class="token operator">=</span> withLayoutContext<span class="token operator">&lt;</span>
  StackNavigationOptions<span class="token punctuation">,</span>
  <span class="token keyword">typeof</span> Navigator<span class="token punctuation">,</span>
  StackNavigationState<span class="token operator">&lt;</span>ParamListBase<span class="token operator">></span><span class="token punctuation">,</span>
  StackNavigationEventMap
<span class="token operator">></span><span class="token punctuation">(</span>Navigator<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>修改<code>_layout.tsx</code>:  </p>
<pre class="language-javascript" data-language="javascript"><code class="language-javascript"><span class="token operator">&lt;</span>JsStack<span class="token operator">></span>
  <span class="token operator">&lt;</span>Stack<span class="token punctuation">.</span>Screen name<span class="token operator">=</span><span class="token string">"(tabs)"</span> options<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token punctuation">&#123;</span> headerShown<span class="token operator">:</span> <span class="token boolean">false</span> <span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span> <span class="token operator">/</span><span class="token operator">></span>
  <span class="token operator">&lt;</span>JsStack<span class="token punctuation">.</span>Screen
    name<span class="token operator">=</span><span class="token string">"filters"</span>
    options<span class="token operator">=</span><span class="token punctuation">&#123;</span><span class="token punctuation">&#123;</span>
      <span class="token operator">...</span>TransitionPresets<span class="token punctuation">.</span>ModalPresentationIOS<span class="token punctuation">,</span>
      presentation<span class="token operator">:</span> <span class="token string">"modal"</span><span class="token punctuation">,</span>
      gestureEnabled<span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span>
  <span class="token operator">/</span><span class="token operator">></span>
<span class="token operator">&lt;</span><span class="token operator">/</span>JsStack<span class="token operator">></span></code></pre>

<p>在使用过程中我发现他会和<code>zeego</code>冲突,导致zeego功能不可用.<br>解决方法是在使用到<code>zeego</code>的page里在额外套一个Stack,也就是wrap一个Stack(其他的layout也可以,就是不能直接被JsStack包裹),这样zeego就可以工作了.</p>
]]></content>
      <tags>
        <tag>react native</tag>
        <tag>expo router</tag>
      </tags>
  </entry>
  <entry>
    <title>windwos下运行hadoop相关代码(hdfs,mr)</title>
    <url>/2022/04/16/run-hadoop-application-on-windows/</url>
    <content><![CDATA[<p>最近在学习一些<code>hadoop</code>相关的知识,需要在本地运行<code>hdfs</code>和<code>mr</code>相关的代码.<br>因为开发机使用的是windows,所以会有一些不太方便.  </p>
<p>可以在windows下解压<code>hadoop</code>,然后在应用的<code>env</code>中指定<code>HADOOP_HOME</code>为解压路径.<br>在windows中需要<code>winutils.exe</code>工具,当前网上好心人预编译好的只到<code>3.3.0</code>.<br>见: <a href="https://kontext.tech/article/446/hadoop-330-winutils">https://kontext.tech/article/446/hadoop-330-winutils</a>  </p>
<span id="more"></span>
<p>如果学习使用的是这个和之前的版本,那问题不大,只需要下载再把文件放入<code>bin</code>目录即可.<br>这种方式比较常见的错误会是:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">Exception</span> in thread <span class="token string">"main"</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span></span>RuntimeException</span><span class="token operator">:</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>io<span class="token punctuation">.</span></span>FileNotFoundException</span><span class="token operator">:</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>io<span class="token punctuation">.</span></span>FileNotFoundException</span><span class="token operator">:</span> HADOOP_HOME and hadoop<span class="token punctuation">.</span>home<span class="token punctuation">.</span>dir are unset<span class="token punctuation">.</span> <span class="token operator">-</span>see https<span class="token operator">:</span><span class="token operator">/</span><span class="token operator">/</span>wiki<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>org<span class="token operator">/</span>hadoop<span class="token operator">/</span><span class="token class-name">WindowsProblems</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span>Shell</span><span class="token punctuation">.</span><span class="token function">getWinUtilsPath</span><span class="token punctuation">(</span><span class="token class-name">Shell</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">735</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span>Shell</span><span class="token punctuation">.</span><span class="token function">getSetPermissionCommand</span><span class="token punctuation">(</span><span class="token class-name">Shell</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">270</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span>Shell</span><span class="token punctuation">.</span><span class="token function">getSetPermissionCommand</span><span class="token punctuation">(</span><span class="token class-name">Shell</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">286</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">setPermission</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">978</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">mkOneDirWithMode</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">660</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">mkdirsWithOptionalPermission</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">700</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">mkdirs</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">672</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">mkdirsWithOptionalPermission</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">699</span><span class="token punctuation">)</span>
  <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span></code></pre>

<p>这是<code>HADOOP_HOME</code>没有设置.  </p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">Exception</span> in thread <span class="token string">"main"</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span></span>UnsatisfiedLinkError</span><span class="token operator">:</span> <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>io<span class="token punctuation">.</span>nativeio<span class="token punctuation">.</span></span>NativeIO</span>$<span class="token class-name">Windows</span><span class="token punctuation">.</span><span class="token function">access0</span><span class="token punctuation">(</span><span class="token class-name">Ljava</span><span class="token operator">/</span>lang<span class="token operator">/</span><span class="token class-name">String</span><span class="token punctuation">;</span><span class="token class-name">I</span><span class="token punctuation">)</span><span class="token class-name">Z</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>io<span class="token punctuation">.</span>nativeio<span class="token punctuation">.</span></span>NativeIO</span>$<span class="token class-name">Windows</span><span class="token punctuation">.</span><span class="token function">access0</span><span class="token punctuation">(</span><span class="token class-name">Native</span> <span class="token class-name">Method</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>io<span class="token punctuation">.</span>nativeio<span class="token punctuation">.</span></span>NativeIO</span>$<span class="token class-name">Windows</span><span class="token punctuation">.</span><span class="token function">access</span><span class="token punctuation">(</span><span class="token class-name">NativeIO</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">793</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>FileUtil</span><span class="token punctuation">.</span><span class="token function">canRead</span><span class="token punctuation">(</span><span class="token class-name">FileUtil</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">1218</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>FileUtil</span><span class="token punctuation">.</span><span class="token function">list</span><span class="token punctuation">(</span><span class="token class-name">FileUtil</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">1423</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>RawLocalFileSystem</span><span class="token punctuation">.</span><span class="token function">listStatus</span><span class="token punctuation">(</span><span class="token class-name">RawLocalFileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">601</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>FileSystem</span><span class="token punctuation">.</span><span class="token function">listStatus</span><span class="token punctuation">(</span><span class="token class-name">FileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">1972</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>FileSystem</span><span class="token punctuation">.</span><span class="token function">listStatus</span><span class="token punctuation">(</span><span class="token class-name">FileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">2014</span><span class="token punctuation">)</span>
	at <span class="token class-name"><span class="token namespace">org<span class="token punctuation">.</span>apache<span class="token punctuation">.</span>hadoop<span class="token punctuation">.</span>fs<span class="token punctuation">.</span></span>FileSystem</span>$<span class="token number">4.</span><span class="token generics"><span class="token punctuation">&lt;</span>init<span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token class-name">FileSystem</span><span class="token punctuation">.</span>java<span class="token operator">:</span><span class="token number">2180</span><span class="token punctuation">)</span></code></pre>

<p>这是使用的<code>winutils</code>版本不对.</p>
<p>除此之外还可以利用<code>idea</code>的<code>WSL</code>和<code>SSH</code>运行方式.<br>点击右上角的运行旁的下拉按钮, 打开<code>Run/Debug Configuration</code>,修改<code>Run on</code>为<code>SSH</code>或<code>WSL</code>.  </p>
<p>因为本地已经搭了集群的虚拟机,并且配置了<code>java</code>和<code>hadoop</code>相关环境,所以直接用<code>SSH</code>运行就通过了检查.<br>其中会有一个文件的拷贝过程,需要等待一点点时间.<br>要注意的是运行会在远端的服务器(这里是本地的虚拟机),所以要测试的路径/资源都要设置在远端.    </p>
<p>之后的运行就和在本地没什么区别,也一样可以调试.</p>
]]></content>
  </entry>
  <entry>
    <title>还未(划掉)毕业的这几天</title>
    <url>/2018/09/07/school-days-memory/</url>
    <content><![CDATA[<p>在图灵社区写的文,顺便同步过来.  </p>
<hr>
<span id="more"></span>
<p>本文来自图灵社区 fairjm@ituring  </p>
<hr>
<p>在毕业前用的一个社区文集(私密)的名字.<br>当时以周记,日记的形式记录了临毕业的一个月.<br>再回首已经过了三年多了.<br>正式变为了一只码农,社畜.  </p>
<p>之前熟悉的9月开学感觉已经非常遥远了.<br>偶尔不开心的时候就想着要回去读书.<br>身边倒是有一些同事重新回了校园,但自己大抵是没有这样的勇气和魄力吧.<br>上了那么久的学,做梦有时还会梦见诸如要考试自己没复习的场景,害怕挂科了怎么办,随即在梦中清醒自己,发现已经毕业了.<br>最落寞的一次最近梦到自己在一间教室,本来很熟悉的同学突然都不认识了,也不知道自己为何会出现在这教室里.  </p>
<p>说来,我还是更喜欢大学时的生活.<br>上完课就有大把时间挥霍,干自己喜欢的事.<br>(自己大学除了一两节公选课没去外,必修课之类的没逃过课… …毕竟不想挂科)<br>大一学完<code>java</code>学<code>java web</code>.<br>大二学<code>SSH</code>全家桶,安卓开发.<br>大三学了<code>Go</code>,<code>Scala</code>,顺带看了点<code>Play</code>和<code>Akka</code>.<br>大四实习了一个学期,下学期逃回来做毕设.<br>最后用<code>Scala</code>做了一个算法的毕设,老师嫌看起来累再用<code>Java</code>重新撸了一遍.<br>顺带帮助几位同学顺利毕业.<br>大学就是可以无忧无虑学着自己喜欢的东西,生命中一段比较平淡而又充实的日子.<br>实习,工作基本就是以自己的兴趣谋生,但会做着自己不喜欢的事,且学习时间减少了.<br>现在公司11小时左右,睡觉7-8小时.好在有双休,但一天肯定会用来补觉.<br>家里有矿的话我肯定立即跑了:)</p>
<p>常看到说要珍惜大学生活.<br>但其实每个人都有自己的兴趣爱好.<br>大部分普通人在经历初高中学业负担(更惨的可能小学开始吧…),压抑自己兴趣后,大学可以说是另一段人生中为数不多的”儿时时光”,正值青春充沛的体力,悠闲的空余时间,不过要小心挂科.<br>无所谓珍惜不珍惜,只要过得自己开心就行,做一些自己喜欢的事.<br>毕竟在之后一段时间内,再也不会有这样的机会了~</p>
]]></content>
  </entry>
  <entry>
    <title>shardingsphere-jdbc 水平分表学习记录</title>
    <url>/2022/11/05/shardingsphere-jdbc-learn-note/</url>
    <content><![CDATA[<p>前司使用的是自己魔改的<code>TDDL</code>,在家时间比较多就尝试学一些业内比较常用的中间件.  </p>
<p>这里记录一下学习中遇到的一些问题.  </p>
<h1 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h1><p>设置的比较简单(太懒了就测试了几个表), 两个分库, 各有几张分表.<br>sharding-test_0</p>
<ul>
<li>order_0 (order_id)</li>
<li>order_1 </li>
<li>order_item_0 (order_id)</li>
<li>order_item_1</li>
<li>user_0 (user_id)</li>
<li>user_1</li>
<li>address (用来做broadcast表)</li>
</ul>
<pre class="language-sql" data-language="sql"><code class="language-sql"><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> <span class="token punctuation">`</span>order_0<span class="token punctuation">`</span> <span class="token punctuation">(</span>
  <span class="token punctuation">`</span>order_id<span class="token punctuation">`</span> <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token punctuation">`</span>user_id<span class="token punctuation">`</span> <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token punctuation">`</span>address_id<span class="token punctuation">`</span> <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span><span class="token punctuation">`</span>order_id<span class="token punctuation">`</span><span class="token punctuation">)</span>
<span class="token punctuation">)</span> <span class="token keyword">ENGINE</span><span class="token operator">=</span><span class="token keyword">InnoDB</span> <span class="token keyword">DEFAULT</span> <span class="token keyword">CHARSET</span><span class="token operator">=</span>utf8mb4 <span class="token keyword">COLLATE</span><span class="token operator">=</span>utf8mb4_0900_ai_ci<span class="token punctuation">;</span>

<span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> <span class="token punctuation">`</span>order_item_0<span class="token punctuation">`</span> <span class="token punctuation">(</span>
  <span class="token punctuation">`</span>order_item_id<span class="token punctuation">`</span> <span class="token keyword">bigint</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token punctuation">`</span>order_id<span class="token punctuation">`</span> <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span><span class="token punctuation">`</span>order_item_id<span class="token punctuation">`</span><span class="token punctuation">)</span>
<span class="token punctuation">)</span> <span class="token keyword">ENGINE</span><span class="token operator">=</span><span class="token keyword">InnoDB</span> <span class="token keyword">DEFAULT</span> <span class="token keyword">CHARSET</span><span class="token operator">=</span>utf8mb4 <span class="token keyword">COLLATE</span><span class="token operator">=</span>utf8mb4_0900_ai_ci<span class="token punctuation">;</span>

<span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> <span class="token punctuation">`</span>user_0<span class="token punctuation">`</span> <span class="token punctuation">(</span>
  <span class="token punctuation">`</span>user_id<span class="token punctuation">`</span> <span class="token keyword">bigint</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token punctuation">`</span>user_name<span class="token punctuation">`</span> <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">45</span><span class="token punctuation">)</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span><span class="token punctuation">`</span>user_id<span class="token punctuation">`</span><span class="token punctuation">)</span>
<span class="token punctuation">)</span> <span class="token keyword">ENGINE</span><span class="token operator">=</span><span class="token keyword">InnoDB</span> <span class="token keyword">DEFAULT</span> <span class="token keyword">CHARSET</span><span class="token operator">=</span>utf8mb4 <span class="token keyword">COLLATE</span><span class="token operator">=</span>utf8mb4_0900_ai_ci<span class="token punctuation">;</span>

<span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> <span class="token punctuation">`</span>address<span class="token punctuation">`</span> <span class="token punctuation">(</span>
  <span class="token punctuation">`</span>address_id<span class="token punctuation">`</span> <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token punctuation">`</span>address_name<span class="token punctuation">`</span> <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">45</span><span class="token punctuation">)</span> <span class="token keyword">DEFAULT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
  <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span><span class="token punctuation">`</span>address_id<span class="token punctuation">`</span><span class="token punctuation">)</span>
<span class="token punctuation">)</span> <span class="token keyword">ENGINE</span><span class="token operator">=</span><span class="token keyword">InnoDB</span> <span class="token keyword">DEFAULT</span> <span class="token keyword">CHARSET</span><span class="token operator">=</span>utf8mb4 <span class="token keyword">COLLATE</span><span class="token operator">=</span>utf8mb4_0900_ai_ci<span class="token punctuation">;</span></code></pre>

<p>spring-boot-starter使用2.7.5<br>shardingsphere-jdbc-core-spring-boot-starter使用5.2.1  </p>
<p>测试的时候最好直接跑,不要用单测,会被自动回滚掉.<br>可以定义多个<code>ApplicationRunner</code>来测试.</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyApplicationRunner</span> <span class="token keyword">implements</span> <span class="token class-name">ApplicationRunner</span> <span class="token punctuation">&#123;</span>

    <span class="token annotation punctuation">@Autowired</span>
    <span class="token keyword">private</span> <span class="token class-name">JdbcTemplate</span> jdbcTemplate<span class="token punctuation">;</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token keyword">final</span> <span class="token class-name">ApplicationArguments</span> args<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">Exception</span> <span class="token punctuation">&#123;</span></code></pre>

<p>用<code>JdbcTemplate</code>方便点也省去了依赖更多的东西.<br>返回自增key的代码样例:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token class-name">KeyHolder</span> keyHolder <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">GeneratedKeyHolder</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
jdbcTemplate<span class="token punctuation">.</span><span class="token function">update</span><span class="token punctuation">(</span>connection <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">PreparedStatement</span> ps <span class="token operator">=</span> connection<span class="token punctuation">.</span><span class="token function">prepareStatement</span><span class="token punctuation">(</span><span class="token string">"insert into user(`user_name`) values (?)"</span><span class="token punctuation">,</span>
            <span class="token class-name">Statement</span><span class="token punctuation">.</span>RETURN_GENERATED_KEYS<span class="token punctuation">)</span><span class="token punctuation">;</span>
    ps<span class="token punctuation">.</span><span class="token function">setString</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token string">"cc2"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> ps<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">,</span> keyHolder<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"key:"</span> <span class="token operator">+</span> keyHolder<span class="token punctuation">.</span><span class="token function">getKey</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>


<h1 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h1><p>本来使用yaml的配置,但看了一下有点太乱,先用properties的代替.<br>配的时候有比较多的问题,几个配置错误会导致没法启动或者测试时报错,但配完之后感觉整体逻辑还是比较清晰的.  </p>
<pre class="language-properties" data-language="properties"><code class="language-properties"><span class="token attr-name">spring.shardingsphere.mode.type</span><span class="token punctuation">=</span><span class="token attr-value">Standalone</span>
<span class="token attr-name">spring.shardingsphere.props.sql-show</span><span class="token punctuation">=</span><span class="token attr-value">true</span>

<span class="token comment"># logic datasource</span>
<span class="token attr-name">spring.shardingsphere.datasource.names</span><span class="token punctuation">=</span><span class="token attr-value">shard00,shard01</span>

<span class="token comment"># real datasource</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard00.type</span><span class="token punctuation">=</span><span class="token attr-value">com.zaxxer.hikari.HikariDataSource</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard00.driver-class-name</span><span class="token punctuation">=</span><span class="token attr-value">com.mysql.jdbc.Driver</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard00.jdbc-url</span><span class="token punctuation">=</span><span class="token attr-value">jdbc:mysql://localhost:3306/sharding_test_0</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard00.username</span><span class="token punctuation">=</span><span class="token attr-value">root</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard00.password</span><span class="token punctuation">=</span>

<span class="token attr-name">spring.shardingsphere.datasource.shard01.type</span><span class="token punctuation">=</span><span class="token attr-value">com.zaxxer.hikari.HikariDataSource</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard01.driver-class-name</span><span class="token punctuation">=</span><span class="token attr-value">com.mysql.jdbc.Driver</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard01.jdbc-url</span><span class="token punctuation">=</span><span class="token attr-value">jdbc:mysql://localhost:3306/sharding_test_1</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard01.username</span><span class="token punctuation">=</span><span class="token attr-value">root</span>
<span class="token attr-name">spring.shardingsphere.datasource.shard01.password</span><span class="token punctuation">=</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.actual-data-nodes</span><span class="token punctuation">=</span><span class="token attr-value">\
  shard0$->&#123;0..1&#125;.user_$->&#123;0..1&#125;</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.actual-data-nodes</span><span class="token punctuation">=</span><span class="token attr-value">\
  shard0$->&#123;0..1&#125;.order_item_$->&#123;0..1&#125;</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.actual-data-nodes</span><span class="token punctuation">=</span><span class="token attr-value">\
  shard0$->&#123;0..1&#125;.order_$->&#123;0..1&#125;</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.address.actual-data-nodes</span><span class="token punctuation">=</span><span class="token attr-value">\
  shard0$->&#123;0..1&#125;.address</span>


<span class="token comment"># database strategy and table strategy</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.database-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">user_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.database-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_db_user</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_db_user.type</span><span class="token punctuation">=</span><span class="token attr-value">MOD</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_db_user.props.sharding-count</span><span class="token punctuation">=</span><span class="token attr-value">2</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.table-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">user_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.table-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_table_user</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_table_user.type</span><span class="token punctuation">=</span><span class="token attr-value">HASH_MOD</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_table_user.props.sharding-count</span><span class="token punctuation">=</span><span class="token attr-value">2</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.database-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">order_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.database-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_db_order</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_db_order.type</span><span class="token punctuation">=</span><span class="token attr-value">MOD</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_db_order.props.sharding-count</span><span class="token punctuation">=</span><span class="token attr-value">2</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.table-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">order_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.table-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_table_order</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_table_order.type</span><span class="token punctuation">=</span><span class="token attr-value">HASH_MOD</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.sharding-algorithms.alg_table_order.props.sharding-count</span><span class="token punctuation">=</span><span class="token attr-value">2</span>
<span class="token comment"># order_item and order use the same strategy</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.database-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">order_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.database-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_db_order</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.table-strategy.standard.sharding-column</span><span class="token punctuation">=</span><span class="token attr-value">order_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.table-strategy.standard.sharding-algorithm-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_table_order</span>

<span class="token comment"># key generator</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.key-generate-strategy.column</span><span class="token punctuation">=</span><span class="token attr-value">user_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.user.key-generate-strategy.key-generator-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_snowflake</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.key-generate-strategy.column</span><span class="token punctuation">=</span><span class="token attr-value">order_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order.key-generate-strategy.key-generator-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_snowflake</span>

<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.key-generate-strategy.column</span><span class="token punctuation">=</span><span class="token attr-value">order_item_id</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.tables.order_item.key-generate-strategy.key-generator-name</span><span class="token punctuation">=</span><span class="token attr-value">alg_snowflake</span>

<span class="token comment"># key generator algorithm</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.key-generators.alg_snowflake.type</span><span class="token punctuation">=</span><span class="token attr-value">SNOWFLAKE</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.key-generators.alg_uuid.type</span><span class="token punctuation">=</span><span class="token attr-value">UUID</span>

<span class="token comment"># binding table and broadcast table</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.binding-tables[0]</span><span class="token punctuation">=</span><span class="token attr-value">order,order_item</span>
<span class="token attr-name">spring.shardingsphere.rules.sharding.broadcast-tables</span><span class="token punctuation">=</span><span class="token attr-value">address</span></code></pre>

<p>也就分为逻辑datasource定义, 真实的datasource定义.<br>对于每个逻辑表,定义分库分表规则,如果需要生成分布式key,定义key的生成算法.<br>分别对应<code>spring.shardingsphere.datasource.</code>前缀和<code>spring.shardingsphere.rules.sharding</code>前缀.</p>
<p>对于SNOWFLAKE要注意数据库的字段类型要bigint,int不够放.</p>
<h1 id="启动报错"><a href="#启动报错" class="headerlink" title="启动报错"></a>启动报错</h1><pre class="language-none"><code class="language-none">***************************
APPLICATION FAILED TO START
***************************

Description:

An attempt was made to call a method that does not exist. The attempt was made from the following location:

    org.apache.shardingsphere.infra.util.yaml.constructor.ShardingSphereYamlConstructor$1.&lt;init&gt;(ShardingSphereYamlConstructor.java:44)

The following method did not exist:

    &#39;void org.apache.shardingsphere.infra.util.yaml.constructor.ShardingSphereYamlConstructor$1.setCodePointLimit(int)&#39;

The calling methods class, org.apache.shardingsphere.infra.util.yaml.constructor.ShardingSphereYamlConstructor$1, was loaded from the following location:

    jar:file:&#x2F;.m2&#x2F;repository&#x2F;org&#x2F;apache&#x2F;shardingsphere&#x2F;shardingsphere-infra-util&#x2F;5.2.1&#x2F;shardingsphere-infra-util-5.2.1.jar!&#x2F;org&#x2F;apache&#x2F;shardingsphere&#x2F;infra&#x2F;util&#x2F;yaml&#x2F;constructor&#x2F;ShardingSphereYamlConstructor$1.class

The called methods class, org.apache.shardingsphere.infra.util.yaml.constructor.ShardingSphereYamlConstructor$1, is available from the following locations:

    jar:file:&#x2F;.m2&#x2F;repository&#x2F;org&#x2F;apache&#x2F;shardingsphere&#x2F;shardingsphere-infra-util&#x2F;5.2.1&#x2F;shardingsphere-infra-util-5.2.1.jar!&#x2F;org&#x2F;apache&#x2F;shardingsphere&#x2F;infra&#x2F;util&#x2F;yaml&#x2F;constructor&#x2F;ShardingSphereYamlConstructor$1.class

The called methods class hierarchy was loaded from the following locations:

    null: file:&#x2F;.m2&#x2F;repository&#x2F;org&#x2F;apache&#x2F;shardingsphere&#x2F;shardingsphere-infra-util&#x2F;5.2.1&#x2F;shardingsphere-infra-util-5.2.1.jar
    org.yaml.snakeyaml.LoaderOptions: file:&#x2F;.m2&#x2F;repository&#x2F;org&#x2F;yaml&#x2F;snakeyaml&#x2F;1.30&#x2F;snakeyaml-1.30.jar


Action:

Correct the classpath of your application so that it contains a single, compatible version of org.apache.shardingsphere.infra.util.yaml.constructor.ShardingSphereYamlConstructor$1</code></pre>

<p>很明显的一个以来冲突问题, 主要是这行代码:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token class-name">ShardingSphereYamlConstructor</span><span class="token punctuation">(</span><span class="token keyword">final</span> <span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">></span></span> rootClass<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">super</span><span class="token punctuation">(</span>rootClass<span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token class-name">LoaderOptions</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        
        <span class="token punctuation">&#123;</span>
            <span class="token function">setCodePointLimit</span><span class="token punctuation">(</span><span class="token class-name">Integer</span><span class="token punctuation">.</span>MAX_VALUE<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">ShardingSphereYamlConstructFactory</span><span class="token punctuation">.</span><span class="token function">getInstances</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span>each <span class="token operator">-></span> typeConstructs<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span>each<span class="token punctuation">.</span><span class="token function">getType</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> each<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">ShardingSphereYamlShortcutsFactory</span><span class="token punctuation">.</span><span class="token function">getAllYamlShortcuts</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token punctuation">(</span>key<span class="token punctuation">,</span> value<span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token function">addTypeDescription</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">TypeDescription</span><span class="token punctuation">(</span>value<span class="token punctuation">,</span> key<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>rootClass <span class="token operator">=</span> rootClass<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>snakeyaml的版本冲突,使用的版本中<code>LoaderOptions</code>没有<code>setCodePointLimit</code>这个方法.<br>使用的springboot的依赖的是<code>1.30.0</code>,显式依赖<code>1.33.0</code>即可.  </p>
<pre class="language-xml" data-language="xml"><code class="language-xml">        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">></span></span>
            <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">></span></span>org.yaml<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">></span></span>
            <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">></span></span>snakeyaml<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">></span></span>
            <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>version</span><span class="token punctuation">></span></span>1.33<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>version</span><span class="token punctuation">></span></span>
        <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">></span></span>
```  

# 配置错误导致的报错  
这类报错种类比较多  
比如  
* `DataNodesMissedWithShardingTableException`
* `ShardingRuleNotFoundException`  
* `InconsistentShardingTableMetaDataException`  

等等, 启动就会失败, 因为是读取了配置解析异常.  

这种就要看看对应的错误和配置.  

不过有点奇怪的是一些错误没有打出详细的报错信息.比如:</code></pre>
<p>Caused by: org.apache.shardingsphere.sharding.exception.metadata.DataNodesMissedWithShardingTableException: null<br>    at org.apache.shardingsphere.sharding.rule.TableRule.lambda$checkRule$4(TableRule.java:246) ~[shardingsphere-sharding-core-5.2.1.jar:5.2.1]<br>    at org.apache.shardingsphere.infra.util.exception.ShardingSpherePreconditions.checkState(ShardingSpherePreconditions.java:41) ~[shardingsphere-infra-util-5.2.1.jar:5.2.1]<br>    at org.apache.shardingsphere.sharding.rule.TableRule.checkRule(TableRule.java:245) ~[shardingsphere-sharding-core-5.2.1.jar:5.2.1]</p>
<pre class="language-none"><code class="language-none">
看了下是基类没调用super,导致message没有值.看了下这个已经在master分支修好了:
&#96;&#96;&#96;java
    public ShardingSphereSQLException(final SQLState sqlState, final int typeOffset, final int errorCode, final String reason, final Object... messageArguments) &#123;
        this(sqlState.getValue(), typeOffset, errorCode, reason, messageArguments);
    &#125;
    
    public ShardingSphereSQLException(final String sqlState, final int typeOffset, final int errorCode, final String reason, final Object... messageArguments) &#123;
        this.sqlState &#x3D; sqlState;
        vendorCode &#x3D; typeOffset * 10000 + errorCode;
        this.reason &#x3D; null &#x3D;&#x3D; reason ? null : String.format(reason, messageArguments);
        &#x2F;&#x2F; missing super(resaon) here
    &#125;</code></pre>

<h1 id="数据库自动生成的key不能作为route-key"><a href="#数据库自动生成的key不能作为route-key" class="headerlink" title="数据库自动生成的key不能作为route key"></a>数据库自动生成的key不能作为route key</h1><p>但是分布式生成的key可以, 这个在FAQ里有, 有这个错误是刚开始配分布式key的时候配错了.</p>
<p><a href="https://shardingsphere.apache.org/document/current/cn/faq/">原文:</a></p>
<blockquote>
<p>[分片] ShardingSphere 除了支持自带的分布式自增主键之外，还能否支持原生的自增主键？<br>回答：</p>
<p>是的，可以支持。但原生自增主键有使用限制，即不能将原生自增主键同时作为分片键使用。 由于 ShardingSphere 并不知晓数据库的表结构，而原生自增主键是不包含在原始 SQL 中内的，因此 ShardingSphere 无法将该字段解析为分片字段。如自增主键非分片键，则无需关注，可正常返回；若自增主键同时作为分片键使用，ShardingSphere 无法解析其分片值，导致 SQL 路由至多张表，从而影响应用的正确性。 而原生自增主键返回的前提条件是 INSERT SQL 必须最终路由至一张表，因此，面对返回多表的 INSERT SQL，自增主键则会返回零。</p>
</blockquote>
<h1 id="分表分库的规则思考"><a href="#分表分库的规则思考" class="headerlink" title="分表分库的规则思考"></a>分表分库的规则思考</h1><p>最开始的时候对于分库分表无脑两个都用了MOD, 但因为分区数和分表数是一样的(都是2).<br>所以mod 2数据的分布也是一样的,这就导致了<code>sharding_test_0</code>的<code>user_1</code>是没有数据的,<code>sharding_test_1</code>的<code>user_0</code>也是没有数据的.<br>分表了个寂寞.  </p>
<p>不只是一样,其实只要分库和分表数最大公约数不为1如果无脑MOD都会有倾斜的问题.<br>可以代码验证下:</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">int</span> dbShard <span class="token operator">=</span> <span class="token number">6</span><span class="token punctuation">;</span>
<span class="token keyword">int</span> tableShard <span class="token operator">=</span> <span class="token number">32</span><span class="token punctuation">;</span>

<span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Tuple2</span><span class="token punctuation">&lt;</span><span class="token class-name">Integer</span><span class="token punctuation">,</span> <span class="token class-name">Integer</span><span class="token punctuation">></span><span class="token punctuation">,</span> <span class="token class-name">Integer</span><span class="token punctuation">></span></span> count <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">TreeMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> dbShard<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> j <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> j <span class="token operator">&lt;</span> tableShard<span class="token punctuation">;</span> j<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        count<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span><span class="token class-name">Tuple</span><span class="token punctuation">.</span><span class="token function">tuple</span><span class="token punctuation">(</span>i<span class="token punctuation">,</span> j<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span>

<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">100000</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    count<span class="token punctuation">.</span><span class="token function">computeIfPresent</span><span class="token punctuation">(</span><span class="token class-name">Tuple</span><span class="token punctuation">.</span><span class="token function">tuple</span><span class="token punctuation">(</span>i <span class="token operator">%</span> dbShard<span class="token punctuation">,</span> i <span class="token operator">%</span> tableShard<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token punctuation">(</span>k<span class="token punctuation">,</span> v<span class="token punctuation">)</span> <span class="token operator">-></span> v <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
count<span class="token punctuation">.</span><span class="token function">forEach</span><span class="token punctuation">(</span><span class="token punctuation">(</span>k<span class="token punctuation">,</span>v<span class="token punctuation">)</span> <span class="token operator">-></span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>k <span class="token operator">+</span> <span class="token string">":"</span> <span class="token operator">+</span> v<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>

<p>因为前司我经手的项目用的都是分表,还没有到分库,没有意识到这个问题,也算是一点点小经验吧,要考虑下分库分表的规则组合会不会导致数据倾斜.</p>
<p>其他还有些实践中的问题,当时没有记录把配置整对之后也不知道怎么复现了.<br>不得不说shardingsphere-jdbc的易用性是非常高了,通俗易懂.</p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>shardingsphere官网: <a href="https://shardingsphere.apache.org/">https://shardingsphere.apache.org</a><br>shardingsphere-jdbc配置: <a href="https://shardingsphere.apache.org/document/current/cn/user-manual/shardingsphere-jdbc/yaml-config/">https://shardingsphere.apache.org/document/current/cn/user-manual/shardingsphere-jdbc/yaml-config/</a><br>shardingsphere FAQ: <a href="https://shardingsphere.apache.org/document/current/cn/faq/">https://shardingsphere.apache.org/document/current/cn/faq/</a><br><a href="https://stackoverflow.com/questions/14537546/how-to-get-generated-id-after-i-inserted-into-a-new-data-record-in-database-usin">How to get generated ID after I inserted into a new data record in database using Spring JDBCTemplate?</a>    </p>
]]></content>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Shopify App订阅 APPLY_ON_NEXT_BILLING_CYCLE实在太坑了</title>
    <url>/2024/03/08/shopify-app-subscription-apply-on-next-billing-cycle-sucks/</url>
    <content><![CDATA[<h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>最近，我们在开发Shopify应用时，遇到了一个非常恼人的问题。我们希望允许商家降级他们当前的订阅计划，但是要求降级要在下一个计费周期才生效，当前的计划应该保持不变直到下一个计费周期。</p>
<p>我们发现了Shopify的 <code>APPLY_ON_NEXT_BILLING_CYCLE</code> 替换行为，本以为这就是我们想要的功能。但是，在测试时，我们发现当前的订阅立即被取消，降级后的订阅直接变成了 <code>ACTIVE</code> 状态，而这个行为应该是在下一个计费周期才触发的。</p>
<span id="more"></span>

<h2 id="Shopify，你在逗我吗？"><a href="#Shopify，你在逗我吗？" class="headerlink" title="Shopify，你在逗我吗？"></a>Shopify，你在逗我吗？</h2><p>这特么是什么鬼设计？文档里写得清清楚楚，降级的订阅应该在下一个计费周期才生效，但实际上却是立即生效。这不是坑人吗？</p>
<p>更可笑的是，测试订阅和正式订阅的行为还不一致，<code>test:true</code>只会发生订阅的直接替换,旧订阅被直接取消,新订阅立即生效,订阅周期立即调整为新订阅,不会出现文档中的各种行为。这不是在逼我们用正式订阅来测试吗？那测试订阅还有个毛用？</p>
<h2 id="订阅和计费周期分不清？"><a href="#订阅和计费周期分不清？" class="headerlink" title="订阅和计费周期分不清？"></a>订阅和计费周期分不清？</h2><p>Shopify似乎把订阅和计费周期的概念混淆了。对他们来说，只要有新的计费周期，旧的订阅就可以立即取消，新的订阅就可以立即生效。但对我们开发者来说，我们需要知道哪些订阅是活跃的，哪些是延期的。</p>
<p>现在Shopify的做法，让我们根本无法区分一个订阅是否应该保持活跃状态。他们只关心到什么时间节点需要向用户收多少钱。  </p>
<p>我们app降级的例子,这个例子在standard下会触发延期: </p>
<pre class="language-none"><code class="language-none">STANDARD  
Cancels the merchant&#39;s current app subscription immediately and replaces it with the newly created app subscription, with the exception of the following scenarios where replacing the current app subscription will be deferred until the start of the next billing cycle.  
1. The current app subscription is annual and the newly created app   subscription is annual, using the same currency, but is of a lesser value.  
2. The current app subscription is annual and the newly created app subscription is monthly and using the same currency.  
3. The current app subscription and the newly created app subscription are identical except for the discount value.  </code></pre>

<p>用户先购买了Pro的年付套餐,下一个bill时间是2025-03-08,然后他降级到了Basic的月付,猜猜shopify会返回什么?<br>它会返回active的套餐是Basic,下一个bill时间是2025-03-08!!!!!<br>而我们期望的是他返回active的套餐是Pro年付,到2025-03-08之后再变成Basic月付.  </p>
<p>shopify只关心了他要在2025年3月8日收取用户Basic的套餐, 但是他完全忽略了当前active的应该是Pro套餐.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Shopify的这个订阅替换功能，简直就是一个大坑。</p>
<p>我真的是忍无可忍，只能在这里吐槽一下。Shopify，你们好歹也是一个大公司，能不能把这些基础功能设计得更人性化一点？不要再坑开发者了好吗？  </p>
<p>相比之下Stripe的设计就合理的多,它会保持套餐的有效,直到下一个周期才会改变套餐.</p>
<p>参考链接:  </p>
<p><a href="https://shopify.dev/docs/api/admin-graphql/2023-10/enums/AppSubscriptionReplacementBehavior">https://shopify.dev/docs/api/admin-graphql/2023-10/enums/AppSubscriptionReplacementBehavior</a></p>
<p><a href="https://community.shopify.com/c/webhooks-and-events/working-of-appsubscriptionreplacementbehavior-apply-on-next/td-p/2002698">https://community.shopify.com/c/webhooks-and-events/working-of-appsubscriptionreplacementbehavior-apply-on-next/td-p/2002698</a></p>
]]></content>
      <tags>
        <tag>shopify</tag>
      </tags>
  </entry>
  <entry>
    <title>shopify插件开发的问题记录</title>
    <url>/2023/08/19/shopify-plugin-development-issue-tracking/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>Shopify作为流行的电商平台,支持安装各种插件扩展功能<br>插件开发中难免会遇到各种问题,记录和跟踪这些问题非常重要<br>emmmm…之前遇到过一些问题,但时间有点长记得不清楚了… …<br>这里先记录一些最近遇到的,之后再不断补充</p>
<span id="more"></span>  

<h1 id="商家卸载-重新安装token会改变"><a href="#商家卸载-重新安装token会改变" class="headerlink" title="商家卸载/重新安装token会改变"></a>商家卸载/重新安装token会改变</h1><p>不要使用token作为商家的唯一标识符, token会在商家卸载重装之后发生改变.<br>可以使用shop的Id作为唯一标识符, 这个是不变的.<br>并且记得用户重新安装之后更新token.  </p>
<p>相关api： <a href="https://shopify.dev/docs/api/admin-rest/2023-07/resources/shop">https://shopify.dev/docs/api/admin-rest/2023-07/resources/shop</a></p>
<h1 id="admin-api获取商品分页问题"><a href="#admin-api获取商品分页问题" class="headerlink" title="admin api获取商品分页问题"></a>admin api获取商品分页问题</h1><p>相关api: <a href="https://shopify.dev/docs/api/admin-rest/2023-07/resources/product#get-products?ids=632910392,921728736">Products &gt; Product &gt; Retrieve a list of products</a></p>
<p>这个api提供了一个sinceId和limit的参数,看着就是用于分页的.  </p>
<blockquote>
<p>since_id<br>Return only products after the specified ID.</p>
</blockquote>
<blockquote>
<p>limit ≤ 250 default 50<br>Return up to this many results per page.</p>
</blockquote>
<p>但这里有个细节文档没写, 如果你第一次不传sinceId=0,那么返回的list不是按照id排序的!<br>他会按照title排序导致第二次的分页结果是错误的.<br>因为这个带有一定的随机性, 而且数据本身是正常返回的, 可能不太容易发现这个问题.</p>
<p>社区里问这个的挺多的:<br><a href="https://community.shopify.com/c/graphql-basics-and/rest-api-pagination-with-since-id/m-p/1345075">Rest Api Pagination With Since_id</a>  </p>
<p>官方博客里也有:<br><a href="https://www.shopify.com/nz/partners/blog/relative-pagination">How to Use Relative Pagination In Your Application</a>  </p>
<blockquote>
<p> To ensure the first page is sorted by id, you can include a since_id parameter with a value of 0. This form of pagination already existed on all endpoints prior to the 2019-07 API version.  </p>
</blockquote>
<p>应该是当成一个默认已知的约定了吧…</p>
]]></content>
      <tags>
        <tag>shopify</tag>
        <tag>troubleshooting</tag>
      </tags>
  </entry>
  <entry>
    <title>短信服务构建总结</title>
    <url>/2019/03/31/sms-server-building-summary/</url>
    <content><![CDATA[<p>又来水文了…<br>但感觉没什么其他内容可写,就将之前做的一个消息平台稍微做点总结.<br>简单说说短信模块的实现,做的东西不复杂,权当总结了~.  </p>
<span id="more"></span>
<h1 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h1><h2 id="功能需求"><a href="#功能需求" class="headerlink" title="功能需求"></a>功能需求</h2><p>功能需求比较直接.<br>需要提供一些短信和统计功能即可:  </p>
<ul>
<li>通知短信</li>
<li>验证码短信</li>
<li>语音短信</li>
<li>发送量统计  </li>
</ul>
<h2 id="非功能需求"><a href="#非功能需求" class="headerlink" title="非功能需求"></a>非功能需求</h2><p>主要是以下几点  </p>
<ul>
<li>稳定的发送保证:<br>分为两部分,一个是服务本身的稳定性,另一个是短信服务提供商的稳定性.<br>服务本身稳定是自己需要实现的,可以通过微服务平台已经有的一些工具保证,这里不进行展开.而另一个是需要外部保证的,外部需要保证的东西永远是脆弱的,所以不能仅仅使用一家服务商,需要使用多家.  </li>
<li>方便扩展:<br>接着第一点,服务商可能会有更换的需求,需要保证能够方便接入下架服务商.</li>
<li>资产保护:<br>短信相比邮件,微信通知等的一个不同是他花费比较高,一条的计价在3分左右,较长的短信会被拆为多条,如果不加限制进行保护很容易造成大量的资损.</li>
</ul>
<h1 id="技术选型"><a href="#技术选型" class="headerlink" title="技术选型"></a>技术选型</h1><p>有了上面的需求之后就是选用什么技术了.<br>基础的web服务就使用公司现有的即可.<br>对于存储,因为涉及到发送信息和报告的记录,量可能会很大(其实上线之后也还好,3个月不到千万记录),统计时可能会按照某些发送内容统计,使用MySQL之类的可能会不太靠谱,选择使用ES.<br>对于安全,一定需要做手机号每日发送量和发送频率等的限制,这个用redis可以较为轻松解决.  </p>
<p>最后一个简化版的架构图如下:<br><img src="/images/1244488-20190331180608613-425422922.png"></p>
<h1 id="实现细节"><a href="#实现细节" class="headerlink" title="实现细节"></a>实现细节</h1><p>上面都是这个服务战略上的一些东西,这里说一些战术上的.  </p>
<h2 id="接入多家服务商"><a href="#接入多家服务商" class="headerlink" title="接入多家服务商"></a>接入多家服务商</h2><p>需要有多家服务商的接入,一家服务商可能同时供应多个短信功能.<br>同时同一个短信功能需要轮流调用所有提供的服务商,并在一家失败后进行重试.<br>也需要支持灵活的配置,可以禁用掉一些服务商,也可以方便加入新的.<br>在实现上先定义了各个发送功能接口,例如:  </p>
<pre class="language-none"><code class="language-none">interface NotiSmsSender &#123;
    SmsSubmitResult sendNoti(NotiRequest request);
&#125;  


interface VerifyCodeSmsSender &#123;
    SmsSubmitResult sendVerifyCode(VerifyCodeRequest request);
&#125;</code></pre>
<p>对应的服务商实现不同的接口,提供不同的服务,每个对应的实现都有一个id字段,用于之后的配置.  </p>
<p>现在有了一坨sender,接下来要定义router,用来进行请求的分发.<br>router内含有sender列表,配置router内的sender就可以实现服务商的下线功能了.<br>大致流程如下:<br><img src="/images/1244488-20190401021438477-1987189345.png">  </p>
<p>实际实现中使用了Akka,各个sender是一个actor,router负责监督各个sender的健康情况,此外天然获得了超时时间的能力(底层使用了异步HttpClient,actor内部不会发生阻塞).</p>
<h2 id="发送限制设置"><a href="#发送限制设置" class="headerlink" title="发送限制设置"></a>发送限制设置</h2><p>关于手机号和ip的发送数量计数使用<code>reids</code>的<code>incr</code>配合时间戳相关的key就可以很简单完成,这里不累述这个.  </p>
<p>发送限制的话首先要针对不同功能,因为不同功能的使用量是截然不同的,并且也有隔离的作用,一个功能超限不能影响其他功能.  </p>
<p>其次,发送的限制需要分层级,当达到了全局最大限制后,就算没有超过单手机限制也不应该发出去了.<br>使用的层次是:<br>功能全局次数(次数) -&gt; 单接入应用限制(次数/白名单/开关) -&gt; 单手机/ip限制(次数/白名单/开关) -&gt; 风控服务(开关)<br>最后次数要根据统计情况和业务更改做适当调整.  </p>
<h2 id="性能"><a href="#性能" class="headerlink" title="性能"></a>性能</h2><p>这个服务很明显是IO密集的,就没什么计算,主要是调用各种外部服务,等IO返回.<br>所以对于这些各个用来做请求的线程池就可以配置稍微大一点,实际最主要的就是HTTP的连接池.但连接超时,读取超时也不适合设置过大.<br>实际测试也没多大问题,平日一天短信请求量也在几w左右,并没有太大的瓶颈.    </p>
<p>其他的一些点和普通的web服务也大同小异了,没有什么特别的技巧.<br>其实之前一些文章也是在构建这个服务的时候写的,这段时间没写什么就当划水了,比如:<br><a href="https://www.cnblogs.com/fairjm/p/10035181.html">Akka实践一些总结</a><br><a href="https://www.cnblogs.com/fairjm/p/elasticsearch_in_trouble.html">ES踩坑笔记</a><br>这个服务构建的时候根据需要用了些之前没有使用的组件,也算额外获得了一些回报吧~</p>
]]></content>
  </entry>
  <entry>
    <title>杂谈20190505</title>
    <url>/2019/05/05/snippet-20190505/</url>
    <content><![CDATA[<p>本来说是每个月至少写一篇的,似乎上个月不小心直接鸽了…  </p>
<span id="more"></span>
<p>有一些想写的东西,比如之前看到<code>blazor</code>或者<code>LiveView</code>,写点前后端渲染相关的,但又感觉自己的知识面过于狭窄,内容不够全不说可能还会写错误导别人所以作罢.甚至还想写瓶颈优化类的,但自己写的代码那么屎都没自觉还写文说怎么优化… …<br>就杂聊一些近况吧.<br>(说到<code>blazor</code>,这东西改名真够频繁的,最开始是一个实验性质项目,之后吸收到 .NET Core 3.最开始是blazor,服务端渲染是razor component,在某一次改动后变成了整个叫razor component,blazor是client hosting model,最近因为概念理解起来会造成障碍又改回了第一个….真的是够折腾的) </p>
<p>最近在(重新)看<code>elixir</code>,上一次似乎是4年多快5年前了,过了这么多年,<code>elixir</code>的流行程序依旧那样,看来是火不了了:(,适合做中间件的语言,结果始终没有火起来,然而新兴的<code>Rust</code>和<code>Go</code>已经在攻城略地.<br>这充分说明语言的流行与否与语言本身优秀与否可能关系不大(举个不恰当的例子,<code>java</code>和<code>C#</code>在国内的流行程度),而与宣发和大厂背书可能关系大一点.<br>更悲惨的是<code>erlang</code>了,在流行度上面.<br>说到这个,上个月<code>erlang</code>之父 Joe Armstrong过世了,非常突然,毕竟前几天还看到在推上和人聊硬件编程相关的事,还有之前的阿怪,一些熟悉的人名不经意间就已经不在了.  </p>
<p>说到<code>elixir</code>有一些感悟,详细的实现还没看,基本也是看一些说明性质的东西.<br>因为本身就是基于<code>BEAM</code>的,所以天然获得了抢占式的process调度,这点和协作式的协程有很大不同,基于yield主动放弃控制权转移给其他人的协程 vs 每个process有固定的时间片,各有利弊.<br>但这一切也都是基于系统线程上的,所以遇到的问题也是类似,遇上阻塞的系统调用怎么办.<br><code>elixir</code>(BEAM)这边的话还是老套路用IO线程池,调度器发现阻塞调用时会把阻塞的任务丢给线程池,把当前process挂起,让对应的线程去执行其他process,这点和<code>Go</code>的思想是类似的(但<code>Go</code>的做法是将当前P的goroutine交给其他的P).<br>这些本质上其实都是一些魔法.<br>便于编写代码的魔法.<br>回到java的世界,没有这样的运行时,没有这样的发现阻塞调用的机制.这些操作都是手动做的,把阻塞的IO扔到IO线程池做异步化自己写回调,实现的东西是类似的,不过因为是要手写,所以代码的好坏取决于人,又因为是协作的,所以java在少糖少魔法的情况下,代码的质量水平可能会相对堪忧.<br>语法糖和各种魔法还是很重要的.在工程角度上.  </p>
<p>上面说到<code>Go</code>,想起了群里讨论的时候M P G里的P被说成了核,但是一脸蒙蔽,M已经是系统线程了,M应该是接近调度器之类的东西吧,M下挂个核是什么意思? 后来找了下原文,发现原来是个类比… …</p>
<blockquote>
<p>A P can be thought of like a CPU in the OS scheduler and the contents of the p type like per-CPU state.   </p>
</blockquote>
<p>一些翻译不走心然后各种引用就这么传下来了…  </p>
<p>基础概念不也是这样吗,感觉自己年少无知的一些概念完全是错的,最近也是打自己脸打得疼,这些概念可能不直接影响写的代码,但是影响看事的广度和深度.比如把AIO和多线程绑定,觉得使用AIO会造成线程数增多,上下文开销增加,但首先使用IOCP这种和线程无关的实现不会增加多少线程,其次,IO线程池的线程主要被阻塞并不会有运行CPU密集任务那样来的那么大开销,不过开多了内存占用倒是会变多.(上述这些话不知道什么时候又要打脸了)  </p>
<p>差不多也到这样.<br>本来想写点工作之类的,但想想毕竟和公司相关,还是算了.工作这种每个人有自己感悟,每个人也参与其中,写出来大抵也差不多,还是算了.  </p>
<hr>
<p>一些资料:<br><a href="https://dotnet.microsoft.com/apps/aspnet/web-apps/client">https://dotnet.microsoft.com/apps/aspnet/web-apps/client</a>  </p>
<p><a href="https://shift.infinite.red/phoenix-liveview-round-up-the-story-so-far-3cbb1648e940">https://shift.infinite.red/phoenix-liveview-round-up-the-story-so-far-3cbb1648e940</a>  </p>
<p><a href="https://hackernoon.com/remembering-joe-a-quarter-of-a-century-of-inspiration-and-friendship-3ddded0831ab">https://hackernoon.com/remembering-joe-a-quarter-of-a-century-of-inspiration-and-friendship-3ddded0831ab</a></p>
<p><a href="https://github.com/golang/go/blob/master/src/runtime/HACKING.md">https://github.com/golang/go/blob/master/src/runtime/HACKING.md</a></p>
]]></content>
  </entry>
  <entry>
    <title>spring boot应用 从mysql迁移h2</title>
    <url>/2022/03/20/spring-boot-mysql-to-h2/</url>
    <content><![CDATA[<p>此前有个应用用了<code>mysql</code>做数据存储，但实际存储的数据很少就几张表加起来一共几千行，并且一直没有修改的需求，应用内也只是进行查询。<br>实际上是静态的数据。  </p>
<p>因为使用的是云服务商的产品，想着之后就不续费了需要将数据迁移。  </p>
<p>有几个选项：  </p>
<ol>
<li>改用文件存储，读取到内存后处理；</li>
<li>换成嵌入式数据库。  <span id="more"></span>
1和2都需要将数据倒出并重新组织，并且1的成本更高需要修改之前<code>DAL</code>的逻辑。<br>于是选择2，考虑是<code>java</code>的应用且需要和<code>mysql</code>有比较好的兼容，考虑使用<code>H2</code>。  </li>
</ol>
<p>又因为数据量很少，占用内存不大，并且每次初始化也占用很少的时间，那考虑直接用内存的模式，应用启动的时候进行初始化。  </p>
<h1 id="迁移步骤"><a href="#迁移步骤" class="headerlink" title="迁移步骤"></a>迁移步骤</h1><h2 id="引入依赖"><a href="#引入依赖" class="headerlink" title="引入依赖"></a>引入依赖</h2><p>首先需要增加H2依赖。<br>在<code>pom.xml</code>引入：</p>
<pre class="language-xml" data-language="xml"><code class="language-xml"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">></span></span>com.h2database<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">></span></span>h2<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">></span></span>
    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>version</span><span class="token punctuation">></span></span>$&#123;h2.version&#125;<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>version</span><span class="token punctuation">></span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">></span></span></code></pre>

<h2 id="修改配置"><a href="#修改配置" class="headerlink" title="修改配置"></a>修改配置</h2><p>原先的配置需要从<code>mysql</code>迁移至<code>H2</code>.<br>在<code>application.properties</code>中修改datasource配置:  </p>
<pre class="language-none"><code class="language-none">spring.datasource.url&#x3D;jdbc:h2:mem:dbname;MODE&#x3D;MYSQL;DATABASE_TO_LOWER&#x3D;TRUE</code></pre>
<p>不需要配user和password.  </p>
<h2 id="表-数据迁移"><a href="#表-数据迁移" class="headerlink" title="表/数据迁移"></a>表/数据迁移</h2><p>这一步可以用数据库管理工具，例如<code>datagrip</code>，<code>dbeaver</code>等创建一个<code>h2</code>数据库做实验。<br>让spring初始化只需要在<code>resources</code>下放<code>schema.sql</code>和<code>data.sql</code>即可。<br>前者放表定义，后者放导入sql的语句。  </p>
<p>表的DDL可以通过<code>show create table table_name</code>得到，或者用数据库管理工具导出即可。<br>兼容需要一定的修改：  </p>
<ol>
<li>去除<code>unsigned</code>；</li>
<li>timestamp/datetime的 <code>ON UPDATE</code> 语句不支持，因为这里不会更新直接去掉；</li>
<li>表的comment和charset去除；</li>
<li>bit的默认值的b去除。</li>
</ol>
<p>大致这么处理即可。<br>其他的部分基本兼容。  </p>
<p>数据导入用mysqldump或者数据库管理工具倒出即可。<br>这一步因为只有整型，字符串，时间等类型也没有任何冲突。  </p>
<p>将DDL放入<code>schema.sql</code>，初始化数据的sql放入<code>data.sql</code>即可。  </p>
<h2 id="应用SQL修改"><a href="#应用SQL修改" class="headerlink" title="应用SQL修改"></a>应用SQL修改</h2><p>应用用的是<code>mybatis</code>，进行测试，找出不兼容的语句。<br>比较多的是在<code>H2</code>中<code>&quot;</code>是表示列名，字符串修改为<code>&#39;</code>即可，以及bit不需要b前缀。<br>因为没有更多复杂的查询，所以这一步还算顺利。  </p>
<h2 id="乱码处理"><a href="#乱码处理" class="headerlink" title="乱码处理"></a>乱码处理</h2><p>因为导入的sql文件有编码，在不同平台<code>file.encoding</code>的值不一致，会出现导入的数据乱码的问题。<br>一般把sql文件设置为<code>UTF-8</code>，应用启动时指定<code>-Dfile.encoding=utf-8</code>即可（<code>java -jar</code>启动系统参数要放在<code>jar</code>包前，不然无效）。<br>或者在main中用  <code>System.setProperty(&quot;file.encoding&quot;, &quot;utf-8&quot;);</code> 指定。  </p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p><a href="http://matthewcasperson.blogspot.com/2013/07/exporting-from-mysql-to-h2.html">Exporting from MySQL to H2</a><br><a href="https://www.baeldung.com/spring-boot-h2-database">Spring Boot With H2 Database</a>  </p>
]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>H2</tag>
        <tag>spring boot</tag>
      </tags>
  </entry>
  <entry>
    <title>Prometheus使用入门</title>
    <url>/2018/08/14/start-prometheus/</url>
    <content><![CDATA[<p>Monitoring with Prometheus读书笔记<br>原书见:<br><a href="https://www.safaribooksonline.com/library/view/monitoring-with-prometheus/9780988820289/">https://www.safaribooksonline.com/library/view/monitoring-with-prometheus/9780988820289/</a>  </p>
<hr>
<span id="more"></span>
<p>摘录了一些内容,稍微整理了下,更详细的内容可以阅读原书.   </p>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>Prometheus灵感来源于谷歌的borgmon.<br>他的设计专注于最近发生的事,而不是几个星期几个月前的,默认只保存15天的时间序列.  </p>
<p>内置PromQL查询语言,可以方便进行metric查询和定义新的metric.  </p>
<p>数据格式</p>
<pre class="language-none"><code class="language-none">&lt;time series name&gt;&#123;&lt;label name&gt;&#x3D;&lt;label value&gt;, ...&#125;  </code></pre>
<p>例子:  </p>
<pre class="language-none"><code class="language-none">total_website_visits&#123;site&#x3D;&quot;MegaApp&quot;, location&#x3D;&quot;NJ&quot;, instance&#x3D;&quot;webserver&quot;,job&#x3D;&quot;web&quot;&#125;</code></pre>

<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>安装过程比较简单,官网下载之后解压即可.<br>windows下可以使用choco安装:  </p>
<pre class="language-none"><code class="language-none">choco install prometheus</code></pre>

<p>也可以使用docker运行:  </p>
<pre class="language-none"><code class="language-none">docker run -p 9090:9090 prom&#x2F;prometheus  </code></pre>

<p>解压后自带一个配置文件,启动时带上文件路径即可:  </p>
<pre class="language-none"><code class="language-none">prometheus --config.file &quot;&#x2F;etc&#x2F;prometheus&#x2F;prometheus.yml&quot;</code></pre>
<p>(放置在了<code>/etc/prometheus</code>下)  </p>
<p>默认跑在9090端口.  </p>
<h1 id="监控容器和节点"><a href="#监控容器和节点" class="headerlink" title="监控容器和节点"></a>监控容器和节点</h1><p>节点(机器)监控可以使用node exporter.<br>下载node exporter 解压 复制到可执行中</p>
<pre class="language-none"><code class="language-none">wget https:&#x2F;&#x2F;github.com&#x2F;prometheus&#x2F;node_exporter&#x2F;releases&#x2F;download&#x2F;v0.16.0&#x2F;node_exporter-0.16.0.linux-amd64.tar.gz
$ tar -xzf node_exporter-*
$ sudo cp node_exporter-*&#x2F;node_exporter &#x2F;usr&#x2F;local&#x2F;bin&#x2F;</code></pre>
<p>默认是9100端口,可以通过<code>/metrics</code>访问.<br>收集的数据较多,不需要的指标可以通过<code>--no-模块名</code>禁用,同时一些没有开启的模块可以通过<code>--模块名</code>开启.  </p>
<p>容器监控可以使用cadvisor  </p>
<pre class="language-none"><code class="language-none">docker run \
  --volume&#x3D;&#x2F;:&#x2F;rootfs:ro \
  --volume&#x3D;&#x2F;var&#x2F;run:&#x2F;var&#x2F;run:rw \
  --volume&#x3D;&#x2F;sys:&#x2F;sys:ro \
  --volume&#x3D;&#x2F;var&#x2F;lib&#x2F;docker&#x2F;:&#x2F;var&#x2F;lib&#x2F;docker:ro \
  --volume&#x3D;&#x2F;dev&#x2F;disk&#x2F;:&#x2F;dev&#x2F;disk:ro \
  --publish&#x3D;8080:8080 \
  --detach&#x3D;true \
  --name&#x3D;cadvisor \
  google&#x2F;cadvisor:latest</code></pre>
<p>监听在8080端口.  </p>
<p>对应的Prometheus配置修改:  </p>
<pre class="language-yml" data-language="yml"><code class="language-yml">scrape_configs:
  - job_name: &#39;prometheus&#39;
    static_configs:
      - targets: [&#39;localhost:9090&#39;]
  - job_name: &#39;node&#39;
    static_configs:
      - targets: [&#39;138.197.26.39:9100&#39;, &#39;138.197.30.147:9100&#39;, &#39;138.197.30.163:9100&#39;]
  - job_name: &#39;docker&#39;
    static_configs:
      - targets: [&#39;138.197.26.39:8080&#39;, &#39;138.197.30.147:8080&#39;, &#39;138.197.30.163:8080&#39;]</code></pre>
<p>(ip等信息写在targets中)  </p>
<h2 id="Label"><a href="#Label" class="headerlink" title="Label"></a>Label</h2><p>注意改变一个label或增加一个新的label会创建一个新的time series.  </p>
<p>对收集的metrics的label进行操作.<br>可以去掉不必要的metrics和label,增加编辑label.  </p>
<p>有两个阶段可以修改:  </p>
<ul>
<li>relabel_configs:用来修改服务发现过来的元数据label.  </li>
<li>metric_relabel_configs:获得数据存储到磁盘之前操作  </li>
</ul>
<p>如图所示:<br><img src="/images/1244488-20180815003536046-1338003383.png"></p>
<p>一些配置(和job_name平级):  </p>
<pre class="language-none"><code class="language-none">metric_relabel_configs:
  - source_labels: [__name__]
    separator: &#39;,&#39;
    regex: &#39;(container_tasks_state|container_memory_failures_total)&#39;
    action: drop
</code></pre>
<p><code>__name__</code>是metrics的保留label,表示这个metric的名字,这里将这些名字用<code>,</code>拼起来,过滤掉符合正则的名字的metrics.  </p>
<pre class="language-none"><code class="language-none">metric_relabel_configs:
- source_labels: [id]
  regex: &#39;&#x2F;docker&#x2F;([a-z0-9]+);&#39;
  replacement: &#39;$1&#39;
  target_label: container_id  </code></pre>
<p>替换掉id符合正则的内容产生另一个名为container_id的label.<br>此外有一个<code>honor_labels</code>的参数可以控制替换后的label和现有重名的问题,默认为false,现有的冲突会加上exported_的前缀.设置为true,会忽略该替换的label.  </p>
<pre class="language-none"><code class="language-none">metric_relabel_configs:
  - regex: &#39;kernelVersion&#39;
    action: labeldrop</code></pre>
<p>移除掉现有符合正则的label.  </p>
<h2 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics"></a>Metrics</h2><h3 id="CPU利用率"><a href="#CPU利用率" class="headerlink" title="CPU利用率"></a>CPU利用率</h3><p>使用node_cpu_seconds_total<br>如图:<br><img src="/images/1244488-20180815012218488-729181577.png"></p>
<p>计算5min为取样每秒的瞬时利用率</p>
<pre class="language-none"><code class="language-none">100 - avg (irate(node_cpu_seconds_total&#123;job&#x3D;&quot;node&quot;,mode&#x3D;&quot;idle&quot;&#125;[5m])) by (instance) * 100</code></pre>
<p>5m是range vector,表示使用记录的上一个5分钟的数据.<br>irate是瞬时变化率,适合变化较频繁的metric.<br>avg是因为有多个metrics(分别是每核的)  </p>
<h3 id="CPU饱和度"><a href="#CPU饱和度" class="headerlink" title="CPU饱和度"></a>CPU饱和度</h3><p>node_load*<br>后面的*有1 5 15,表示多少分钟.<br>该metric和核数有关,查看核数可以使用:</p>
<pre class="language-none"><code class="language-none">count by (instance)(node_cpu_seconds_total&#123;mode&#x3D;&quot;idle&quot;&#125;)</code></pre>

<h3 id="内存使用"><a href="#内存使用" class="headerlink" title="内存使用"></a>内存使用</h3><p>node_memory_*<br>列出空闲内存  </p>
<pre class="language-none"><code class="language-none">(node_memory_MemTotal_bytes - (node_memory_MemFree_bytes + node_memory_Cached_bytes + node_memory_Buffers_bytes)) &#x2F; node_memory_MemTotal_bytes * 100</code></pre>

<h3 id="内存饱和度"><a href="#内存饱和度" class="headerlink" title="内存饱和度"></a>内存饱和度</h3><p>监控paging in和paging out的内存<br>/proc/vmstat<br>node_vmstat_pswpin<br>node_vmstat_pswpout<br>单位是KB  </p>
<pre class="language-none"><code class="language-none">1024 * sum by (instance) (
       (rate(node_vmstat_pgpgin[1m])
       + rate(node_vmstat_pgpgout[1m]))
)</code></pre>
<p>算出每分钟的变化量<br>rate是平均变化率,相比irate适合变化幅度不剧烈的数据.  </p>
<h3 id="磁盘使用率"><a href="#磁盘使用率" class="headerlink" title="磁盘使用率"></a>磁盘使用率</h3><p>node_filesystem_*<br>使用率:</p>
<pre class="language-none"><code class="language-none">(node_filesystem_size_bytes&#123;mountpoint&#x3D;&quot;&#x2F;&quot;&#125; - node_filesystem_free_bytes&#123;mountpoint&#x3D;&quot;&#x2F;&quot;&#125;) &#x2F; node_filesystem_size_bytes&#123;mountpoint&#x3D;&quot;&#x2F;&quot;&#125; * 100</code></pre>
<p>可以用=<del>匹配正则 相对的有!</del></p>
<pre class="language-none"><code class="language-none">predict_linear(node_filesystem_free_bytes&#123;job&#x3D;&quot;node&quot;&#125;[1h], 4*3600) &lt; 0</code></pre>
<p>通过1h的估计量 看看4h后是否会用完</p>
<h3 id="exporter的可用性监控"><a href="#exporter的可用性监控" class="headerlink" title="exporter的可用性监控"></a>exporter的可用性监控</h3><p>可以使用up.  </p>
<pre class="language-none"><code class="language-none">up&#123;job&#x3D;&quot;node&quot;&#125;</code></pre>
<p>返回1表示node_exporter存活.  </p>
<h2 id="持久化Query"><a href="#持久化Query" class="headerlink" title="持久化Query"></a>持久化Query</h2><p>除了直接查询,query还可以在其他场合持久化使用:  </p>
<ul>
<li>Recording rule - 通过query创建新的metrics</li>
<li>Alerting rules - 生成报警</li>
<li>Visualization - 可视化  </li>
</ul>
<p>设置prometheus.yml的rules_files块,增加一个新的rule:  </p>
<pre class="language-none"><code class="language-none">rule_files:
  - &quot;rules&#x2F;node_rules.yml&quot;</code></pre>
<p>对应的该文件内容如下:  </p>
<pre class="language-none"><code class="language-none">groups:
- name: node_rule
  rules:
  - record: instance:node_cpu:avg_rate5m
    expr: 100 - avg (irate(node_cpu_seconds_total&#123;job&#x3D;&quot;node&quot;,mode&#x3D;&quot;idle&quot;&#125;[5m])) by (instance) * 100</code></pre>
<p>配置文件修改后可以使用<code>promtool</code>的对应命令,比如<code>promtool check rules node_rules.yml</code>进行检查.  </p>
<p>可以在Prometheus上通过<code>/rules</code>查看:<br><img src="/images/1244488-20180815013756421-454186948.png"></p>
<h2 id="可视化"><a href="#可视化" class="headerlink" title="可视化"></a>可视化</h2><p>使用grafana<br>centos下安装:</p>
<pre class="language-none"><code class="language-none">wget https:&#x2F;&#x2F;s3-us-west-2.amazonaws.com&#x2F;grafana-releases&#x2F;release&#x2F;grafana-5.2.2-1.x86_64.rpm 
sudo yum localinstall grafana-5.2.2-1.x86_64.rpm </code></pre>

<p>配置在<code>/etc/grafana/grafana.ini</code>.<br>通过<code>sudo service grafana-server start</code>启动服务.<br>默认使用3000端口.<br>默认用户名和密码在配置文件,为admin:admin.<br>登陆后增加数据源选择Prometheus即可.<br>然后可以新增dashboard,配置对应的查询:<br><img src="/images/1244488-20180815014142829-114979814.png"></p>
<hr>
<p>文中可能会有不少错误,欢迎提出.<br>原书的内容更为详尽和丰富,有兴趣的可以阅读原书.  </p>
<hr>
<p>参考资料:<br><a href="https://prometheus.io/docs/prometheus/latest/getting_started/">https://prometheus.io/docs/prometheus/latest/getting_started/</a>   </p>
<p><a href="https://github.com/prometheus/node_exporter">https://github.com/prometheus/node_exporter</a></p>
<p><a href="https://github.com/google/cadvisor">https://github.com/google/cadvisor</a> </p>
<p><a href="https://grafana.com/">https://grafana.com/</a>  </p>
<p><a href="https://legacy.gitbook.com/book/songjiayang/prometheus/details">https://legacy.gitbook.com/book/songjiayang/prometheus/details</a></p>
]]></content>
  </entry>
  <entry>
    <title>异步/同步,阻塞/非阻塞,单线程/多线程概念梳理</title>
    <url>/2019/02/24/sync-async-blocking-multithre/</url>
    <content><![CDATA[<p>最近看了一文说到异步一定是单线程的,顿时就傻眼了,对相关概念和同事进行了一些讨论和总结.<br>文中的描述是以我的理解来说的,可能不一定准确甚至正确,有错欢迎指正.  </p>
<p>这三个概念我认为是描述不同的维度的,概念正交.    </p>
<span id="more"></span>
<h1 id="异步-同步"><a href="#异步-同步" class="headerlink" title="异步 同步"></a>异步 同步</h1><p>异步和同步是不同的流程设计风格.<br>但存在依赖关系的操作之间是同步的,也就是如果操作B依赖操作A的返回,那么B必须要在A结束后才能执行.<br>比如你要读取文件然后对文件内容进行处理,那么读取内容和处理内容就是同步的.  </p>
<p>而异步这是操作间没有依赖关系,或者先后顺序并不重要.<br>比如用户登陆要给登陆奖励,在确认用户可登陆后之后的登陆流程和发放奖励间并无依赖关系,那么他们就可以异步执行.  </p>
<p>这个概念主要描述依赖关系和流程设计.  </p>
<h1 id="阻塞-非阻塞"><a href="#阻塞-非阻塞" class="headerlink" title="阻塞 非阻塞"></a>阻塞 非阻塞</h1><p>这个概念是描述操作是否会立即返回的.<br>这个概念常常和异步/同步混在一起.<br>同步就一定是阻塞的吗,我觉得这不一定,主要还是取决于同步执行里的各个操作是否是阻塞的.  </p>
<p>非阻塞操作可以被用在异步API的实现.<br>基于单线程的异步API实现里的操作一般都会要求为非阻塞.  </p>
<h1 id="单线程-多线程"><a href="#单线程-多线程" class="headerlink" title="单线程 多线程"></a>单线程 多线程</h1><p>这两个概念常和异步 同步混在一起.<br>比如认为异步和多线程绑定的,但其实不然,有很多基于Eventloop的单线程实现.<br>单线程 多线程主要描述的是运行的环境.<br>如图:<br><img src="/images/1244488-20190225002929991-1118096007.png"><br>(来源参考3)<br>这张图表述了他们的一种组合.  </p>
<h1 id="一些实现中相关的概念"><a href="#一些实现中相关的概念" class="headerlink" title="一些实现中相关的概念"></a>一些实现中相关的概念</h1><p>在实现中,为了高效的执行这些概念会被组合起来使用.<br>单线程的异步操作,需要依赖非阻塞操作(不然单个线程就直接阻塞了).而这里异步的目的是为了提高线程的利用率,这在IO密集的应用中比较有效.<br>但如果操作本身是计算密集的,那么单线程的异步操作就没有太大的意义了.  </p>
<p>而如果无法满足操作都是非阻塞的,那常常会使用多线程来规避主线程(例如服务器中的请求处理线程和GUI中的UI线程)阻塞.<br>这种一般会返回<code>Future</code>等类似的占位符,并提供非阻塞查询结果是否返回,或者支持回调函数.<br>这在java中比较常见,毕竟java世界中的阻塞操作比较多.  </p>
<p>上述概念和IO的关系.<br>一些对应的API.<br><img src="/images/1244488-20190225003047863-763080176.png"><br>要记住的是这些操作和单线程/多线程都没什么关系.<br>更多的内容之前有翻译一篇文章: <a href="https://www.cnblogs.com/fairjm/p/translate-the-various-kinds-of-io-blocking-non.html">各个类型的IO - 阻塞, 非阻塞,多路复用和异步</a><br>对应语言的各种IO要看使用了系统提供的哪些类型的API.<br>例如Java,在Linux中的AIO是多线程模拟实现的,而在windows下使用了IOCP使用.  </p>
<p>暂时想到的就是这些,后续有一些想法会陆续补充.  </p>
<p>再次提醒一下..本文中的不少观点都是自己梳理理解的,不一定准确甚至正确,如有问题欢迎指正 ^_^</p>
<p>参考资料:<br>1.<a href="https://blog.slaks.net/2014-12-23/parallelism-async-threading-explained/">https://blog.slaks.net/2014-12-23/parallelism-async-threading-explained/</a>  </p>
<p>2.<a href="https://stackoverflow.com/questions/41770985/i-o-blocking-in-green-threads">https://stackoverflow.com/questions/41770985/i-o-blocking-in-green-threads</a>    </p>
<p>3.<a href="https://stackoverflow.com/questions/748175/asynchronous-vs-synchronous-execution-what-does-it-really-mean">https://stackoverflow.com/questions/748175/asynchronous-vs-synchronous-execution-what-does-it-really-mean</a></p>
]]></content>
  </entry>
  <entry>
    <title>[翻译]各个类型的IO -  阻塞, 非阻塞,多路复用和异步</title>
    <url>/2019/01/30/translate-the-various-kinds-of-io-blocking-non/</url>
    <content><![CDATA[<p>同事推荐,感觉写的不错就试着翻译了下.<br>原文链接: <a href="https://www.rubberducking.com/2018/05/the-various-kinds-of-io-blocking-non.html">https://www.rubberducking.com/2018/05/the-various-kinds-of-io-blocking-non.html</a><br>作者: <a href="https://www.blogger.com/profile/15503004383957399868">Didier A.</a></p>
<p>我发现对于软件程序员来说很难分清楚各种类型的IO.对于阻塞,非阻塞,多路复用和异步IO有很多的混淆点.<br>所以我想尝试解释清楚各种IO类型意味着什么</p>
<span id="more"></span>
<h1 id="在硬件层面"><a href="#在硬件层面" class="headerlink" title="在硬件层面."></a>在硬件层面.</h1><p>在现代操作系统中,IO(输入/输出)是一种和外围设备交换数据的方式.包括读写磁盘或SSD,通过网络发送和接受数据,在显示器上显示,接入键盘和鼠标输入,等等.  </p>
<p>现代操作系统和外围设备的交流取决于外围设备的特定类型以及他们的固件版本和硬件能力.<br>通常来说,你可以认为外围设备是很高级的,他们可以同时处理多个并发的读写数据请求.也就是说,串行交流的日子一去不返了.<br>在这些场景中,外围设备和CPU间的交流在硬件层面都是异步的.  </p>
<p>这个异步机制被称为硬件中断.<br>想想一个简单的场景,CPU请求外围设备去读取一些数据,接着CPU会进入一个无限循环,每一次都会检查外围设备的数据是否可用,直到获得了数据为止.<br>这种方法被称为轮询(<code>polling</code>),因为CPU需要保持检查外围设备.<br>在现代硬件中,取而代之发生的是CPU请求外围硬件执行操作,然后就忘了这件事,继续处理其他的CPU指令.只要外围设备做完了,他会通过电路中断来通知CPU.<br>这发生在硬件中,CPU因此不需要停下来或者检查这个外围设备,可以继续执行其他的工作,直到周边设备说已经做完了.  </p>
<h1 id="在软件层面"><a href="#在软件层面" class="headerlink" title="在软件层面"></a>在软件层面</h1><p>现在我们了解了硬件中发生的事,我们可以移动到软件这一侧了.<br>在这一层IO通过多种方式被暴露:阻塞,非阻塞,多路复用和异步.让我们一个个来仔细解释.  </p>
<h2 id="阻塞"><a href="#阻塞" class="headerlink" title="阻塞"></a>阻塞</h2><p>还记得用户程序如何在一个进程内运行，代码是在线程的上下文中执行的吗？<br>你总是会遇到需要编写一个需要从文件中读取数据的程序的情况.<br>使用阻塞IO,你所做的是从你的线程中请求操作系统,将线程置于休眠(sleep),当数据可用于被消费时操作系统会唤醒线程.  </p>
<p>也就是说,阻塞IO之所以被称为阻塞是因为使用他的线程会被阻塞直到IO完成.</p>
<h2 id="非阻塞"><a href="#非阻塞" class="headerlink" title="非阻塞"></a>非阻塞</h2><p>阻塞IO的问题是当你的线程在休眠时,他除了等IO完成不能干其他事.<br>有时候,你的程序可能没有其他事可做了.<br>但如果还有其他事需要做的话,能在等待IO的时候并发做可是极好的.  </p>
<p>其中一种实现方式被称为非阻塞IO.<br>他的思想是当你读取一个文件时,OS只是简单返回给你文件的内容或者一个等待状态告诉你IO还未完成,而不是将线程休眠.<br>他不会阻塞你的线程,但之后检查IO是否完成的工作还是交给了你.<br>这意味着当处于等待状态时,你可以去做一些工作,当你再次需要IO时,可以再读取一次,那时候IO可能已经完成了,文件的内容会返回,如果还是处于等待状态的话,你可以选择继续做其他事.  </p>
<h2 id="多路复用"><a href="#多路复用" class="headerlink" title="多路复用"></a>多路复用</h2><p>非阻塞IO的问题是如果你在等待IO的过程中要做的其他事情就是另外的IO的话,事情会变得很奇怪.  </p>
<p>在一个好的场景下,你请求OS去读取文件A的内容,然后去做一些重计算的工作,做完之后再去检查文件A是否完成读取,如果完成了,你再做一些关于这个文件内容的操作,不然就继续做其他的工作,循环往复.<br>但在一个坏的场景中,你没有重计算的工作要去做,而是需要去读取另一个文件B.<br>那除了等待他们还有什么事要做呢?<br>没有了,你的程序就进入了一个死循环,判断文件A是否被读取完毕,接着再去判断文件B,一遍又一遍.<br>要么你使用简单的状态轮询,这会导致过多消耗CPU,或者你手动加入一些随意的休眠时间,不过这也意味着你将延迟知道IO完成,这会降低程序的吞吐.</p>
<p>为了避免这个问题,你可以使用多路复用IO来代替.<br>他所做的是你再次阻塞在IO上,但这次不仅仅是一个一个的IO操作,你可以将所有需要的IO操作塞入队列,阻塞在所有的操作上.  当其中有一个IO完成之后OS会唤醒你.<br>一些多路复用的实现提供了更多的控制,你可以设置在特定一些IO操作完成之后再被唤醒,例如A和C文件或B和D文件完成的时候.  </p>
<p>所有你可以调用非阻塞读取文件A,然后非阻塞读取文件B,最后告诉操作系统将我的线程置于休眠,当A和B的IO都完成的时候或其中一个完成的时候再唤醒他.  </p>
<h2 id="异步"><a href="#异步" class="headerlink" title="异步"></a>异步</h2><p>多路复用IO的问题是在IO完毕前你还是处于休眠状态.<br>又一次,这对一些程序来说可行,那些除了等待IO操作完成外没有其他操作要去执行的程序.<br>但有时候,你确实需要去做其他事情.<br>可能你正在计算PI的数字,同时也在汇总一些文件的值.<br>你想要进行的操作是将所有的读操作入队列,当等待他们读取完成前,你可以继续计算PI.当一个文件读取完成后,你可以汇总他的值,然后继续进行PI的计算直到另一个文件完成读取.  </p>
<p>为了让这可行,你需要一种方式当IO完成时中断PI的计算,并且你需要IO来执行这个操作当他完成时.  </p>
<p>这通过事件回调完成.执行读操作的调用会需要一个回调,并且调用立即返回.当IO完成时,操作系统会挂起你的线程,并执行你的回调.当回调完成时,他会恢复你的线程.</p>
<h1 id="多线程-vs-单线程"><a href="#多线程-vs-单线程" class="headerlink" title="多线程 vs 单线程?"></a>多线程 vs 单线程?</h1><p>你可能已经注意到我所描述的所有线程都是关于单个线程的,也就是你的主线程.<br>真相是,IO的执行不依赖于线程,这我在最开始就已经解释过了,外围设备都是在他们自己的电路里异步执行IO.<br>所以阻塞,非阻塞,多路复用和异步IO都是可能在单线程模型中被执行的.<br>这也是为什么并发IO可以不借助于多线程支持来工作.</p>
<p>现在,对于处理IO操作完成的结果,或者请求IO操作很明显是可以多线程的,如果你需要的话.这允许你在并发IO之上执行并发计算.所以没有什么东西阻止多线程和这些IO机制结合.  </p>
<p>事实上,这里也有第五种受欢迎的基于多线程的IO.<br>他经常被混淆为非阻塞IO或异步IO,因为他对外暴露出的是类似他们的接口.<br>真相是,他是假装的非阻塞或异步IO.他的工作方式很简单,他使用阻塞IO,但是每个阻塞操作都是在他自己的线程中(注:多线程环境,非主线程中).<br>现在取决于他的实现机制,他要么接收一个回调,或者使用一种轮询模型,比如返回一个Future对象.</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>我希望这篇文章可以帮助你澄清对多种IO的理解.还有很重要的一点需要注意,他们不是被所有的操作系统和所有的外围设备支持的.相似的,不是所有的编程语言都暴露了操作系统支持的所有IO类型的API.</p>
<p>这边请,所有类型的IO都解释了.  </p>
<p>希望能对你有所帮助.  </p>
<h1 id="更多阅读"><a href="#更多阅读" class="headerlink" title="更多阅读"></a>更多阅读</h1><p><a href="https://stackoverflow.com/a/50205346/172272">non-blocking IO vs async IO and implementation in Java</a>  </p>
<p><a href="http://blog.omega-prime.co.uk/2015/09/03/asynchronous-and-non-blocking-io/">Asynchronous and non-blocking IO</a>  </p>
<p><a href="http://www.linux-mag.com/id/331/">Multiplexed I/O</a>  </p>
<p><a href="https://en.wikipedia.org/wiki/Reactor_pattern">Reactor pattern</a>  </p>
<p><a href="https://en.wikipedia.org/wiki/Proactor_pattern">Proactor pattern</a>  </p>
<p><a href="http://blog.stephencleary.com/2013/11/there-is-no-thread.html">There is no thread</a></p>
<p><a href="http://davmac.org/davpage/linux/async-io.html">Asynchronous I/O and event notification on linux</a>  </p>
<p><a href="https://stackoverflow.com/questions/87892/what-is-the-status-of-posix-asynchronous-i-o-aio">What is the status of POSIX asynchronous I/O (AIO)?</a>  </p>
<p><a href="http://lse.sourceforge.net/io/aio.html">Kernel Asynchronous I/O (AIO) Support for Linux</a>  </p>
<p><a href="https://msdn.microsoft.com/en-us/library/windows/desktop/aa365198(v=vs.85).aspx">I/O Completion Ports</a>  </p>
<h1 id="免责声明"><a href="#免责声明" class="headerlink" title="免责声明"></a>免责声明</h1><p>我不是一个系统层面的程序员,我也不是一个操作系统提供的所有种类IO方面的专家.这篇文章是我尽可能总结我所知的内容,更偏向于中间层面的知识.所以如果你发现有任何问题的话请指正我.  </p>
]]></content>
      <tags>
        <tag>翻译</tag>
      </tags>
  </entry>
  <entry>
    <title>[翻译]async: 什么是blocking</title>
    <url>/2021/05/09/translation-async-what-is-blocking/</url>
    <content><![CDATA[<p>原文来自:<a href="https://ryhl.io/blog/async-what-is-blocking/">https://ryhl.io/blog/async-what-is-blocking/</a><br>作者:<a href="https://ryhl.io/">Alice Ryhl</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
<p><code>Rust</code>的<code>async</code>/<code>await</code>特性是通过一种称为协作式调度(cooperative scheduling)的机制来实现的，这对于编写异步<code>Rust</code>代码的人来说有一些重要的影响。  </p>
<p>这篇博文的目标读者是异步<code>Rust</code>的新用户。我将使用<code>Tokio</code>运行时作为示例，但这里提出的观点适用于任何异步运行时。  </p>
<p>如果你只从这篇文章中记住一件事，那应该是:<br>异步代码不应该长时间不到达<code>.await</code>。(注:指的是运行中) </p>
<span id="more"></span>

<h1 id="Blocking-vs-non-blocking-code"><a href="#Blocking-vs-non-blocking-code" class="headerlink" title="Blocking vs. non-blocking code"></a>Blocking vs. non-blocking code</h1><p>编写一个可以同时处理许多事情的应用程序的质朴的方法是为每个任务生成一个新线程。如果任务的数量很少，这就是一个完美的解决方案，但是随着任务的数量变得越来越多，你最终会因为数量过多的线程而遇到问题。这个问题在不同的编程语言中有不同的解决方案，但它们都归结为同一件事: 非常快速地切换每个线程上当前运行的任务，这样所有的任务都有机会运行。在<code>Rust</code>中，这种切换发生在当你<code>.await</code>一些事上。</p>
<p>在写异步rust 时，短语<code>阻塞线程(blocking the thread)</code>意味着<code>阻止运行时切换当前任务</code>。这可能是一个主要问题，这意味着同一运行时上的其他任务将停止运行，直到线程不再被阻塞。为了防止这种情况，我们应该编写能够快速切换的代码，也就是不要长时间不进行<code>.await</code>。  </p>
<p>让我们来举个例子:  </p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">use</span> <span class="token namespace">std<span class="token punctuation">::</span>time<span class="token punctuation">::</span></span><span class="token class-name">Duration</span><span class="token punctuation">;</span>

<span class="token attribute attr-name">#[tokio::main]</span>
<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Hello World!"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// No .await here!</span>
    <span class="token namespace">std<span class="token punctuation">::</span>thread<span class="token punctuation">::</span></span><span class="token function">sleep</span><span class="token punctuation">(</span><span class="token class-name">Duration</span><span class="token punctuation">::</span><span class="token function">from_secs</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Five seconds later..."</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>上面的代码看起来是正确的，运行它将看起来正常工作。但它有一个致命的缺陷: 它阻塞了线程。上述情况没有其他任务，所以看不出问题，但在真正的程序中就不一样了。为了说明这一点，考虑下面的例子:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">use</span> <span class="token namespace">std<span class="token punctuation">::</span>time<span class="token punctuation">::</span></span><span class="token class-name">Duration</span><span class="token punctuation">;</span>

<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">sleep_then_print</span><span class="token punctuation">(</span>timer<span class="token punctuation">:</span> <span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Start timer &#123;&#125;."</span><span class="token punctuation">,</span> timer<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// No .await here!</span>
    <span class="token namespace">std<span class="token punctuation">::</span>thread<span class="token punctuation">::</span></span><span class="token function">sleep</span><span class="token punctuation">(</span><span class="token class-name">Duration</span><span class="token punctuation">::</span><span class="token function">from_secs</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Timer &#123;&#125; done."</span><span class="token punctuation">,</span> timer<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>

<span class="token attribute attr-name">#[tokio::main]</span>
<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token comment">// The join! macro lets you run multiple things concurrently.</span>
    <span class="token namespace">tokio<span class="token punctuation">::</span></span><span class="token macro property">join!</span><span class="token punctuation">(</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<pre class="language-none"><code class="language-none">Start timer 1.
Timer 1 done.
Start timer 2.
Timer 2 done.
Start timer 3.
Timer 3 done.</code></pre>
<p>这个例子将花费三秒钟运行，timer将在没有任何并发的情况下一个接一个地运行。原因很简单: <code>Tokio</code>运行时无法将一个任务切换为另一个任务，因为这种切换只能发生在<code>.await</code>。因为<code>sleep_then_print</code>中没有<code>.await</code>，在运行时就不会发生切换。</p>
<p>然而，如果我们使用<code>Tokio</code>的<a href="https://docs.rs/tokio/0.3/tokio/time/fn.sleep.html">sleep</a>函数，它使用一个<code>.await</code>来睡眠，那么这个函数就会正常工作:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">use</span> <span class="token namespace">tokio<span class="token punctuation">::</span>time<span class="token punctuation">::</span></span><span class="token class-name">Duration</span><span class="token punctuation">;</span>

<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">sleep_then_print</span><span class="token punctuation">(</span>timer<span class="token punctuation">:</span> <span class="token keyword">i32</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Start timer &#123;&#125;."</span><span class="token punctuation">,</span> timer<span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token namespace">tokio<span class="token punctuation">::</span>time<span class="token punctuation">::</span></span><span class="token function">sleep</span><span class="token punctuation">(</span><span class="token class-name">Duration</span><span class="token punctuation">::</span><span class="token function">from_secs</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token keyword">await</span><span class="token punctuation">;</span>
<span class="token comment">//                                            ^ execution can be paused here</span>

    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Timer &#123;&#125; done."</span><span class="token punctuation">,</span> timer<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>

<span class="token attribute attr-name">#[tokio::main]</span>
<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token comment">// The join! macro lets you run multiple things concurrently.</span>
    <span class="token namespace">tokio<span class="token punctuation">::</span></span><span class="token macro property">join!</span><span class="token punctuation">(</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
        <span class="token function">sleep_then_print</span><span class="token punctuation">(</span><span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    <span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<pre class="language-none"><code class="language-none">Start timer 1.
Start timer 2.
Start timer 3.
Timer 1 done.
Timer 2 done.
Timer 3 done.</code></pre>
<p>这段代码只需一秒钟就可以运行，并且如预期地在同一时间正确地运行所有三个函数。</p>
<p>要知道，事情并不总是这么明显。通过使用<a href="https://docs.rs/tokio/0.3/tokio/macro.join.html">tokio::join！</a>，这三个任务都保证在同一个线程上运行，但如果你用<a href="https://docs.rs/tokio/0.3/tokio/fn.spawn.html">tokio::spawn</a>替换它，并使用一个多线程的运行时，你将能够同时运行多个阻塞任务直到用完线程。默认的<code>Tokio</code>运行时按照核心数生成线程，你的电脑通常会有8个<code>CPU</code>。这足以使你在本地测试时忽略这个问题，并在真实场景下运行代码时非常快地耗尽线程。</p>
<p>为了给出多少时间算的上过久的定义，一个好的经验法则是每个<code>.await</code>的时间间隔不超过10到100微秒。这取决于你正在编写的应用程序的类型。</p>
<h1 id="如果我想要阻塞呢"><a href="#如果我想要阻塞呢" class="headerlink" title="如果我想要阻塞呢?"></a>如果我想要阻塞呢?</h1><p>有时候我们只是想阻塞线程。这是完全正常的。有两个常见的原因:</p>
<p>1.昂贵的<code>CPU-Bound</code>(受限于CPU资源的)计算<br>2.Synchronous IO. 同步IO  </p>
<p>在这两种情况下，我们都在处理一个会在一段时间内阻止任务<code>.await</code>的操作。为了解决这个问题，我们必须将阻塞操作移动到<code>Tokio</code>线程池外部的线程中去。关于这一点有三种形式可以使用:  </p>
<p>1.使用<a href="https://docs.rs/tokio/0.3/tokio/task/fn.spawn_blocking.html">tokio::task::spawn_blocking</a>函数<br>2.使用<a href="https://lib.rs/crates/rayon">rayon</a> crate<br>3.通过<a href="https://doc.rust-lang.org/stable/std/thread/fn.spawn.html">std::thread::spawn</a>创建一个专门的线程  </p>
<p>让我们仔细浏览下每个解决方案，看看什么时候应该使用它。  </p>
<h2 id="spawn-blocking函数"><a href="#spawn-blocking函数" class="headerlink" title="spawn_blocking函数"></a>spawn_blocking函数</h2><p><code>Tokio</code>运行时包含一个单独的线程池，专门用于运行阻塞函数，你可以使用 <a href="https://docs.rs/tokio/0.3/tokio/task/fn.spawn_blocking.html">spawn_blocking</a>在其上运行任务。这个线程池的上限是大约500个线程，因此可以在这个线程池上进行大量阻塞操作。</p>
<p>由于线程池有如此多的线程，它最适合用于阻塞 <code>I/O</code> (如与文件系统交互)或使用阻塞数据库库(如<a href="https://lib.rs/crates/diesel">diesel</a>)。  </p>
<p>线程池不适合昂贵的<code>CPU-bound</code>计算，因为线程池的线程数量比计算机上的 CPU核心数量多得多。当线程数等于CPU核心数时，<code>CPU-bound</code>的计算运行效率最高。也就是说，如果只需要运行几个<code>CPU-bound</code>的计算，我不会责怪你用<code>spawn_blocking</code>运行它们，因为这样做非常简单。</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token attribute attr-name">#[tokio::main]</span>
<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token comment">// This is running on Tokio. We may not block here.</span>

    <span class="token keyword">let</span> blocking_task <span class="token operator">=</span> <span class="token namespace">tokio<span class="token punctuation">::</span>task<span class="token punctuation">::</span></span><span class="token function">spawn_blocking</span><span class="token punctuation">(</span><span class="token closure-params"><span class="token closure-punctuation punctuation">|</span><span class="token closure-punctuation punctuation">|</span></span> <span class="token punctuation">&#123;</span>
        <span class="token comment">// This is running on a thread where blocking is fine.</span>
        <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"Inside spawn_blocking"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// We can wait for the blocking task like this:</span>
    <span class="token comment">// If the blocking task panics, the unwrap below will propagate the</span>
    <span class="token comment">// panic.</span>
    blocking_task<span class="token punctuation">.</span><span class="token keyword">await</span><span class="token punctuation">.</span><span class="token function">unwrap</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<h2 id="The-rayon-crate"><a href="#The-rayon-crate" class="headerlink" title="The rayon crate"></a>The rayon crate</h2><p><a href="https://lib.rs/crates/rayon">rayon</a>是一个著名的库，它提供了一个专门用于昂贵的<code>CPU-bound</code>计算的线程池，你可以将它与<code>Tokio</code>一起用于此目的。与<code>spawn_blocking</code>不同，<code>rayon</code>的线程池的最大线程数量很少，这就是为什么它适合进行昂贵计算的原因。</p>
<p>我们将计算大列表的和作为例子，但是请注意在实践中，除非数组非常非常大，否则只计算和是足够便宜的，可以直接在<code>Tokio</code>运行。  </p>
<p>使用<code>rayon</code>的主要危险是，在等待<code>rayon</code>完成时，必须注意不要阻塞线程。要做到这一点，可以将<a href="https://docs.rs/rayon/1/rayon/fn.spawn.html">rayon::spawn</a> 和<a href="https://docs.rs/tokio/0.3/tokio/sync/oneshot/index.html">tokio::sync::oneshot</a>结合起来，就像这样:  </p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">parallel_sum</span><span class="token punctuation">(</span>nums<span class="token punctuation">:</span> <span class="token class-name">Vec</span><span class="token operator">&lt;</span><span class="token keyword">i32</span><span class="token operator">></span><span class="token punctuation">)</span> <span class="token punctuation">-></span> <span class="token keyword">i32</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> <span class="token punctuation">(</span>send<span class="token punctuation">,</span> recv<span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token namespace">tokio<span class="token punctuation">::</span>sync<span class="token punctuation">::</span>oneshot<span class="token punctuation">::</span></span><span class="token function">channel</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// Spawn a task on rayon.</span>
    <span class="token namespace">rayon<span class="token punctuation">::</span></span><span class="token function">spawn</span><span class="token punctuation">(</span><span class="token keyword">move</span> <span class="token closure-params"><span class="token closure-punctuation punctuation">|</span><span class="token closure-punctuation punctuation">|</span></span> <span class="token punctuation">&#123;</span>
        <span class="token comment">// Perform an expensive computation.</span>
        <span class="token keyword">let</span> <span class="token keyword">mut</span> sum <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
        <span class="token keyword">for</span> num <span class="token keyword">in</span> nums <span class="token punctuation">&#123;</span>
            sum <span class="token operator">+=</span> num<span class="token punctuation">;</span>
        <span class="token punctuation">&#125;</span>

        <span class="token comment">// Send the result back to Tokio.</span>
        <span class="token keyword">let</span> _ <span class="token operator">=</span> send<span class="token punctuation">.</span><span class="token function">send</span><span class="token punctuation">(</span>sum<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// Wait for the rayon task.</span>
    recv<span class="token punctuation">.</span><span class="token keyword">await</span><span class="token punctuation">.</span><span class="token function">expect</span><span class="token punctuation">(</span><span class="token string">"Panic in rayon::spawn"</span><span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span>

<span class="token attribute attr-name">#[tokio::main]</span>
<span class="token keyword">async</span> <span class="token keyword">fn</span> <span class="token function-definition function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> nums <span class="token operator">=</span> <span class="token macro property">vec!</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">;</span> <span class="token number">1024</span> <span class="token operator">*</span> <span class="token number">1024</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"&#123;&#125;"</span><span class="token punctuation">,</span> <span class="token function">parallel_sum</span><span class="token punctuation">(</span>nums<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token keyword">await</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>
<p>上述例子使用了<code>rayon</code>的线程池来运行昂贵的操作。请注意，例子中对 <code>parallel_sum</code>的每个调用只使用了<code>rayon</code>线程池中的一个线程。如果你的应用程序中有很多对<code>parallel_ sum</code>的调用，那么这是有意义的，但是也可以使用<code>rayon</code>的并行迭代器在几个线程上计算和:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">use</span> <span class="token namespace">rayon<span class="token punctuation">::</span>prelude<span class="token punctuation">::</span></span><span class="token operator">*</span><span class="token punctuation">;</span>

<span class="token comment">// Spawn a task on rayon.</span>
<span class="token namespace">rayon<span class="token punctuation">::</span></span><span class="token function">spawn</span><span class="token punctuation">(</span><span class="token keyword">move</span> <span class="token closure-params"><span class="token closure-punctuation punctuation">|</span><span class="token closure-punctuation punctuation">|</span></span> <span class="token punctuation">&#123;</span>
    <span class="token comment">// Compute the sum on multiple threads.</span>
    <span class="token keyword">let</span> sum <span class="token operator">=</span> nums<span class="token punctuation">.</span><span class="token function">par_iter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">sum</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// Send the result back to Tokio.</span>
    <span class="token keyword">let</span> _ <span class="token operator">=</span> send<span class="token punctuation">.</span><span class="token function">send</span><span class="token punctuation">(</span>sum<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p>注意，当使用并行迭代器时，仍然需要<code>rayon::spawn</code>调用，因为并行迭代器是阻塞的。</p>
<h2 id="生成一个专用线程"><a href="#生成一个专用线程" class="headerlink" title="生成一个专用线程"></a>生成一个专用线程</h2><p>如果阻塞操作是需要永久运行下去的，那么应该在专用线程上运行它。例如，考虑一个管理数据库连接的线程，他需要使用通道来接收要执行的操作。因为这个线程在循环中监听该通道，所以它永远不会退出。  </p>
<p>在上述两个线程池中的任何一个上运行这样的任务都会是一个问题，因为它实际上从池中永久地带走了一个线程。一旦这样做了几次，线程池中就没有更多的线程了，所有其他阻塞任务都无法执行。  </p>
<p>当然，如果你不在意每次启动一个新线程的成本，也可以使用专用线程来运行较短生命周期的任务。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>如果你忘了，以下是你需要记住的主要事情:<br>异步代码不应该长时间不到达<code>.await</code>。  </p>
<p>下面你将看到一张备忘单，上面列出了当你想要阻塞的时候可以使用的方法:  </p>
<div style="overflow-x: auto;max-width: 100%;">

<table>
<thead>
<tr>
<th></th>
<th>CPU-bound computation</th>
<th>Synchronous IO</th>
<th>Running forever</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>spawn_blocking</code></strong></td>
<td>次优</td>
<td>OK</td>
<td>No</td>
</tr>
<tr>
<td><strong><code>rayon</code></strong></td>
<td>OK</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><strong>专用线程</strong></td>
<td>OK</td>
<td>OK</td>
<td>OK</td>
</tr>
</tbody></table>
</div>

<p>最后，我建议阅读<code>Tokio</code>教程中关于<a href="https://tokio.rs/tokio/tutorial/shared-state">共享状态</a>的章节。本章将解释如何在异步代码中正确使用 <a href="https://doc.rust-lang.org/stable/std/sync/struct.Mutex.html">std::sync::Mutex</a>，并更深入地说明为什么即使锁定<code>Mutex</code>是阻塞的，也可以这样做。(剧透一下: 如果你的阻塞时间很短，它真的算阻塞吗?)  </p>
<p>我还强烈推荐<code>Tokio</code>博客中的一篇文章: <a href="https://tokio.rs/blog/2020-04-preemption">Reducing tail latencies with automatic cooperative task yielding</a>。  </p>
<p>感谢<a href="https://v5.chriskrycho.com/">Chris Krycho</a>和<a href="https://snor.re/">snocl</a>阅读本文草稿并提供有用的建议。所有的错误都是我自己的。</p>
<p>原文来自:<a href="https://ryhl.io/blog/async-what-is-blocking/">https://ryhl.io/blog/async-what-is-blocking/</a><br>作者:<a href="https://ryhl.io/">Alice Ryhl</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
]]></content>
      <tags>
        <tag>rust</tag>
        <tag>翻译</tag>
      </tags>
  </entry>
  <entry>
    <title>[翻译]单线程共享可变性的问题</title>
    <url>/2022/10/26/translation-the-problem-with-shared-mutability/</url>
    <content><![CDATA[<p>原文链接: <a href="https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/">https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/</a><br>作者: <a href="https://manishearth.github.io/">Manish Goregaokar</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
<p><strong>注</strong>: 这篇文章写的比较早,早期的Rust编译器有很多不友善的地方,代码示例中部分当时无法编译的现在已经可以了,译者会增改一些代码导致和原文不同.</p>
<hr>
<p>Edit(2017年1月):我重新发现了<a href="http://smallcultfollowing.com/babysteps/blog/2013/06/11/on-the-connection-between-memory-management-and-data-race-freedom/">Niko的文章</a>,文章也提到了这一点并达到了同样的认识.我怀疑我下意识地从这篇文章中得到了这个想法,至少是部分的想法.</p>
<p>这是我一直想写的一篇文章;Rust 1.0的发布给了我继续接下去的完美动力.</p>
<p>虽然这篇文章是讨论Rust设计中的一个选择,并且例子都是用Rust写的,但这里讨论的原则在很大程度上适用于其它语言.我还将尝试让那些没有Rust背景的人更容易理解这篇文章;如果需要解释一些代码或术语,请告诉我.</p>
<p>我将在这里讨论的是Rust做的一个选择,不允许同一数据有多个可变别名(或者当有active的不可变别名时只能有一个可变别名) ，即使是在同一个线程.本质上讲,它不允许做这样的事情:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">let</span> <span class="token keyword">mut</span> x <span class="token operator">=</span> <span class="token class-name">Vec</span><span class="token punctuation">::</span><span class="token function">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> ptr <span class="token operator">=</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> x<span class="token punctuation">;</span> <span class="token comment">// 获取队`x`的可变引用</span>
    ptr<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 允许</span>
    <span class="token keyword">let</span> y <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 不允许 (无法编译): 只要`ptr`是active的,</span>
                  <span class="token comment">// x不能读取 ...</span>
    x<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token comment">// .. 也不能写入</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"&#123;:?&#125;"</span><span class="token punctuation">,</span> ptr<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 这段是译者新加 现在的编译器限制判断是否active更加智能 不是看整个作用域了</span>
<span class="token punctuation">&#125;</span>

<span class="token comment">// 另外一种,</span>

<span class="token keyword">let</span> <span class="token keyword">mut</span> x <span class="token operator">=</span> <span class="token class-name">Vec</span><span class="token punctuation">::</span><span class="token function">new</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
x<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 允许</span>
<span class="token punctuation">&#123;</span>
    <span class="token keyword">let</span> ptr <span class="token operator">=</span> <span class="token operator">&amp;</span>x<span class="token punctuation">;</span> <span class="token comment">// 创建一个不可变引用</span>
    <span class="token keyword">let</span> y <span class="token operator">=</span> ptr<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 允许, 不改变值</span>
    <span class="token keyword">let</span> y <span class="token operator">=</span> x<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">;</span> <span class="token comment">// 同样允许</span>
    x<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 不允许 (无法编译): 只要`ptr`是active,</span>
               <span class="token comment">// `x`被冻结了可变性</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"&#123;:?&#125;"</span><span class="token punctuation">,</span> ptr<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 译者新加 原因一样</span>
<span class="token punctuation">&#125;</span></code></pre>
<span id="more"></span>
<p>这相当于是”读写锁”(Read-Write lock,<code>RWLock</code>)模式,只不过它不在一个多线程的环境中使用,而且”锁”是通过静态分析完成的(编译时的”borrow checking”).</p>
<p>语言的初学者会有一个反复出现的问题,为什么会有这种设计.<a href="http://doc.rust-lang.org/nightly/book/ownership.html">所有权语义</a>和不可变<a href="http://doc.rust-lang.org/nightly/book/references-and-borrowing.html">借用</a>是容易理解的,因为其他一些语言比如像C++也有一些具体的例子来解决这些概念所防止的问题.对于只有一个所有者和多个借用者(静态保证存活时间不会超过所有者)是有意义的,可以防止比如释放之后使用(<code>use-after-free</code>)这类的问题.</p>
<p>但是使用多个句柄来改变一个对象会带来什么问题呢?我们为什么需要 <code>RWLock</code>模式?</p>
<h1 id="它会导致内存不安全"><a href="#它会导致内存不安全" class="headerlink" title="它会导致内存不安全"></a>它会导致内存不安全</h1><p>这个问题是针对Rust的,我保证这将是唯一针对Rust的答案.</p>
<p>Rust的<a href="http://doc.rust-lang.org/nightly/book/enums.html">enums</a>提供了代数数据类型的一种形式.Rust的enum允许”包含”数据,例如你可以有一个enum:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">enum</span> <span class="token type-definition class-name">StringOrInt</span> <span class="token punctuation">&#123;</span>
    <span class="token class-name">Str</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token keyword">i64</span><span class="token punctuation">)</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>该类型可以是具有关联字符串的<code>Str</code>,也可以是具有关联整数的<code>Int</code>.</p>
<p>对于这样的一个enum,我们可以造成一个segfault:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">let</span> <span class="token keyword">mut</span> x <span class="token operator">=</span> <span class="token class-name">Str</span><span class="token punctuation">(</span><span class="token string">"Hi!"</span><span class="token punctuation">.</span><span class="token function">to_string</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 创建一个具有关联字符串"Hi!"的`Str`实例</span>
<span class="token keyword">let</span> y <span class="token operator">=</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> x<span class="token punctuation">;</span> <span class="token comment">// 创建一个x的可变引用</span>

<span class="token keyword">if</span> <span class="token keyword">let</span> <span class="token class-name">Str</span><span class="token punctuation">(</span><span class="token keyword">ref</span> insides<span class="token punctuation">)</span> <span class="token operator">=</span> x <span class="token punctuation">&#123;</span> <span class="token comment">// 如果x是`Str`, 将其内部数据借用给`insides`</span>
    <span class="token operator">*</span>y <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 设置`*y`为`Int(1), 因此也是将`x`设置为`Int(1)`</span>
    <span class="token macro property">println!</span><span class="token punctuation">(</span><span class="token string">"x says: &#123;&#125;"</span><span class="token punctuation">,</span> insides<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// Uh oh!</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>这个例子,我们使<code>insides</code>引用失效,因为将<code>x</code>设置为<code>Int(1)</code>意味着内部不再有字符串.但是,<code>insides</code>仍然是对<code>String</code>的引用,并且生成的程序集将尝试解引用指向曾经分配的字符串的指针所在的内存位置,并且可能最终尝试解引用1或附近的一些数据,从而导致Segfault.</p>
<p>目前为止还不错.我们知道,为了让Rust风格的enums在Rust中安全工作,我们需要RWLock模式.但是还有其他的我们需要RWLock模式的原因吗?没有多少语言有这样的enums,所以这对它们来说应该不是什么问题.</p>
<h1 id="迭代器无效化"><a href="#迭代器无效化" class="headerlink" title="迭代器无效化"></a>迭代器无效化</h1><p>啊,每次问到上面这个问题的时候几乎都会提到这个例子.虽然我自己经常使用这个例子(并且觉得这是一个非常合适的例子,可以很快地解释),我也发现它有点逃避,原因我将在下面解释.这也是我写这篇文章的部分原因;对于那些想要深入研究的人来说,应该有一个更好的答案。</p>
<p>迭代器失效涉及使用类似迭代器的工具时,同时以某种方式修改底层数据集.</p>
<p>比如</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">let</span> buf <span class="token operator">=</span> <span class="token macro property">vec!</span><span class="token punctuation">[</span><span class="token number">1</span><span class="token punctuation">,</span><span class="token number">2</span><span class="token punctuation">,</span><span class="token number">3</span><span class="token punctuation">,</span><span class="token number">4</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

<span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token operator">&amp;</span>buf <span class="token punctuation">&#123;</span>
    buf<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>i<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>首先,这将导致无限循环(如果它能编译通过,但其实很不行,Rust会阻止这个行为).等价的C++例子是<a href="http://stackoverflow.com/questions/5638323/modifying-a-data-structure-while-iterating-over-it">这个</a>,我每次都会<a href="http://manishearth.github.io/Presentations/Rust/#/1/2">使用</a>它。</p>
<p>无限循环甚至不是真正的问题.真正的问题是过不了多久我们就会得到一个segfault.在内部,vectors有一块确定的分配空间来使用.如果vector增长超过了这个空间,需要进行一个新的,更大的空间分配(同时释放旧的分配空间),因为vectors必须使用连续的内存.</p>
<p>这意味着当vector超出其容量时,它将重新分配,使得存储在迭代器中的引用失效,并导致use-after-free.</p>
<p>当然,这种case有一个简单的解决方案,在迭代器中存储<code>Vec</code>/<code>Vector</code>对象的引用,而不是直接指向堆中的vector的指针.这会导致迭代器额外的间接指向(注:要解引用两次)或更大的堆栈大小(取决于如何实现它),但总体上可以防止内存不安全.</p>
<p>但这仍然会在涉及多维vectors的更复杂的情况下引发问题.</p>
<h1 id="“这实际上就是多线程”"><a href="#“这实际上就是多线程”" class="headerlink" title="“这实际上就是多线程”"></a>“这实际上就是多线程”</h1><blockquote>
<p>在足够复杂的单线程程序中,能够改变状态的别名实际上等同于不加锁访问多线程间共享的数据</p>
</blockquote>
<p><del>(以上是我对其他人的引用的解释,但我找不到原文,也不记得是谁写的)</del> 我找到了原文,来自于<a href="https://www.reddit.com/r/rust/comments/2x0h17/whats_your_killer_rust_feature/cow3zod/">kmc</a>的评论:  </p>
<blockquote>
<p>我的直觉是,离我自己的代码比较远的其他代码就像在另一个线程中,尽管我可以推断它将对共享的可变状态做什么.</p>
</blockquote>
<p>让我们退一步来看看为什么我们需要在多线程程序中使用锁.这取决于缓存和内存的工作方式;我们永远不用担心两个进程同时写入同一个内存位置导致产生一个混合的值,或者在写入过程中发生读取.  </p>
<p>我们真正需要担心的是我们脚下的地毯会被拉出来.在编写一组相关的读/写操作时,应该考虑到一些不变量,而在这些不变量中间可能发生的任意读/写操作将使这些不变量失效.例如,一段代码可能首先读取一个vector的length,然后用for循环以length为界遍历这个vector.这里假设的不变量是vector的length.如果在其他线程中对vector调用<code>pop()</code>,那么在读取到length之后但在其他地方读取之前,这个不变量可能会失效,这可能会导致在最后一次遍历中出现Segfault或use-after-free。</p>
<p>但是,在单线程代码中,我们可能会遇到(在精神上)类似的情况:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">let</span> x <span class="token operator">=</span> <span class="token function">some_big_thing</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">let</span> len <span class="token operator">=</span> x<span class="token punctuation">.</span>some_vec<span class="token punctuation">.</span><span class="token function">len</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token number">0</span><span class="token punctuation">..</span>len <span class="token punctuation">&#123;</span>
    x<span class="token punctuation">.</span><span class="token function">do_something_complicated</span><span class="token punctuation">(</span>x<span class="token punctuation">.</span>some_vec<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>我们有相同的不变量(len);但是我们能确定<code>x.do_something_complicated()</code>不会修改<code>x.some_vec</code>吗?在一个复杂的代码库中,<code>do_something_complicated()</code>自身会调用许多其他函数,这些函数也可能修改x,因此很难对其进行检查.</p>
<p>当然,上面的例子是一个简化和人为设计的例子;但是这样的错误也可能发生在大型代码库中的 - 在大型代码库中,许多被调用的方法都有副作用,而这些副作用可能并不总是明显的.</p>
<p>这意味着在大型代码库中,我们会遇到和多线程代码一样的问题.当一个人不完全确定每行代码在做什么时,维护不变量是非常困难的.可以通过阅读代码(这需要一段时间)可以确定这一点,但是未来代码的修改可能会导致要再次阅读.一直这样做是不切实际的,最后bug会突然出现.</p>
<p>另一方面,有一个对于这种情况不会发生的静态保证是很好的.如果静态保证会让代码变得过于复杂(或者只是想避免借用检查器),Rust中可以使用单线程RWlock风格的<a href="https://doc.rust-lang.org/core/cell/struct.RefCell.html">RefCell</a>类型。它是一种提供内部可变性的类型，其行为类似于借用检查器的运行时版本.可以用其他语言编写类似的包装.</p>
<p>修改:对于许多基础类型比如简单的整型,共享可变性并不是一个主要问题.对于这些类型,我们有一个类型称为<a href="http://doc.rust-lang.org/nightly/std/cell/struct.Cell.html">Cell</a>,让这些可变性和共享同时进行.这适用于所有的<code>Copy</code>类型,即只需要在栈上进行复制的类型.(而不涉及指针或其他间接方式的类型)</p>
<p>这种类型的bug也是可重入性问题的一个很好的来源.</p>
<h1 id="安全抽象"><a href="#安全抽象" class="headerlink" title="安全抽象"></a>安全抽象</h1><p>上一节中的问题使得编写安全的抽象变得非常困难,尤其在使用泛型时.这个问题在Rust下更加明显(在Rust中,抽象被期望是安全的,最好是低开销的),这也并不是任何语言所独有的.</p>
<p>你公开的每个方法都有一个预期要遵循的约定.很多时候,约定是由类型安全本身处理的,或者你可能有一些基于错误的模型来抛出非约定的数据(比如,除以零).  </p>
<p>但是,随着API(可以是内部的,也可以是公开的)变得越来越复杂,约定也变得越来越复杂.在运行时也不总是可能验证约定是否被违反,比如即便使用断言,很多情况下在复杂的代码中也很难防止迭代器失效.</p>
<p>创建一个方法并添加文档”前两个参数不应指向同一内存”是容易的.但是如果这个方法被其他方法使用,约定就会变得更加复杂,更加难以表达或检查.当涉及到泛型时,情况只会变得更糟;你有时无法强制不出现共享的可变别名,或者无法在文档中描述不允许的内容.API使用者也不一定遵从文档.</p>
<p>这使得编写安全,通用的抽象变得越来越困难.这样的抽象依赖于不变量,而这些不变量通常会被前一节中描述的问题所打破.强制这些不变量并不总是容易的.而且这样的抽象要么被误用,要么一开始就没有,选择了一个更重的选项.一般来说,这样的抽象或模式会被完全避开,即使它们可能会提高性能,因为它们是有风险并且难以维护.即使当前版本的代码是正确的,将来可能会有人再次改变某些东西来打破不变量.</p>
<p>我<a href="http://manishearth.github.io/blog/2015/05/03/where-rust-really-shines/">之前的文章</a>描述了一种情况,Rust能够选择较轻的道路而C++在获得同样的保证下会更难.</p>
<p>请注意,这是一个比可变别名更广泛的问题.Rust也有这个问题,但是当涉及到可变的别名时就没有了.修复可变别名非常重要,因为当没有可变别名时,我们可以对程序做出很多假设.通过查看一行代码，我们可以知道局部变量发生了什么.如果存在可变别名,那么其他局部变量也有可能被修改.一个非常简单的例子是:</p>
<pre class="language-rust" data-language="rust"><code class="language-rust"><span class="token keyword">fn</span> <span class="token function-definition function">look_ma_no_temp_var_l33t_interview_swap</span><span class="token punctuation">(</span><span class="token operator">&amp;</span><span class="token keyword">mut</span> x<span class="token punctuation">,</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> y<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token operator">*</span>x <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">+</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
    <span class="token operator">*</span>y <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">-</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
    <span class="token operator">*</span>x <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">-</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
<span class="token comment">// or</span>
<span class="token keyword">fn</span> <span class="token function-definition function">look_ma_no_temp_var_rockstar_interview_swap</span><span class="token punctuation">(</span><span class="token operator">&amp;</span><span class="token keyword">mut</span> x<span class="token punctuation">,</span> <span class="token operator">&amp;</span><span class="token keyword">mut</span> y<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token operator">*</span>x <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">^</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
    <span class="token operator">*</span>y <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">^</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
    <span class="token operator">*</span>x <span class="token operator">=</span> <span class="token operator">*</span>x <span class="token operator">^</span> <span class="token operator">*</span>y<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code></pre>

<p>在上面两种情况下,当两个引用是相同时(Rust中不会发生),代码不是交换两个变量的值,而是将两个变量设置为零.用户(库的内部用户或API使用者)可能希望<code>swap()</code>在提供相同的引用时不会改变任何内容,但上面的代码做的是完全不同的事.这个假设可以在程序中使用;比如,在进行数组排序时,因为slot正在与它自己进行比较,所以<code>swap()</code>应该跳过,不应该改变任何东西;但上述的代码会改变,然后排序函数会用零填充所有内容.这个问题可以通过在文档中写参数的前置条件和使用断言来解决,但是随着<code>swap()</code>在其他方法中被使用,写文档变得越来越困难. </p>
<p>当然,上面的例子是为了说明问题人为的.现实中<code>swap()</code>实现具有一些先决条件,不能在这种条件下使用.此外,在大多数交换算法中,会有当比较元素与其自身时忽略的边界检查.  </p>
<p>但是这个例子只是对手头问题的一个简单描述.  </p>
<p>在Rust中,由于静态检查因此不需要太担心这些问题,并且可以设计健壮的API,因为知道什么时候某些东西不可变可以帮助简化不变量.</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>不符合RWLock模式的别名使用是危险的.如果使用的是Rust这样的语言,那就不需要担心.如果正在使用C++这样的语言,它可能会导致内存不安全,所以要非常小心.如果使用Java或Go这样的语言,虽然它不会导致内存不安全,但是它会导致复杂的代码出现问题.  </p>
<p>这也不意味着这个问题迫使你切换到Rust.如果你觉得可以避免写出这种问题的API,那这也是一种有效的解决方法.在使用GC的语言中,这个问题要罕见得多,因此你可以不费吹灰之力地完全避免它.使用运行时检查和断言来维护不变量也是可以的;毕竟性能并不是一切.  </p>
<p>但这是编程中的一个问题;请确保在设计代码时考虑到这一点.  </p>
<p>讨论:<a href="https://news.ycombinator.com/item?id=9560158">HN</a>,<a href="http://www.reddit.com/r/rust/comments/369jnx/the_problem_with_singlethreaded_shared_mutability/">Reddit</a></p>
<hr>
<p>原文链接: <a href="https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/">https://manishearth.github.io/blog/2015/05/17/the-problem-with-shared-mutability/</a><br>作者: <a href="https://manishearth.github.io/">Manish Goregaokar</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
]]></content>
      <tags>
        <tag>rust</tag>
        <tag>翻译</tag>
      </tags>
  </entry>
  <entry>
    <title>[翻译]没有线程</title>
    <url>/2023/02/22/translation-there-is-no-thread/</url>
    <content><![CDATA[<p>原文来自:<a href="https://blog.stephencleary.com/2013/11/there-is-no-thread.html">https://blog.stephencleary.com/2013/11/there-is-no-thread.html</a><br>作者:<a href="https://blog.stephencleary.com/">Stephen Cleary</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
<p>这是最纯粹形式的异步的一个基本事实：没有线程。</p>
<p>反对这一事实的人很多。”不”,他们喊道,”如果我正在等待手术，一定有一个线程在等待！它可能是一个线程池线程。或者操作系统线程！或者带有设备驱动程序的东西……”</p>
<p>不要理会那些哭声。如果异步操作是纯粹的，那么就没有线程。</p>
<span id="more"></span>
<p>这是最纯粹形式的异步的一个基本事实：没有线程。</p>
<p>反对这一事实的人很多。”不”,他们喊道,”如果我正在等待手术，一定有一个线程在等待！它可能是一个线程池线程。或者操作系统线程！或者带有设备驱动程序的东西……”</p>
<p>不要理会那些哭声。如果异步操作是纯粹的，那么就没有线程。</p>
<p>持怀疑态度的人并不相信。让我们迁就他们一下。</p>
<p>我们将跟踪异步操作一直到硬件，特别注意 .NET 部分和设备驱动程序部分。我们必须通过省略一些中间层细节来简化这种描述，但我们不会偏离事实。</p>
<p>考虑一个通用的“写入”操作（写入到文件、网络流、USB 烤面包机等）。我们的代码很简单：</p>
<pre class="language-C#" data-language="C#"><code class="language-C#">private async void Button_Click(object sender, RoutedEventArgs e)
&#123;
  byte[] data &#x3D; ...
  await myDevice.WriteAsync(data, 0, data.Length);
&#125;</code></pre>

<p>我们已经知道UI线程在<code>await</code>期间不会被阻塞。问题是是否会有另一个线程必须在阻塞的祭坛上牺牲自己，以便UI线程可以存活？</p>
<p>抓住我的手。我们将深入研究。</p>
<p>第一站：库（例如，进入BCL的代码）。让我们假设<code>WriteAsync</code>是使用<a href="http://msdn.microsoft.com/en-us/library/system.threading.overlapped.aspx?WT.mc_id=DT-MVP-5000058">.NET中的标准P/Invoke 异步I/O系统</a>，该系统基于overlapped I/O。所以这个操作会在设备底层的<code>HANDLE</code>上启动一个Win32的overlapped I/O。</p>
<p>然后操作系统转向设备驱动程序并要求它开始进行写操作。这个操作会首先构造一个表示写请求的对象；这被称为I/O请求数据包(I/O Request Packet，IRP)。</p>
<p>设备驱动程序接收到IRP并向设备发出写数据的命令。如果设备支持直接内存访问 (DMA)，这就像将缓冲区地址写入设备寄存器一样简单。这就是设备驱动程序所能做的；它将IRP标记为”pending”并返回到操作系统那层。</p>
<p><img src="/images/tint01.png"></p>
<p>事实的核心在这里找到：设备驱动程序在处理IRP时不允许阻塞。这意味着如果IRP不能被立即完成，那么它<strong>必须</strong>被异步处理。即使对于同步API也是如此！在设备驱动程序级别，所有（重要的）请求都是异步的。</p>
<blockquote>
<p>引用 Tomes of Knowledge的话，”不管是什么类型的I/O请求，向驱动程序发出的代表应用程序的内部I/O操作都是异步执行的”。</p>
</blockquote>
<p>随着IRP”pending”，操作系统返回到库，库将未完成的任务返回给按钮单击事件handler，该handler挂起异步方法，UI线程继续执行。</p>
<p>我们已经按照请求深入到系统的深渊，一直到物理设备。</p>
<p>写操作现在”进行中”。那有多少线程正在处理它？  </p>
<p>答案是没有。</p>
<p>没有设备驱动程序线程、OS线程、BCL线程或线程池线程处理该写操作。没有线程。</p>
<p>现在，让我们随着内核恶魔之地的回应回到凡人世界。</p>
<p>在写入请求开始后的某个时间，设备完成了写入并通过中断通知CPU。</p>
<p>设备驱动程序的中断服务例程(Interrupt Service Routine，ISR)响应中断。中断是CPU级别的事件，会暂时从正在运行的线程中夺走对CPU的控制。您可以将ISR视为”借用”当前运行的线程，但我更倾向于将ISR理解为在如此低的级别上执行，以至于”线程”的概念不存在——因此可以说它们是位于所有线程的”下方”的线程。</p>
<p>ISR所做的只是告诉设备”谢谢你的中断”并将延迟过程调用(Deferred Procedure Call,DPC)放入队列。</p>
<p>当CPU不被中断打扰时，它将一一处理DPC。DPC的也在很低的层次上被执行，以至于说”线程”并不完全正确；与ISR一样，DPC直接在线程系统”之下”的CPU上执行。</p>
<p>DPC获取代表写入请求的IRP并将其标记为”完成”。然而，”完成”状态仅存在于操作系统级别；进程有着自己的内存空间，必须被通知操作已经完成。因此，操作系统将一个特殊内核模式异步过程调用 (Asynchronous Procedure Call, APC) 放入到拥有<code>HANDLE</code>的线程的队列。</p>
<p>由于BCL库使用标准的P/Invoke overlapped I/O系统，它已经通过I/O完成端口(I/O Completion Port, IOCP)注册了handler，IOCP是线程池的一部分。因此，一个I/O线程池线程被短暂借用来执行APC，APC会通知任务已完成。</p>
<p>该任务已经捕获了UI上下文，因此它不会直接在线程池的线程上继续<code>async</code>方法。相反，它将该方法的continuation放入UI上下文的队列中，并且UI线程将在之后恢复执行该方法。</p>
<p>因此，我们看到在写请求运行期间没有线程参与。只有当请求完成时，各种线程被”借用”或暂时将工作放入队列。这项工作通常在毫秒左右（例如，线程池上运行的APC）到微秒左右（例如，ISR）。但是没有线程被阻塞，只是在等待该请求完成。</p>
<p><img src="/images/tint02.png"></p>
<p>现在，我们遵循的是”标准化”的路径并进行了一些简化。有无数的变种，但核心事实是一样的。 </p>
<p>“在某处必须有一个线程在处理异步操作”的想法是不正确的。</p>
<p>解放你的思想。不要试图找到这个”异步线程”——这是不可能的。相反，只需尝试了解一下真相：</p>
<p><strong>没有线程</strong></p>
<p>原文来自:<a href="https://blog.stephencleary.com/2013/11/there-is-no-thread.html">https://blog.stephencleary.com/2013/11/there-is-no-thread.html</a><br>作者:<a href="https://blog.stephencleary.com/">Stephen Cleary</a><br>翻译 by <a href="https://bingowith.me/">abaabaqua</a></p>
]]></content>
      <tags>
        <tag>翻译</tag>
      </tags>
  </entry>
  <entry>
    <title>lambda和匿名内部类使用外部变量为什么要语义final?</title>
    <url>/2018/10/19/why-lambda-inner-class-only-access-final-var/</url>
    <content><![CDATA[<p>今天群里讨论<code>java</code>的<code>lambda</code>实现.<br>后来不断衍生谈到了为什么lambda和匿名内部类只能使用语义<code>final</code>的外部变量.  </p>
<span id="more"></span>
<p>最开始以为是java的lambda实现问题,编译期魔法会把外部引用作为参数传入所以在内部变化也影响不了下次调用的值,所以就干脆final了,如果用类的属性来保管这个变量就可以了.  </p>
<pre class="language-python" data-language="python"><code class="language-python">In <span class="token punctuation">[</span><span class="token number">64</span><span class="token punctuation">]</span><span class="token punctuation">:</span>  <span class="token keyword">def</span> <span class="token function">outer</span><span class="token punctuation">(</span>a<span class="token punctuation">:</span><span class="token builtin">int</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>      <span class="token keyword">def</span> <span class="token function">inner</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>          <span class="token keyword">nonlocal</span> a
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>          a <span class="token operator">=</span> a <span class="token operator">+</span> a
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>          <span class="token keyword">return</span> a
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>      <span class="token keyword">return</span> inner
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>

In <span class="token punctuation">[</span><span class="token number">65</span><span class="token punctuation">]</span><span class="token punctuation">:</span> x <span class="token operator">=</span> outer<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>

In <span class="token punctuation">[</span><span class="token number">66</span><span class="token punctuation">]</span><span class="token punctuation">:</span> x
Out<span class="token punctuation">[</span><span class="token number">66</span><span class="token punctuation">]</span><span class="token punctuation">:</span> <span class="token operator">&lt;</span>function __main__<span class="token punctuation">.</span>outer<span class="token punctuation">.</span><span class="token operator">&lt;</span><span class="token builtin">locals</span><span class="token operator">></span><span class="token punctuation">.</span>inner<span class="token operator">></span>

In <span class="token punctuation">[</span><span class="token number">67</span><span class="token punctuation">]</span><span class="token punctuation">:</span> x<span class="token punctuation">(</span><span class="token punctuation">)</span>
Out<span class="token punctuation">[</span><span class="token number">67</span><span class="token punctuation">]</span><span class="token punctuation">:</span> <span class="token number">2</span>

In <span class="token punctuation">[</span><span class="token number">68</span><span class="token punctuation">]</span><span class="token punctuation">:</span> x<span class="token punctuation">(</span><span class="token punctuation">)</span>
Out<span class="token punctuation">[</span><span class="token number">68</span><span class="token punctuation">]</span><span class="token punctuation">:</span> <span class="token number">4</span>

In <span class="token punctuation">[</span><span class="token number">69</span><span class="token punctuation">]</span><span class="token punctuation">:</span> x<span class="token punctuation">(</span><span class="token punctuation">)</span>
Out<span class="token punctuation">[</span><span class="token number">69</span><span class="token punctuation">]</span><span class="token punctuation">:</span> <span class="token number">8</span>
</code></pre>
<p>举例就是这种情况<br>lambda用参数传入外部int,如果在方法里修改了,下次调用这个lambda依旧是以前的值.  </p>
<p>后来又去看了眼匿名内部类的实现</p>
<pre class="language-java" data-language="java"><code class="language-java"><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">InnerTest</span> <span class="token punctuation">&#123;</span>

    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
        <span class="token class-name">String</span> name <span class="token operator">=</span> <span class="token string">"123"</span><span class="token punctuation">;</span>
        <span class="token class-name">Runnable</span> r <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Runnable</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
            <span class="token annotation punctuation">@Override</span>
            <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">&#125;</span>
        <span class="token punctuation">&#125;</span><span class="token punctuation">;</span>
        r<span class="token punctuation">.</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">&#125;</span>
<span class="token punctuation">&#125;</span></code></pre>
<pre class="language-none"><code class="language-none">InnerTest$1(java.lang.String val$name);
    0  aload_0 [this]
    1  aload_1 [val$name]
    2  putfield InnerTest$1.val$name : java.lang.String [12]
    5  aload_0 [this]
    6  invokespecial java.lang.Object() [14]
    9  return
     Line numbers:
       [pc: 0, line: 1]
       [pc: 5, line: 11]
     Local variable table:
       [pc: 0, pc: 10] local: this index: 0 type: new InnerTest()&#123;&#125;
     Method Parameters:
       final synthetic val$name</code></pre>
<p>构造方法字节码明明都存下来了呀…为什么那时候就要求<code>final</code>了  </p>
<p>查阅了一下,发现这样有个很大的问题.<br>这个外部变量在匿名内部类初始化之后就被固定了下来,之后他如果被重新赋值(引用类型内部状态修改除外),就会出现内部无法看见外部,外部也无法看见内部的问题… …<br>举个简单的例子:  </p>
<pre class="language-python" data-language="python"><code class="language-python">In <span class="token punctuation">[</span><span class="token number">101</span><span class="token punctuation">]</span><span class="token punctuation">:</span> <span class="token keyword">def</span> <span class="token function">outer</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">:</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>     <span class="token keyword">def</span> <span class="token function">inner</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>         <span class="token keyword">nonlocal</span> x
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>         x <span class="token operator">=</span> x<span class="token operator">+</span><span class="token number">1</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>         <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"inner"</span><span class="token operator">+</span><span class="token builtin">str</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>     inner<span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>     <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">"outer"</span><span class="token operator">+</span><span class="token builtin">str</span><span class="token punctuation">(</span>x<span class="token punctuation">)</span><span class="token punctuation">)</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>     x <span class="token operator">=</span> x<span class="token operator">+</span><span class="token number">1</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>     inner<span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">:</span>

In <span class="token punctuation">[</span><span class="token number">102</span><span class="token punctuation">]</span><span class="token punctuation">:</span> outer<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>
inner2<span class="token punctuation">(</span>inner内部<span class="token operator">+</span><span class="token number">1</span><span class="token punctuation">)</span>
outer2<span class="token punctuation">(</span>外部看到这个变化<span class="token punctuation">)</span>
inner4<span class="token punctuation">(</span>外部<span class="token operator">+</span><span class="token number">1</span> 内部<span class="token operator">+</span><span class="token number">1</span><span class="token punctuation">)</span></code></pre>

<p>这段在py下能正常工作的代码,如果java没有<code>final</code>限制的话,就会变成</p>
<pre class="language-none"><code class="language-none">inner2(inner内部+1)
outer1(外部看不到inner变化)
inner3(inner内部+1)</code></pre>
<p>所以这个约定和闭包实现没关系…<br>还是考虑在定义变量的作用域下规避掉因为java实现问题导致上面的结果返回…我既然没有<code>nonlocal</code>这样的机制(毕竟不支持引用传递….),索性就用<code>final</code>限制起来.  </p>
<p>呜呼哀哉  </p>
<hr>
<p>参考资料:</p>
<p><a href="https://stackoverflow.com/questions/4732544/why-are-only-final-variables-accessible-in-anonymous-class">https://stackoverflow.com/questions/4732544/why-are-only-final-variables-accessible-in-anonymous-class</a> </p>
<p><a href="https://en.wikipedia.org/wiki/Closure_(computer_programming)">Closure_(computer_programming)</a></p>
]]></content>
  </entry>
  <entry>
    <title>工作总结</title>
    <url>/2018/06/26/work-summary/</url>
    <content><![CDATA[<p>一直想写这篇,但是持续拖延… …<br>主要总结下这几年做的一些较大且完整的功能,过于零散的小功能以及和公司业务强相关的东西就不做记录了.<br>顺序是新的在前,旧的在后,不断补充,就当自己的总结.<br>都是后端相关的工作.<br>实习的时候参与网站社区相关开发,毕业之后短时间参与了一个项目的零碎开发,之后一直做App后端相关的开发工作,今年年初转到对商家服务的组.<br>基础技术是<code>spring</code>,<code>mybatis</code>之类的就不每个都写了.    </p>
<span id="more"></span>
<p>2014年实习<br>网站功能后端开发<br>网站新版社区后端开发<br>后端团队为3-4人  </p>
<p>2015.6-2018.1:<br>App后端开发<br>主要涉及面向ToC的三个App和面向ToB的一个App<br>后端团队为2人(后来增加为3人)</p>
<p>2018.1 - 至今<br>Saas产品开发  </p>
<h2 id="消息中心服务"><a href="#消息中心服务" class="headerlink" title="消息中心服务"></a>消息中心服务</h2><p>2018年10月 - 12月<br>使用技术: <code>ES</code>, <code>Akka</code><br>建设外部消息服务(短信,邮件,语音通知,企业微信等)的访问服务.<br>因为对发送记录有检索和统计的需求,且发送量比较大,短信记录几十w条每月增加,保留短信原文.使用ES作为后备存储.<br>提供多家运营商的,且对发送时间有限制,使用akka封装各个发送端.  </p>
<hr>
<h2 id="企业库项目"><a href="#企业库项目" class="headerlink" title="企业库项目"></a>企业库项目</h2><p>2018年8月<br>与网站ToC端合作,对方增加普通用户录入企业信息入口,提交的数据提供给Saas端.<br>主要功能为公司搜索(使用opensearch 数据库查询为后备),审核状态查询,后台审核功能.    </p>
<hr>
<h2 id="权限重构"><a href="#权限重构" class="headerlink" title="权限重构"></a>权限重构</h2><p>2018上半年<br>账号体系分为根账号和子账号,子账号权限继承自根账号硬编码排除部分.之后需求是根账号可以管理角色和分配角色给子账号.<br>对这一模块进行了重构,建立新的角色服务,引入了角色体系,表面是RBAC实现,但实际实现为了后期灵活(比如产品要直接给账号加权限)使用了角色,权限引用次数的设计.<br>并增加对应的中台服务.</p>
<hr>
<h2 id="任务体系"><a href="#任务体系" class="headerlink" title="任务体系"></a>任务体系</h2><p>2018上半年<br>使用技术:<code>kafka</code>,<code>redis</code>.<br>完成任务-积分-等级的体系.<br>任务定义包括新手任务(只能完成一次),周期任务(每段周期内完成n次),周期积分阶梯任务(每段时间内完成n次,不同的n积分阶梯不同).<br>运用原有的<code>CDC</code>模块,订阅kafka消息,部分任务完成的事件监听表变化实现,新手任务和周期任务立即结算积分,周期积分阶梯任务使用定时器+redis全局锁实现.<br>后续完成积分抽奖功能,对接现有的抽奖中台.    </p>
<hr>
<h2 id="数据统计-接手"><a href="#数据统计-接手" class="headerlink" title="数据统计(接手)"></a>数据统计(接手)</h2><p>2018上半年<br>使用技术:<code>ES</code>,<code>mongo</code>,<code>spark</code><br>原本负责人调走,接手维护改进的工作.<br>整个数据模块基于mongo+ES+spark完成,数据源包括数据库和kafka消息.<br>使用ETL导入带ES,spark进行计算后结果写入mongo.<br>主要优化了几个历史遗留的性能问题和bug,了解整个的设计思想.<br>写了一篇总结:<br><a href="https://www.cnblogs.com/fairjm/p/painful-to-maintain-data-statistic-system.html">https://www.cnblogs.com/fairjm/p/painful-to-maintain-data-statistic-system.html</a>  </p>
<hr>
<h2 id="规则引擎"><a href="#规则引擎" class="headerlink" title="规则引擎"></a>规则引擎</h2><p>2017年下半年<br>使用技术:<code>groovy</code><br>使用groovy的scriptEngine实现了一个简单的动态脚本.<br>本来想用于一些动态业务规则的配置,不过还没有投入到业务中去.<br>最初版本为: <a href="https://github.com/fairjm/common-rules">https://github.com/fairjm/common-rules</a> .   </p>
<hr>
<h2 id="feed流"><a href="#feed流" class="headerlink" title="feed流"></a>feed流</h2><p>2017年下半年<br>使用技术:<code>kafka</code><br>之前做过一版使用redis存储触发事件的实体id(发帖就存入帖子id),每个用户一个list,支持显示1000条动态.<br>这一版是一个新的业务场景,要求的数据量比之前大,考虑之后放在redis里不合适,和组内另一位同事合作完成.<br>最初实现采用了推的设计,每个用户有自己的timeline和feed队列(存储于数据库, feed根据用户id分表分库).<br>timeline队列对应自己发的内容,自己的时间线,feed是关注的人的内容,是关注的人的timeline的副本(两者格式类似).<br>用户完成动作之后使用kafka推送到消息(包含feed所需数据)到feed服务并写入.<br>上线之后原开发组被拆了,没有进一步改进.  </p>
<h2 id="网站安全相关"><a href="#网站安全相关" class="headerlink" title="网站安全相关"></a>网站安全相关</h2><h2 id="App-扫码登录"><a href="#App-扫码登录" class="headerlink" title="App 扫码登录"></a>App 扫码登录</h2><h2 id="图片PK活动"><a href="#图片PK活动" class="headerlink" title="图片PK活动"></a>图片PK活动</h2><h2 id="App-AB-test模块"><a href="#App-AB-test模块" class="headerlink" title="App AB test模块"></a>App AB test模块</h2><h2 id="设计购买业务-接手"><a href="#设计购买业务-接手" class="headerlink" title="设计购买业务(接手)"></a>设计购买业务(接手)</h2><h2 id="短信服务重构"><a href="#短信服务重构" class="headerlink" title="短信服务重构"></a>短信服务重构</h2><h2 id="redis监控"><a href="#redis监控" class="headerlink" title="redis监控"></a>redis监控</h2><h2 id="社区编辑器后端"><a href="#社区编辑器后端" class="headerlink" title="社区编辑器后端"></a>社区编辑器后端</h2><hr>
<h2 id="业务思考"><a href="#业务思考" class="headerlink" title="业务思考"></a>业务思考</h2><hr>
<p>2018-06-26 init</p>
]]></content>
  </entry>
</search>
